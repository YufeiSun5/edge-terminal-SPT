# AI Collaboration Board

## 文件模型

`AI_BOARD.md` 是本项目唯一活跃 AI 协作看板，必须与 `MEMORY.md` 同级放在仓库根目录。

`.ai/docs/` 只保存稳定设计文档、闭合记录、阶段总结和归档材料，不承载 open/blocked 工作项，不创建第二个活跃看板。

已闭合看板记录按日期迁移到 `.ai/docs/archive/`。当前迁移记录：`.ai/docs/archive/2026-05-31-ai-board-closed-records.md`。

## AI Identities

| Identity | Owner | Scope | Boundary |
| --- | --- | --- | --- |
| Frontend AI | `frontend-ai` | Electron + React UI、sidecar 调用封装、登录/SSO、开机自启动/托盘、三语文案、视觉和交互。 | 不实现采集/存储核心，不直接访问 RabbitMQ/MySQL，不绕过后端 API。 |
| Backend AI | `backend-ai` | Go 后端、MQTT、SSE/WS 实时出口、HTTP/RabbitMQ 控制通道、配置、DTO、安装器后端钩子。 | 前端可见 API/DTO/错误语义变化必须写入看板。 |
| Test AI | `test-ai` | 单元测试、smoke、E2E、lab、压测、发布门禁证据。 | 默认不改产品行为；修测试夹具必须说明范围。 |
| Review AI | `review-ai` | 架构审阅、上线评估、风险清单、跨模块微调。 | 先列风险和缺口；跨身份修改必须说明原因和影响。 |

## Active Board

| ID | Owner | Type | Status | Item | Next Action |
| --- | --- | --- | --- | --- | --- |
| EB-003 | `backend-ai` | integration | open | 方法类/控制类调用需面向主服务器提供 RabbitMQ 或 HTTP 通道。 | 确认主通道、命令幂等语义和错误返回格式。 |
| EB-006 | `test-ai` | test | open | 建立 Go 后端最小测试和 smoke 验证门禁。 | `go test ./...`、`go build ./cmd/edge-backend`、`desktop/npm run lint|build|test` 当前通过；已新增 `go run -tags smoke_tools ./tools/smokehealth` 覆盖后端 `/health`、登录、核心只读 API、项目成员接口和旧 `/devices=404` 可用性，已新增 `go run -tags smoke_tools ./tools/smokeeb017` 和 `go run -tags smoke_tools ./tools/smokeeb045` 覆盖多项目检测与正式 STRING 任务请求链路，EB-045 smoke 已覆盖配置参数包里的检测配置、`process_params`、`plc_writes`、`storage_prepare`、运行中限值调整、检测静音 `muted=1`、报表登记、受控 HTTP 请求、特征值刷新事件、通知 WS/HTTP 未读已读联动、固定时长自动结束、合格持续自动结束、检测生命周期 `detection.run_started/run_paused/run_resumed/run_stopped/run_abnormal_stop` 通知、检测 OK 结果通知、检测业务超限报警进入/等级变化/恢复通知、变量默认报警进入/等级变化/恢复通知、默认报警落库查询、WS 写请求审计、WS 异常停止命令审计、任务流控制变量写入审计和检测 NG 结果通知。MQTT/KIO payload 热路径已补 `internal/pipeline` 回归，覆盖真实 `Objs/PVs` payload、业务名重映射、PVs fallback、质量码和 topic 索引不串台。新增 `desktop/npm run smoke:settings` 覆盖桌面设置页登录、`/projects`、旧 `/devices=404`、项目成员接口、项目成员 UI，以及运行日志预览使用居中弹窗且无侧边抽屉。下一步补更多桌面端业务流 smoke。 |
| EB-016 | `frontend-ai` | feature | open | 工位页补齐检测任务操作闭环、多设备运行总览和虚变量创建入口。 | typed API、管理中心新建虚变量入口、工位页开始检测弹窗、标准/报表模板选择、start/stop/abnormal-stop mutation、权限显隐、按任务进入历史曲线已完成；下一步补更清晰的多设备运行总览和报表模板管理 UI。 |
| EB-017 | `test-ai` | test | open | 多项目并行检测任务与虚变量测试 smoke。 | 已新增可重复 smoke 工具 `go run -tags smoke_tools ./tools/smokeeb017`，现场验证 INT/STRING 虚变量创建、两个项目同时 running、同项目二次启动 409、WS 写虚拟变量、历史样本归属正确、停止单项目不影响另一个项目。已补 `Kernel.Start()` 级重启恢复测试，证明数据库中 running 检测任务会重新进入 runtime `TaskManager` 和 `/detection-runs/active`。剩余：桌面端操作流 smoke。 |
| EB-019 | `backend-ai` | feature | open | WebSocket 实时与命令通道：实时变量、检测任务状态、通知和写操作统一经后端分发。 | `/api/v1/ws` 已支持实时变量/检测任务快照、通知、检测任务写命令和 `command.write_variable`；实时变量快照支持全量、按 `project_id`、按单个/多个 `var_id` 和 `source_type/gateway_id` 过滤，`var_id` 订阅走直取避免先组全量；运行期 `subscribe.var_ids[]` 可接收数字或字符串形式 64 位 ID，订阅回执返回 `var_id_texts`。`topic=notifications` 推送 `notification.event`，同时 `Channels.Notify` 已经通过 `NotificationDispatcher` 写入 `sys_notifications/sys_notification_recipients`，HTTP 提供通知列表、未读数和标记已读；recipient 生成已支持 `target_type=all/user/role/project`，其中 `project` 在配置 `sys_project_members` 后按成员和 `notify_enabled` 精确分发，未配置成员的项目暂时全员兜底；设置页用户管理已提供项目成员和通知接收开关配置。`smokeeb045` 已验证 `detection.run_started/run_paused/run_resumed/run_stopped/run_abnormal_stop`、`detection.features_updated`、`detection.result_ok`、检测业务 `alarm.limit.enter/level_change/recover`、变量默认 `alarm.limit.enter/level_change/recover` 与 `detection.result_ng` 通知可同时经 WS 即时到达、经 HTTP 按同一 `event_uid` 查到并标记单条已读；变量默认报警还会经 `GET /api/v1/limit-alarms?scope=default` 读回落库记录。变量写入统一走 `VariableWriteService`，虚拟变量更新内存并可触发 task-flow，物理变量必须满足写入约束后经 KIO service 发布，不绕过后端直写 MQTT，WS 写变量回执返回 `result.var_id_text`；WS 异常停止检测命令已验证会写成功审计。剩余工作是主站控制通道和更多现场组合 smoke。 |
| EB-020 | `backend-ai` | feature | open | 写操作审计和运行日志完善。 | P0 已完成受保护 HTTP 写请求统一审计：`POST/PATCH/PUT/DELETE` 通过 runtime middleware 写入 `sys_audit_logs`，并新增 `GET /api/v1/audit-logs` 只读分页查询接口供设置页/审计页使用；审计 detail 只保留请求 ID、命令 ID、路由、状态、actor、latency 和错误摘要，不保存请求/响应正文。`smokeeb045` 已追加真实 HTTP 断言：WS `command.write_variable` 可按 `command_id` 查到 `ws.command.write_variable` 审计，WS `command.detection.abnormal_stop` 可按 `command_id` 查到成功审计，任务流 `builtin.write_control_variables` 可按目标变量查到 `task_flow.write_variable` 成功审计。后续运行日志继续覆盖 MQTT/WS/worker/存储异常，KIO WS 写命令接入时必须复用同一审计模型。 |
| EB-029 | `test-ai` | test | open | 后端分阶段联调和回归门禁。 | 每阶段跑 `gofmt`、`go test ./...`、覆盖率不低于 70%、`go vet ./...`、`go build ./cmd/edge-backend`；补 HTTP/WS smoke、审计日志断言、storage routes 断言、多项目检测和超限事件回归；完成后保持 `127.0.0.1:18080` 后端运行给前端测试。 |
| EB-034 | `frontend-ai` | feature | closed | 前端适配变量默认上下限、默认报警开关和运行中同步选项。 | 变量编辑和虚变量创建已接入 `default_alarm_enabled/default_limit_ll/default_limit_l/default_limit_h/default_limit_hh/default_limit_deadband/default_violation_hold_ms/default_recover_hold_ms`；变量编辑已提供 `apply_to_running` 运行中同步选项和风险提示，明确只同步当前 running 快照、不修改检测标准业务限值。检测配置标准项已支持 `alarm_enabled/check_cycle_ms/check_on_start` 等字段。工位页当前运行任务已新增“标准快照”入口，调用 `GET /api/v1/detection-runs/{id}` 展示检测业务限值、变量默认报警、变量默认限值和默认报警策略。Playwright 已验证字段可见、运行快照展示和 `apply_to_running=true` 保存请求 200。后续可按归档规则迁出本 closed 项。 |
| EB-037 | `backend-ai`/`frontend-ai` | feature | closed | 项目动态宽表存储：storage routes、检测运行快照、自动建表扩列和宽表 writer。 | 后端已完成 route 驱动存储主路径；前端工位页已接入当前运行任务“存储快照”弹窗，调用 `GET /api/v1/detection-runs/{id}/storage-routes` 展示当次冻结 route 的目标、表/列、触发、周期、死区和首帧存储，并完成端到端开始检测、查看快照、停止检测 smoke。历史查询页已接入同一“存储快照”入口，任务链接使用 `project_id` 查询历史并兼容旧 `device_id` 链接读取。设置中心存储路由新增搜索和启用状态筛选，Playwright 已验证创建 enabled route、筛选定位、编辑禁用和删除全链路。后续可按归档规则迁出本 closed 项。 |
| EB-041 | `backend-ai`/`frontend-ai` | breaking-change | open | 将旧“设备”领域统一改为“项目”，不保留 `/devices` 兼容入口。 | 后端已改用 `projects` API/DTO/文档和项目动态宽表命名；`/api/v1/devices` 已返回 404。前端首轮兼容适配已完成，但页面和 shared type 仍保留大量 `device/device_id/device_code` 别名；前端迁移闭合拆到 EB-047，完成后 EB-041 才能关闭。 |
| EB-042 | `backend-ai`/`frontend-ai` | breaking-change | open | 彻底删除变量属性里的存储频率、触发、首帧快照、目标表列字段。 | 后端已从变量 API 中移除存储属性；前端变量编辑、虚变量创建和变量 PATCH/POST 已停止展示/提交 `store_*`、`storage_*`、`startup_snapshot_enable`，并新增 storage routes UI 管理目标表列、触发、周期、死区和首帧存储。下一步补 storage route 权限/筛选和运行快照展示。 |
| EB-045 | `backend-ai`/`frontend-ai` | feature | open | 条件事件脚本任务系统：多变量条件、内置业务动作、JS 脚本和执行日志。 | 后端已补 P0/P1：`GET /api/v1/task-flow-runs`、`GET /api/v1/task-flow-runs/:id`、`GET /api/v1/task-flow-runs/:id/sql-logs`；`task_flow_runs` 记录 `origin_flow_id/origin_run_id/depth`；新增 `builtin.write_variable`，只允许写虚拟变量，写审计并默认阻断自递归；JavaScript runtime API 已支持 `realtime.get/getMany/getByName/project/write`，其中 `get/getMany/write` 支持字符串形式 64 位 `var_id`，快照/写入结果返回 `var_id_text`，`trigger/context` 提供 `trigger_var_id_text`，`steps_json` 的 `source=event` 保存校验也允许 `trigger_var_id_text`；`write` 同样只写虚拟变量、复用审计且默认不触发递归。物理变量下设继续走 WS/HTTP 的 `VariableWriteService + KIOWriteService`，不让任务执行器假写现场；新增任务模板、静音、运行中限值调整、storage prepare、报表结果登记和 HTTP 请求模块。任务模板已补固定时长检测、合格持续检测、暂停/恢复、限值调整、特征值刷新、报表登记，并支持 `optional/default` 参数绑定。最新迭代明确“HTTP 配置/查询，现场动作走 STRING 虚拟变量触发任务系统”：`builtin.start_detection_run` 支持 `custom_items/limit_check_enabled/end_policy/qualified_hold_ms`，运行中限值支持 `items` 批量调整，`builtin.mute_detection_alarms` 已通过真实 smoke 证明可在当前报警进入后返回 `muted=1`，`builtin.refresh_features` 会刷新特征值、写 `features_updated` 事件并推送 `detection.features_updated` 通知，固定时长和合格持续守护支持后端启动恢复；检测启动/结束已派发 `project_start/project_end` 生命周期任务流；`schedule` 已接入轻量扫描器，`schedule_interval_ms` 控制定时间隔。`task_params` 已区分检测配置、工艺/报表参数和 PLC 下设参数；进风口面积等参数可从同一 `process_params` 来源冻结到检测追溯，并由 `builtin.write_control_variables` 受控写入 PLC；已新增集成测试证明同一 STRING 请求会先执行 PLC 下设，再启动检测并冻结 `process_params/plc_writes`。 |
| EB-046 | `frontend-ai` | feature | open | 侧边菜单新增“任务”模块，承载后端业务编排和 EB-045 task-flows。 | 模板实例化入口、后端模块 action 下拉、`steps_json` 数组编辑、`schedule_interval_ms` 表单、执行记录表、SQL 日志表和 `task-modules.params_schema` 动态参数表单已接入；任务页新增“发送任务请求”入口，可选择 watched `STRING` 虚拟变量并结构化填写检测启动参数、`process_params` 工艺/报表参数和 `plc_writes` PLC 下设计划，通过 WS `command.write_variable` 写入虚拟变量触发任务系统，不使用手动运行接口；弹窗已补“请求 JSON 预览”和监听任务流提示，显示最终写入 STRING 虚拟变量的内容，明确配置参数包含检测配置、过程参数和 PLC 下设引用，并提示 PLC 下设行必须选择 `var_id` 才会进入最终 JSON。最新迭代把后端已有的 `stop_detection/pause_detection/resume_detection/mute_detection_alarms/storage_snapshot/storage_prepare/update_detection_limits/refresh_detection_features/register_report/http_request` 纳入同一正式请求入口，并按命令显示运行控制、宽表准备、运行中限值调整、报表登记和 HTTP 调用参数，预览 payload 会自动去掉无关字段。Playwright 已验证 `/task-flow-templates`、`/task-modules`、`/task-flows`、`/task-flow-runs`、`/task-flow-runs/:id/sql-logs` 200，并验证任务请求弹窗渲染配置参数、进风口面积默认参数、监听任务流提示、命令切换和 JSON 预览；端到端 smoke 已证明通过 WS 写 watched STRING 虚拟变量可触发后端 task-flow run success。下一步继续收敛暂停/恢复、运行中限值等高频业务入口的易用性。 |
| EB-047 | `backend-ai`/`frontend-ai` | feature | open | 12 项目批量初始化、KIO 变量归属和业务名重映射，为检测业务端到端测试准备真实项目变量。 | 后端 `POST /api/v1/variables/bulk-remap/kio-projects` 已完成并在本地库执行：确保 `AC-01..AC-12` 项目，把 `台N_xx` 原名变量分到项目 N，保留 `raw_name/source_path` 和写入配置，写入业务 `var_name`、三语显示名、变量分组、启用状态和 disabled 默认 storage route，并重载运行态；未启用默认 route 会随业务名同步列名，已启用 route 不自动改；批量 remap 响应项已返回 `var_id_text`。前端已在设置中心变量模块提供“KIO 项目初始化”入口，支持 dry-run 预览、执行确认、结果摘要和明细表；Playwright smoke 已验证 dry-run 请求 200 且请求体 `dry_run=true`。下一步继续完成页面层 `device` 命名收敛和真实执行前的现场确认流程。 |
| EB-047 | `frontend-ai` | cleanup | open | 前端“设备”到“项目”迁移闭合，移除页面层旧 device 命名和测试缺口。 | 三语可见文案中的“项目/设备”“设备/项目”等混合说法已清理；工位页开始检测表单页面层已改用 `project_id` 提交，左侧项目二级菜单 smoke 确认 URL 使用 `project_id`。设置页变量分配、批量分配、虚变量创建、storage route 变量联动、检测标准弹窗，以及检测配置页标准保存、任务页项目筛选/任务创建/任务请求弹窗均已收口到 `project_id/project_code` 页面语义；旧 `device_id/device_code` 仅在 DTO 兼容层或 helper fallback 保留。最新 Playwright 已验证 `/api/v1/projects` 200、`/api/v1/devices` 404、检测配置新增标准弹窗无 `device_id` 字段、任务新增和任务请求弹窗可见项目字段。下一步继续清理 shared type/edge-status API 的别名命名，并补变量分配、创建虚变量、检测启动/停止、storage routes 项目筛选的完整关闭 smoke。 |
| EB-048 | `frontend-ai` | feature | open | 变量属性设定和任务系统二次适配闭合。 | 按下方 EB-048 清单接入变量资产属性、storage routes、任务模块/模板、执行记录和正式 STRING 虚拟变量触发链。重点变化：变量不再承载存储策略，检测业务限值不等同变量默认限值，任务正式参数不走手动运行弹窗，页面不得直接调用 KIO 下设绕过后端变量写 service。后端已补 `GET /api/v1/detection-runs/current?project_id=...`，前端可用它拿当前 running/paused run snapshot 来拼实时 OK/NG；全局顶栏通知中心已接入 HTTP 未读数、通知列表、单条已读、全部已读、WS `topic=notifications` 即时刷新、通知目标跳转，并支持未读、通知类型和项目筛选。后端已补 `GET/PUT /api/v1/projects/:id/members`，前端设置页用户管理已接入项目成员和通知接收开关配置，保存走 `PUT /api/v1/projects/:id/members`。任务页已提供正式 STRING 虚拟变量请求入口和 JSON 预览；shared API 中变量、实时快照、bulk remap、任务流、报警、历史和特征值相关 `var_id` 已统一为 `VarIdentifier`，避免页面层把大 ID 固化成 number。正式业务请求端到端 smoke 已由 `backend/tools/smokeeb045` 覆盖启动、配置参数冻结、PLC 下设计划、`storage_prepare`、运行中限值调整、检测静音 `muted=1`、报表登记、受控 HTTP 请求、暂停、恢复、任务流停止、WS 异常停止、特征值刷新事件、通知 WS/HTTP 未读已读联动、固定时长自动结束、合格持续自动结束、检测生命周期 `detection.run_started/run_paused/run_resumed/run_stopped/run_abnormal_stop` 通知、检测 OK 结果通知、检测业务超限报警进入/等级变化/恢复通知、变量默认报警进入/等级变化/恢复通知、默认报警落库查询、WS 写请求审计、WS 异常停止命令审计、任务流控制变量写入审计和检测 NG 结果通知；剩余主要是前端低代码编辑体验和更多现场数据组合继续收敛。 |
| EB-049 | `backend-ai`/`review-ai` | future-plan | open | 边缘端与主服务器协同：数据库同步只负责只读镜像，控制命令回到边缘端执行，主服务器基于同步到位的数据执行 Excel 报表任务和通知。 | 已新增 `backend/docs/边缘端与主服务器协同计划.md` 并同步到全链路图。未来实施前需要确认命令通道 HTTP/RabbitMQ、命令幂等、边缘端 `detection_report_requests` 数据就绪表、主服务器报表任务表、主服务器通知中心和数据到位检查策略；当前不允许主服务器直接改同步库里的边缘端业务表来控制现场。 |

### EB-019/020/021 前置设计文档

- `backend/docs/变量属性检测配置与WS前置设计.md`
- `backend/docs/后端日志WS检测实施顺序.md`
- `backend/docs/运行态清洗与实时业务视图设计.md`
- `backend/docs/变量默认上下限与业务报警装配设计.md`
- `backend/docs/任务触发动态存储与双报警实施计划.md`
- `backend/docs/任务变量流转与内置模块设计.md`
- `backend/docs/边缘端全链路数据流转与分发图.md`
- `backend/docs/边缘端与主服务器协同计划.md`
- 结论：WebSocket 读写合一可行，但 WS 写和检测超限必须以完整变量属性和检测配置为前置。
- 当前实施顺序：P0 日志/审计地基 -> P1 变量属性与写入约束 -> P2 检测配置语义补齐 -> P3 WS 实时读 -> P3.5 运行态清洗/topic 索引/项目设备视图 -> P4 WS 写命令 -> P5 检测事件与超限记录 -> P6 联调回归。
- 后端任务拆分：P0=EB-020 日志/审计地基；P1=EB-024 变量属性、存储映射、写入约束；P2=EB-025 检测配置和运行快照；P3=EB-026 WS 只读实时；P3.5=EB-033 运行态清洗、topic 索引和项目设备实时视图；P4=EB-027 WS 写命令；P5=EB-028 检测事件/超限/摘要；P6=EB-029 测试联调。P3 之前不做 WS 写，P4 之前所有写操作必须先有审计。
- P1 旧存储映射结论已废弃：变量属性不再描述入库业务名、存储目标、目标表/列、动态表单字段 key、查询别名、存储频率或首帧存储；这些全部归 `sys_storage_routes`。
- 自动发现变量只生成来源候选；只有完成项目绑定、业务属性、写入属性和检测标准配置后，才允许参与检测、历史存储、超限判断或 WS 写命令。
- JGHJ 旧后端参考已纳入设计：旧 `sys_variables` 包含读写模式、提取路径、缩放偏移、报警上下限、存储策略、防抖和计算来源；旧运行态 `Tag` 保存 current/last value、字符串值、质量码、防抖窗口和报警状态。
- EB-041 后端破坏性改名后，项目统一使用 `sys_projects.project_id/project_code` 语义；旧 `/devices`、`device_id`、`device_code` 后端契约不再保留。`site_no` 只作为站点/项目编号或辅助分组。
- EB-033/041 追加固定：自动发现变量不是运行态变量。发现入库默认 `enabled=false`、`project_id=null`；只有分配项目并启用后的变量才进入 `TagManager` 清洗、WS、历史存储或检测后续业务。
- EB-033 旧暂缓项已拆分：报警 enter/recover 首段已完成，level_change/静音/运行中标准调整归 EB-039；KIO/WS 变量下设完整闭环归 EB-019/027；多变量组合触发任务和倒排索引归 EB-045 `sys_task_flows + var_id -> flow_ids`。
- 变量默认上下限属于变量资产属性，检测业务实际判定仍以检测标准/运行快照为准；运行中修改变量默认属性只有显式 `apply_to_running=true` 时才同步 running 任务的 `variable_default_*` 快照，不覆盖业务标准 `limit_*`。
- 检测启动首帧存储和首帧判断分开：`storage_routes.store_on_start` 决定检测开始是否投递当前值到存储队列，检测标准项 `check_on_start` 决定检测开始是否立刻用当前值做一次业务判断；`check_cycle_ms` 独立于存储 route 周期。
- 新总计划固定后续拆分：任务触发系统只通过倒排索引触发 service 动作；动态存储使用按项目宽表和 `sys_storage_routes/detection_run_storage_routes`；检测业务报警负责业务上下限、静音和等级变化；变量默认报警走统一超限表 `scope=default` 且不带静音。
- EB-045 修正任务流转结论：变量是一切任务业务的根基。普通业务优先做内置模块，前端填写的参数必须写入 STRING 虚拟变量；任务系统由变量变化触发，解析 `trigger_value` JSON 为 `task_params`，`steps_json` 承载多步骤链，执行期间通过 run context 传递中间变量；JavaScript 只作为开发者高级模块。
- EB-045 追加配置参数边界：`task_params` 不是只有检测上下限，还包含工艺/报表参数和 PLC 下设参数。工艺参数如进风口面积应保存在 `process_params` 并冻结到本次检测；若需要写 PLC，则由显式任务步骤调用后端变量写 service，走写入约束、KIO/PLC service、审计日志和可选回读确认。
- 2026-05-30 审查结论：`sys_task_rules` 相关 EB-038/043 已过时并关闭，统一由 EB-045 `sys_task_flows + var_id -> flow_ids` 承载；“变量默认报警独立表”设计已废弃，EB-040 改为统一超限事件语义；EB-021/028/044 后端首段已完成并关闭。

### EB-047 前端项目迁移闭合要求

- 后端契约已经破坏性切换为项目语义：前端页面层不得继续新增 `device/device_id/device_code` 命名。
- shared API 兼容层可以短期把旧页面别名映射到 `project_id/project_code`，但必须集中在 `desktop/src/features/edge-status/api.ts` 或类型兼容段，不允许散落在页面组件。
- 必改范围：`desktop/src/shared/api/types.ts`、`desktop/src/features/edge-status/api.ts`、`StationOperationPage`、`HistoryQueryPage`、`SettingsPage`、`DetectionConfigPage`、`TaskFlowsPage` 和三语 i18n 文案。
- URL/query 参数应优先使用 `project_id`；旧 `device_id` 查询参数只允许做一次性读取兼容，页面内部状态必须转成 `project_id`。
- 表单字段统一用 `project_id`，包括变量分配、虚变量创建、检测启动、检测配置筛选、storage route、task-flow 项目筛选。
- 文案统一为“项目”，不再把项目称作设备；真正物理设备/采集站点另用 gateway/source 表达。
- 关闭 EB-047 前必须跑：`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test`。
- smoke 必须记录：登录后 `GET /api/v1/projects` 200、`GET /api/v1/devices` 404、变量分配到项目、创建虚变量、检测启动/停止、历史按 `project_id/task_id` 查询、storage routes 项目筛选、任务页项目筛选和任务创建。

### EB-048 前端变量属性与任务系统交付清单

#### 变量属性设定

- `GET /api/v1/variables` 已实现变量列表，支持 `gateway_id/enabled/discovered/source_type/keyword` 过滤。该接口返回配置属性，不用于高频实时刷新；项目页实时值使用 `GET /api/v1/realtime/variables?project_id=...` 或 `var_id=...`。
- `POST /api/v1/variables` 已支持创建 `source_type=virtual` 或 `manual` 变量。虚拟变量支持 `INT/FLOAT/BOOL/STRING`，可传 `project_id/project_code/var_group/var_name/display_name/display_name_en/display_name_ja/data_type/unit/decimal_places/scale_factor/offset_val`。
- `PATCH /api/v1/variables/{variable_id}` 已支持编辑变量资产属性：三语显示名、业务名、类型、单位、小数位、缩放/偏移、清洗防抖、写入约束、默认上下限/默认报警和启用状态。`apply_to_running=true` 只同步当前 running 任务里的 `variable_default_*` 快照，不覆盖检测标准业务限值，前端必须给二次确认。
- `PATCH /api/v1/variables/{variable_id}/assignment` 用于把候选变量分配到项目并启用，字段为 `project_id/project_code/var_group/enabled`。
- `POST /api/v1/variables/bulk-remap/kio-projects` 是现场初始化工具，支持 `dry_run`，用于把 `台N_xx` KIO 原名批量归属到 `AC-01..AC-12`，并生成 disabled 默认 storage route。前端可放在调试/初始化入口，不应放成普通用户日常操作。
- 写入约束 UI 必须保留：`writable=true` 时要求 `rw_mode=W|RW`、`write_path`、`write_data_type`，可选 `write_min/write_max/write_enum/write_requires_audit`。普通变量写入必须走 WS `command.write_variable` 或后端内置 `builtin.write_variable`，不允许页面直接调用 MQTT/KIO。
- 变量默认报警字段是资产默认值：`default_alarm_enabled/default_limit_ll/default_limit_l/default_limit_h/default_limit_hh/default_limit_deadband/default_violation_hold_ms/default_recover_hold_ms`。它们不等同检测业务运行限值；检测业务实际判定仍看检测标准项和运行快照。
- 超限报警追溯统一走 `GET /api/v1/limit-alarms`，`scope=default` 查询变量默认报警，`scope=detection` 查询检测业务报警；不传 `scope` 时返回两类报警，前端可用它承载报警列表、筛选和追溯入口。`status` 查询支持 `active/recovered/closed`，其中 `closed` 是查询别名，返回体仍使用内部状态值 `recovered`；响应带 `var_id_text`，前端筛选/跳转优先使用字符串 ID。
- 桌面 shared API 已追平 `limit-alarms`：`LimitAlarm`、`LimitAlarmListParams`、`LimitAlarmListResponse` 类型和 `getLimitAlarms(params)` client 已可用；工位页报警日志已接入弹窗展示。
- 原变量级存储字段已废弃并从后端变量 API 删除：前端不得展示或提交 `store_*`、`storage_table`、`storage_value_column`、`storage_target`、`startup_snapshot_enable` 等旧字段。

#### Storage Routes

- `GET/POST/PATCH/DELETE /api/v1/storage-routes` 已实现存储路由 CRUD。字段为 `project_id/var_id/route_code/storage_target/table_name/column_name/column_type/form_field_key/query_alias/trigger_mode/cycle_ms/deadband/store_on_start/enabled`。`var_id` 支持字符串形式的大 64 位 ID，响应带 `var_id_text` 供前端精确回传。
- `GET /api/v1/storage-routes` 支持 `project_id/var_id/enabled` 过滤。前端变量详情可以显示该变量的路由列表，存储配置页必须按项目筛选；筛选大 ID 时优先传字符串 `var_id`。
- `GET /api/v1/detection-runs/{id}/storage-routes` 已实现运行任务冻结路由查询，返回 `{items,count}`；前端任务详情可用它展示当次检测实际使用的表、列、触发方式、周期、死区和首帧存储配置。
- `storage_routes.store_on_start` 控制检测开始时是否做首帧存储；检测首帧判断由检测标准项 `check_on_start` 控制，二者不能在 UI 上绑成同一个开关。
- 创建/启用 route 后，后端会在检测启动或 schema prepare 阶段准备项目宽表和缺失列；移除变量不删除历史宽表列，前端文案不要暗示会删列。

#### 检测配置和任务运行接口

- 前端选择/编辑“检测配置”时走 HTTP CRUD，不直接查数据库：`GET /api/v1/detection-standards?project_id=...&enabled=true` 列表，`GET /api/v1/detection-standards/{id}` 取标准项，`POST/PATCH/PUT /api/v1/detection-standards` 增删改标准和标准项。标准项 `var_id` 支持字符串形式的大 64 位 ID，响应带 `var_id_text`；选择当前检测配置本质上是在启动检测时传 `standard_id`，或由 task-flow 的 `builtin.start_detection_run` 参数传入 `standard_id`。
- 前端展示“当前运行检测配置”时不要再读最新标准表，要读运行快照：优先 `GET /api/v1/detection-runs/current?project_id=...`，它会返回该项目当前 running 或 paused 的检测详情；也可用 `GET /api/v1/detection-runs?project_id=...&status=running&limit=1` 或先 `GET /api/v1/detection-runs/active` 找到 `task_id`，再 `GET /api/v1/detection-runs/{id}`。返回体里的 `standard_items` 是本次检测冻结快照，字段包含 `check_enabled/alarm_enabled/store_enabled/check_cycle_ms/check_on_start/limit_*/limit_deadband/violation_hold_ms/recover_hold_ms/quality_policy/variable_default_*`。
- 实时 OK/NG 展示推荐组合：WS 或 `GET /api/v1/realtime/variables?project_id=...` 提供实时值；HTTP `GET /api/v1/detection-runs/{id}` 提供当前 run 配置快照；HTTP `GET /api/v1/detection-runs/{id}/summary` 和 `/events` 提供任务级结果/事件。不要把上下限配置塞进实时变量 WS 消息里，否则通用实时通道会被某一类业务污染。
- 即时通知推荐接 WS：`topic=notifications` 会收到 `notification.event`，payload 为 `RuntimeNotification`。当前类型包括 `alarm.limit.enter/recover/level_change`、`detection.run_started/stopped/abnormal_stop/paused/resumed`、`detection.result_ok/result_ng`、`detection.features_updated`。断线补历史和未读状态走 HTTP 通知中心接口：`GET /api/v1/notifications`、`GET /api/v1/notifications/unread-count`、`POST /api/v1/notifications/{id}/read`、`POST /api/v1/notifications/read-all`。通知列表支持 `unread/type/level/project_id/limit/offset`，返回 `{items,total,limit,offset}`，`payload` 是 JSON 对象；变量相关通知和 WS 事件都带 `var_id_text`。
- 项目通知接收人配置走 `GET /api/v1/projects/{id}/members` 和 `PUT /api/v1/projects/{id}/members`，权限为 `manage_users`。`GET` 空成员列表固定返回 `items:[]`，不返回 `null`。`PUT` 请求为 `{members:[{user_id,member_role,notify_enabled}]}`；配置后 `target_type=project` 只发给启用用户且 `notify_enabled=true` 的项目成员，未配置成员的项目临时全员兜底。
- 检测标准项已支持业务报警和判断周期：`alarm_enabled/check_cycle_ms/check_on_start/check_method/target_value/limit_deadband/violation_hold_ms/recover_hold_ms/quality_policy/limit_ll/limit_l/limit_h/limit_hh/store_enabled/check_enabled`。
- 运行任务接口已支持暂停/恢复和结果查询：`POST /api/v1/detection-runs/{id}/pause`、`POST /api/v1/detection-runs/{id}/resume`、`GET /api/v1/detection-runs/{id}/summary`、`GET /api/v1/detection-runs/{id}/events`、`GET /api/v1/detection-runs/{id}/features`；特征值和历史数据行同样返回 `var_id_text`。
- `duration_ms`/固定时长结果会扣除暂停时长；前端展示累计检测时长时应使用后端 summary 或任务字段，不要自行用 wall-clock 简算。

#### 任务系统接口

- `GET /api/v1/task-modules` 已返回后端内置模块及 `params_schema`。前端任务编辑器应按 schema 动态渲染参数表单，当前模块包括检测启动/结束/暂停/恢复、固定时长守护、合格持续守护、报警静音、运行中限值调整、特征值刷新、storage snapshot/prepare、受控写变量、报表登记、HTTP 请求、context set、JavaScript。启动检测模块的 schema 已显式包含 `process_params` 和 `plc_writes`，用于承载进风口面积等工艺/报表参数以及 PLC 下设计划。
- `GET /api/v1/task-flow-templates` 已返回 13 个后端模板，覆盖变量请求启动/停止检测、固定时长检测、合格持续检测、暂停、恢复、存储快照、静音、运行中限值调整、特征值刷新、报表登记、受控写变量和独立 PLC 控制参数下设。变量请求启动、固定时长检测、合格持续检测模板都会先生成 `builtin.write_control_variables` 步骤，再生成 `builtin.start_detection_run` 步骤；`process_params/plc_writes/operator_note/report_template_id` 都可从 watched STRING 虚拟变量参数透传。前端应提供“从模板创建任务”，再允许用户编辑参数绑定。
- `GET/POST/PATCH /api/v1/task-flows` 已实现任务流配置，`GET /api/v1/task-flows/{id}` 已实现单条详情。核心字段：`project_id/flow_code/name/enabled/trigger_type/condition_script/action_type/action_script/action_payload/steps_json/timeout_ms/cooldown_ms/hold_ms/schedule_interval_ms/priority/remark/vars`。`trigger_type=schedule` 时前端应填写 `schedule_interval_ms`，单位毫秒；过渡期若未填写但有 `cooldown_ms`，后端会用 `cooldown_ms` 作为定时间隔。保存 `schedule` 任务时必须满足 `schedule_interval_ms > 0` 或 `cooldown_ms > 0`，否则返回 `400`。创建时 `timeout_ms` 可省略使用后端默认值，但时间字段不能为负；编辑时如果显式提交 `timeout_ms`，必须大于 0。`PATCH` 已支持修改 `project_id`，替换 `vars` 时后端会按新项目同步变量绑定。创建必须提供 `project_id > 0`、非空 `flow_code` 和非空 `name`；编辑时若提交这些字段，`project_id=0` 或空白编码/名称会返回 `400`。创建、编辑和列表筛选会校验 `trigger_type/action_type/vars.role/vars.var_id/steps_json[].module/steps_json[].params.*.source`，其中 `vars[].var_id` 必须大于 0，`trigger_type=data_change` 必须至少有一个 `watch` 变量，参数绑定 `source` 只允许 `literal/trigger_param/event/context`；`source=event` 的 `key` 只允许 `trigger_type/project_id/trigger_var_id/trigger_var_id_text/trigger_value/gateway_id/topic/origin_flow_id/origin_run_id/depth/request_id/at`。`steps_json` 允许省略以使用旧单动作字段，也允许单个 step object 调试容错，但显式 `[]` 会返回 `400`，显式 step `code` 不能重复，避免保存无动作任务或运行上下文互相覆盖。非法值返回 `400`，前端应直接展示后端错误并阻止保存。
- `DELETE /api/v1/task-flows/{id}` 已实现删除任务流配置和变量绑定；执行历史 `task_flow_runs/task_flow_sql_logs` 保留。
- `steps_json` 推荐由前端发送 JSON 字符串化后的 step 数组：`[{code,module,params,script}]`。后端为了调试容错也接受单个 step object，但前端正式生成必须用数组；同一任务里的显式 `code` 必须唯一，因为执行结果和上下文变量会按 `code` 写入。
- `vars` 用于声明变量角色，`watch` 变量进入 `var_id -> flow_ids` 倒排索引，`read/write` 用于 UI 和审阅意图。正式业务入口要求 `watch` 一个 `STRING` 虚拟变量。
- 正式业务参数不再来自 `POST /task-flows/{id}/run` 弹窗。前端应把用户填写的参数 JSON 写入 watched `STRING` 虚拟变量，例如 `{"command":"start_detection","project_id":1,"standard_id":5,"duration_sec":3600,"process_params":{"inlet_area_m2":1.25},"plc_writes":[{"var_id":"3901","value_from":"process_params.inlet_area_m2"}]}`，由数据变化触发任务系统并解析为 `task_params`。
- 任务参数需要分层展示：检测配置字段用于 `builtin.start_detection_run`；工艺/报表参数进入本次检测冻结配置；PLC 下设字段只能由后端受控写变量模块执行，前端不得直接调用 KIO/MQTT。进风口面积这类值只在 `process_params` 填一次，`plc_writes.value_from` 引用它，不放进 `custom_items`。
- `POST /api/v1/task-flows/{id}/run` 只保留开发者调试触发，不承载正式业务参数。
- `GET /api/v1/task-flow-runs`、`GET /api/v1/task-flow-runs/{id}`、`GET /api/v1/task-flow-runs/{id}/sql-logs` 已实现执行记录和 SQL 日志查询，列表支持 `project_id/flow_id/flow_code/status/trigger_type/trigger_var_id/origin_flow_id/from/to/limit/offset`。`status` 只接受 `pending/running/success/failed/timeout/skipped`，`trigger_type` 只接受 `manual/data_change/schedule/project_start/project_end`，执行记录列表要求筛选 ID 为正数、`from <= to`、`limit > 0`、`offset >= 0`，SQL 日志查询要求 `limit > 0`；非法枚举、ID、时间范围或分页返回 `400`，执行记录不存在返回 `404`，避免前端传错后误判为空列表或空日志。
- 后端 service 公共错误映射已统一：缺失数据库记录返回 `404`，引用保护和同项目重复 running 返回 `409`，普通校验错误返回 `400`。前端详情页、任务页和设置页遇到 `404` 时应展示“资源不存在或已删除”，不要当作表单参数错误处理。
- 任务内 JavaScript 可用，但普通业务优先选择内置模块。允许开发者脚本通过后端受控能力访问 realtime、storage、db，但前端要把它标记为高风险开发者功能，并展示 SQL 日志。

#### 前端验收

- 必跑：`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`。
- HTTP smoke：登录后验证 `GET /api/v1/variables`、`GET /api/v1/storage-routes?project_id=1`、`GET /api/v1/task-modules`、`GET /api/v1/task-flow-templates`、`GET /api/v1/task-flow-runs?limit=10`。
- 业务 smoke：创建 STRING 请求虚拟变量，按模板创建 task-flow，WS `command.write_variable` 写入 JSON，确认 `task_flow_runs` 成功，并能看到检测启动/停止、可选存储、可选报警、暂停/恢复、特征值刷新至少一条链路。后端可重复现场链路使用 `backend/go run -tags smoke_tools ./tools/smokeeb045`，它会创建临时项目、STRING 请求变量、虚拟控制变量、测量变量、默认报警变量和 data_change flows，并验证启动检测冻结 `custom_items/process_params/plc_writes`、storage route 冻结、`builtin.storage_prepare`、运行中限值调整、`builtin.mute_detection_alarms` 返回 `muted=1`、`builtin.register_report`、`builtin.http_request`、暂停、恢复、任务流停止、WS 异常停止、`builtin.refresh_features` 写事件、`detection.features_updated` 通知的 WS 即时推送和 HTTP 未读/已读、固定时长自动结束、合格持续自动结束、检测生命周期 `detection.run_started/run_paused/run_resumed/run_stopped/run_abnormal_stop` 通知、`detection.result_ok` 通知、检测业务报警 `enter/level_change/recover`、变量默认报警 `enter/level_change/recover`、默认报警 `scope=default` 落库查询、WS 写请求审计、WS 异常停止命令审计、任务流控制变量写入审计和停止后的 `detection.result_ng` 通知；这些通知均验证 WS 即时推送和 HTTP 未读/已读。

## EB-018 施工流程

文档位置：

- 主设计文档：`后端模块化审阅与演进计划.md`
- 业务约束文档：`多设备并行检测任务设计计划.md`
- 后端现状文档：`backend/docs/backend-architecture.md`
- 交互看板：`AI_BOARD.md`

施工原则：

1. 只做中等粒度拆分，不做形式化过度分层。
2. 不新增 `internal/detection` 包，避免和 `internal/services/detection_runs_service.go` 形成双入口。
3. 新增后端接口必须走 `runtime/handlers + services`，不要继续直接塞进 `runtime/kernel.go`。
4. `database.Repository` 可以按文件拆分，但先不拆 package，对外方法名保持兼容。
5. `models.go` 可以按文件拆分，但先不拆 package，GORM table name、JSON 字段和 API DTO 不变。
6. 前端可见 API、DTO、错误码、权限语义变化，必须同步 `AI_BOARD.md`，并追平 `desktop/src/shared/api/types.ts` 和 feature API。

施工顺序：

1. P0 冻结边界：确认 `runtime/handlers` 只处理 HTTP 入参、权限注册、状态码和响应；`services` 处理业务不变量、状态转换、repo/cache 协调；`database` 处理持久化；`pipeline` 保持实时热路径。
2. P1 新业务不进旧大文件：虚变量、检测任务、报表模板、后续主站控制入口都继续使用 handler/service；新增错误使用明确 error，并在 handler 映射 HTTP 状态码。
3. P2 拆旧 `kernel.go`：按 `users_handler.go`、`devices_handler.go`、`history_handler.go`、`detection_standards_handler.go`、`gateways_handler.go`、`kio_handler.go` 顺序迁移旧路由，迁移期间保持 URL、权限、DTO、错误语义不变。
4. P3 拆 `repository.go` 文件：拆成 `auth_repo.go`、`gateways_repo.go`、`variables_repo.go`、`devices_repo.go`、`history_repo.go`、`detection_repo.go`、`reports_repo.go`；仍保留同一个 `database` package 和同一个 `Repository` 类型。
5. P4 拆 `models.go` 文件：拆成 `auth.go`、`gateway.go`、`device.go`、`variable.go`、`history.go`、`detection.go`、`report.go`、`runtime_tag.go`；仍保留同一个 `models` package。
6. P5 前端契约追平：补 `source_type`、`createVariable`、`DetectionRun` 列表/详情 DTO、`ReportTemplate` DTO、start/stop/abnormal-stop mutation、`history/data?task_id` 查询入口。

每阶段验收：

- 后端改动后至少运行 `gofmt`、`backend/go test ./...`、`backend/go build ./cmd/edge-backend`。
- API/DTO/错误语义变化必须在本看板追加 Activity Log。
- 迁移旧路由时不得改变现有 URL、权限 capability、JSON 字段和状态码。
- 前端可见变更必须由 `frontend-ai` 在 EB-016 或新任务中接入 typed API。
- 如果某阶段无法跑测试，必须在 Activity Log 写明未跑原因。

明确禁止：

- 禁止把检测运行再拆出第二套 `internal/detection` 入口。
- 禁止为每个 endpoint 建一个 service。
- 禁止为普通 CRUD 引入无实际收益的 interface。
- 禁止让 handler 直接操作 `TagManager` 热路径；需要通过 service 协调。
- 禁止把报表模板混进检测标准，二者是相关但独立的业务概念。

## Local Test Credentials

以下凭据只用于本地联调和实验环境，生产交付前必须替换。

| 用途 | 位置/接口 | 账号/标识 | 密码/Token | 备注 |
| --- | --- | --- | --- | --- |
| Edge 本地登录 | `POST /api/v1/auth/login` | `admin` | `Admin@12345` | 默认 bootstrap 管理员，拥有 `admin` 角色全部本地 capability。 |
| 本地 MySQL | `backend/configs/config.json` | `root` | `root` | 数据库名 `spindle_edge`。 |
| 默认 MQTT broker | `backend/configs/config.json` gateway `default-kingiot-kio` | `Admin` | `admin` | broker `tcp://127.0.0.1:1883`，topic `datachange_S_KIO_Project`。 |
| KIO 写入凭据 | `backend/configs/config.json` gateway `default-kingiot-kio` | `sa` | `C12E01F2A13FF5587E1E9E4AEDB8242D` | 用于 KIO write payload，不是 MQTT broker 登录。 |
| 主站 service token | `auth.service_clients` 或 `EDGE_MAIN_SERVICE_TOKEN` | 待配置 | 待配置 | 当前运行配置为空；单元测试中的 `main-token` 仅测试用，不是本地运行凭据。 |

## Board Rules

1. 开工前先声明身份，再读 Active Board。
2. 新问题必须加到 Active Board，分配合法 `Owner`。
3. 解决后把闭合结论、施工说明和测试证据迁移到对应稳定文档或 `.ai/docs/archive/`；`AI_BOARD.md` 只保留仍未闭合的项。
4. 无法推进时改为 `blocked`，写清缺什么。
5. API、DTO、错误语义、页面范围变化，先更新 Active Board，再同步稳定契约文档。
6. `test-ai` 必须记录验证命令、通过/失败结果、未跑原因。
7. `review-ai` 必须优先列风险、缺口和阻塞。
8. 最终回复必须说明身份、处理的 Board 项、仍 open/blocked 的项。
9. 不按固定条数裁剪看板；清理看板时只迁移 `closed`，所有 `open/blocked` 必须保留。
10. Activity Log 的 `open` 只表示当前仍需要交接的信息；每条 `open` 记录必须关联 Active Board 项或写清关闭条件。若记录只是历史证据或阶段说明，应先把耐久结论合并到对应 EB 行、稳定文档或归档文件，再转为 `closed` 并迁出。

## Activity Log Format

```text
- YYYY-MM-DD HH:mm | <frontend-ai/backend-ai/test-ai/review-ai> | <question/decision/answer/blocker/review/test> | <影响范围> | <open/closed/blocked>
```

## Activity Log

- 2026-05-31 08:08 | backend-ai/frontend-ai/test-ai | test/api | 设置页 smoke 和项目成员空列表契约补齐：新增 `desktop/scripts/smoke-settings.mjs` 与 `desktop/npm run smoke:settings`，覆盖桌面登录、`GET /api/v1/projects`、旧 `/api/v1/devices=404`、`GET /api/v1/projects/{id}/members`、设置页项目成员 UI，以及“运行日志预览”使用居中 `Modal` 且无侧边 `Drawer`；后端 `ListProjectMembers` 空结果规范为 `items:[]`，前端 settings 端兼容空数组；kernel MQTT publish/subscribe 回归允许本机 broker 可达时返回 200。验证通过 `go test ./internal/database ./internal/runtime/handlers`、`go test ./internal/runtime -run TestKernelAuthAndBusinessRoutes -count=1`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 72.4%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`、`desktop/npm run lint`、`desktop/npm run test -- --run`、`desktop/npm run build`、`desktop/npm run smoke:settings`；后端已重启 PID 35628，`/health` ok，tags=1100。 | closed

- 2026-05-31 08:05 | frontend-ai/test-ai | feature/test | EB-048 项目成员配置 UI 接入：设置页用户管理模块新增项目成员配置区，可按项目读取 `GET /api/v1/projects/{id}/members`，添加/移除本地用户、编辑成员角色和通知接收开关，并通过 `PUT /api/v1/projects/{id}/members` 保存。验证通过 `desktop/npm run lint`、`desktop/npm run test -- --run`、`desktop/npm run build`，后端 `/health` ok；Playwright 登录设置页后切到用户管理，确认“项目成员/通知接收人/接收通知”渲染。 | closed
- 2026-05-31 07:38 | backend-ai/test-ai | api/test | EB-019 项目通知成员收敛首段落地：新增 `sys_project_members`、`GET /api/v1/projects/:id/members`、`PUT /api/v1/projects/:id/members`，项目通知在配置成员后按 `notify_enabled=true` 的启用用户生成 `sys_notification_recipients`，未配置成员的项目继续全员兜底以避免升级后通知无人接收；`smokehealth` 已追加项目成员接口真实检查。验证通过定向 repository/handler/model/smokehealth 测试、`go test ./...`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 72.4%、`go vet ./...`、`go vet -tags smoke_tools ./tools/smokehealth`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 15684，`/health` ok，KIO gateway active，tags=1100，真实 `smokehealth` 通过。 | closed
- 2026-05-31 07:36 | frontend-ai/test-ai | feature/test | 驾驶舱按 Codex 官网产品页气质重做视觉层级：`/#/model-cockpit` 改为低饱和浅灰蓝背景、细网格、白色产品面板式玻璃卡、深色高对比文字和克制标题背板，保留工位同源动态暖光边缘；修复进入 3D 驾驶舱时继承滚动位置导致首屏整体上移的问题，并重新平衡 Three.js 相机、缩放和模型贴底位置。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，Playwright 1920×1080 截图 `output/playwright/model-cockpit-codex-style-pass5.png`。 | closed
- 2026-05-31 07:27 | backend-ai/test-ai | test/tool | EB-006 后端 health/API smoke 工具落地：新增 `backend/tools/smokehealth`，使用 `go run -tags smoke_tools ./tools/smokehealth` 验证 `/health`、登录、`auth/me`、projects、variables、runtime channels、gateway configs、task modules/templates/runs、notifications unread count、active detection runs，以及旧 `/api/v1/devices` 返回 404。验证通过 `go test -tags smoke_tools ./tools/smokehealth`、真实 smoke、`go test ./...`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 72.3%、`go vet ./...`、`go vet -tags smoke_tools ./tools/smokehealth`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；`/health` ok，KIO gateway active，tags=1100。 | closed
- 2026-05-31 07:21 | backend-ai/test-ai | test | EB-006 MQTT/KIO payload 热路径回归补强：新增 `backend/internal/pipeline.TestProcessMessageKIOPayloadUpdatesOnlyIndexedKnownTags`，用真实 KingIOT/KIO `Objs/PVs` payload 直接触发 `processMessage`，验证业务名重映射后仍按原始 KIO `JSONPath` 更新数值/布尔/字符串变量、PVs fallback 可用、质量码进入 runtime snapshot，并证明 `gateway/topic` 索引不会误更新其他 topic 变量。验证通过 `backend/go test ./internal/pipeline -run TestProcessMessageKIOPayloadUpdatesOnlyIndexedKnownTags -count=1`、`backend/go test ./...`、`backend/go test ./... -coverprofile all-coverage.out` 总覆盖率 72.3%、`backend/go vet ./...`、`backend/golangci-lint run ./...` 0 issues、`backend/go build ./cmd/edge-backend`；`/health` ok，KIO gateway active，tags=1100。 | closed
- 2026-05-31 07:13 | backend-ai/test-ai | test/tool | EB-045/048 检测静音 smoke 补强：`backend/tools/smokeeb045` 在 NG 场景中于 `alarm.limit.enter` 后写入正式 STRING 请求 `{"command":"mute_detection_alarms","task_id":...}`，验证 `builtin.mute_detection_alarms` 通过 task-flow 返回 `muted=1`；随后继续写入更高超限值验证 `alarm.limit.level_change` 仍会触发，再写回合格值验证 `alarm.limit.recover` 和停止后的 `detection.result_ng` 仍走 WS/HTTP 未读已读闭环。现场实测通过：项目 `38`，请求变量 `735730343`，控制变量 `810150200`，测量变量 `755186266`，默认报警变量 `773322031`，主任务 `72`，固定任务 `73`，合格持续任务 `74`，异常停止任务 `75`，NG 任务 `76`。验证通过 `backend/go test -tags smoke_tools ./tools/smokeeb045`、`backend/go vet -tags smoke_tools ./tools/smokeeb045`、`backend/go run -tags smoke_tools ./tools/smokeeb045 -wait 12s`、`backend/go test ./...`、`backend/go test ./... -coverprofile all-coverage.out` 总覆盖率 72.3%、`backend/go vet ./...`、`backend/golangci-lint run ./...` 0 issues、`backend/go build ./cmd/edge-backend`。 | closed
- 2026-05-31 07:06 | backend-ai/test-ai | test/tool | EB-019/020/045/048 异常停止通知和审计 smoke 补强：`backend/tools/smokeeb045` 新增独立异常停止检测场景，通过正式 STRING 请求启动一条临时检测，再用 WS `command.detection.abnormal_stop` 停止，验证任务 `end_type=abnormal_stop`、`detection_run_events` 存在 `run_abnormal_stop`、`detection.run_abnormal_stop` 通过 WS 即时到达、HTTP 通知列表按同一 `event_uid` 读回并可标记单条已读，同时通过 `/api/v1/audit-logs?action=ws.command.detection.abnormal_stop&target_type=ws_command&target_id=command.detection.abnormal_stop&result=success` 查到成功审计。现场实测通过：项目 `37`，请求变量 `844112758`，控制变量 `861440106`，测量变量 `760675846`，默认报警变量 `801264795`，主任务 `67`，固定任务 `68`，合格持续任务 `69`，异常停止任务 `70`，NG 任务 `71`。验证通过 `backend/go test -tags smoke_tools ./tools/smokeeb045`、`backend/go vet -tags smoke_tools ./tools/smokeeb045`、`backend/go run -tags smoke_tools ./tools/smokeeb045 -wait 12s`、`backend/go test ./...`、`backend/go test ./... -coverprofile all-coverage.out` 总覆盖率 72.3%、`backend/go vet ./...`、`backend/golangci-lint run ./...` 0 issues、`backend/go build ./cmd/edge-backend`。 | closed
- 2026-05-31 06:56 | backend-ai/test-ai | test/tool | EB-019/045/048 检测生命周期通知 smoke 补强：`backend/tools/smokeeb045` 将 `topic=notifications` WS 提前到正式任务启动前打开，验证同一主检测任务的 `detection.run_started`、`detection.run_paused`、`detection.run_resumed`、`detection.run_stopped` 均通过 WS 即时到达，并通过 HTTP 通知列表按同一 `event_uid` 读回和标记单条已读。现场实测通过：项目 `36`，请求变量 `884347133`，控制变量 `864933664`，测量变量 `887256240`，默认报警变量 `816235002`，主任务 `63`，固定任务 `64`，合格持续任务 `65`，NG 任务 `66`。验证通过 `backend/go test -tags smoke_tools ./tools/smokeeb045`、`backend/go vet -tags smoke_tools ./tools/smokeeb045`、`backend/go run -tags smoke_tools ./tools/smokeeb045 -wait 12s`、`backend/go test ./...`、`backend/go test ./... -coverprofile all-coverage.out` 总覆盖率 72.3%、`backend/go vet ./...`、`backend/golangci-lint run ./...` 0 issues、`backend/go build ./cmd/edge-backend`。 | closed
- 2026-05-31 06:51 | backend-ai/test-ai | test/tool | EB-019/020/045/048 写操作审计 smoke 补强：`backend/tools/smokeeb045` 新增审计断言，启动检测的正式 STRING 请求会通过 `GET /api/v1/audit-logs?action=ws.command.write_variable&target_type=ws_command&target_id=command.write_variable&result=success` 按 `command_id` 查到 `ws.command.write_variable` 审计；任务流 `builtin.write_control_variables` 下设控制变量后会通过 `GET /api/v1/audit-logs?action=task_flow.write_variable&target_type=variable&target_id=<control_var>&result=success` 查到成功审计。第一次现场运行发现 smoke 匹配 JSON 空格过严，已修正为按 command_id 值匹配，并清理失败运行留下的临时 running 任务 `58`。最终现场实测通过：项目 `35`，请求变量 `731948864`，控制变量 `836796853`，测量变量 `749522000`，默认报警变量 `762652944`，主任务 `59`，固定任务 `60`，合格持续任务 `61`，NG 任务 `62`。验证通过 `backend/go test -tags smoke_tools ./tools/smokeeb045`、`backend/go vet -tags smoke_tools ./tools/smokeeb045`、`backend/go run -tags smoke_tools ./tools/smokeeb045 -wait 12s`、`backend/go test ./...`、`backend/go test ./... -coverprofile all-coverage.out` 总覆盖率 72.3%、`backend/go vet ./...`、`backend/golangci-lint run ./...` 0 issues、`backend/go build ./cmd/edge-backend`。 | closed
- 2026-05-31 06:44 | backend-ai/test-ai | test/tool | EB-019/040/045/048 默认报警 smoke 补强：`backend/tools/smokeeb045` 新增独立默认报警虚拟变量，先通过 `PATCH /api/v1/variables/{id}` 开启 `default_alarm_enabled/default_limit_h/default_limit_hh/default_limit_deadband/default_*_hold_ms`，再用 WS 写值 `43 -> 50 -> 25` 验证变量默认报警 `alarm.limit.enter`、`alarm.limit.level_change`、`alarm.limit.recover` 均通过 `topic=notifications` WS 即时到达，并通过 HTTP 通知列表按同一 `event_uid` 读回和标记单条已读；最后通过 `GET /api/v1/limit-alarms?scope=default&project_id=33&var_id=880491729&status=recovered` 读回 `task_id=0/alarm_type=above_hh` 默认报警落库记录。现场实测通过：项目 `33`，请求变量 `740493979`，控制变量 `797483878`，测量变量 `813498905`，默认报警变量 `880491729`，主任务 `54`，固定任务 `55`，合格持续任务 `56`，NG 任务 `57`。验证通过 `backend/go test -tags smoke_tools ./tools/smokeeb045`、`backend/go run -tags smoke_tools ./tools/smokeeb045 -wait 12s`、`backend/go test ./...`、`backend/go test ./... -coverprofile all-coverage.out` 总覆盖率 72.3%、`backend/go vet ./...`、`backend/golangci-lint run ./...` 0 issues、`backend/go build ./cmd/edge-backend`。 | closed
- 2026-05-31 06:36 | backend-ai/test-ai | test/tool | EB-019/045/048 报警等级变化通知 smoke 补强：`backend/tools/smokeeb045` 的 NG 场景改为先以测量值 `43` 启动检测触发 `alarm.limit.enter`，再写入 `50` 触发 `alarm.limit.level_change`，再写回 `25` 触发 `alarm.limit.recover`，最后停止检测验证 `detection.result_ng`；四类通知均通过 `topic=notifications` WS 即时到达，并通过 HTTP `/api/v1/notifications` 按同一 `event_uid` 读回、确认未读、标记单条已读。现场实测通过：项目 `32`，请求变量 `795489926`，控制变量 `831328992`，测量变量 `760750926`，主任务 `50`，固定任务 `51`，合格持续任务 `52`，NG 任务 `53`。验证通过 `backend/go test -tags smoke_tools ./tools/smokeeb045`、`backend/go run -tags smoke_tools ./tools/smokeeb045 -wait 12s`、`backend/go test ./...`、`backend/go test ./... -coverprofile all-coverage.out` 总覆盖率 72.3%、`backend/go vet ./...`、`backend/golangci-lint run ./...` 0 issues、`backend/go build ./cmd/edge-backend`。 | closed
- 2026-05-31 06:31 | backend-ai/test-ai | bugfix/test | EB-019/045/048 超限恢复通知闭环修复：`TaskManager` 的检测报警恢复事件补齐 `project_id/project_code/test_no/var_name/display_name/alarm_level/limit_value` 等上下文，变量默认报警恢复事件也补齐变量和限值上下文；同时修复恢复事件 `limit_value` 指针指向运行态字段、状态清零后通知限值被清掉的问题。`backend/tools/smokeeb045` 新增 NG 场景中的恢复验证：首帧超上限触发 `alarm.limit.enter` 后写回合格值，等待 `alarm.limit.recover` 经 WS 即时推送，并通过 HTTP 通知列表按同一 `event_uid` 读回和标记已读，再停止检测验证 `detection.result_ng`。验证通过 `backend/go test ./internal/pipeline -run "TestProcessMessageEnqueuesDetectionLimitAlarmEnterAndRecover|TestProcessMessageEnqueuesDefaultLimitAlarmWithoutDetection" -count=1`、`backend/go test -tags smoke_tools ./tools/smokeeb045`、`backend/go test ./...`、`backend/go test ./... -coverprofile all-coverage.out` 总覆盖率 72.3%、`backend/go vet ./...`、`backend/golangci-lint run ./...` 0 issues、`backend/go build ./cmd/edge-backend`；后端已重启为 PID 23492，`/health` ok，真实 smoke 项目 `31`、主任务 `46`、固定任务 `47`、合格持续任务 `48`、NG 任务 `49` 通过。 | closed
- 2026-05-31 06:03 | backend-ai/test-ai | test/tool | EB-045/048 正式 STRING 任务请求 smoke 继续覆盖组合业务：`backend/tools/smokeeb045` 新增 `start_fixed_duration_detection`、`start_qualified_hold_detection` 和 `refresh_features` 三个 data_change flow，通过 WS 写 watched STRING 请求变量验证特征值刷新会写 `features_updated` 事件，固定时长检测会自动以 `end_type=fixed_duration` 停止，合格持续检测会在测量变量落在运行快照限值内后自动以 `end_type=qualified_hold` 停止。现场实测通过：项目 `25`，请求变量 `817149116`，控制变量 `848021045`，测量变量 `866666943`，主任务 `25`，固定时长任务 `26`，合格持续任务 `27`。门禁通过 `backend/go test -tags smoke_tools ./tools/smokeeb045`、`backend/go vet -tags smoke_tools ./tools/smokeeb045`、`backend/go run -tags smoke_tools ./tools/smokeeb045 -wait 12s`、`backend/go test ./...`、`backend/go test ./... -coverprofile all-coverage.out` 总覆盖率 72.3%、`backend/go vet ./...`、`backend/golangci-lint run ./...` 0 issues、`backend/go build ./cmd/edge-backend`；`/health` ok，后端仍运行 PID 8500。 | closed
- 2026-05-31 06:11 | backend-ai/test-ai | test/tool | EB-019/045/048 通知联动 smoke 补强：`backend/tools/smokeeb045` 在 `builtin.refresh_features` 前打开 `topic=notifications` WS，触发后验证收到 `detection.features_updated` 的 `notification.event`，再通过 HTTP `/api/v1/notifications` 按同一 `event_uid` 读回未读通知并调用单条已读接口。现场实测通过：项目 `26`，请求变量 `800423698`，控制变量 `838618853`，测量变量 `831066790`，主任务 `28`，固定时长任务 `29`，合格持续任务 `30`，通知 HTTP ID `66`。门禁通过 `backend/go test -tags smoke_tools ./tools/smokeeb045`、`backend/go vet -tags smoke_tools ./tools/smokeeb045`、`backend/go run -tags smoke_tools ./tools/smokeeb045 -wait 12s`、`backend/go test ./...`、`backend/go test ./... -coverprofile all-coverage.out` 总覆盖率 72.3%、`backend/go vet ./...`、`backend/golangci-lint run ./...` 0 issues、`backend/go build ./cmd/edge-backend`；`/health` ok，后端仍运行 PID 8500。 | closed
- 2026-05-31 06:14 | backend-ai/test-ai | test/tool | EB-019/045/048 检测结果通知 smoke 补强：`backend/tools/smokeeb045` 在固定时长检测和合格持续检测自动结束后，继续验证 `detection.result_ok` 通过 `topic=notifications` WS 即时推送，并通过 HTTP 通知列表按同一 `event_uid` 读回未读通知且可标记单条已读。现场实测通过：项目 `27`，请求变量 `806090321`，控制变量 `838500467`，测量变量 `858345221`，主任务 `31`，固定时长任务 `32`，合格持续任务 `33`。门禁通过 `backend/go test -tags smoke_tools ./tools/smokeeb045`、`backend/go vet -tags smoke_tools ./tools/smokeeb045`、`backend/go run -tags smoke_tools ./tools/smokeeb045 -wait 12s`、`backend/go test ./...`、`backend/go test ./... -coverprofile all-coverage.out` 总覆盖率 72.3%、`backend/go vet ./...`、`backend/golangci-lint run ./...` 0 issues、`backend/go build ./cmd/edge-backend`；`/health` ok，后端仍运行 PID 8500。 | closed
- 2026-05-31 06:20 | backend-ai/test-ai | test/tool | EB-019/045/048 报警和 NG 结果通知 smoke 补强：`backend/tools/smokeeb045` 新增首帧超上限 NG 场景，先把测量变量写到超上限，再通过正式 STRING 任务请求启动检测，验证 `alarm.limit.enter` 经 WS 即时推送并可通过 HTTP 通知列表读回和标记已读；随后用任务流停止该检测，验证 summary 产出 `detection.result_ng`，同样走 WS/HTTP 未读已读闭环。修复 smoke 等待同一 flow 重复执行时可能命中旧 run 的问题，新增按 `min_run_id` 等待新执行记录。现场实测通过：项目 `29`，请求变量 `833432810`，控制变量 `870345172`，测量变量 `755951840`，主任务 `38`，固定时长任务 `39`，合格持续任务 `40`，NG 任务 `41`。门禁通过 `backend/go test -tags smoke_tools ./tools/smokeeb045`、`backend/go vet -tags smoke_tools ./tools/smokeeb045`、`backend/go run -tags smoke_tools ./tools/smokeeb045 -wait 12s`、`backend/go test ./...`、`backend/go test ./... -coverprofile all-coverage.out` 总覆盖率 72.3%、`backend/go vet ./...`、`backend/golangci-lint run ./...` 0 issues、`backend/go build ./cmd/edge-backend`；`/health` ok，后端仍运行 PID 8500。 | closed
- 2026-05-31 05:57 | frontend-ai/test-ai | feature/test | EB-047 前端补齐 KIO 项目初始化入口：设置中心变量模块新增“KIO 项目初始化”按钮和弹窗，支持 `project_count/project_code_prefix/raw_project_prefix/var_group/var_name_prefix/remap_var_name/enable` 配置、dry-run 预览、真实执行确认、结果摘要和明细表；执行成功后刷新项目、变量和 storage routes 查询。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`；Playwright 登录 `/#/settings` 后打开弹窗并点击预览，确认 `POST /api/v1/variables/bulk-remap/kio-projects` 请求体包含 `dry_run=true` 且返回 200，摘要显示匹配 504，截图 `output/playwright/settings-kio-remap-dryrun-smoke.png`。 | closed

- 2026-05-31 05:56 | backend-ai/test-ai | test/tool | EB-045/048 正式 STRING 任务请求 smoke 继续扩展：`backend/tools/smokeeb045` 新增 `register_report` 和 `http_request` 两个 data_change flow，通过 WS 写 watched STRING 请求变量验证 `builtin.register_report` 把报表结果写入 `detection_run_reports` 并可从检测任务详情读回，`builtin.http_request` 受控调用本机 `/health` 返回 200。现场实测通过：项目 `24`，请求变量 `752661683`，控制变量 `824993196`，测量变量 `798429688`，任务 `24`。门禁通过 `backend/go test -tags smoke_tools ./tools/smokeeb045`、`backend/go vet -tags smoke_tools ./tools/smokeeb045`、`backend/go run -tags smoke_tools ./tools/smokeeb045 -wait 12s`、`backend/go test ./...`、`backend/go test ./... -coverprofile all-coverage.out` 总覆盖率 72.3%、`backend/go vet ./...`、`backend/golangci-lint run ./...` 0 issues、`backend/go build ./cmd/edge-backend`；`/health` ok，后端仍运行 PID 8500。 | closed
- 2026-05-31 05:50 | backend-ai/test-ai | test/tool | EB-045/048 正式 STRING 任务请求 smoke 扩展：`backend/tools/smokeeb045` 现在会创建测量变量并启用 storage route，通过同一个 watched STRING 请求验证 `custom_items` 冻结到 `detection_run_standard_items`、`process_params/plc_writes` 追溯、`builtin.storage_prepare`、`builtin.update_detection_limits` 运行中更新 `limit_h/limit_hh/check_cycle_ms/hold_ms`，再验证暂停、恢复和 `task_flow_stop`。现场实测通过：项目 `23`，请求变量 `742576737`，控制变量 `739076088`，测量变量 `842238391`，任务 `23`。门禁通过 `backend/go test -tags smoke_tools ./tools/smokeeb045`、`backend/go vet -tags smoke_tools ./tools/smokeeb045`、`backend/go run -tags smoke_tools ./tools/smokeeb045 -wait 12s`、`backend/go test ./...`、`backend/go test ./... -coverprofile all-coverage.out` 总覆盖率 72.3%、`backend/go vet ./...`、`backend/golangci-lint run ./...` 0 issues、`backend/go build ./cmd/edge-backend`；`/health` ok，后端仍运行 PID 8500。 | closed
- 2026-05-31 05:37 | backend-ai/test-ai | test/tool | EB-045/048 正式 STRING 任务请求 smoke 工具落地：新增 `backend/tools/smokeeb045`，使用 `go run -tags smoke_tools ./tools/smokeeb045` 创建临时项目、STRING 请求虚拟变量、虚拟控制变量和 4 个 data_change task-flow，通过 WS `command.write_variable` 依次验证启动检测、冻结 `process_params/plc_writes`、暂停、恢复和 `task_flow_stop` 结束。现场实测通过：项目 `21`，请求变量 `724990601`，控制变量 `732388972`，任务 `21`。门禁通过 `backend/go test ./...`、`backend/go test ./... -coverprofile all-coverage.out` 总覆盖率 72.3%、`backend/go vet ./...`、`backend/golangci-lint run ./...` 0 issues、`backend/go build ./cmd/edge-backend`；`/health` ok，后端仍运行 PID 8500。 | closed
- 2026-05-31 05:31 | backend-ai/test-ai | api/test | EB-045 通知闭环补强：`builtin.refresh_features` 不再只刷新 `detection_run_features`，现在会复用统一特征值刷新路径，写入 `detection_run_events(features_updated)` 并通过通知总线发布 `detection.features_updated`，让手动/任务流触发的特征值刷新和检测停止后的自动刷新语义一致。验证通过 `backend/go test ./internal/pipeline`、`backend/go test ./...`、`backend/go test ./... -coverprofile all-coverage.out` 总覆盖率 72.3%、`backend/go vet ./...`、`backend/golangci-lint run ./...`、`backend/go build ./cmd/edge-backend`。后端已重启为 PID 8500，`/health` ok，tags=1041。 | closed

- 2026-05-31 05:35 | frontend-ai/test-ai | feature/test | 驾驶舱按 Codex 官网产品页气质再收敛：背景改成低饱和浅灰蓝产品层次，卡片统一为更轻的白蓝玻璃面板、细边框、轻阴影和工位同源暖光边缘；弱化标题 SVG 背板和装饰线，指标卡保留实时值、上下限、OK/NG、优化率；3D 模型缩放和相机重新平衡，完整落在中间区域且不再顶到顶部卡片或底部裁切。顺手收口工位页项目参数迁移残留，把可见状态和报警弹窗从旧 `selectedDevice/statusDevice` 引用改为项目语义。验证通过 `desktop/npm run lint`、`desktop/npm run build`；Playwright 1920×1080 登录后截图 `output/playwright/model-cockpit-codex-pass-3.png`。 | closed

- 2026-05-31 05:22 | backend-ai/test-ai | api/test | EB-045 任务流配置参数边界和精确 ID 校验补齐：确认配置参数包含检测配置、`process_params` 工艺/报表参数和 `plc_writes` PLC 下设计划，进风口面积等值只放 `process_params`，PLC 下设用 `value_from` 引用；后端 `steps_json` 参数绑定的 `source=event` 允许 `trigger_var_id_text`，`GET /api/v1/task-modules` 的 JavaScript runtime note 同步说明 string var_id 与精确 ID 字段。验证通过 `backend/go test ./internal/runtime/handlers`、`backend/go test ./internal/pipeline ./internal/services`、`backend/go test ./...`、`backend/go test ./... -coverprofile all-coverage.out` 总覆盖率 72.3%、`backend/go vet ./...`、`backend/golangci-lint run ./...`、`backend/go build ./cmd/edge-backend`。后端已重启为 PID 41496，`/health` ok，tags=1041。 | closed

- 2026-05-31 05:14 | frontend-ai/test-ai | feature/test | EB-037 storage route 管理端到端闭合：设置中心“存储路由”模块新增搜索框和启用状态筛选，可按路由编号、变量、表/列、项目定位 route。Playwright 登录 `/#/settings` 后进入存储路由，创建临时 route `codex-smoke-604921`，请求 `POST /api/v1/storage-routes` 携带字符串 `var_id`、`project_id=1`、`enabled=true`；筛选定位该行后编辑为 `enabled=false`，`PATCH /api/v1/storage-routes/529` 返回成功；再通过删除确认调用 `DELETE /api/v1/storage-routes/529` 并确认筛选结果消失。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`、`git diff --check`，截图 `output/playwright/settings-storage-route-crud-smoke.png`。EB-037 前端侧闭合。 | closed

- 2026-05-31 05:12 | backend-ai/frontend-ai/test-ai | api/test | 精确变量 ID 残留风险继续收敛：任务流 JavaScript `realtime.get/getMany/write` 支持字符串形式 64 位变量 ID，脚本内 `trigger.var_id_text`、`context.trigger_var_id_text`、实时快照和写变量结果都能拿到精确字符串；`POST /api/v1/variables/bulk-remap/kio-projects` 响应项新增 `var_id_text`；前端 shared DTO 把 `VariableConfig`、`TagSnapshot`、bulk remap item 的 `var_id` 改为 `VarIdentifier`。验证通过 `backend/go test ./internal/pipeline ./internal/services`、`backend/go test ./...`、`backend/go test ./... -coverprofile all-coverage.out` 总覆盖率 72.3%、`backend/go vet ./...`、`backend/golangci-lint run ./...`、`backend/go build ./cmd/edge-backend`、`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`；后端已重启为 PID 40428，`/health` ok，tags=1041。 | closed

- 2026-05-31 05:05 | backend-ai/frontend-ai/test-ai | api/test | WS 精确变量 ID 契约补齐：运行期 `subscribe.var_ids[]` 支持数字或字符串形式 64 位 ID，`connection.ready/subscription.updated` 响应新增 `var_id_texts`，`command.write_variable` 回执 `result` 新增 `var_id_text` 并统一 `project_confirmed` 小写 JSON 字段；任务流内置写变量结果也返回 `var_id_text`。前端 shared DTO 增加 `RealtimeWebSocketSubscription.var_id_texts` 和 `VariableWriteResult`。验证通过 `backend/go test ./internal/runtime/handlers ./internal/services ./internal/pipeline`、`backend/go test ./...`、`backend/go test ./... -coverprofile all-coverage.out` 总覆盖率 72.3%、`backend/go vet ./...`、`backend/golangci-lint run ./...`、`backend/go build ./cmd/edge-backend`、`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`；后端已重启为 PID 8020，`/health` ok，tags=1041。 | closed

- 2026-05-31 05:03 | frontend-ai/test-ai | test | EB-034 运行中同步保存 smoke 完成：Playwright 登录 `/#/settings`，打开已分配变量“编辑配置”，点击“同步到当前运行任务快照”并保存；捕获到 `PATCH /api/v1/variables/9212397624135540847` 请求体包含 `apply_to_running=true`，后端返回 200。截图 `output/playwright/settings-apply-to-running-save-smoke.png`。本项此前已验证默认报警字段、风险提示、工位运行标准快照展示；EB-034 前端侧闭合。 | closed

- 2026-05-31 04:59 | frontend-ai/test-ai | feature/test | EB-037/047 历史页存储快照和项目参数收口：历史查询页运行存储快照入口已验证可用，任务链接优先使用 `project_id` 查询历史，旧 `device_id` 只作为兼容读取；验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`。Playwright 1920x1080 登录 `/#/history?task_id=20&project_id=1&test_no=RUN-20260530204958`，确认历史请求为 `/api/v1/history/data?task_id=20&project_id=1&limit=5000` 且不含 `device_id`，点击“存储快照”后 `/api/v1/detection-runs/20/storage-routes` 返回 200，弹窗标题和 route 列可见，截图 `output/playwright/history-storage-snapshot-project-param-smoke.png`。 | closed

- 2026-05-31 04:53 | backend-ai/frontend-ai/test-ai | api/test | 精确变量 ID 契约继续补齐到通知、报警、历史和特征值：后端 `RuntimeNotification`、HTTP 通知列表、`GET /api/v1/limit-alarms`、`GET /api/v1/history/data`、`GET /api/v1/detection-runs/{id}/features` 响应新增 `var_id_text`；前端 shared DTO、工位页存储快照和历史页存储快照优先显示/使用字符串 ID。验证通过 `backend/go test ./internal/models ./internal/runtime/handlers`、`backend/go test ./...`、`backend/go test ./... -coverprofile all-coverage.out` 总覆盖率 72.2%、`backend/go vet ./...`、`backend/golangci-lint run ./...`、`backend/go build ./cmd/edge-backend`、`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`；后端已重启为 PID 3484，`/health` ok，tags=1041。 | closed

- 2026-05-31 04:53 | frontend-ai/test-ai | feature/test | EB-034 运行标准快照前端展示：工位页当前 running 任务新增“标准快照”按钮，复用运行快照接口 `GET /api/v1/detection-runs/{id}`，在弹窗表格中并排展示检测标准限值、变量默认报警开关、变量默认上下限、默认回差和超限/恢复保持时间，便于确认检测业务限值与变量资产默认值的区别。三语文案已补齐。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`、`git diff --check`；Playwright 1920x1080 登录工位页点击“标准快照”，确认接口 `GET /api/v1/detection-runs/20` 返回 200，标题、检测限值、变量默认限值和默认报警列可见，截图 `output/playwright/station-run-standard-snapshot-smoke.png`。 | closed

- 2026-05-31 04:43 | backend-ai/frontend-ai/test-ai | api/test | 精确变量 ID 契约扩大到检测配置和 storage routes：后端 `POST /api/v1/variables`、检测标准项和存储路由 `var_id` 请求支持字符串形式 64 位 ID；`DetectionStandardItem`、`DetectionRunStandardItem`、`StorageRoute`、`DetectionRunStorageRoute` 响应新增 `var_id_text`。前端 shared DTO、设置页和检测配置页改为优先使用 `var_id_text`/字符串 ID，避免浏览器 number 精度丢失。同步确认“配置参数”包含检测配置、`process_params` 工艺/报表参数和 `plc_writes` PLC 下设计划，进风口面积等参数只放 `process_params`，下设通过 `value_from` 引用。验证通过 `backend/go test ./internal/runtime/handlers`、`backend/go test ./...`、`backend/go test ./... -coverprofile all-coverage.out` 总覆盖率 72.3%、`backend/go vet ./...`、`backend/golangci-lint run ./...`、`backend/go build ./cmd/edge-backend`、`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`；后端已重启为 PID 11184，`/health` ok，tags=1041。 | closed

- 2026-05-31 04:35 | frontend-ai/test-ai | feature | EB-034 变量默认报警前端适配：设置中心变量编辑和虚变量创建新增“默认报警”分区，接入 `default_alarm_enabled/default_limit_ll/default_limit_l/default_limit_h/default_limit_hh/default_limit_deadband/default_violation_hold_ms/default_recover_hold_ms`；变量编辑新增 `apply_to_running` 运行中同步选项和黄色风险提示，说明只同步当前 running 任务变量默认报警快照，不修改检测标准业务限值。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`；Playwright 登录 `/#/settings`，打开创建虚变量和编辑已分配变量弹窗，确认默认报警字段、运行中同步选项和风险提示可见，截图 `output/playwright/settings-variable-default-alarm-smoke.png`。 | closed

- 2026-05-31 04:31 | backend-ai/test-ai | feature/test | EB-045/046/048 精确变量 ID 和任务配置参数契约补强：后端变量、实时快照、任务流变量和任务流执行记录响应新增 `var_id_text/trigger_var_id_text` 精确字符串字段；WS `command.write_variable`、任务流 `vars[].var_id` 和内置模块 `var_id/plc_writes[].var_id/custom_items[].var_id` 支持字符串形式 64 位 ID，避免浏览器 `number` 精度丢失导致 STRING 虚拟变量或 PLC 下设写错目标。前端任务请求弹窗改用精确字符串 ID 选择 watched STRING 变量和 PLC 下设变量；配置参数口径保持为检测配置 + `process_params` 工艺/报表参数 + `plc_writes` PLC 下设计划，进风口面积等参数只在 `process_params` 填一次，PLC 下设用 `value_from` 引用。验证通过：`backend/go test ./internal/runtime/handlers`、`backend/go test ./internal/pipeline`、`backend/go test ./...`、`backend/go test ./... -coverprofile all-coverage.out` 总覆盖率 72.3%、`backend/go vet ./...`、`backend/go build ./cmd/edge-backend`、`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`；后端已重启为 PID 36880，`/health` ok，现场 smoke 创建自动大 ID STRING 虚拟变量 `9212397624135540849`、用字符串 var_id 建 task-flow、WS 写入触发成功，run_id=8/status=success。 | closed

- 2026-05-31 04:25 | frontend-ai/test-ai | feature | 驾驶舱按 Codex 官网产品页方向重新降噪：移除右侧趋势曲线，右侧改为实时值、上下限、OK/NG 和优化率信息卡；撤掉驾驶舱内联玻璃覆盖，统一使用低饱和玻璃卡、细边框、轻阴影和工位同源 600px 暖光边缘；标题 SVG 背板、背景和卡片文字层级改为更克制的产品面板风格，3D 模型继续作为中心主视觉并下移放大。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`；Playwright 1920×1080 截图 `output/playwright/model-cockpit-codex-pass-1.png`，确认曲线节点为 0。 | closed
- 2026-05-31 04:00 | frontend-ai/test-ai | test | EB-048 通知中心补齐筛选：顶栏通知弹层新增未读/全部、通知类型和项目筛选，查询走 `GET /api/v1/notifications` 的 `unread/type/project_id` 参数，并收紧弹层高度/定位避免筛选条被顶出视口。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`；Playwright 登录后打开通知中心，点击未读、选择“检测开始”和项目，确认最终请求为 `/api/v1/notifications?unread=true&type=detection.run_started&project_id=2&limit=20`，截图 `output/playwright/notification-filters-smoke.png` | closed

- 2026-05-31 03:46 | frontend-ai/test-ai | test | `/#/model-cockpit` 视觉按 Codex 官网产品页气质再次收敛：移除重 HUD 标题背板，背景改为更低饱和蓝白产品层次，卡片统一为更通透的浅玻璃面板并保留工位同源暖光边缘，去掉 3D 模型上的临时诊断流线，模型改为更干净的产品展示。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，1920×1080 截图 `output/playwright/model-cockpit-codex-polish-v2-1920x1080.png` | closed

- 2026-05-31 03:34 | frontend-ai/test-ai | test | 系统设置日志体验修正：运行日志正文不再嵌在设置页或侧向窗口里，改为点击“运行日志预览”打开居中大弹窗；设置页只保留日志摘要、刷新和打开日志位置按钮。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，Playwright 登录进入 `/#/settings` 后打开系统模块和日志弹窗，确认 `.settings-log-modal` 可见、`.ant-drawer` 数量为 0，截图 `output/playwright/settings-log-modal-smoke.png` | closed
- 2026-05-31 03:44 | frontend-ai/test-ai | feature | EB-046/048 任务页新增正式 STRING 虚拟变量触发入口：“发送任务请求”弹窗可选择 watched `STRING` 虚拟变量，结构化填写 `command/project_id/test_no/standard_id/duration_sec/qualified_hold_ms/limit_check_enabled/enable_storage/enable_alarm/operator_note/report_template_id/custom_items`，并把进风口面积等工艺参数放入 `process_params`、PLC 下设计划放入 `plc_writes`；提交时通过 WS `command.write_variable` 写入虚拟变量触发任务系统，不走手动运行接口也不绕过后端写 PLC。验证通过 `desktop/npm run lint`、`desktop/npm run test -- --run`、`desktop/npm run build`，Browser smoke 登录 `/#/tasks` 后打开任务请求弹窗，确认“工艺/报表参数”“PLC 下设计划”和 `inlet_area_m2` 默认行可见，截图 `output/playwright/task-request-modal-smoke.png` | closed
- 2026-05-31 03:50 | frontend-ai/test-ai | feature | EB-046/048 任务请求弹窗补“请求 JSON 预览”和校验：预览区使用同一套 payload 构造逻辑展示最终写入 watched `STRING` 虚拟变量的 JSON；`custom_items` JSON 无效时禁用提交；PLC 下设区提示必须选择 `var_id` 才会进入最终 JSON，避免用户误以为未完成行已发出。验证通过 `desktop/npm run lint`、`desktop/npm run test -- --run`、`desktop/npm run build`；Playwright smoke 登录 `/#/tasks` 打开弹窗，确认预览、`start_detection`、`process_params`、`inlet_area_m2`、PLC 下设区和 `var_id` 提示可见，截图 `output/playwright/task-request-preview-smoke.png` | closed
- 2026-05-31 03:57 | frontend-ai/test-ai | feature | EB-046/048 任务请求弹窗补清“配置参数”语义：配置参数明确包含检测配置、过程参数和 PLC 下设引用；选择请求变量时显示是否有启用任务流监听该 watched `STRING` 变量，避免写入后没有业务响应。验证通过 `desktop/npm run lint`、`desktop/npm run test -- --run`、`desktop/npm run build`；Playwright smoke 登录 `/#/tasks` 打开弹窗，确认“配置参数”、进风口面积 `inlet_area_m2` 和监听任务流提示可见，截图 `output/playwright/task-request-config-params-smoke.png` | closed
- 2026-05-31 04:05 | frontend-ai/test-ai | feature | EB-046/048 任务请求弹窗扩展正式业务命令：新增 `stop_detection/pause_detection/resume_detection/mute_detection_alarms/storage_snapshot/update_detection_limits/refresh_detection_features/register_report`，按命令展示运行控制、运行中限值调整和报表登记字段；payload 构造按命令过滤无关字段，避免开始检测参数泄漏到限值调整或报表登记请求。验证通过 `desktop/npm run lint`、`desktop/npm run test -- --run`、`desktop/npm run build`；Playwright smoke 切换 `update_detection_limits` 和 `register_report`，确认对应字段和 JSON 预览，截图 `output/playwright/task-request-command-coverage-smoke.png` | closed
- 2026-05-31 04:12 | frontend-ai/test-ai | feature | EB-046/048 任务请求弹窗补齐剩余低代码命令：新增 `storage_prepare` 和 `http_request` 参数区，`storage_prepare` 只准备项目宽表/动态列不写采样，`http_request` 支持 `url/headers/body/timeout_ms` 且 headers 作为 JSON 参与预览校验；payload 构造继续按命令过滤无关字段。验证通过 `desktop/npm run lint`、`desktop/npm run test -- --run`、`desktop/npm run build`；Playwright smoke 切换 `storage_prepare` 和 `http_request`，确认字段与 JSON 预览，截图 `output/playwright/task-request-storage-http-smoke.png` | closed
- 2026-05-31 04:16 | frontend-ai/test-ai | test | EB-046/048 正式 STRING 虚拟变量触发链端到端 smoke 通过：登录后创建临时 `STRING` 虚拟变量和临时 data_change 任务流，WS `command.write_variable` 写入 `{command:"codex_e2e_context"}` 返回 `command.ack`，后端 `task-flow-runs` 记录 `run_id=6/status=success`，随后删除临时任务流和变量。首次使用后端自动生成的大 64 位 `var_id` 时暴露 JS number 精度风险，已用显式小范围 `var_id=700766631` 复测通过，并清理首次遗留变量 `9212397624135540849`；后续需要评估前端是否应把大 `var_id` 当 string 处理。 | closed

- 2026-05-31 03:30 | frontend-ai | test | EB-048 通知中心目标跳转完成：通知项点击后会先标记已读；若通知带 `task_id` 且账号有 `view_history`，跳转到 `/#/history?task_id=...&device_id=...&test_no=...`，否则按 `project_id` 回到对应工位。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，Playwright 点击真实通知 `RUN-20260530190426` 后进入历史页并触发 `GET /api/v1/history/data?task_id=19&project_id=1&limit=5000` 200，截图 `output/playwright/notification-target-navigation-smoke.png` | closed

- 2026-05-31 03:25 | backend-ai/test-ai | api | EB-045 预置检测模板补齐 PLC 下设顺序：`GET /api/v1/task-flow-templates` 中变量请求启动、固定时长检测、合格持续检测模板都会先生成 `builtin.write_control_variables` 再生成 `builtin.start_detection_run`，并透传 `process_params/plc_writes/operator_note/report_template_id`，避免固定时长/合格持续模板只冻结 PLC 下设计划但未实际下设。验证通过 handler 定向回归、`go test ./...`、覆盖率 72.5%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 39948，`/health` ok，tags=1041，登录后 smoke `task-flow-templates=13` 且 `builtin.write_control_variables` 出现 4 次 | open

- 2026-05-31 03:23 | frontend-ai | test | EB-048 通知中心接入 WS 即时刷新：新增 `subscribeRealtimeWebSocket` 封装，顶栏 Shell 订阅 `topic=notifications`，收到 `notification.event` 后刷新通知列表和未读数，并带 3 秒重连；验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，Playwright 登录后确认页面打开 `ws://127.0.0.1:18080/api/v1/ws?...&topic=notifications`，Node WS smoke 确认后端返回 `connection.ready` 且订阅 topics 为 `notifications` | closed

- 2026-05-31 03:19 | backend-ai/test-ai | api | 配置资源缺失语义继续收紧：`POST/DELETE /api/v1/detection-standards/{id}/favorite`、`DELETE /api/v1/detection-standards/{id}`、`PATCH/DELETE /api/v1/report-templates/{id}`、`PATCH/DELETE /api/v1/storage-routes/{id}` 在目标资源不存在时返回 `404`；检测标准和报表模板引用保护仍返回 `409`，storage route 已冻结到检测运行时删除仍返回 `409`。验证通过 handler/kernel/repository 定向回归、`go test ./...`、覆盖率 72.5%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 41596，`/health` ok，tags=1041，登录后 smoke 七个缺失配置资源接口均返回 404 | open
- 2026-05-31 03:14 | frontend-ai | test | EB-048 全局通知中心首段接入：顶栏新增通知铃铛和未读角标，弹层调用 `GET /api/v1/notifications/unread-count` 与 `GET /api/v1/notifications?limit=20`，支持单条点击标记已读和全部已读；验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，Playwright 模拟登录、打开通知弹层，确认未读/列表接口 200、渲染 20 条通知，截图 `output/playwright/global-notifications-popover-smoke.png`；另验证点击首条通知调用 `POST /api/v1/notifications/22/read` 返回 200 | closed

- 2026-05-31 03:12 | backend-ai/test-ai | api | 资源编辑/删除缺失语义继续收紧：`PATCH /api/v1/projects/{id}`、`PATCH /api/v1/variables/{id}`、`PATCH /api/v1/variables/{id}/assignment`、`DELETE /api/v1/variables/{id}`、`PATCH /api/v1/gateway-configs/{id}`、`DELETE /api/v1/gateway-configs/{id}` 在目标资源不存在时统一返回 `404`，不再返回 `500` 或静默成功；变量分配/删除和网关删除仓储层会检查 `RowsAffected=0`。验证通过定向 handler/kernel 回归、`go test ./...`、覆盖率 72.3%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 27444，`/health` ok，tags=1041，登录后 smoke 六个缺失资源写接口均返回 404 | open
- 2026-05-31 03:05 | backend-ai/test-ai | api | 检测标准缺失资源错误语义收紧：`PATCH /api/v1/detection-standards/{id}` 和 `PUT /api/v1/detection-standards/{id}/items` 在标准不存在时返回 `404`，不再暴露为 `500`；handler 和 kernel 回归已覆盖。同步确认 EB-045 配置参数语义：`process_params` 承载进风口面积等工艺/报表参数，`plc_writes` 只描述受控 PLC 下设计划，正式入口仍通过 watched `STRING` 虚拟变量触发任务系统。验证通过定向 handler/runtime/pipeline 测试、`go test ./...`、覆盖率 72.1%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 41100，`/health` ok，tags=1041，smoke 两个缺失检测标准接口均为 404，`task-modules` 返回 `process_params/plc_writes` | open
- 2026-05-31 03:06 | frontend-ai | test | EB-037 工位页接入运行存储路由快照：新增 `getDetectionRunStorageRoutes` typed API 和“存储快照”弹窗，当前检测运行中可查看当次冻结 route 的目标、表/列、触发、周期、死区和首帧存储；验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，Playwright 模拟登录 `/#/?device_id=1`、点击开始检测、打开存储快照、确认 `GET /api/v1/detection-runs/19/storage-routes` 200 且弹窗渲染 route 行，再停止检测，截图 `output/playwright/station-storage-routes-snapshot-smoke.png` | closed

- 2026-05-31 02:55 | frontend-ai | test | `/#/model-cockpit` 按 Codex 官网产品页方向继续收敛：背景改为低饱和蓝白产品层次，标题 SVG 从重 HUD 蓝板改为轻量半透明矢量框，卡片保留工位同源 600px 暖光边缘但降低厚重阴影和杂色，顶部/指标卡文字层级改为深色产品面板风格；验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，Playwright 1920×1080 登录后截图 `output/playwright/model-cockpit-codex-clean-1920x1080.png`，相关接口 200 | closed

- 2026-05-31 02:50 | frontend-ai | test | EB-046 任务页动态参数表单完成：按 `task-modules.params_schema` 渲染模块参数，支持 project/variable/select/boolean/number/object/array/text/code/any，并在编辑时同步 `action_payload` 与第一条 `steps_json` step；验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，Playwright 模拟登录后进入 `/#/tasks`、打开模板创建弹窗并切换 `builtin.write_variable`，确认参数字段渲染、接口 200、payload 同步，截图 `output/playwright/task-flows-module-params-smoke.png` | closed

- 2026-05-31 02:25 | frontend-ai | test | `/#/model-cockpit` 视觉重调：参考 Codex 官网的克制产品页层级，恢复工位同源动态蓝色光场和玻璃受光边缘，弱化标题 SVG 背板，顶部卡片改为图标/主值/说明结构，3D 模型固定中性光照并放大下移；验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，1920×1080 截图 `output/playwright/model-cockpit-redesign-1920-v2.png` | closed

- 2026-05-31 02:59 | backend-ai/test-ai | api | 后端公共错误映射修正：`services.HTTPStatusForError` 现在把 `gorm.ErrRecordNotFound` 映射为 HTTP `404`，引用保护和同项目重复 running 仍为 `409`，普通校验错误仍为 `400`；复用该映射的检测任务、存储路由、任务执行记录和 WS 命令错误语义更一致。新增 service 回归断言。验证通过定向 service/handler/runtime 测试、`go test ./...`、覆盖率 72.1%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 41456，`/health` ok 且 KIO gateway active，登录后 smoke 缺失检测任务 stop 和缺失 task-flow run 均返回 404 | open
- 2026-05-31 02:55 | backend-ai/test-ai | api | EB-045 任务执行记录筛选参数硬化：`GET /api/v1/task-flow-runs` 现在要求 `project_id/flow_id/trigger_var_id/origin_flow_id` 为正数，并要求 `from <= to`，避免前端把空 ID 或反向时间范围误传后只得到空列表；新增 handler 回归覆盖四类非法 ID 和反向时间范围。验证通过定向 handler 测试、`go test ./...`、覆盖率 72.1%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 27040，`/health` ok 且 KIO gateway active，登录后 smoke `project_id=0`、`trigger_var_id=-1`、反向时间范围均返回 400 | open
- 2026-05-31 02:50 | backend-ai/test-ai | api | EB-045 任务执行记录不存在语义修正：`GET /api/v1/task-flow-runs/:id` 和 `/task-flow-runs/:id/sql-logs` 在 run 不存在时返回 `404`，避免前端把不存在的执行记录误显示成空日志或普通参数错误；新增 handler 回归覆盖缺失 run 详情和缺失 run SQL 日志。验证通过定向 handler 测试、`go test ./...`、覆盖率 71.8%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 3440，`/health` ok 且 KIO gateway active，登录后 smoke 两个不存在 run 接口均返回 404 | open
- 2026-05-31 02:46 | backend-ai/test-ai | api | EB-045 任务 SQL 日志查询参数硬化：`GET /api/v1/task-flow-runs/:id/sql-logs` 现在拒绝 `limit<=0`，避免前端任务页 SQL 日志传错 limit 后被仓储层静默改成默认 100 条；新增 handler 回归覆盖 `limit=0`。验证通过定向 handler 测试、`go test ./...`、覆盖率 71.8%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 41604，`/health` ok 且 KIO gateway active，登录后 smoke `task-flow-runs/1/sql-logs?limit=0` 返回 400 | open
- 2026-05-31 02:42 | backend-ai/test-ai | api | EB-045 任务执行记录分页参数硬化：`GET /api/v1/task-flow-runs` 现在拒绝 `limit<=0` 和 `offset<0`，与通知、报警等分页接口保持一致，避免前端任务页传错分页时被仓储层静默归一化成默认页；新增 handler 回归覆盖 `limit=0`、`limit=-1`、`offset=-1`。验证通过定向 handler 测试、`go test ./...`、覆盖率 71.8%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 25584，`/health` ok 且 KIO gateway active，登录后 smoke `task-flow-runs?limit=0` 和 `offset=-1` 均返回 400 | open
- 2026-05-31 02:35 | backend-ai/test-ai | api | EB-045 任务流时间字段硬化：`POST /api/v1/task-flows` 拒绝负数 `timeout_ms/cooldown_ms/hold_ms/schedule_interval_ms`，`PATCH` 显式提交 `timeout_ms` 时必须大于 0，其他时间字段不能为负；避免创建时静默归零、编辑时写入即时超时或负数间隔。新增 create/patch 回归。验证通过定向 handler 测试、`go test ./...`、覆盖率 71.8%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 22240，`/health` ok 且 KIO gateway active，登录后 smoke `timeout_ms=-1` 返回 `400 timeout_ms cannot be negative` | open
- 2026-05-31 02:30 | backend-ai/test-ai | api | EB-045 schedule 任务流配置硬化：`POST/PATCH /api/v1/task-flows` 要求 `trigger_type=schedule` 的任务必须配置 `schedule_interval_ms > 0` 或兼容期 `cooldown_ms > 0`，避免扫描器永久跳过保存成功的空转定时任务；新增 create 和 patch 回归。验证通过定向 handler 测试、`go test ./...`、覆盖率 71.8%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 8060，`/health` ok 且 KIO gateway active，登录后 smoke schedule 无间隔返回 `400 schedule task flow requires schedule_interval_ms or cooldown_ms greater than 0` | open
- 2026-05-31 02:26 | backend-ai/test-ai | api | EB-045 data_change 任务流配置硬化：`POST/PATCH /api/v1/task-flows` 要求 `trigger_type=data_change` 的任务至少绑定一个 `watch` 变量，因为只有 watch 变量会进入 `var_id -> flow_ids` 倒排索引；新增 create 和 patch 回归。验证通过定向 handler 测试、`go test ./...`、覆盖率 71.8%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 42568，`/health` ok 且 KIO gateway active，登录后 smoke `data_change` 仅绑定 read 变量返回 `400 data_change task flow requires at least one watch var` | open
- 2026-05-31 02:21 | backend-ai/test-ai | api | EB-045 任务流变量绑定硬化：`POST/PATCH /api/v1/task-flows` 拒绝 `vars[].var_id <= 0`，避免保存无法进入 `var_id -> flow_ids` 倒排索引的 watch/read/write 绑定；新增 create/patch 回归。验证通过定向 handler 测试、`go test ./...`、覆盖率 71.7%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 39292，`/health` ok 且 KIO gateway active，登录后 smoke `var_id=0` 创建 task-flow 返回 `400 invalid task flow var_id: 0` | open
- 2026-05-31 02:17 | backend-ai/test-ai | api | EB-045 任务流 step code 唯一性硬化：`POST/PATCH /api/v1/task-flows` 拒绝 `steps_json` 中重复的显式 `code`，避免多步骤结果写入 run context 时互相覆盖；执行器 `taskFlowSteps` 同步修剪 code 并防御历史脏配置。新增 handler create/patch 回归和 pipeline 重复 code 回归。验证通过定向 handler/pipeline 测试、`go test ./...`、覆盖率 71.7%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 9632，`/health` ok 且 KIO gateway active，登录后 smoke 重复 step code 创建 task-flow 返回 `400 duplicate task flow step code: same` | open
- 2026-05-31 02:09 | backend-ai/test-ai | api | EB-045 空步骤链配置硬化：`POST/PATCH /api/v1/task-flows` 拒绝显式 `steps_json=[]`，避免保存后任务运行成功但没有执行任何模块；执行器 `taskFlowSteps` 也增加同样防御，防止历史脏配置静默成功。新增 handler 回归覆盖 create/patch 空步骤链。验证通过定向 handler 和 pipeline 测试、`go test ./...`、覆盖率 71.6%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 36744，`/health` ok，登录后 smoke 空步骤链创建 task-flow 返回 `400 steps_json cannot be empty` | open
- 2026-05-31 02:05 | backend-ai/test-ai | api | EB-045 任务流 event 参数 key 校验硬化：`POST/PATCH /api/v1/task-flows` 在 `steps_json[].params` 使用 `source=event` 时提前校验 `key`，只允许执行器真实支持的 `trigger_type/project_id/trigger_var_id/trigger_value/gateway_id/topic/origin_flow_id/origin_run_id/depth/request_id/at`，避免保存后运行时才报 unknown event key。新增 handler 回归覆盖 create/patch 的坏 event key。验证通过定向 handler 测试、`go test ./...`、覆盖率 71.6%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 41056，`/health` ok，登录后 smoke 坏 event key 创建 task-flow 返回 `400 invalid task flow event key at params.project: project` | open
- 2026-05-31 02:00 | backend-ai/test-ai | api | EB-045 任务流步骤参数绑定校验硬化：`POST/PATCH /api/v1/task-flows` 现在会递归校验 `steps_json[].params` 中显式声明的 `source`，只允许 `literal/trigger_param/event/context`，非法来源直接返回 `400`，避免保存后运行时才因参数绑定失败。新增 handler 回归覆盖 create/patch 的坏参数来源。验证通过定向 handler 测试、`go test ./...`、覆盖率 71.6%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 10288，`/health` ok，登录后 smoke 坏参数来源创建 task-flow 返回 `400 invalid task flow param source at params.project_id: trigger` | open
- 2026-05-31 01:56 | backend-ai/test-ai | api | EB-045 任务流配置必填字段硬化：`POST /api/v1/task-flows` 要求 `project_id > 0`、非空 `flow_code` 和非空 `name`；`PATCH` 拒绝 `project_id=0` 或空白 `flow_code/name`，避免保存不可识别的空壳任务配置。新增 handler 回归覆盖缺项目、空编码、空名称和 patch 改坏字段。验证通过定向测试、`go test ./...`、覆盖率 71.6%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 2648，`/health` ok，登录后 smoke 缺 `project_id` 创建 task-flow 返回 `400 project_id is required` | open
- 2026-05-31 01:49 | backend-ai/test-ai | api | EB-045 任务流配置写入端硬化：`POST/PATCH /api/v1/task-flows` 和列表 `trigger_type` 筛选新增枚举校验，非法 `trigger_type/action_type/vars.role/steps_json[].module` 直接返回 `400`，避免保存不可运行的任务配置；同时规范化合法枚举大小写和空白。新增 handler 回归覆盖坏触发类型、坏动作类型、坏 step module、坏变量角色和坏列表筛选。验证通过定向测试、`go test ./...`、覆盖率 71.6%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 8608，`/health` ok，登录后 smoke 创建坏 action task-flow 返回 400 | open
- 2026-05-31 01:42 | backend-ai/test-ai | api | EB-045 任务执行记录查询参数硬化：`GET /api/v1/task-flow-runs` 对 `status` 和 `trigger_type` 做枚举校验，非法值返回 `400`，避免前端任务页传错筛选项时静默得到空列表；同一 handler 回归覆盖非法 `status=done` 和 `trigger_type=unknown`。验证通过定向测试、`go test ./...`、覆盖率 71.5%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 41508，`/health` ok，登录后 smoke `task-flow-runs?status=done` 返回 400 | open
- 2026-05-31 01:38 | backend-ai/test-ai | test | EB-045 任务执行记录查询回归补强：新增 handler 测试 `TestTaskFlowsHandlerListRunsFiltersFailedAndTimeoutDistinctly`，构造业务失败文本含 timeout 但状态为 `failed` 的记录和真实 `timeout` 记录，验证 `GET /api/v1/task-flow-runs?status=failed|timeout` 分开返回，支撑前端任务页执行记录筛选。验证通过定向测试、`go test ./...`、覆盖率 71.5%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`，`/health` ok | open
- 2026-05-31 01:36 | backend-ai/test-ai | api | EB-045 任务模块 schema 对齐配置参数语义：`GET /api/v1/task-modules` 的 `builtin.start_detection_run.params_schema` 显式暴露 `process_params` 和 `plc_writes`，前端动态表单可把检测配置、工艺/报表参数和 PLC 下设计划作为同一个 STRING 虚拟变量业务参数包展示；新增 handler 回归断言 schema 包含进风口面积语义。验证通过定向 handler 测试、`go test ./...`、覆盖率 71.5%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 4984，`/health` ok，登录后 smoke `task-modules=17` 且启动检测模块返回 `process_params/plc_writes` | open
- 2026-05-31 01:30 | backend-ai/test-ai | test | EB-045 真超时回归补齐：新增 `TestTaskFlowJavaScriptTimeoutStatus`，用真实 JavaScript 无限循环触发 Goja interrupt，验证 `task_flow_runs` 语义仍会返回 `timeout`，同时继续保证业务错误文本中的 timeout 不误归类。验证通过定向测试、`go test ./...`、覆盖率 71.5%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`，`/health` ok 且 KIO gateway active、队列为 0 | open
- 2026-05-31 01:26 | backend-ai/test-ai | test | EB-045 任务执行状态语义收紧：`task_flow_runs.status=timeout` 只用于真实上下文/网络/脚本超时，不再因为普通业务错误文本包含 `timeout` 就误归类；PLC ack 等业务失败保持 `failed`。新增 `TestTaskFlowStatusForErrDoesNotClassifyBusinessTimeoutTextAsTimeout`。验证通过定向测试、`go test ./...`、覆盖率 71.4%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`，`/health` ok 且 KIO gateway active、队列为 0 | open
- 2026-05-31 01:23 | backend-ai/test-ai | test | EB-045 PLC 写入失败保护补强：新增 `TestTaskFlowStringPayloadStopsDetectionWhenPLCWriteFails`，覆盖 `process_params` 可解析但后端变量写入 service 返回失败时，任务流必须失败、检测任务不创建，并保留失败审计。验证通过定向测试、`go test ./...`、覆盖率 71.4%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`，`/health` ok 且 KIO gateway active、队列为 0 | open
- 2026-05-31 01:18 | backend-ai/test-ai | test | EB-045 PLC 参数失败保护补强：新增 `TestTaskFlowStringPayloadStopsDetectionWhenPLCParamMissing`，覆盖 watched STRING 请求里的 `plc_writes.value_from` 指向不存在参数时，任务流必须失败、变量 writer 不被调用、检测任务不创建，并写入失败审计。验证通过定向测试、`go test ./...`、覆盖率 71.4%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`，`/health` ok 且 KIO gateway active | open
- 2026-05-31 01:18 | backend-ai | decision | EB-045 配置参数语义再次对齐：一次检测启动的 STRING 虚拟变量 payload 是业务参数包，不只是检测上下限；检测配置进入运行快照，`process_params` 承载进风口面积等工艺/报表参数，`plc_writes` 通过 `value_from` 引用同一来源并由 `builtin.write_control_variables` 受控下设 PLC。已同步 `backend/docs/边缘端全链路数据流转与分发图.md` 和 `backend/docs/backend-architecture.md` | open
- 2026-05-31 01:11 | backend-ai/test-ai | test | EB-017 重启恢复证据补强：新增 `TestKernelStartRecoversRunningDetectionTask`，先在数据库创建带检测标准快照和 storage route 快照的 running 检测任务，再新建 Kernel 调 `Start()`，断言该任务恢复进 runtime `TaskManager`，且 `/api/v1/detection-runs/active` 返回恢复后的任务。验证通过 `go test ./internal/runtime -run TestKernelStartRecoversRunningDetectionTask -count=1`、`go test ./...`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 71.3%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`，`/health` ok，后端 PID 3224 继续运行 | open
- 2026-05-31 01:10 | backend-ai/test-ai | test | EB-045 任务参数闭环补强：新增 `TestTaskFlowStringPayloadWritesPLCBeforeStartingDetection`，覆盖 watched STRING 虚拟变量请求携带 `process_params.inlet_area_m2` 和 `plc_writes` 时，任务流先调用 `builtin.write_control_variables` 通过后端 writer 下设 PLC，再调用 `builtin.start_detection_run` 创建检测任务，并把 `process_params/plc_writes` 冻结进 `custom_config_json`；测试同时断言下设审计写入。验证通过 `go test ./internal/pipeline -run TestTaskFlowStringPayloadWritesPLCBeforeStartingDetection -count=1`、`go test ./...`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 71.3%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`，`/health` ok 且后端继续运行 | open
- 2026-05-31 00:59 | backend-ai/test-ai | test | EB-017 多项目并行检测 smoke 工具落地：新增 `backend/tools/smokeeb017`，需用 `go run -tags smoke_tools ./tools/smokeeb017` 执行，避免常规覆盖率把 smoke main 包计入产品门禁；实测创建 `SMOKE-EB017-20260531005715-A/B` 项目 `19/20`、INT/STRING 虚变量 `731570092/782246637/825972572`，启动并行任务 `17/18`，同项目重复启动返回 409，WS `command.write_variable` 写虚变量后历史入库可查，停止项目 A 后项目 B 仍 running，随后正常停止。验证通过 `go run -tags smoke_tools ./tools/smokeeb017`、`go test ./...`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 71.3%、`go vet ./...`、`golangci-lint run ./...`、`go build ./cmd/edge-backend`，`/health` ok 且后端继续运行 | open
- 2026-05-31 00:39 | backend-ai/test-ai | api | EB-045 配置参数与 PLC 下设链路落地：新增任务模块 `builtin.write_control_variables` 和模板 `write-control-variables`，支持从 `task_params.process_params` 通过 `value_from` 解析进风口面积等工艺值，调用后端 `VariableWriteService` 统一走写入约束、KIO/PLC service 和审计；`builtin.start_detection_run` 会把 `process_params/plc_writes` 冻结进 `custom_config_json`。验证通过 `go test ./internal/pipeline` 定向、`go test ./internal/runtime/handlers` 定向、`go test ./...`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 71.3%、`go vet ./...`、`golangci-lint run ./...`、`go build ./cmd/edge-backend`；后端已重启 PID 9252，smoke 验证 `/health`、登录、`task-modules=17`、`task-flow-templates=13` | open
- 2026-05-31 00:26 | backend-ai/review-ai | decision | EB-045 任务配置参数边界补充：正式 `task_params` 需要区分检测配置、工艺/报表参数和 PLC 下设参数；进风口面积等参数应从同一 `process_params` 来源冻结到检测追溯，并可通过后续 `builtin.write_control_variables` 受控下设到 PLC，仍必须走后端写变量 service、审计和可选回读确认 | open
- 2026-05-30 23:49 | backend-ai | api | EB-040 默认报警首段落地：`detection_limit_alarms` 新增 `scope` 字段，检测业务报警默认 `scope=detection`，变量默认报警写 `scope=default/task_id=0`；新增 `TaskManager.EvaluateDefaultLimitAlarm`，数据变化和周期扫描都会按 `sys_tags.default_alarm_enabled/default_limit_*/default_*_hold_ms` 触发 enter/recover/level_change，默认报警不带静音且不参与检测摘要 | open
- 2026-05-30 23:56 | backend-ai | api | EB-040 新增统一超限报警查询接口：`GET /api/v1/limit-alarms` 受 `view_realtime` 权限保护，返回 `{items,total,limit,offset}`，支持 `scope=default|detection` 以及 `project_id/task_id/test_no/var_id/status/alarm_type/level/from/to/limit/offset` 筛选；前端可用同一入口展示变量默认报警和检测业务报警明细 | open
- 2026-05-30 23:39 | backend-ai | api | EB-037 补齐检测运行存储快照查询：新增 `GET /api/v1/detection-runs/:id/storage-routes`，权限 `view_realtime`，返回 `{items,count}`；同时修正检测启动冻结逻辑，默认 route 仍作为 disabled 建议自动保证存在，但运行快照会冻结同一变量的所有 enabled storage routes，包括用户自定义 route | open
- 2026-05-30 23:34 | backend-ai | api | EB-045/046 继续补齐任务流编辑闭环：新增 `GET /api/v1/task-flows/:id`，返回单条任务流配置和变量绑定，供前端编辑器直达、刷新和删除后确认使用；权限仍为 `system_settings` | open
- 2026-05-30 21:18 | backend-ai | api | EB-045/046 继续补齐任务流管理闭环：`PATCH /api/v1/task-flows/:id` 支持修改 `project_id`，替换 `vars` 时后端同步变量绑定项目；新增 `DELETE /api/v1/task-flows/:id` 删除任务流配置和变量绑定，执行历史 `task_flow_runs/task_flow_sql_logs` 保留用于审计；前端任务页可以取消“编辑已有任务时项目不可改”的限制并增加删除动作 | open
- 2026-05-30 21:04 | backend-ai | api | EB-045 继续迭代定时任务：`sys_task_flows` 新增 `schedule_interval_ms`，`POST/PATCH /api/v1/task-flows` 读写该字段；`TaskFlowExecutor` 新增轻量 schedule scanner，每秒扫描 `trigger_type=schedule` 的启用任务，到期后投递现有任务流队列并写 `task_flow_runs`；兼容期若 schedule flow 未填 `schedule_interval_ms` 但填了 `cooldown_ms`，后端用 `cooldown_ms` 作为定时间隔；前端任务表单需新增定时间隔字段 | open
- 2026-05-30 20:48 | backend-ai | api | EB-045 继续迭代项目生命周期任务流：检测启动成功后派发 `TaskFlowEvent(project_start)`，检测正常结束、任务流结束或异常结束后派发 `TaskFlowEvent(project_end)`；触发源覆盖任务流内置检测模块和 HTTP/WS 复用的 `DetectionRunsService`；生命周期 `task_params` 携带 `task_id/project_id/project_code/test_no/status/end_type/trigger_type`，可用于存储准备、特征值刷新、报表登记和通知；`schedule` 触发类型仍为配置元数据，尚未接定时调度 worker | open
- 2026-05-30 20:30 | backend-ai | api | EB-045 继续迭代：JavaScript 任务模块补齐多变量实时读取 API：`realtime.getMany([var_id])`、`realtime.getByName(var_name, project_id?)`、`realtime.project(project_id?)`；`realtime.write(var_id,value,options?)` 改为复用任务流写变量审计，只允许写虚拟变量，默认 `trigger=false`，需要继续触发任务时必须显式传 `trigger:true/max_depth`；`builtin.write_variable` 同步收紧为只写虚拟变量，物理变量下设必须走 WS/HTTP 的 KIO 写服务；`GET /api/v1/task-modules` 的 JavaScript 模块返回 `runtime_api`，前端无需硬编码脚本能力说明 | open
- 2026-05-30 19:35 | backend-ai | api | EB-045 继续迭代：检测业务正式入口保持 STRING 虚拟变量触发任务系统；`sys_detection_tasks` 新增 `limit_check_enabled/end_policy/qualified_hold_ms/custom_config_json`；`builtin.start_detection_run` 支持 `custom_items` 自定义检测项、`limit_check_enabled` 是否启用上下限、`end_policy=manual|fixed_duration|qualified_hold` 和 `qualified_hold_ms`；`builtin.update_detection_limits` 支持 `items` 批量调整运行中检测项并刷新 active task；WS read limit 从 32 KiB 提升到 256 KiB 以承载低频业务配置 payload；新增 `GET /api/v1/detection-standards/favorites|recent`、`POST/DELETE /api/v1/detection-standards/:id/favorite` | open
- 2026-05-30 19:35 | backend-ai | decision | 前端边界确认：检测配置模板、收藏、最近使用、任务历史和当前运行快照可走 HTTP；开始/停止/暂停/恢复/消音/运行中改上下限等改变现场业务状态的动作必须写入 STRING 虚拟变量，由 `sys_task_flows` data_change 触发内置业务模块执行；本轮不做拖拽式前端，只给未来拖拽编辑器保留模块 schema 和参数来源 | open
- 2026-05-30 17:15 | backend-ai | decision | 已给 frontend-ai 新增 EB-048 交付清单：明确变量属性设定、storage routes、检测配置/运行接口、任务模块/模板/执行记录 API、正式 STRING 虚拟变量触发链、`steps_json` 数组契约、`apply_to_running` 风险提示和旧变量级存储字段废弃范围 | open
- 2026-05-30 17:35 | backend-ai | api | EB-019 通知总线首段完成：新增非持久运行态 `Channels.Notify` 和 `NotificationHub`；WS 新增 `topic=notifications` 和 `notification.event`，payload 为 `RuntimeNotification`，字段包括 `id/type/level/project_id/project_code/task_id/test_no/var_id/var_name/display_name/message/payload/occurred_at`；当前通知类型覆盖 `alarm.limit.enter/recover/level_change`、`detection.run_started/stopped/abnormal_stop/paused/resumed`、`detection.result_ok/result_ng`、`detection.features_updated`。前端 shared type 已同步，前端需接入全局通知/工位通知展示 | open
- 2026-05-30 18:35 | backend-ai | api | EB-019 通知中心持久化落地：新增 `sys_notifications`、`sys_notification_recipients`，`Channels.Notify` 由 `NotificationDispatcher` 先落库再通过 `NotificationHub` 在线推送；新增 `GET /api/v1/notifications`、`GET /api/v1/notifications/unread-count`、`POST /api/v1/notifications/{id}/read`、`POST /api/v1/notifications/read-all`，权限为 `view_realtime`。前端 shared DTO/API 已同步，EB-048 需接 UI 未读数、列表和已读操作 | open
- 2026-05-30 18:52 | backend-ai | api | EB-019 通知分发对象细化：`NotificationDispatcher` 落库生成 recipient 时已按 `target_type` 分发，`all` 给全部启用本地用户，`user` 给指定用户 ID，`role` 给指定角色用户；`project` 的项目成员收敛已由 2026-05-31 07:38 记录补齐。 | closed
- 2026-05-30 17:30 | backend-ai | decision | 明确前端实时检测配置获取方式：配置管理走 HTTP `detection-standards` CRUD；“当前配置”不是最新标准表，而是检测启动后冻结的 `detection_run_standard_items`，前端通过 `GET /api/v1/detection-runs?project_id=...&status=running&limit=1` 或 `GET /api/v1/detection-runs/{id}` 获取；实时值仍走 WS/`realtime/variables`，由前端按 run snapshot 拼 OK/NG 展示 | open
- 2026-05-30 17:45 | backend-ai | api | 新增 `GET /api/v1/detection-runs/current?project_id=...`，返回某项目当前 running 或 paused 检测详情，包含冻结 `standard_items`、`storage_routes`、notes 和 reports；缺 `project_id` 返回 400，无当前任务返回 404。前端 shared API 新增 `getCurrentDetectionRun(projectId)` | open
- 2026-05-30 15:55 | backend-ai | api | EB-047 首段实施：新增 `POST /api/v1/variables/bulk-remap/kio-projects`，用于本地 KIO 调试批量确保 12 个项目并按 `台N_xx` 原名把变量归属到项目 N，重映射 `var_name/display_name*`，创建 disabled 默认 storage route，并重载 `TagManager`；前端 shared API 已补 `bulkRemapKioProjects` 类型与调用 | open
- 2026-05-30 16:01 | backend-ai/test-ai | test | EB-047 执行与验证：本地 dry-run 命中 504 个 `台N_xx` KIO 变量；真实执行创建缺失项目 6 个并更新 504 个变量，形成 `AC-01..AC-12`，项目实时快照为 AC-01 46 个、AC-02..AC-12 各 42 个，`/health` tags=1028；复测 `台1_41` 保留 `raw_name/write_path=台1_41` 和 `RW` 写入约束，业务名为 `kio_01_41`，disabled 默认 route 列同步为 `kio_01_41` | open
- 2026-05-30 16:01 | backend-ai/test-ai | test | EB-047 门禁通过：`go test ./...`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 70.1%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；前端 `npm run lint`、`npm run build`、`npm run test -- --run` 通过；后端已用新二进制重启并保持 `127.0.0.1:18080` PID 49904 | open
- 2026-05-30 16:28 | backend-ai | api | EB-045/046 任务模板继续迭代：`GET /api/v1/task-flow-templates` 增加固定时长检测、合格持续检测、暂停、恢复、运行中限值调整、特征值刷新和报表登记模板；任务流参数绑定支持 `optional=true` 和 `default`，前端 shared API 补任务模块、模板、执行记录和 SQL 日志 typed 调用 | open
- 2026-05-30 16:28 | backend-ai/test-ai | test | EB-045/046 验证通过：`go test ./...`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 70.2%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；前端 `npm run lint`、`npm run build`、`npm run test -- --run` 通过；后端重启 PID 60884，`/health` ok，smoke 确认 task modules=16、task-flow templates=12 | open
- 2026-05-30 13:55 | backend-ai | decision | EB-045 任务模块继续迭代：新增 task-flow-runs/SQL 日志查询 API、受控 write_variable、origin/depth 防递归、任务模板、检测静音/level_change/运行中限值调整、storage_prepare、register_report、http_request；前端需适配 EB-046 新契约 | open
- 2026-05-30 13:58 | backend-ai | test | EB-045 验证通过：`go test ./...`、覆盖率 70.1%、`go vet ./...`、`golangci-lint run ./...`、`go build ./cmd/edge-backend`；后端已重启并通过 `/health`、登录、`task-modules/task-flow-templates/task-flow-runs` smoke | open
- 2026-05-27 09:55 | review-ai | decision | AI 协作文档体系初始化 | open
- 2026-05-27 09:55 | test-ai | test | `backend/go test ./...` 通过，当前无测试文件 | open
- 2026-05-27 12:20 | frontend-ai | decision | 前端技术选型固定为 Electron + React 19 + TS 6 + Vite 8 + Ant Design + TanStack Query + Zustand + XState；Luckysheet 仅允许走隔离 adapter | open
- 2026-05-27 12:20 | frontend-ai | decision | 初始化 `desktop/`，Electron main 负责 Go sidecar 启动、健康轮询和日志入口，renderer 只走 preload 与 typed HTTP client | open
- 2026-05-27 12:35 | test-ai | test | `desktop/npm run build`、`desktop/npm run lint`、`backend/go test ./...`、`desktop/npx electron-builder --dir`、`desktop/npm run package` 通过；Playwright 已截图验证 `http://127.0.0.1:4173` 首屏 | open
- 2026-05-28 16:57 | frontend-ai | decision | 从 `react-design-prototype` 迁入工位操作与历史查询页面，新增工作台导航；历史查询暂用前端样例数据，真实历史 API 待 backend-ai 提供 | open
- 2026-05-28 17:24 | frontend-ai | decision | 工位操作页按原型补齐左侧卡片池自适应高度、拖拽占位/overlay、胶囊按钮、动态背景与橘黄色物理光源高光 | open
- 2026-05-28 17:46 | frontend-ai | decision | 对照运行中的 `react-design-prototype` 继续还原工位页：卡片图表改回原型 ChartLine 黑线/MinMax 标签，右侧表格改回 table 结构、独立表头和 hover 滚动条 | open
- 2026-05-28 18:26 | frontend-ai | decision | 修复工位右侧固定表头下滚动条默认可见问题；历史查询页按原型拆分为图表、表格、甘特弹窗与样式组件，并新增 `.ai/skills/edge-glass-prototype-style` 风格还原 skill | open
- 2026-05-28 18:27 | frontend-ai | test | `desktop/npm run lint` 与 `desktop/npm run build` 通过；Playwright 已对 `/#/station`、`/#/history`、历史甘特弹窗和原型 `/reports` 同视口截图 | open
- 2026-05-28 18:34 | frontend-ai | decision | 设置页范围确认：实时数据源服务接 MQTT 站点、设备/项目分组、发现变量与数据库变量；历史数据源服务仅预留数据库连接来源配置位 | open
- 2026-05-28 19:38 | frontend-ai | decision | 新增 `/#/settings` 系统设置页、侧边栏入口、三语文案、typed API 扩展；实时数据源页接 gateway-configs/gateways/devices/variables/discover/assignment，历史数据源保持预留说明 | open
- 2026-05-28 19:39 | frontend-ai | test | `desktop/npm run lint` 与 `desktop/npm run build` 通过；Playwright 已截图验证 `/#/settings` 实时数据源与历史数据源两个 tab | open
- 2026-05-28 19:53 | frontend-ai | decision | 设置页信息架构调整为变量优先：项目/设备分组与变量列表为主，MQTT 站点降级为一次性采集源配置；本地后端离线时明确提示无法加载站点/分组/变量 | open
- 2026-05-28 19:54 | frontend-ai | test | `desktop/npm run lint` 与 `desktop/npm run build` 通过；确认当前 `127.0.0.1:18080` 未监听导致站点数据无法加载，Playwright 已截图验证离线提示 | open
- 2026-05-28 20:05 | frontend-ai | decision | 设置页进一步收敛为“站点概览 + 左侧变量筛选 + 右侧变量全信息表”；MQTT 站点新增/编辑/发现变量移动到弹窗，不再占主工作区 | open
- 2026-05-28 20:06 | frontend-ai | test | `desktop/npm run lint` 与 `desktop/npm run build` 通过；Playwright 已验证真实站点/变量数据渲染和站点弹窗 | open
- 2026-05-28 20:20 | frontend-ai | decision | 设置页重构为管理中心：左侧模块导航覆盖变量列表、实时数据源、历史数据源、系统设置；变量列表为默认主工作区并占主要面积 | open
- 2026-05-28 20:21 | frontend-ai | test | `desktop/npm run lint` 与 `desktop/npm run build` 通过；Playwright 已验证管理中心默认变量工作区 | open
- 2026-05-28 20:28 | frontend-ai | decision | 管理中心模块入口移动到标题右侧展示卡片，移除左侧模块导航，释放主区域宽度给变量筛选与变量表 | open
- 2026-05-28 20:29 | frontend-ai | test | `desktop/npm run lint` 与 `desktop/npm run build` 通过；Playwright 已验证卡片入口版管理中心 | open
- 2026-05-28 20:43 | frontend-ai | decision | 工位操作改为桌面首屏，边缘概览降级为调试看板；左侧工位操作按项目/设备自动扩展二级菜单，工位页按选中设备过滤实时变量与状态卡 | open
- 2026-05-28 20:44 | frontend-ai | test | `desktop/npm run lint` 与 `desktop/npm run build` 通过；Playwright 验证 `/#/` 首页工位、6 个设备二级菜单、`device_id=1` 精确高亮与状态卡切换 | open
- 2026-05-29 00:03 | frontend-ai | test | `desktop/npm run package` 完成 Go 后端与前端构建但旧 `release/win-unpacked` 文件锁导致首次安装包输出失败；改用全新输出目录验证 `electron-builder --dir` 可运行包、打包后 Electron 启动内置 `resources/backend/edge-backend.exe`，`/health` 返回 ok 且 tags=521；随后 `release-installer-smoke` 完整生成 nsis、portable 与 blockmap 产物 | open
- 2026-05-28 19:54 | backend-ai | test | 新增 `backend/tools/dbinit` 初始化 Docker 映射的本地 MySQL schema；`go test ./...`、`go build ./cmd/edge-backend` 通过；启动 `go run ./cmd/edge-backend`，`/health` 与 `/api/v1/runtime/channels` 返回 200 | open
- 2026-05-28 19:58 | backend-ai | answer | 为本地桌面/浏览器来源补充 Gin CORS 与 OPTIONS 预检；`go test ./...`、`go build ./cmd/edge-backend` 通过，Playwright 验证 `/#/settings` 无离线提示且浏览器上下文可读取 3 个变量 | open
- 2026-05-28 20:14 | review-ai | decision | 新增登录权限与 SSO 安全设计，明确本地用户 JWT、主站 service token、一次性 SSO ticket、三角色 capability 和后续 API 维护规则 | open
- 2026-05-28 23:57 | review-ai | decision | 将登录权限落地从 EB-004 总项拆分为后端认证鉴权 EB-008、前端登录权限守卫 EB-009、前端用户管理页面 EB-010 | open
- 2026-05-29 00:34 | backend-ai | answer | 在看板新增 Local Test Credentials，集中记录本地登录、MySQL、MQTT、KIO 写入和 service token 配置状态，供前端联调使用 | open
- 2026-05-29 00:52 | frontend-ai | decision | 以工位操作和历史查询为基准统一设置页、调试看板的浅色动态背景、玻璃面板、胶囊按钮和滚动条风格；调试看板登录/主站文案更新为当前认证状态 | open
- 2026-05-29 00:50 | backend-ai | decision | 新增根目录 `计划.md`，基于当前后端设计和 JG HJ 旧实现，明确采集与业务模块按 raw、clean、business、detection、export、验收分 9 次主迭代实现 | open
- 2026-05-29 01:41 | backend-ai | decision | 前端可见 DTO 变更：`GET/POST/PATCH /api/v1/devices` 增加/支持 `display_name`、`display_name_en`、`display_name_ja`；`name` 保留兼容，前端展示应按语言读取 display 字段并 fallback 到 `name`/`device_code` | open
- 2026-05-29 11:10 | review-ai | review | 新增 `多设备并行检测任务设计计划.md`，结合根目录需求、Word 需求、当前 Go 后端与 React 前端状态，明确并行检测不变量、API 增量、复杂度、Go 规范审阅和防瞎搞硬规则 | open
- 2026-05-29 11:10 | review-ai | decision | 看板新增 EB-015/EB-016/EB-017，分别留给后端、前端、测试推进多设备并行检测闭环 | open
- 2026-05-29 09:46 | frontend-ai | decision | 新增 EB-014：后端检测标准 EB-012 已完成但前端管理入口缺失，需要作为 frontend-ai 下一项处理 | open
- 2026-05-29 11:25 | review-ai | review | 补充虚变量设计边界：当前模型有 `placeholder/discovered` 但没有创建 API/UI；检测开始、状态、结果类 int 变量应作为虚变量由后端服务写入，不应伪装成 PLC 发现变量 | open
- 2026-05-29 11:45 | review-ai | review | 深度复查后发现后端已存在 `internal/services` 与 `internal/runtime/handlers`，虚变量、检测运行、报告模板后端已部分落地；新增 `后端模块化审阅与演进计划.md`，修正为沿用现有 handler/service，不再建议新增 `internal/detection` 包 | open
- 2026-05-29 11:50 | review-ai | decision | 在交互看板为 EB-018 补充详细施工流程，列明文档位置、P0-P5 执行顺序、每阶段验收和禁止事项，避免后续后端模块化按错误边界施工 | open
- 2026-05-29 13:22 | backend-ai | review | 确认实时与写操作首选 WebSocket，但必须经过后端 service，不允许客户端绕过后端直接写 MQTT；新增 EB-019/EB-020/EB-021 追踪 WS 通道、写操作审计和检测超限/事件流水 | open
- 2026-05-29 13:28 | frontend-ai | decision | EB-016 部分完成：管理中心新增“创建虚变量”入口，支持选择项目/设备并创建 `source_type=virtual` 的 `INT/FLOAT/BOOL/STRING` 变量；工位页检测 start/stop/abnormal-stop 与任务历史入口继续 open | open
- 2026-05-29 13:43 | frontend-ai | decision | EB-016 继续推进：工位页接入开始检测弹窗、标准/报表模板选择、start/stop/abnormal-stop mutation、任务历史跳转；历史查询页支持 `task_id`、`device_id`、`test_no` 查询参数 | open
- 2026-05-29 13:52 | frontend-ai | decision | 修复 EB-016 虚变量入口：新建虚变量弹窗补齐中文/英文/日文显示名，提交时英文/日文空值回退到中文显示名，避免三语显示断层 | open
- 2026-05-29 13:28 | backend-ai | decision | 补充 EB-019 前置约束：变量可以自动发现，但自动发现只生成候选来源变量；必须由变量属性/设备绑定/检测标准配置确认后，才允许参与检测、历史存储、超限判断或 WS 写命令 | open
- 2026-05-29 13:32 | backend-ai | decision | 新增 `backend/docs/变量属性检测配置与WS前置设计.md`，对照 JGHJ 旧后端变量模型，明确来源变量、业务变量、运行态变量和检测配置项的分层，以及 WS 写和检测超限的前置数据结构约束 | open
- 2026-05-29 13:41 | backend-ai | decision | 新增 `backend/docs/后端日志WS检测实施顺序.md`，明确先补全 EB-020 写操作审计和运行日志，再推进变量属性、检测配置、WS 实时读、WS 写命令和检测超限事件 | open
- 2026-05-29 13:52 | backend-ai | decision | 补充变量存储映射设计：变量属性负责 `storage_name/storage_target/storage_table/storage_value_column/form_field_key/query_alias`，上下限仍留在检测配置/运行快照，`store_enabled` 只表示某检测标准是否存该变量 | open
- 2026-05-29 14:02 | backend-ai | decision | 新增 EB-022：历史数据源 MySQL 配置从前端占位改为后端配置文件读写能力；接口必须隐藏明文密码，保存后要求重启后端生效，不在运行中热切换当前 GORM 连接 | open
- 2026-05-29 14:20 | backend-ai | decision | 新增 EB-023 给前端适配验收历史数据源 MySQL 配置页；新增 EB-024 到 EB-029 将后端后续拆为日志审计、变量属性/存储映射、检测配置、WS 只读、WS 写、检测事件/超限和联调回归七个阶段 | open
- 2026-05-29 15:05 | backend-ai | decision | 已给前端留下 EB-030：适配变量属性、存储映射、写入约束、防抖和启动快照高级编辑 UI；后端继续推进 EB-025 检测配置语义和运行快照冻结 | open
- 2026-05-29 16:02 | backend-ai | decision | 新增 EB-031：在 WS 写命令前先补强只读 WS 的断线重连和连接保护，避免后续写命令建立在脆弱连接模型上 | open
- 2026-05-29 16:35 | backend-ai | decision | EB-027 完成检测任务 WS 写命令首段：`command.detection.start/stop/abnormal_stop` 复用 `DetectionRunsService`，成功返回 `command.ack`，失败返回结构化 error，所有命令写 `sys_audit_logs`；KIO WS 写留到拆出 KIO service 后实施 | open
- 2026-05-29 16:36 | backend-ai | test | EB-027 首段验证通过：`go test ./...`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 70.0%、`go vet ./...`、`go build ./cmd/edge-backend`、`desktop/npm run lint`、`desktop/npm run build` 通过；`golangci-lint` 本机未安装 | open
- 2026-05-29 16:38 | backend-ai | test | 已重启本地后端 PID 70660 并保持 `127.0.0.1:18080` 运行；smoke 验证 `/health` ok、WS `command.detection.start` 返回 `cmd-smoke-start` ack、WS `command.detection.stop` 返回 `cmd-smoke-stop` ack | open
- 2026-05-29 14:25 | backend-ai | decision | EB-020 P0 实施：新增 runtime HTTP 写操作审计 middleware，受保护 `POST/PATCH/PUT/DELETE` 自动写 `sys_audit_logs`；审计 detail 只保留 request/command/route/status/actor/latency/error 元数据，不保存请求体或响应体，避免泄露密码、token、KIO 凭据和数据库连接密码 | open
- 2026-05-29 14:28 | backend-ai | test | EB-020 P0 验证通过：`go test ./...`、`go test ./... -coverprofile all-coverage.out` 生成 `backend/all-coverage.out` 且总覆盖率 70.5%、`go vet ./...`、`go build ./cmd/edge-backend` 通过；`golangci-lint` 本机未安装；已重启本地后端并 smoke 验证 `/health` ok、登录 ok、`POST /api/v1/system/database-config/test` 返回 `{"ok":true}` 且回传 `X-Request-ID=smoke-audit-1` | open
- 2026-05-29 15:06 | backend-ai | decision | EB-024 P1 实施：`sys_tags`、`TagConfig`、`POST/PATCH /api/v1/variables` 和 `desktop/src/shared/api/types.ts` 补齐变量存储映射、读写约束、防抖与启动快照字段；服务层增加默认值和写入约束校验，自动发现变量和虚变量默认不可写，缺少 `write_path` 的可写变量会被拒绝 | open
- 2026-05-29 20:40 | backend-ai | decision | EB-021 先落地超限报警生命周期表：新增 `detection_limit_alarms`，一条记录覆盖某任务内某变量从超限开始到恢复，字段包含任务/设备/标准项/变量快照、报警类型和等级、开始/峰值/恢复值、阈值、质量、开始/最后/恢复时间和持续时长；暂不实现实时判定状态机 | open
- 2026-05-29 23:05 | backend-ai | decision | 新增 `backend/docs/变量默认上下限与业务报警装配设计.md`；变量默认上下限作为 `sys_tags` 资产属性保留，检测标准项新增 `alarm_enabled`，检测启动时把变量默认属性冻结到运行快照 `variable_default_*`，变量 PATCH 可通过 `apply_to_running=true` 只同步 running 任务变量默认快照 | open
- 2026-05-29 23:05 | backend-ai | decision | 新增 EB-034 给前端：适配变量默认上下限、默认报警开关、检测标准项 `alarm_enabled`、任务详情运行快照 `variable_default_*` 和变量编辑 `apply_to_running` 风险提示 | open
- 2026-05-29 23:58 | backend-ai | decision | EB-020 追加后端审计日志查询：新增 `GET /api/v1/audit-logs`，受 `system_settings` 权限保护，支持 actor/action/target/result/time/limit/offset 过滤，响应 `{items,total,limit,offset}`；同步前端共享 DTO/API 便于设置页后续接入 | open
- 2026-05-29 23:59 | backend-ai | test | EB-020 审计日志查询验证通过：`gofmt`、`backend/go test ./...`、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`、`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test` 均通过；新增 kernel 测试覆盖查询成功、非法 limit、非法时间 | open
- 2026-05-29 23:46 | backend-ai | decision | 检测配置补 `check_cycle_ms/check_on_start` 并同步运行快照；变量属性补 `default_violation_hold_ms/default_recover_hold_ms`。检测开始首帧存储按变量 `startup_snapshot_enable`，检测开始首帧判断按业务 `check_on_start`，二者不绑定；`check_cycle_ms=0` 时快照继承变量 `store_cycle_sec` | open
- 2026-05-30 00:43 | backend-ai | decision | EB-021/EB-028 首段完成：新增 `Alarm` 队列和批量入库 worker，`TaskManager` 在设备运行上下文维护业务超限状态；数据变化和周期扫描按运行快照 `alarm_enabled/check_enabled/check_cycle_ms/limit_*/limit_deadband/violation_hold_ms/recover_hold_ms/quality_policy` 触发上限、下限报警进入和恢复，清洗层仍只更新实时值和质量戳 | open
- 2026-05-30 01:05 | backend-ai | decision | EB-028 继续推进：新增 `detection_run_events` 和 `detection_run_summaries`，检测开始、正常停止、异常停止写轻量事件；摘要按需从任务、历史数据和超限报警表刷新，避免把高频报警明细重复写成第二套事件表造成双倍写入压力 | open
- 2026-05-30 01:05 | backend-ai | api | 新增前端可见接口：`GET /api/v1/detection-runs/:id/summary` 返回 `result_status/history_rows/alarm_total/alarm_active/alarm_recovered/alarm_above_h/alarm_above_hh/alarm_below_l/alarm_below_ll/first_alarm_at/last_alarm_at`；`GET /api/v1/detection-runs/:id/events?limit=` 返回任务生命周期事件列表；前端共享 DTO/API 已同步 | open
- 2026-05-30 01:55 | backend-ai | decision | EB-037 首段实施：新增 storage route 模型和运行快照，`sys_tags.storage_*` 保留兼容，设备宽表主路径由 `sys_storage_routes/detection_run_storage_routes` 承载；检测启动时冻结 routes 并确保 `rt_device_{device_id}_data` 和动态列存在，写入热路径暂未切到宽表 writer | open
- 2026-05-30 09:16 | backend-ai | decision | EB-037 追加宽表写入路径：`DetectionTask`/`ActiveTask` 携带运行 storage routes，`StoreTask` 携带路由快照；`InsertHistoryBatch` 保持写 `rt_history_data` 兼容表，同时对 `wide_table` routes 写入 `rt_device_{device_id}_data`，同一 `(task_id,sample_bucket_ms)` 使用 upsert 更新动态列 | open
- 2026-05-30 09:27 | backend-ai | decision | EB-037 补齐宽表历史读取和批量合并：宽表 writer 先按 `table/task_id/sample_bucket_ms` 聚合多变量后一次 upsert；`GET /api/v1/history/data` 在 `task_id/device_id/test_no` 命中 `detection_run_storage_routes` 时优先读取 `rt_device_{device_id}_data`，并重建旧 `HistoryData` DTO，前端接口字段不变 | open
- 2026-05-30 09:48 | backend-ai | decision | EB-037 存储设计审查后推翻变量级存储频率：变量属性只保留采集、清洗、写入约束和默认报警；存储目标表/列、周期、变化死区、首帧存储和启用状态全部归 `sys_storage_routes`；新增 storage routes API，前端需停止把 `store_cycle_sec/store_deadband/store_trigger/store_mode/storage_table` 作为变量编辑主入口 | open
- 2026-05-30 10:15 | backend-ai | decision | 用户确认本轮不保留旧 `/devices` 兼容入口；新增 EB-041/042/043/044：项目命名统一、变量存储属性删除、任务系统倒排索引、存储总线。前端需同步迁移到 `/api/v1/projects`、`project_id/project_code` 和 `storage-routes` 配置 UI | open
- 2026-05-30 10:40 | backend-ai | decision | EB-041/042/043/044 首段实施：后端只注册 `/api/v1/projects`，模型和 schema 切到 `sys_projects/project_id/project_code/rt_project_*`；启动迁移会把旧 `sys_devices/device_id/device_code` 改到项目命名并删除变量存储属性列；变量 API 不再接收/返回 `store_*`、`storage_*`、`startup_snapshot_enable`；新增 `sys_task_rules` 和 `TaskRuleIndex` 倒排索引地基；存储 worker 改为 `StorageBus` 按 `project_id + table_name` 分 bucket 批量 flush | open
- 2026-05-30 11:10 | frontend-ai | decision | 适配 EB-041/042 后端破坏性契约：前端 shared DTO/API 将项目字段 `project_id/project_code` 映射到旧页面别名，`getDevices/createDevice/updateDevice` 实际调用 `/api/v1/projects`；变量创建/编辑/分配和历史查询参数会发送项目字段并过滤变量级存储字段；设置中心新增“存储路由”模块，通过 `/api/v1/storage-routes` 管理项目宽表目标、列、触发、周期、死区和启用状态 | open
- 2026-05-30 11:30 | backend-ai | decision | EB-045 条件事件任务系统首段实施：新增任务流模型、任务变量引用、执行日志、SQL 日志、goja JS 引擎、异步执行队列和 priority 小批量排序；数据变化热路径只通过 `var_id -> flow_ids` 倒排索引投递任务，不直接执行脚本；内置 `builtin.storage_snapshot` 复用 StorageBus 写入，不新建数据库连接 | open
- 2026-05-30 11:45 | backend-ai | decision | 已给 frontend-ai 新增 EB-046：侧边菜单新增“任务”模块承载后端 task-flows，列明当前可接 API、页面字段、权限、三语、手动运行、脚本编辑器、执行记录和 SQL 日志占位要求；后端仍需后续补执行记录/SQL 日志查询 API | open
- 2026-05-30 12:06 | frontend-ai | decision | EB-046 前端首段完成：新增 `/#/tasks` 路由和侧边菜单“任务”入口，受 `system_settings` 权限保护；新增 task-flows typed API、任务列表、新建/编辑弹窗、项目筛选、变量角色绑定、条件脚本、动作脚本/参数、手动运行按钮、执行记录和 SQL 日志占位 tab；编辑已有任务时项目选择禁用，因为当前后端 PATCH 未开放 project_id 修改 | open
- 2026-05-30 12:30 | backend-ai | api | 新增 `GET /api/v1/task-modules`；扩展 `POST/PATCH /api/v1/task-flows` 支持 `steps_json`；`sys_task_flows` 新增 `steps_json` 字段，旧 `action_type/action_payload/action_script` 保持单步骤兼容；正式业务参数不由手动运行接口承载 | open
- 2026-05-30 12:50 | backend-ai | decision | 已删除 `backend/docs/任务数据流转与内置模块设计.md`，新增 `backend/docs/任务变量流转与内置模块设计.md`；任务业务参数只从 watched STRING 虚拟变量的 JSON 字符串解析为 `task_params`，参数绑定来源改为 `trigger_param`，HTTP 手动运行只保留开发者调试触发 | open
- 2026-05-30 13:20 | backend-ai | api | EB-045 新增任务流内置检测模块：`builtin.start_detection_run` 支持 `enable_storage/enable_alarm/duration_sec/standard_id/report_template_id`，新增 `stop_detection_run/pause_detection_run/resume_detection_run/fixed_duration_guard/qualified_hold_guard/refresh_features`；`sys_detection_tasks.status` 新增 `paused`，`end_type` 新增 `fixed_duration/qualified_hold/task_flow_stop`；新增 `detection_run_features`，并开放 `POST /api/v1/detection-runs/:id/pause|resume`、`GET /api/v1/detection-runs/:id/features` | open
- 2026-05-30 13:40 | backend-ai | api | 暂停时长语义修正：`sys_detection_tasks` 新增 `pause_started_at`、`paused_duration_ms`；暂停记录起点，恢复累计暂停时长并顺延 `expected_end_at`，停止 paused 任务会把当前暂停段计入 `paused_duration_ms`，`detection_run_summaries.duration_ms` 扣除所有暂停时间 | open
- 2026-05-30 16:58 | backend-ai | api | EB-045 任务流执行器兼容补强：`steps_json` 正式契约仍推荐多步骤数组，但后端现在也接受单个 step object 并包装为单步骤执行，避免调试脚本或 PowerShell 单元素数组序列化成对象后导致任务失败 | open
- 2026-05-31 06:16 | frontend-ai/test-ai | test | 驾驶舱按 Codex 官网产品页方向继续重做视觉层级：背景去掉大块科幻光团，改为低饱和蓝白产品页层次和细网格；标题 SVG 降噪，卡片改为更透明的白蓝玻璃、轻阴影、细边框并保留工位同源暖光边缘；3D 模型下移放大但避免继续裁切。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`、`git diff --check`，1920×1080 截图 `output/playwright/model-cockpit-codex-style-v4-1920x1080.png` | closed
- 2026-05-31 06:27 | frontend-ai/test-ai | test | EB-047 前端项目迁移继续收口：三语资源中清理“项目/设备”“设备/项目”“Project/device”“プロジェクト/設備”等混合文案；工位页开始检测表单页面层改用 `project_id`，`DetectionRunStartPayload` 改为项目主字段，旧 `device_id` 仅保留为兼容 alias。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`、`git diff --check`；Playwright 登录后点击左侧项目二级菜单，确认 URL 为 `/#/?project_id=1` 且不含 `device_id`，设置页可见文本无旧混合文案，截图 `output/playwright/project-copy-cleanup-smoke.png` | closed
- 2026-05-31 06:49 | frontend-ai/test-ai | test | 驾驶舱按用户反馈继续参考 Codex 官网产品页风格收敛：`model-cockpit.css` 改为白底、低饱和软蓝渐变、细网格、轻玻璃卡和更干净的深色文字层级，保留动态光源驱动的暖色卡片边缘；设置页变量分配、批量分配、虚变量创建和 storage route 变量联动继续收口到 `project_id/project_code`，`VariableAssignmentPayload.device_code` 改为兼容可选字段。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`、`git diff --check`；Playwright 1920×1080 截图 `output/playwright/model-cockpit-codex-style-1920x1080.png` | closed
- 2026-05-31 07:08 | frontend-ai/test-ai | test | EB-047 前端项目语义继续闭合：检测配置页和设置页检测标准弹窗保存字段从 `device_id/device_code` 改为 `project_id/project_code`；任务页内部项目列表、变量筛选、动态参数项目选择和运行日志展示改为项目语义，旧变量 `device_id/device_code` 只通过 helper fallback 读取；三语资源继续清理可见混合文案。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`、`git diff --check`；Playwright 登录后验证 `/api/v1/projects=200`、`/api/v1/devices=404`，并点击 `/#/detection-config` 新增标准、`/#/tasks` 新增任务和发送任务请求弹窗，截图 `output/playwright/detection-config-project-field-smoke.png`、`output/playwright/task-flows-project-request-smoke.png` | closed
