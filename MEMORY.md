# MEMORY

Last updated: 2026-06-01 14:35 Asia/Shanghai

## 当前阶段

2026-06-01 14:35 `frontend-ai` 已推进 EB-055 前端一套代码适配 Edge/Main Server 首段：`desktop/` 新增 `VITE_APP_ROLE=edge|main_server`、`VITE_MAIN_API_BASE_URL`、统一 `env.apiBaseUrl` 和 runtime feature gating，HTTP/SSE/WS 不再固定 edge baseURL；Electron main 在 `main_server` 角色只检查主服务器后端健康、不启动 `edge-backend.exe`，日志使用 `main-server-desktop.log`，watchdog 关闭；状态页在主服务器角色显示同步数据库、边缘控制目标、查询代理和报表服务，设置页隐藏网关新增/编辑与 KIO 初始化，系统页将重启降级为刷新后端状态并隐藏 sidecar watchdog。已同步三语文案和 `AI_BOARD.md`。验证通过 `desktop/npx tsc -b --pretty false`、`desktop/npm run lint`、`desktop/npm run test -- --run`、默认 edge `desktop/npm run build`、`VITE_APP_ROLE=main_server VITE_MAIN_API_BASE_URL=http://127.0.0.1:19080 desktop/npm run build`；build 仅有既有 Vite 大 chunk 提示。后续继续把剩余 `SidecarStatus/edgeDesktop` 命名升级为中性兼容层，并补主服务器报表生成/文件资产入口。

2026-06-01 14:20 `review-ai` 已按用户要求新增前端一套代码适配 Edge/Main Server 的稳定计划文档 `main-server/docs/frontend-runtime-role-adaptation-plan.md`，并挂到 `AI_BOARD.md` EB-055。文档记录审阅结论：API baseURL 基本集中可配置，主要阻塞在 sidecar/Electron bridge、Edge 状态页、设置页网关/KIO 能力和固定文案；实施建议是新增 `runtimeRole=edge|main_server` 与 feature gating，保持业务 API 路径不分叉，由后端负责查询本地 MySQL、控制请求转发边缘端和主服务器报表生成。该文档作为 `frontend-ai` 后续实施依据；本轮未改前端运行代码，未跑构建测试。

2026-06-01 14:06 后端按 `backend-ai` 新增主服务器端首版拆分目录 `main-server/`，未修改既有边缘端 `backend/` 与 `desktop/` 代码。`main-server/desktop` 复制当前边缘端所有画面，改为主服务器应用名，默认 `VITE_MAIN_API_BASE_URL=http://127.0.0.1:19080`，Vite 开发服务绑定 `0.0.0.0:5273` 支持内网 Web 访问，Electron 壳不再启动 edge sidecar，只检查主服务器后端健康；`main-server/backend` 新增 Go 后端骨架，默认监听 `0.0.0.0:19080`，连接主服务器本地同步 MySQL，提供 `/health`、`/api/v1/main-server/status`、`/api/v1/edge-proxy/*path` 和 `NoRoute` 下的 `/api/v1/**` 迁移期代理，写请求转发边缘端，`edge.query_proxy_enabled=true` 时可临时代理读请求保持页面可用。验证通过 `main-server/backend gofmt`、`go mod tidy`、`go test ./...`、`go build ./cmd/main-server`、`main-server/desktop npm install`、`npm run build`；已启动主服务器后端 `http://127.0.0.1:19080/health` 返回 `status=ok/database_ok=true`，已启动前端预览 `http://127.0.0.1:4273` 返回 200。build 仅有大 chunk/插件耗时提示，npm audit 当前提示 2 个 moderate vulnerability。已在 `AI_BOARD.md` 新增 EB-055，后续需把主服务器 GET 查询路由改成本地 MySQL 只读实现并关闭查询代理，报表生成/文件资产/重新生成 worker 归主服务器。

2026-06-01 13:49 前端按用户反馈微调驾驶舱标题位置：`/#/model-cockpit` 的 PNG 标题背景整体上移 `10px`，保留图片背景、fit-content 宽度和 runzhong V3 右侧趋势列比例。Vite dev smoke 确认标题 `transform=translateY(-10px)`、标题底部到顶部状态卡仍有 `17px` 间距、`usesPng=true`、趋势卡 `2`、页面无整体滚动，截图 `desktop/output/playwright/model-cockpit-title-shifted-up.png`。

2026-06-01 13:22 前端按 `frontend-ai` 修复报表页设置栏和公式结算模型：`/#/reports` 左侧设定区改为整栏可滚动，字段映射列表限制局部高度，避免图表/位置/公式卡片被挤出后不可达；报表工作簿新增“检测任务参数”区，使用 `params_json` 和 `process_params` 语义展示温度上下限、进风口面积、合格保持时间等本次检测冻结参数；公式结算从旧 Excel 字符串样例改为读取当前 Luckysheet 中的报表变量趋势/字段值，计算平均值、最大值、面风速，并输出“合格/不合格”。验证通过 `desktop/npm run lint`、`desktop/npx tsc -b`、`desktop/npm run test -- --run`、`desktop/npm run build`；已重启 `127.0.0.1:4173` 预览服务 PID 43084；Playwright 登录 `/#/reports` 验证左侧设定区 `scrollTop=650`、输出 `J38=51.6 °C/J39=59.4 °C/J40=32.6 %RH/J41=不合格/J42=0.092 m/s`，截图 `desktop/output/playwright/reports-task-formula-scroll-smoke.png`。

2026-06-01 13:16 前端按用户给定图片替换驾驶舱标题背景：将 `C:/Users/SunYufei/Desktop/标题背景.png` 复制为 `desktop/public/cockpit/cockpit-title-bg.png`，`/#/model-cockpit` 标题去掉旧 `CockpitTitleFrame` 圆角玻璃背景，改用 PNG 作为 `.cockpit-title-bar::before` 背景；标题容器 `width: fit-content`，宽度由标题/副标题文本和左右留白撑开。Vite dev smoke 通过：`usesPng=true`、`imageOk=true`、旧标题框节点 `0`、标题约 `601x90`、趋势卡 `2`、页面无整体滚动，截图 `desktop/output/playwright/model-cockpit-title-image-bg.png`。`npx tsc -b --pretty false` 当前被既有 `ReportsPage.tsx` 类型错误阻断：`ReportsPage.tsx(612,21)` 参数数量不匹配，`ReportsPage.tsx(763,34)` union 上不存在 `formula`。

2026-06-01 11:20 后端按 `backend-ai` 推进报表模板参数与检测报表请求契约：新增 `backend/docs/报表模板参数与检测报表请求设计.md`；`sys_report_templates` 增加 `params_schema_json`，报表模板 API 支持 `params_schema` / `params_schema_json`；`detection_run_report_requests` 从“每变量一行”升级为“每份报表请求一行”，冻结模板快照、`variables_json` 变量组和 `params_json` 公式参数，旧 `var_ids/ext_1/ext_2/ext_3` 格式仍兼容但不作为新需求入口。已同步 `backend/docs/backend-architecture.md`、`backend/docs/边缘端全链路数据流转与分发图.md`、`backend/docs/边缘端与主服务器协同计划.md` 和 `AI_BOARD.md` 前端适配清单；验证通过 `go test ./internal/database ./internal/runtime/handlers ./internal/services`、`go test ./...`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 72.4%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`、`/health`、`go run -tags smoke_tools ./tools/smokehealth` 和完整 `go run -tags smoke_tools ./tools/smokebackend -latency-n 30 -latency-c 10 -timeout 120s`；完整 smoke steps=4 elapsed=47.756s，并发延迟 p95=1299.6ms、max=1334.0ms，核心队列/task-flow/notification hub `dropped=0/pressure=false`，workers=12。本地后端已重启为 PID 43928。

2026-06-01 11:10 前端按 `frontend-ai` 修复检测配置页枚举显示仍为英文的问题：后端返回 `numeric_range/bool_equals/string_equals/regex`、`ignore_bad/record_invalid/fail_on_bad` 是稳定协议枚举，问题在前端显示层此前直接把枚举值作为 label。现已在 `/#/detection-config` 和设置中心检测标准编辑表统一改为 i18n label，中文显示“数值范围/布尔等于/文本等于/正则匹配”和“忽略异常质量/记录为无效/异常质量判失败”，并补齐英文、日文 key；提交给后端的枚举值保持不变。验证通过 `desktop/npx tsc -b`、`desktop/npm run lint`、`desktop/npm run build`。

2026-06-01 10:59 前端按 `frontend-ai` 继续修复 `/#/detection-config` 的 `PERF-520-STD / Perf 520 Standard` 卡顿：主表在保留虚拟滚动的同时启用分页，默认 30 条/页，可切 20/30/50/100 并支持跳页；新增/编辑标准弹窗里的标准项表也启用分页，默认 20 条/页。主表 `scroll.y` 改为数字高度，确保 Ant Design Table virtual 稳定生效。验证通过 `desktop/npx tsc -b`、`desktop/npm run lint`、`desktop/npm run build`；Playwright smoke 打开 `PERF-520-STD` 确认分页文本为 520 标准项、默认 30 条/页，整页 `scrollHeight=innerHeight=900`，实际只渲染 10 个可视行、112 个 input、40 个 switch。

2026-06-01 10:56 前端按 `frontend-ai` 修复 `/#/detection-config` 卡顿和编辑层级问题：检测标准列表从左侧卡片栏改为顶部工具条最左侧下拉框，移除左栏后释放横向空间给变量设置；主表从只读快照改为直接可编辑表格，检测/存储/报警/启动判定开关、判定周期、方法、目标值、上下限、回差、超限/恢复保持、质量策略都可在表内修改，点击“保存”直接提交当前标准和标准项；变量加入标准也放在主表上方，不再为了变量设置进入二级弹窗。性能上启用 Ant Design Table 虚拟滚动，520 行标准的可见 DOM 从 5720 个输入框/2080 个开关降到 110 个输入框/40 个开关；检测配置页禁用背景动态噪声和重动画，降低渲染压力。验证通过 `desktop/npx tsc -b`、`desktop/npm run lint`、`desktop/npm run build`；Playwright smoke 确认 `/#/detection-config` 整页 `scrollHeight=innerHeight=900`、`windowScrollY=0`、顶部检测标准下拉 1 个、左侧标准卡片 0 个、虚拟表格可见行 10 行。

2026-06-01 10:55 前端按 `frontend-ai` 修复历史页真实数据接入后的布局问题：`/#/history` 顶部工具栏从单行左右挤压改为上下两行、状态组和操作组允许换行，长任务号/时间按钮做最大宽度和省略；任务甘特弹窗增加 `max-height: calc(100dvh - 48px)`，内部 lanes 区域独立纵向滚动，项目标签单行省略，避免真实任务多时上下撑出屏幕。验证通过 `desktop/npx eslint src/features/history-query` 和 `git diff --check -- desktop/src/features/history-query/history-query.css`；未做浏览器截图验证，因为当前可用工具未暴露 in-app browser 导航截图能力。

2026-06-01 10:47 前端按 `frontend-ai` 继续增强报表页：`/#/reports` 新增线图图片嵌入位置/尺寸控制，调整 X/Y/W/H 后重新加载线图会按新坐标写入 Luckysheet 图片对象；报表工作簿新增“公式区域”，D 列保留公式文本，J 列作为结算输出，点击“读取公式并结算输出”会从当前 Luckysheet 工作簿读取公式区域、计算公式结果并写回报表输出单元格，输出单元格强制文本格式避免百分比被 Luckysheet 转成内部小数。`luckysheetAdapter` 新增 `getWorkbook()` 供页面读取当前表格。同步修复历史查询模块缺失的 legacy 导出 `HistoryRow/generateHistoryData/historyItemsToRows/buildGanttData`、HistoryQueryPage 的 `?? ||` 构建错误，保证 build 可过。验证通过 `desktop/npm run test -- --run`、`desktop/npm run build`；Playwright 登录 `/#/reports` 后将图表 X 改为 250，加载线图并结算，确认图片 `left=250/top=348/360x190`，公式文本 `D25==ROUND(AVERAGE(C32:C38),1)`，输出 `J25=51.6 °C/J26=59.4 °C/J27=32.6 %RH/J28=48.6 %` 且 `J28` 为文本类型，截图 `desktop/output/playwright/reports-formula-placement-smoke.png`。`desktop/npm run lint` 当前仍被既有 `DetectionConfigPage.tsx` 未使用变量和同步 setState 规则阻断，另有 `HistoryQueryPage.tsx` 同步 setState 旧规则，需后续单独清理。

2026-06-01 10:47 前端按 `frontend-ai` 将 `/#/history` 历史查询页从原型样例数据切换为真实后端接口：任务来源接 `GET /api/v1/detection-runs?limit=200`，历史数据继续走 `GET /api/v1/history/data` 并在前端按 `source_time + var_id_text/var_id` 动态透视为曲线和表格列，任务时间线改用真实检测任务 `started_at/ended_at/status/test_no/project_code`，无数据时显示空态，不再回退随机样例数据；存储快照仍使用 `GET /api/v1/detection-runs/{id}/storage-routes`。接口审计结论：本轮无需新增后端接口。验证：`desktop/npx eslint src/features/history-query src/shared/i18n/resources.ts` 通过，`desktop/npm run test -- --run` 通过；全量 `desktop/npm run lint` 和 `desktop/npm run build` 被当前工作树中无关的 `desktop/src/features/detection-config/DetectionConfigPage.tsx` 未使用变量/缺失 `itemColumns` 等错误阻断。

2026-06-01 10:32 前端按 `frontend-ai` 继续配合后端检测配置/任务系统交接，补齐 `report_request` 前端对接：shared DTO/API 新增 `DetectionRunReportRequest*`、`DetectionRunStartPayload.report_request` 和 `GET /api/v1/detection-runs/{id}/report-requests`；工位开始检测弹窗新增“本次报表变量”和 `ext_1/ext_2/ext_3` 备注字段，提交时生成 `report_request.var_ids/ext_*`；工位“标准快照”弹窗追加报表请求快照表；任务页“发送任务请求”弹窗新增报表变量选择和 ext 字段，JSON 预览会生成 `report_request`。验证通过 `desktop/npx tsc -b`、`desktop/npm run lint`、`desktop/npm run test -- --run`、`desktop/npm run build`；Playwright 登录后确认任务请求弹窗显示“本次报表变量”和 JSON 预览。工位页当前所选项目已有 running 任务，开始检测按钮不可见，因此该入口做代码和构建验证。

2026-06-01 10:21 前端按 `frontend-ai` 修复报表页字段映射不生效：`/#/reports` 的工作簿生成现在解析 A1 单元格地址并按字段映射 upsert 到 Luckysheet `celldata`，模板默认值不再硬编码占住 `C4/H4/C5`，编辑映射后重新挂载即可把字段值写入目标单元格。同步修复驾驶舱 `@phosphor-icons/react` 不存在的 `Activity` 导入，改用库内存在的 `Pulse`，恢复构建。验证通过 `desktop/npm run lint`、`desktop/npm run test -- --run`、`desktop/npm run build`；Playwright 登录 `/#/reports` 后把“机型”映射改为 `B6`，确认 Luckysheet 内部 `B6=CRAC-EDGE`、`C4=null`、`H4=EDGE-3D-01`、`C5=Spindle Lab`，截图 `desktop/output/playwright/reports-field-mapping-smoke.png`。

2026-06-01 10:03 前端按 `frontend-ai` 修复报表页“加载线图到 Excel”图片未出现在指定区域的问题：根因是 Luckysheet adapter 只向初始化参数传 `data`，图片对象没有进入 sheet 级 `images`，同时报表页此前只写入“图表锚点”文字。现已在 `desktop/src/features/spreadsheet/luckysheetAdapter.ts` 统一解析 `{sheets, images}` 工作簿，并把图片兼容挂到首个 sheet 的 `images`；`/#/reports` 会把 mock 任务趋势线图生成 SVG data URL 图片对象，定位到表格右侧可视区域，并保留下方趋势数据表。验证通过 `desktop/npm run lint`、`desktop/npm run test -- --run`、`desktop/npm run build`；Playwright 登录后点击“加载线图到 Excel”，确认 Luckysheet iframe 内 `imageKeys=["report_trend_chart"]`、图片 `360x190` 出现在 `left=300/top=348` 且完整落在 iframe 可视范围内，截图 `desktop/output/playwright/reports-chart-image-area-smoke.png`。

2026-06-01 10:05 前端按 `frontend-ai` 对驾驶舱顶部 5 个卡片的图标实施高感度 UI 改进：由低关联、低精度、且重复度高的原有图标，全新替换为极富精密控制、试验室研究以及国家级质检权威感的 Lucide 图标系列（产品型号换为 `Cpu`、设备序列号换为描述资产编码的 `Barcode`、客户实验室换为代表 Spindle/CR 实验室尖端水准的 `FlaskConical`、测试时长换为精密计时跑表 `Timer`、检测结果换为质量达标认证徽章 `BadgeCheck`）。同时在 `desktop/src/features/model-cockpit/model-cockpit.css` 中，为这五个卡片新增基于 CSS 变量继承的柔美渐变呼吸发光色调（经典蓝、翡翠青、睿智紫、高精橙金、极致安全绿），并将左侧装饰竖小笔连带改为变量同步渐变，极大地提升了整个驾驶舱大屏的全局科技艺术感与 UI 品质。静态校验 `ModelCockpitPage.tsx` 0 errors，所有新图标导入和应用均完整无误。

2026-06-01 09:54 后端按 `backend-ai` 完成 EB-054 检测配置契约补强：`sys_detection_standard_items` 保存/替换时按 `var_id` 从 `sys_tags` 补齐缺失的三语显示名、单位和小数位快照，运行开始后继续冻结到 `detection_run_standard_items`，变量后续改名不影响历史标准/运行快照；`backend/docs/backend-architecture.md` 已列清检测配置页数据源、涉及表、冻结关系和枚举含义，看板已交接前端补固定下拉三语翻译。验证通过 `go test ./internal/database ./internal/runtime/handlers`、`go test ./...`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 72.3%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`、`GET /health` 和 `go run -tags smoke_tools ./tools/smokehealth`；本地后端已重启为 PID 3532。

2026-06-01 09:50 前端按 runzhong V3 大屏比例调整驾驶舱右侧趋势列：参考 `runzhong/frontend_V3/src/components/SPDShowPage02.vue` / `SPDShowPage03.vue` 的 `.spd-show-page-grid`，右侧列使用 `1.7fr / (1.8fr + 1.9fr + 1.7fr) = 31.48%`；`/#/model-cockpit` 改为主区域 `3.7fr` + 右侧趋势列 `1.7fr`，窄屏沿用 runzhong 的 `4fr / 1.5fr` 近似比例。验证通过 `desktop/npm run build`；Playwright 1920×1080 smoke 实测主区域 `1110px`、右侧 `510px`、右侧占比 `31.48%`，趋势卡和图表各 2 个，页面无整体滚动，截图 `desktop/output/playwright/model-cockpit-runzhong-right-ratio.png`。

2026-06-01 09:44 前端继续补齐 `/#/reports`：字段映射改为可编辑目标单元格输入；新增任务线图预览卡，使用本地 mock 趋势数据展示吹出口温度和吸入口湿度；“加载线图到 Excel”会在无后端状态下把趋势数据区和图表锚点写入 Luckysheet 样例工作簿并重新挂载，后续接真实任务曲线和图表图片/对象写入。验证通过 `desktop/npm run lint`、`desktop/npm run test -- --run`、`desktop/npm run build`；Playwright 1440×900 smoke 确认折线图 1 个、字段映射输入 6 个、Luckysheet iframe 1 个、加载后显示“已加载线图”、首个映射可改为 `B6`、页面无整体滚动。

2026-06-01 09:33 前端按用户反馈恢复驾驶舱右侧趋势卡：`/#/model-cockpit` 保持中间主玻璃卡内左侧监测表 + 右侧 3D 模型，右侧重新加入两张独立趋势玻璃卡（温度趋势、湿度趋势），趋势曲线由实时当前值和本地样例波形派生，不新增后端 API/DTO；继续保留模型区无统计小卡、无工具条。验证通过 `desktop/npm run build`；Playwright 1920×1080 smoke 确认 `.cockpit-trend-card=2`、`.recharts-surface=2`、`stageStats=0`、`modelToolbars=0`、`documentElement.scrollHeight=viewportHeight=1080`、`windowScrollY=0`，截图 `desktop/output/playwright/model-cockpit-trend-cards-restored.png`。

2026-06-01 09:30 前端按 `frontend-ai` 先行落地无接口报表画面：新增独立 `/#/reports` 页面和侧边菜单入口，参考 runzhong Vue 的报表录入 `EXReport.vue` 与查看 `getEXReport.vue`，提供“报表录入/报表查看”两种模式、模板/检测记录选择、字段映射侧栏和 Luckysheet 画布；Luckysheet vendor 资源复制到 `desktop/public/luckysheet/vendor`，只通过 `src/features/spreadsheet/luckysheetAdapter.ts` 在 iframe 内隔离加载 jQuery/Luckysheet/CSS，页面不直接触碰全局对象。同步清理 2025-05-31 停在 `SettingsPage.tsx` 的半截报表模板管理脚手架，并更新三语 i18n、路由和看板。验证通过 `desktop/npm run lint`、`desktop/npm run test -- --run`、`desktop/npm run build`；浏览器登录后打开 `/#/reports`，确认录入/查看切换、Luckysheet iframe 渲染、页面无整体滚动。报表接口未确认前，页面数据保持本地样例占位。

2026-06-01 09:27 前端继续按用户反馈收敛检测配置页：`/#/detection-config` 从多块展示布局改为单个功能工作区大卡片，顶部压缩为工具条，左侧检测标准栏收窄，旧检测项仅保留为底部紧凑参考标签，删除底部“全部配置”重复表格；页面外层锁定 `overflow:hidden`，表格自身承载横纵滚动。验证通过 `desktop/npx tsc -b`、`desktop/npm run build`、`git diff --check`；Playwright 1440×900 smoke 确认 `documentElement.scrollHeight=viewportHeight=900`、`windowScrollY=0`、检测配置页仅 1 个 `.glass-panel`、左栏宽约 196px。

2026-06-01 09:17 前端按 `frontend-ai` 先完善检测配置管理页：`/#/detection-config` 改用正式项目 API 命名，检测标准可选择默认报表模板；标准项表和编辑弹窗补齐后端运行快照语义里的 `alarm_enabled`、`check_cycle_ms`、`check_on_start`，继续保留检测、存储、上下限、保持时间和质量策略；三语 i18n 已补齐。验证通过 `desktop/npx tsc -b`、`desktop/npm run test -- --run`、`desktop/npm run build`；当时 `desktop/npm run lint` 被既有 `public/luckysheet/dist` 第三方打包文件 ESLint 指令问题阻断，后续已通过忽略隔离 vendor 资源修复。

2026-06-01 09:15 `test-ai` 在用户已启动 IOserver、本机 MQTT gateway 在线窗口内完成后端回归门禁：`/health` ok 且 gateway active、tags 从 1213 增至 1221；`go test ./...`、`go test ./... -coverprofile .\coverage.out` 总覆盖率 72.2%、`go vet ./...`、`golangci-lint run ./...`、`go build ./cmd/edge-backend`、`smokehealth`、EB-017 多项目并行检测 smoke、EB-045 任务流/报警/通知 smoke、完整 `smokebackend -latency-n 30 -latency-c 10 -timeout 120s` 均通过；完整 smoke steps=4 elapsed=28.264s，延迟 p95=400.9ms、max=431.7ms，核心队列/task-flow/notification hub `dropped=0`，workers=12。

2026-06-01 09:05 后端继续按 `backend-ai` 推进任务系统报表解耦：新增 `detection_run_report_requests`，检测启动参数 `report_request` 会冻结为“每个变量一份报表”的主服务器消费快照，支持 `variables`、`var_ids`、`variable_names` 和 `ext_1/ext_2/ext_3` 预留字段；`POST /api/v1/detection-runs`、WS `command.detection.start` 和任务模块 `builtin.start_detection_run` 均可透传，任务详情和 `GET /api/v1/detection-runs/{id}/report-requests` 可读回。已更新 schema、后端架构文档、全链路数据流文档和看板；验证通过 `go test ./...`、覆盖率 72.2%、`go vet ./...`、`golangci-lint run ./...`、`go build ./cmd/edge-backend`、后端重启 `/health` 和 `smokehealth`。

2026-05-31 12:26 交接：用户要求结束本轮，明天继续。本轮不再推进驾驶舱大屏方向；已确认当前源码里的驾驶舱已去除模型自身卡片、模型右侧小卡、右侧趋势图和模型工具条，左侧监测表保留上下限列，并重新验证 `desktop/npm run lint`、`desktop/npm run test -- --run`、`desktop/npm run build` 通过。随后开始 EB-016 报表模板管理 UI 但被叫停；该半截脚手架已在 2026-06-01 清理，并改为独立报表页面。

边缘端项目处于早期开发阶段。当前已落地 Go 后端首版骨架，具备 MQTT 多网关接入、变量发现、项目虚拟分组、检测任务启动/停止、实时内存快照、本地 MySQL 历史入库和基础 HTTP API。

Electron + React 桌面端已初始化在 `desktop/`。主服务器端是独立系统，不在当前目录内。

## 已完成事项

- 已创建 `edge-terminal/backend` Go 后端。
- 已定义本地配置文件 `backend/configs/config.json` 与示例配置。
- 已提供 MySQL schema 与示例设备/变量数据：`backend/deploy/schema.sql`。
- 已记录后端链路说明：`backend/docs/backend-architecture.md`、`backend/docs/边缘端全链路数据流转与分发图.md`。
- 已记录桌面打包方向：`docs-desktop-packaging.md`。
- 已创建 `desktop/`：React 19 + TypeScript 6 + Vite 8 + Electron，包含 sidecar 启动/健康状态首屏、typed API client、JWT 内存 token 通道、SSE client adapter、Luckysheet 隔离 adapter、三语 i18n、工位操作页和历史查询页。
- 已确认边缘端/主服务器端边界：
  - 实时数据由边缘端向主服务器提供 SSE 或 WebSocket。
  - 历史数据和数据库层面的同步交给外部数据库同步软件，本项目不做应用层历史同步。
  - 方法类/控制类调用面向 RabbitMQ 或 HTTP 暴露给主服务器使用。
  - Excel 文件不作为边缘端到主服务器的数据传输能力。
  - 边缘端需要登录、单点登录到主服务器 Web、开机自启动等外围能力。
  - 主服务器只能查询同步镜像库；开始检测、停止检测、修改边缘端检测配置和写变量等控制动作必须通过命令通道回到边缘端执行。主服务器报表任务基于同步到位的数据生成 Excel，并由主服务器维护报表任务和用户通知。
- 已完成 EB-008 Edge Auth Lite 后端落地：本地用户 JWT 登录、主站 service token 校验、一次性 SSO ticket、`sys_users`、`sys_service_clients`、`sys_sso_tickets`、`sys_audit_logs`、`/api/v1/auth/login|me|logout|sso-ticket|sso-ticket/verify` 和现有 `/api/v1` 路由 capability/scope 绑定；`/health` 保持公开。默认 bootstrap admin 为 `admin` / `Admin@12345`，可通过配置或环境变量覆盖；生产交付前必须替换默认 JWT secret 和初始密码。
- 已完成 EB-009 桌面端认证前端：玻璃风格登录页、内存 token 会话、`/auth/me` 会话恢复、路由守卫、401 自动清会话、403 权限页、capability 菜单显隐、退出登录和主站 SSO ticket 入口。Electron-only 能力隔离在 desktop adapter，工位/历史/设置等业务页面保持可迁移到主服务器。
- 已完成 EB-010 用户管理：后端提供 `/api/v1/users` 列表、新增、编辑、启用/禁用、重置密码、删除接口；设置中心新增用户管理模块，支持角色 capability 展示、角色编辑、密码重置、当前用户防误禁用/删除。该页面只依赖 shared API 与 auth store，不依赖 Electron preload，后续可迁移到主服务器复用。
- 已完成 EB-005 桌面外围能力：Electron 主进程提供托盘菜单、关闭主窗最小化到托盘、Windows 登录自启动开关、sidecar 异常退出有限重拉起、日志入口；设置中心系统模块可操作这些能力，浏览器预览态会明确降级。
- 已完成 EB-001 桌面壳与业务页面首轮闭环：工位操作为首屏，历史查询接入 `GET /api/v1/history/data` 真实历史接口并在无历史入库数据时显式标识样例数据；工位、历史、设置、用户管理等业务页面只依赖 shared API/auth store/desktop adapter 边界，避免直接耦合 Electron，后续可迁移到主服务器复用。
- 已完成 EB-011 UI 风格复审修正：以工位操作和历史查询为基准，统一 shell、登录、设置、调试看板的浅色动态背景、玻璃面板、边缘光照、胶囊按钮、表格与滚动条样式；深色侧栏和纯白顶栏已改为同源玻璃壳层。
- 已完成 EB-012 检测标准后端：新增检测标准主表、标准项表和检测运行标准项快照表；检测项拆分 `check_enabled` 与 `store_enabled`；检测启动可传 `standard_id` 冻结当时标准项，历史存储按运行快照 `store_enabled` 过滤变量。
- 已新增 `多设备并行检测任务设计计划.md`：明确多设备并行检测不变量、API 增量、复杂度、Go 规范审阅和前后端落地边界；看板新增 EB-015、EB-016、EB-017 分别给后端、前端、测试推进。
- UI 风格后续方向调整为 Apple Vision Pro 的轻玻璃空间感叠加 Claude 式时尚杂志排版气质：控件减少硬阴影和重色块，信息状态不伪装成操作按钮，导航选中态使用轻薄浮层；普通重点使用蓝色/蓝青色，橙黄色只用于特别重点色，登录页、侧边菜单和通用玻璃面板不得使用黄色点缀。
- 已完成 EB-013 变量管理编辑模型修正：未分配变量以小卡片池展示采集来源和基础属性，支持搜索/筛选、当前结果全选、清空选择和批量分配到项目；已分配变量才可编辑变量名、三语显示名、类型、单位、小数位、缩放、偏移、分组、写入约束、默认报警和启用状态。项目展示按当前语言读取 display 字段并 fallback 到 `name`/`project_code`；存储策略已迁移到 `storage-routes`。
- 已完成 EB-014 检测标准前端管理：管理中心新增检测标准模块，支持标准列表、新增/编辑/删除、设备筛选、标准项选择、上下限、`check_enabled`/`store_enabled`，并补齐三语文案。
- EB-016 前端已完成旧版主要操作闭环，并已完成 EB-041/042 首轮适配：shared API 改走 `/api/v1/projects`，变量/历史/检测相关 DTO 支持 `project_id/project_code`，变量编辑不再提交存储字段，存储配置改由设置中心“存储路由”模块管理。工位页检测操作、任务历史和报表模板入口仍是目标能力，多项目运行总览和报表模板管理 UI 仍待继续。
- 已完成 EB-015 后端：`sys_tags` 增加 `source_type` 并支持 `POST /api/v1/variables` 创建 `INT/FLOAT/BOOL/STRING` 虚变量；检测任务启动改为设备行事务锁，同设备 running 返回 `409`，不同设备可并行；新增任务列表/详情/notes/异常停止、`history/data?task_id`、报表模板元数据和任务报表关联模型；标准和报表模板已加引用删除保护。
- 已补充后端模块化深度审阅：当前后端已存在 `internal/services` 与 `internal/runtime/handlers`，虚变量、检测运行、报告模板已部分按新结构落地；后续模块化应沿用该结构，不再新增 `internal/detection` 包。详见 `后端模块化审阅与演进计划.md` 和 EB-018。
- 已完成 EB-018 后端模块化：`runtime/kernel.go` 收敛为启动装配、middleware、健康检查和模块路由注册；用户、设备、变量、历史、检测标准/任务、网关/KIO、报表模板路由迁入 `runtime/handlers`；`database.Repository` 与 `models` 保持同 package 按领域拆文件；无 API/DTO/schema 变更。
- 已完成 EB-022 历史数据源 MySQL 配置实装：后端提供受 `system_settings` 权限保护的 `/api/v1/system/database-config` 读取/保存接口和 `/api/v1/system/database-config/test` 测试连接接口；GET 隐藏明文密码，PATCH 写回 `backend/configs/config.json` 并提示需要重启后端生效；前端系统设置的历史数据源页已从占位改为 MySQL 表单。
- 已完成 EB-023 历史数据源 MySQL 配置前端适配验收：页面通过 shared API 访问后端配置接口，密码不回显并用 `password_set` 显示状态；连接测试会区分 `ok=true/false`，失败时展示后端错误；保存后显示 `restart_required` 和重启 sidecar 提示；Playwright 已覆盖成功/失败反馈。
- 已完成 EB-020 P0 审计地基：受保护 HTTP 写请求 `POST/PATCH/PUT/DELETE` 通过 runtime middleware 自动写入 `sys_audit_logs`；审计 detail 只保留 `request_id`、`command_id`、route、status、actor、latency 和错误摘要，不保存请求/响应正文，避免密码、token、KIO 凭据和数据库凭据泄露。
- 已完成 EB-024 P1 变量属性地基：`sys_tags`/`TagConfig`/变量 API 补齐存储映射、读写约束、防抖和启动快照字段；自动发现变量与虚变量默认不可写，`writable=true` 必须配置 `rw_mode=W|RW`、`write_path` 和 `write_data_type`；已通过测试、覆盖率、vet、build 和本地 smoke。
- 已完成 EB-025 P2 检测配置语义补齐：`sys_detection_standard_items` 和 `detection_run_standard_items` 补齐 `check_method`、`target_value`、`limit_deadband`、`violation_hold_ms`、`recover_hold_ms`、`quality_policy`；检测启动时把检测语义和变量默认报警快照冻结到运行快照，存储映射改由 `detection_run_storage_routes` 冻结。
- 已完成 EB-030 变量属性前端适配：变量编辑弹窗补齐基础属性、存储映射、写入约束、运行保护分区，高级项默认折叠；写入变量前端校验与后端一致；变量列表展示存储目标和读写模式；检测标准项同步 EB-025 的检测方法、目标值、回差、超限/恢复保持时间和质量策略。
- 已完成 EB-032 检测配置独立页面：左侧新增“检测配置”一级菜单，新增 `/#/detection-config` 页面，复用检测标准 API 展示/维护标准项上下限、检测方法、目标值、超限/恢复保持时间、质量策略和存储策略，并保留旧 Vue 前端 19 个检测项作为参考池；标准项选择从所有变量按 `var_name` 去重，变量项显示复用变量列表三语字段。
- 已完成 EB-026 P3 WebSocket 只读实时通道：新增 `/api/v1/ws`，连接需 Edge 用户 JWT 和 `view_realtime` 权限，浏览器可通过 `access_token` query 鉴权；支持订阅变量快照和检测任务快照，按 `topic/source_type/gateway_id/project_id/var_id` 过滤；本阶段写命令统一返回 `read_only`，EB-027 再接入后端 service 写命令和审计。
- 已完成 EB-031 WebSocket 断线重连保护补强：`/api/v1/ws` 设置 32 KiB read limit、read/write deadline、15 秒 ping/heartbeat、pong 延长读 deadline；断线后服务端 goroutine 退出，客户端重连后重新鉴权、订阅并收到 ready/snapshot。
- EB-027 WebSocket 写命令已完成：`command.detection.start`、`command.detection.stop`、`command.detection.abnormal_stop` 复用 `DetectionRunsService`；`command.write_variable` 复用 `VariableWriteService` 和 `KIOWriteService`，虚拟变量写入内存并可触发 task-flow，物理变量必须满足写约束后由后端 KIO service 发布，不绕过后端直写 MQTT。所有 WS 命令要求 `request_id/command_id/payload`，成功返回 `command.ack`，失败返回结构化 error，并写入 `sys_audit_logs`。
- KingIO 现场写入已验证：默认 KIO `Qid` 改为 `<1_000_000_000` 的小范围递增值，避免纳秒级大整数导致 ack 匹配失败；自动发现 upsert 不再覆盖已分配业务变量的人工写入配置。直接 KIO 写 `台1_39=13.42` 和 WS `command.write_variable` 写 `台1_39=13.43` 均返回 `ProcessStep=100/Result=ok`，实时快照回刷成功。
- 已新增后端实时底座设计文档 `backend/docs/运行态清洗与实时业务视图设计.md`：明确上行链路为 MQTT/KIO 解析后先走 topic 索引和清洗滤网，再进入全局 `TagMap`、项目实时视图和当前任务上下文；当前项目统一使用 `sys_projects.project_id`，后续报警状态机、运行中配置即时生效、KIO/WS 下设完整闭环和多变量组合触发器只做站位。
- 已完成 EB-033 后端实时底座首段：`TagManager` 新增 `gateway/topic -> tags` 与 `project_id -> tags` 索引，`logic_worker` 改为按 `ForMessage(gateway_id, topic)` 取变量，`Tag.UpdateNumeric` 实施 scale/offset 后的 suspicious/debounce/deadband/startup 清洗，WS 项目过滤读取项目实时视图；pending 值不入库，定时存储读取最后稳定值。
- 已完成 EB-033 运行态准入补强：自动发现变量默认只作为 `enabled=false/project_id=null` 候选入库；运行态加载和 `TagManager` 统一只接收 `enabled=true AND project_id IS NOT NULL` 的已知业务变量，未知候选不参与清洗、WS、历史存储或检测后续业务。
- EB-021 已先落地超限报警表设计与初始化：新增 `detection_limit_alarms`，用于记录某检测任务内某变量一次超限报警从开始到恢复的生命周期；已接入 GORM `AutoMigrate` 并同步 `backend/deploy/schema.sql`。
- 已完成变量默认上下限与业务报警装配地基：新增 `backend/docs/变量默认上下限与业务报警装配设计.md`；`sys_tags`/变量 API 增加 `default_alarm_enabled/default_limit_*`，检测标准项增加 `alarm_enabled`，检测启动时把变量默认属性冻结到 `detection_run_standard_items.variable_default_*`；`PATCH /api/v1/variables/{id}` 支持 `apply_to_running=true` 只同步当前 running 任务变量默认快照，不覆盖业务标准上下限。
- 已完成 EB-035 系统设置日志诊断区增强：Electron preload 新增只读 `readLogs`，设置页系统模块可刷新并预览 Go sidecar 本机运行日志尾部，同时保留打开日志文件位置；后端审计日志列表已接入 `GET /api/v1/audit-logs`，系统设置内展示最近 80 条登录、写操作和命令审计记录。
- EB-020 已追加后端审计日志查询接口：`GET /api/v1/audit-logs` 受 `system_settings` 权限保护，支持 actor/action/target/result/time/limit/offset 过滤并返回 `{items,total,limit,offset}`；前端共享 DTO/API 已同步，设置页后续可直接接入审计列表。
- 已完成检测首帧与判断周期补强并在 EB-037 审查后修正边界：变量属性增加 `default_violation_hold_ms/default_recover_hold_ms`；检测标准项和运行快照增加 `check_cycle_ms/check_on_start`，`check_cycle_ms=0` 不再继承变量存储周期；检测开始首帧存储改由 enabled storage route 的 `store_on_start` 控制，检测开始首帧判断仍由业务 `check_on_start` 控制。
- 已完成检测业务上下限报警 `enter/recover` 首段：`TaskManager` 在设备运行上下文维护每任务每变量的业务超限状态，数据变化和周期扫描按运行快照的 `alarm_enabled/check_enabled/check_cycle_ms/limit_*/limit_deadband/violation_hold_ms/recover_hold_ms/quality_policy` 判断；事件进入 `Alarm` 队列，由 storage worker 批量写入 `detection_limit_alarms`，清洗层仍不直接写库。520 变量 10 分钟压测已覆盖上限报警、上限恢复、下限报警、下限恢复和历史入库。
- 已完成检测运行轻量事件和结果摘要：新增 `detection_run_events` 记录检测开始、正常停止、异常停止等低频生命周期事件；新增 `detection_run_summaries` 从任务、历史数据和超限报警表聚合 `result_status/history_rows/alarm_*`；新增 `GET /api/v1/detection-runs/:id/summary` 与 `GET /api/v1/detection-runs/:id/events`，高频报警明细仍只写 `detection_limit_alarms`，避免双写放大。
- 已新增 `backend/docs/任务触发动态存储与双报警实施计划.md`：后续后端拆为任务触发倒排索引、项目宽表动态存储、检测业务报警增强、变量默认报警闭环四条主线；本项目主存储目标调整为按项目宽表 `rt_project_{project_id}_data`，动态列由受控 storage route 生成，自动建表/扩列只允许在配置保存、检测启动或显式 schema 准备阶段发生。
- EB-037 项目宽表动态存储已落地到 route 驱动阶段：新增 `sys_storage_routes` 和 `detection_run_storage_routes` 模型/迁移；变量创建或分配到项目时只生成 disabled 默认 route 建议，不再把变量 `store_*` 当作存储配置；新增 `/api/v1/storage-routes` 管理接口，允许配置默认项目宽表或用户自定义合法表名、列、触发方式、周期、变化死区、首帧存储和启用状态；检测启动时保证默认 route 建议存在，但实际冻结同一变量所有 enabled storage routes，包括用户自定义 route，并确保相关表和缺失动态列存在；运行态 `ActiveTask` 和 `StoreTask` 携带路由快照，Store worker 保持写兼容窄表 `rt_history_data`，同时对 `wide_table` routes 按 `table/task_id/sample_bucket_ms` 合并多变量 upsert；`GET /api/v1/history/data` 在命中运行 storage route 时优先读项目宽表并重建旧 `HistoryData` DTO；`GET /api/v1/detection-runs/{id}/storage-routes` 可查看某次检测冻结下来的实际存储去向。
- EB-041/042/043/044 首段已完成：后端破坏性改名为项目语义，`/api/v1/projects` 替代旧 `/api/v1/devices` 且不保留兼容入口，模型使用 `sys_projects/project_id/project_code/rt_project_*`；启动迁移会改旧表/列并删除变量存储属性列；变量模型、变量 API 和运行态快照已删除存储频率、触发、目标表列和首帧存储字段，存储配置只归 `sys_storage_routes`；存储 worker 改为 `StorageBus`，按 `project_id + table_name` 分 bucket 批量 flush，并避免多表拆桶造成窄历史重复写。旧 `sys_task_rules/TaskRuleIndex` 路线已废弃，任务编排统一迁入 EB-045 `sys_task_flows`。前端首轮已适配 projects/storage-routes 契约，后续逐步清理页面内部旧 device 命名别名。
- `AI_BOARD.md` 已新增 EB-047，专门要求前端尽快闭合“设备”到“项目”迁移：页面层、表单、query 参数、三语文案和测试都要统一到 `project/project_id/project_code`；`device_id` alias 只允许短期集中保留在 shared API 兼容层。
- EB-045 条件事件脚本任务系统首段实施中：新增 `sys_task_flows`、`sys_task_flow_vars`、`task_flow_runs`、`task_flow_sql_logs`；运行态使用 `var_id -> flow_ids` 倒排索引，数据变化只投递异步任务队列；任务执行器小批量取队列并按 `priority` 降序执行，低优先级靠后；内置 `builtin.storage_snapshot` 复用现有 StorageBus，不直接写库；JS 引擎使用 goja，脚本可通过 `realtime.get/realtime.write/storage.snapshot/db.query/db.exec/log.*` 访问受控能力，其中 DB 操作走同一 Repository/GORM 连接并记录 SQL 日志。
- 已完成 EB-046 前端首段：侧边菜单新增“任务”模块和 `/#/tasks` 路由，受 `system_settings` 权限保护；shared API 接入 `GET/POST/PATCH /api/v1/task-flows` 与 `POST /api/v1/task-flows/:id/run`；页面提供项目筛选、任务列表、新建/编辑弹窗、启用开关、触发类型、watch/read/write 变量绑定、priority/cooldown/hold/timeout、条件脚本、动作脚本/参数、手动运行按钮、执行记录与 SQL 日志占位 tab。执行记录与 SQL 日志查询接口后端尚未开放，后续再接真实数据。
- EB-045 已修正为变量流转模型：删除 `backend/docs/任务数据流转与内置模块设计.md`，新增 `backend/docs/任务变量流转与内置模块设计.md`。正式业务参数必须写入 watched `STRING` 虚拟变量，任务系统由数据变化触发并解析 `trigger_value` JSON 为 `task_params`；`sys_task_flows.steps_json` 继续支持多步骤内置模块链，参数来源改为 `trigger_param/event/context/literal`；HTTP 手动运行只保留开发者调试触发，不承载业务参数。验证通过：`go test ./...`、覆盖率 70.0%、`go vet ./...`、`golangci-lint run ./...`、`go build ./cmd/edge-backend`；后端已重启为 PID 26684，`task-modules` smoke 返回 `trigger_param` 参数源。
- 已完成后端待办审查：关闭已完成或废弃的 EB-002、EB-021、EB-028、EB-036、EB-038、EB-043、EB-044；旧 `sys_task_rules` 路线统一并入 EB-045 `sys_task_flows + var_id -> flow_ids`；EB-040 废弃“变量默认报警独立表”设计，改为后续扩展统一超限事件语义；EB-019 剩余范围收敛为受控变量写入、KIO service 化和防递归。
- EB-045 已补齐后端内置检测业务模块首轮：任务流支持启动检测、停止检测、暂停、恢复、固定时长结束守护、合格持续时长结束守护、特征值刷新；检测启动模块可按任务参数选择是否启用存储和业务超限报警；新增 `detection_run_features`，结束后或显式刷新计算每变量 `sample_count/avg/min/max/first/last`。新增 HTTP `POST /api/v1/detection-runs/:id/pause|resume` 和 `GET /api/v1/detection-runs/:id/features`。验证通过：`go test ./...`、覆盖率 70.5%、`go vet ./...`、`golangci-lint run ./...`、`go build ./cmd/edge-backend`。
- 检测暂停语义已修正为“暂停时长不计入累计检测时长”：`sys_detection_tasks` 新增 `pause_started_at` 和 `paused_duration_ms`；暂停记录起点，恢复累计暂停时长并顺延 `expected_end_at`；停止 paused 任务会结算当前暂停段；summary 的 `duration_ms` 会扣除所有暂停时间。验证通过：`go test ./...`、覆盖率 70.7%、`go vet ./...`、`golangci-lint run ./...`、`go build ./cmd/edge-backend`。
- EB-045 任务系统继续补齐：新增任务执行记录/SQL 日志查询 API，`task_flow_runs` 增加 origin/depth 元数据；新增 `builtin.write_variable` 受控写虚拟/可写变量并审计，默认阻断自递归；新增任务模板接口、检测静音、level_change、运行中限值调整、storage prepare、报表结果登记和受控 HTTP 请求模块。KIO 写入 service 化和 WS 变量写命令已由 EB-019/027 接上。
- EB-045/046 继续补齐任务模板和前端 typed API：`GET /api/v1/task-flow-templates` 已提供 13 个模板，覆盖变量请求启动/停止、固定时长检测、合格持续检测、暂停/恢复、存储快照、静音、运行中限值调整、特征值刷新、报表登记、受控写变量和独立 PLC 控制参数下设；变量请求启动、固定时长检测、合格持续检测模板都会先生成 `builtin.write_control_variables` 步骤，再生成 `builtin.start_detection_run` 步骤，并透传 `process_params/plc_writes/operator_note/report_template_id`；任务流参数绑定支持 `optional/default`，前端 shared API 增加任务模块、模板、执行记录和 SQL 日志查询函数。
- EB-045 任务流执行器已补强 `steps_json` 调试容错：推荐契约仍是有序 step 数组，但后端也接受单个 step object 并包装为单步骤执行。重启电脑后已恢复后端并完成虚拟变量链路 smoke：STRING 请求变量触发 task-flow，启动检测、首帧入库、业务超限报警、手动停止和特征值计算均通过；后端门禁通过，覆盖率 70.2%，`golangci-lint` 0 issues，`/health` ok 且 MQTT/KIO gateway active。
- EB-045 任务参数性能边界已复核：520 个 `custom_items` 的 watched STRING payload 实测 166453 bytes，低于 256 KiB 上限，解析约 3.25ms、转换运行快照约 0.56ms；该参数只用于低频业务动作，不进入实时采集热路径。`sys_detection_tasks.custom_config_json` 已改为紧凑追踪 JSON，运行判断事实仍以 `detection_run_standard_items` 快照为准。
- EB-045 JavaScript 开发者脚本能力继续补强：`realtime.getMany/getByName/project` 可用于多变量条件读取；`realtime.write` 已改为复用任务流写变量审计但只允许写虚拟变量，默认 `trigger=false`，显式传 `trigger:true/max_depth` 才会继续触发任务；`builtin.write_variable` 同步收紧为只写虚拟变量，物理变量下设必须走 WS/HTTP 的 `VariableWriteService + KIOWriteService`；`GET /api/v1/task-modules` 的 JavaScript 模块返回 `runtime_api`。
- EB-045 任务参数模型和 PLC 下设模块已补充：正式 `task_params` 区分检测配置、工艺/报表参数和 PLC 下设参数。进风口面积等参数可从同一 `process_params` 来源冻结到本次检测追溯；若需要写 PLC，`builtin.write_control_variables` 会受控调用后端 `VariableWriteService`，继续走写入约束、KIO/PLC service、审计日志和可选 ack 等待。已补集成测试证明同一 STRING 请求会先下设 PLC，再启动检测并冻结 `process_params/plc_writes`。
- EB-045 参数边界再次明确：所谓“配置参数”是一次检测启动请求的业务参数包，不只是检测上下限；检测配置进入运行快照，`process_params` 承载进风口面积等工艺/报表参数，`plc_writes` 只描述如何从这些参数下设到 PLC，三者通过 STRING 虚拟变量一起触发任务流但职责分开。
- EB-045 任务模块 schema 已对齐配置参数语义：`GET /api/v1/task-modules` 的 `builtin.start_detection_run.params_schema` 显式暴露 `process_params` 和 `plc_writes`，前端动态任务表单可以把检测配置、工艺/报表参数和 PLC 下设计划作为同一个 STRING 虚拟变量业务参数包展示。
- 精确变量 ID 契约已扩大到检测配置、storage routes、通知、报警、历史数据和特征值：检测标准项、检测运行标准项、存储路由、运行存储路由、`RuntimeNotification`、HTTP 通知列表、`limit-alarms`、`history/data` 和 `detection-runs/{id}/features` 响应都带 `var_id_text`；对应创建/替换接口可接收字符串 `var_id`，前端设置页、检测配置页、工位页和历史页优先使用字符串 ID，避免大 64 位 ID 在浏览器中丢精度。
- WS 精确变量 ID 契约继续补齐：运行期 `subscribe.var_ids[]` 支持数字或字符串形式 64 位 ID，`connection.ready/subscription.updated` 返回 `var_id_texts`，`command.write_variable` 回执返回 `result.var_id_text`，任务流内置写变量结果也带 `var_id_text`，用于前端重订阅、筛选、跳转和再次下设时避免 JavaScript number 精度损失。
- 精确变量 ID 残留风险继续收敛：任务流 JavaScript `realtime.get/getMany/write` 接受字符串形式 64 位变量 ID，`trigger/context` 暴露 `trigger_var_id_text`，实时快照和写变量结果返回 `var_id_text`；bulk remap 响应项返回 `var_id_text`；前端 shared DTO 把变量、实时快照和 bulk remap item 的 `var_id` 改为 `VarIdentifier`。
- EB-045 配置参数边界固定：一次 STRING 任务请求的参数包包含检测配置、`process_params` 工艺/报表参数和 `plc_writes` PLC 下设计划；进风口面积等低频工艺值只放 `process_params`，PLC 下设步骤用 `value_from` 引用同一值，避免前端重复录入。任务流保存校验已允许 `source=event,key=trigger_var_id_text`，与运行态 `trigger/context` 精确 ID 字段一致。
- EB-045 通知闭环继续补齐：`builtin.refresh_features` 现在刷新 `detection_run_features` 后会写 `detection_run_events(features_updated)` 并通过 `Channels.Notify` 发布 `detection.features_updated`，与检测停止后自动刷新特征值的事件/通知语义一致。
- EB-045 任务执行记录查询已补回归证据：`GET /api/v1/task-flow-runs?status=failed|timeout` 会按状态精确过滤，业务失败文本中包含 timeout 也不会影响前端按 `failed` 和 `timeout` 分开展示。
- EB-045 任务执行记录和 SQL 日志查询参数已硬化：`GET /api/v1/task-flow-runs` 的 `status` 和 `trigger_type` 只接受任务系统枚举值，执行记录列表要求筛选 ID 为正数、`from <= to`、`limit > 0`、`offset >= 0`，SQL 日志查询要求 `limit > 0`，非法值返回 `400`；执行记录不存在时详情和 SQL 日志接口返回 `404`，避免前端传错筛选项或 run id 时静默得到默认结果。
- 后端 service 公共错误映射已修正：`gorm.ErrRecordNotFound` 统一映射为 HTTP `404`，引用保护和同项目重复 running 仍为 `409`，普通校验错误仍为 `400`。任务执行记录详情/SQL 日志、检测任务详情、存储路由等复用 `services.HTTPStatusForError` 的接口会得到一致的缺失资源语义。
- EB-045 任务流配置写入端已硬化：`POST/PATCH /api/v1/task-flows` 和列表 `trigger_type` 筛选会校验任务系统枚举，非法 `trigger_type/action_type/vars.role/vars.var_id/steps_json[].module/steps_json[].params.*.source`、无效时间字段、`data_change` 缺少 `watch` 变量、`schedule` 缺少有效间隔、非法 `event` 参数 key、显式空 `steps_json` 数组以及重复显式 step `code` 返回 `400`；`POST` 还要求 `project_id > 0`、非空 `flow_code/name`，`PATCH` 拒绝把这些字段改成无效值，避免保存不可运行或不可识别的任务配置。
- EB-045 项目生命周期任务流已接入：检测启动成功派发 `project_start`，检测正常结束、任务流结束或异常结束派发 `project_end`；触发源覆盖任务流内置检测模块和 `DetectionRunsService`，生命周期参数包含 `task_id/project_id/project_code/test_no/status/end_type/trigger_type`。
- EB-045 定时任务已接入轻量扫描器：`sys_task_flows.schedule_interval_ms` 控制定时间隔，`trigger_type=schedule` 的启用任务到期后投递现有 `TaskFlowExecutor.input`；兼容期可用 `cooldown_ms` 作为未填 `schedule_interval_ms` 时的间隔兜底。
- EB-045/046 任务流管理闭环继续补齐：`PATCH /api/v1/task-flows/:id` 支持修改 `project_id`，替换 `vars` 时同步变量绑定项目；`DELETE /api/v1/task-flows/:id` 删除任务流配置和变量绑定，但保留 `task_flow_runs/task_flow_sql_logs` 执行历史用于审计。
- EB-017 多项目并行检测 smoke 已补可重复工具：`backend/tools/smokeeb017` 使用 `go run -tags smoke_tools ./tools/smokeeb017` 执行，真实 HTTP/WS 验证专用项目创建、INT/STRING 虚变量创建、两个项目并行 running、同项目重复启动 409、WS 写虚拟变量、历史入库归属和停止隔离。常规 `go test ./... -coverprofile` 不纳入该 smoke main 包，避免影响 70% 覆盖率门禁；`Kernel.Start()` 重启恢复测试已证明数据库 running 检测任务会恢复进 runtime `TaskManager` 和 `/detection-runs/active`。剩余 EB-017 范围是桌面端操作流 smoke。
- EB-006 MQTT/KIO payload 热路径已补单元回归：`internal/pipeline` 现在直接用真实 KingIOT/KIO `Objs/PVs` payload 触发 `processMessage`，验证业务名重映射后仍按原始 `JSONPath` 更新、PVs fallback 可用、质量码进入 runtime snapshot，且 `gateway/topic` 索引不会更新其他 topic 的变量。
- EB-006 后端 health/API smoke 已补可重复工具：`backend/tools/smokehealth` 使用 `go run -tags smoke_tools ./tools/smokehealth` 执行，验证 `/health`、登录、核心只读 API、审计日志筛选查询、项目成员接口、storage routes 项目筛选、检测标准项目筛选、实时变量项目筛选、历史数据项目筛选、报警记录项目筛选、当前运行检测快照查找、报表模板列表、通知筛选列表、筛选未读数、分类已读、当前检测运行、核心运行队列 `logic/discovery/store/alarm/notify`、每个 runtime channel 诊断字段、通知 hub `published/delivered/dropped`、worker `active/starts/exits/panics/health`、旧 `/api/v1/devices=404`，用于前端联调前快速确认后端可用。
- EB-045/048 正式任务请求 smoke 已补可重复工具：`backend/tools/smokeeb045` 使用 `go run -tags smoke_tools ./tools/smokeeb045` 执行，真实 HTTP/WS 验证临时项目、watched STRING 请求变量、虚拟控制变量、测量变量、默认报警变量、data_change flows、启动检测、冻结 `custom_items/process_params/plc_writes`、storage route 快照、`storage_prepare`、运行中限值调整、检测静音 `muted=1`、报表登记、受控 HTTP 请求、暂停、恢复、`task_flow_stop`、WS 异常停止、检测生命周期 `run_started/run_paused/run_resumed/run_stopped/run_abnormal_stop` 通知、特征值刷新事件、`detection.features_updated` 通知 WS/HTTP 未读已读闭环、固定时长自动结束、合格持续自动结束、`detection.result_ok` 检测结果通知、检测业务报警 `alarm.limit.enter/level_change/recover`、变量默认报警 `alarm.limit.enter/level_change/recover`、默认报警 `scope=default` 落库查询、WS 写请求审计、WS 异常停止命令审计、任务流控制变量写入审计和停止后的 `detection.result_ng` 检测结果通知。
- EB-045/046 任务流详情接口已补齐：`GET /api/v1/task-flows/:id` 返回单条配置和变量绑定，供前端编辑器直达和刷新使用。
- AI_BOARD 已新增 EB-048 前端交付清单：变量属性 UI 只编辑采集/显示/清洗/写入约束/默认报警；存储策略统一走 `storage-routes`；检测业务限值走检测标准和运行快照；任务系统正式参数必须写入 watched STRING 虚拟变量，前端任务编辑器应使用 `task-modules` schema 和 `task-flow-templates` 模板生成 `steps_json` 数组，并展示执行记录和 SQL 日志。
- 前端实时 OK/NG 展示边界已明确：实时值走 WS 或 `GET /api/v1/realtime/variables`；检测配置管理走 HTTP `detection-standards` CRUD；当前运行检测配置必须读 `detection-runs` 返回的运行快照 `standard_items`，不能读最新标准表自行推断，因为启动后标准项会冻结且运行中限值调整只更新 run snapshot。
- 后端新增 `GET /api/v1/detection-runs/current?project_id=...`，用于前端按项目直接取得当前 running/paused 检测详情和冻结配置快照；前端 shared API 新增 `getCurrentDetectionRun(projectId)`。验证通过：Go 全包测试、覆盖率 70.2%、vet、golangci-lint、桌面 lint/build/test、HTTP smoke，并已重启后端 PID 14700。
- 边缘端通知中心已从纯 WS 即时通知升级为“持久化 + 未读/已读 + WS 即时推送”：`Channels.Notify` 经 `NotificationDispatcher` 写入 `sys_notifications` 和 `sys_notification_recipients`，HTTP 新增 `/api/v1/notifications`、`/notifications/unread-count`、`/notifications/{id}/read`、`/notifications/read-all`；前端 shared API 已补通知列表、未读数和标记已读函数。recipient 生成已支持 `target_type=all/user/role/project`，其中 `project` 配置 `sys_project_members` 后按成员和 `notify_enabled` 精确分发，未配置成员的项目仍临时兜底给全部启用本地用户。`smokeeb045` 已用真实 `detection.run_started/paused/resumed/stopped/abnormal_stop`、`detection.features_updated`、`detection.result_ok`、检测业务 `alarm.limit.enter/level_change/recover`、变量默认 `alarm.limit.enter/level_change/recover` 和 `detection.result_ng` 事件证明 WS 即时通知、HTTP 同一 `event_uid` 查询和单条已读标记可连通。
- EB-019 项目通知成员首段已落地：新增 `sys_project_members`、`GET /api/v1/projects/{id}/members`、`PUT /api/v1/projects/{id}/members`；项目成员配置 UI 已由设置页承载，`GET` 空成员列表固定返回 `items:[]`，不返回 `null`。`smokehealth` 和 `desktop/npm run smoke:settings` 已追加真实项目成员接口检查。
- EB-048 项目成员前端配置已接入：设置页“用户管理”模块内新增“项目成员/通知接收人”配置区，支持选择项目、添加/移除本地用户、设置成员角色和 `notify_enabled`，保存走 `PUT /api/v1/projects/{id}/members`；前端 shared API 已补 `ProjectMember`/`ProjectMemberUpdate`/`ProjectMembersResponse` 和 `getProjectMembers/replaceProjectMembers`。运行日志预览当前使用居中 `Modal`，`smoke:settings` 已断言没有打开侧边 `Drawer`。
- EB-040 变量默认报警完整闭环已落地：不新增默认报警独立表，`detection_limit_alarms` 使用 `scope=default|detection` 统一记录检测业务报警和变量默认报警；默认报警为 `scope=default/task_id=0`，按 `sys_tags.default_alarm_enabled/default_limit_*/default_violation_hold_ms/default_recover_hold_ms` 在数据变化和周期扫描中维护 enter/recover/level_change，复用 `Channels.Alarm`、`alarm_worker` 和通知总线。默认报警不带静音、不依赖检测任务、不参与检测摘要；`GET /api/v1/limit-alarms` 已提供统一查询和筛选入口，`status=closed` 查询会兼容内部 `recovered` 状态。现场 smoke 已验证虚拟变量默认报警 H 进入、HH 等级变化、恢复、WS/HTTP 通知未读已读闭环和 `scope=default` 落库读回。
- 桌面 shared API 已追平 EB-040 超限报警查询契约：新增 `LimitAlarm`、`LimitAlarmListParams`、`LimitAlarmListResponse` 类型和 `getLimitAlarms(params)`，前端后续可直接接报警列表/筛选页面。
- 工位页已接入 EB-040 报警日志入口：点击“报警日志”打开居中弹窗，按当前项目调用 `GET /api/v1/limit-alarms` 查询最近 100 条，支持全部/默认报警/检测报警切换，展示变量、等级、状态、开始/阈值/恢复值和时间。完整 Electron dev 启动当前因本地 `node_modules/electron/path.txt` 缺失失败，纯 Vite renderer + Playwright smoke 已验证登录和弹窗渲染。
- 已完成 3D 驾驶舱玻璃质感统一：顶部状态卡、监测数据卡、模型卡和趋势图卡改为工位同源磨砂玻璃面板，使用驾驶舱动态背景光源坐标驱动每个卡片的橘黄色受光边缘；顶部五张卡恢复各自 SVG 纹理和轻微色差，中心/图表框也叠加原 SVG 线框纹理，并已增强纹理在当前玻璃配色下的可见度；驾驶舱玻璃面板已抽象为通用 `CockpitGlassPanel`，统一标题槽、内容区、纹理、齿轮纹理标记和尺寸变体。
- 已完成后端待办状态清理：`backend/docs/任务触发动态存储与双报警实施计划.md` 的“当前状态对照”已从旧未完成清单更新为 EB-045/040/存储总线真实状态；`backend/docs/backend-architecture.md` 已明确 `sys_task_rules` 是旧一阶段规则模型，新任务系统以 `sys_task_flows + sys_task_flow_vars` 为准。
- EB-052 后端首段已完成：`sys_notifications` 增加 `expires_at`，`alarm.*` 通知落库时按 `occurred_at+90d` 自动过期；通知列表、未读数、单条已读和全部已读默认排除已过期报警通知。非报警通知不受 90 天规则影响，`detection_limit_alarms` 报警记录仍保留用于报警中心追溯。
- EB-050 后端支撑已补齐：通知列表 `GET /api/v1/notifications` 支持 `unread/type/level/project_id/from/to/keyword/limit/offset`，未读数 `GET /api/v1/notifications/unread-count` 支持 `type/level/project_id/from/to/keyword` 分类统计，批量已读 `POST /api/v1/notifications/read-all` 支持同一组筛选条件并只更新匹配的未读通知；桌面 shared API 已补 `NotificationListParams` 时间/关键字字段、`getNotificationUnreadCount(params)` 和 `markAllNotificationsRead(params)`。

## AI 工程化状态清单

- `AGENTS.md`：已初始化为项目路引入口。
- `MEMORY.md`：已初始化为当前项目记忆。
- `AI_BOARD.md`：已初始化为唯一活跃 AI 协作看板。
- `.ai/` 母本文档体系：已初始化。
- Copilot 薄适配层：已初始化。
- Cursor 薄适配层：已初始化。
- Trae：当前未发现原生规则目录，默认复用 `AGENTS.md` 与 `.ai/` 母本。

## 后续建议

- 实时数据与写命令首选 WebSocket，但必须经过后端鉴权、service 层和审计日志，不允许前端或主站绕过后端直接写 MQTT。
- WebSocket 变量消息需要携带稳定来源标识：`source_type + gateway_id + source_path + var_id + project_id`，以支持多 MQTT 站点和后续非 MQTT 数据源。
- 默认报警不带静音；静音属于检测业务运行态策略，用于抑制某次检测中已经确认且尚未恢复的业务报警，新报警或恢复后再次报警仍需提醒。
- 任务系统参考 JGHJ 的变量触发思想，但后续统一通过 EB-045 `sys_task_flows + sys_task_flow_vars` 的 `var_id -> flow_ids` 倒排索引实现；正式业务参数写入 STRING 虚拟变量，任务触发层只调用 service 或总线，不直接写历史、不直接判断报警。
- 动态存储后续以 `sys_storage_routes` 多路由模型为唯一有效配置；变量级 `store_*` 和 `storage_table/storage_value_column` 已废弃，不应再作为前端配置入口。检测启动冻结 enabled routes 到 `detection_run_storage_routes`，宽表 writer 读取快照写默认项目表或用户自定义合法表。
- AI_BOARD 已将后续工作拆为 EB-023 到 EB-029：前端历史数据源配置页适配验收；后端按 P0 日志审计、P1 变量属性/存储映射、P2 检测配置/快照、P3 WS 只读、P4 WS 写、P5 检测事件/超限、P6 联调回归顺序实施。
- 已新增 `backend/docs/变量属性检测配置与WS前置设计.md`：WS 写命令和检测超限实施前，需要先冻结变量属性、写入约束、运行态字段和检测配置项语义。
- 已新增 `backend/docs/后端日志WS检测实施顺序.md`：后端后续优先补全 EB-020 日志/审计地基，再推进变量属性、检测配置、WS 实时读写和超限事件。
- 已新增 `backend/docs/运行态清洗与实时业务视图设计.md`：先补运行态清洗、`gateway/topic -> tags` 索引、全局变量 map 和项目/设备实时视图，再继续扩展 KIO/WS 写、报警状态机和规则触发器。
- EB-033 已完成首段实施和验证，后续继续推进 KIO/WS 写、报警状态机、运行中配置即时生效和规则触发器时必须沿用该实时底座。
- 变量属性设计已修正：变量只负责采集身份、清洗、显示名、写入约束和默认报警；存储目标、目标表/列、动态表单字段 key、查询别名、周期、变化死区和首帧存储属于 `sys_storage_routes`；检测配置负责上下限、`check_enabled` 和本次检测的 `store_enabled`。
- 前端需按 EB-034 适配变量默认上下限、默认报警开关、检测标准项 `alarm_enabled`、运行快照 `variable_default_*` 和变量编辑 `apply_to_running` 风险提示。
- 补充 Go 后端单元测试和最小 smoke 测试脚本。
- 形成稳定的边缘端 API/DTO 契约文档后放入 `.ai/docs/`。

## 待确认

- WebSocket 消息 envelope、订阅粒度、命令幂等、错误格式和前端接入顺序。<!-- 待确认 -->
- RabbitMQ/HTTP 是否仍作为主站控制通道的后续补充通道。<!-- 待确认 -->
- 登录权限设计已确定首版方向：本地用户 JWT + 主站 service token + 一次性 SSO ticket + `guest/admin/developer` 三角色 capability；主站域名、service token 下发/轮换和 developer 是否可临时 KIO 写仍待确认。<!-- 待确认 -->
- Electron + React 前端技术细节、UI 组件库、状态管理和 i18n 方案已初步确定；详见 `AGENTS.md` 的 Frontend Technical Selection。
- Windows 开机自启动、托盘、日志路径、服务守护和安装器方案。<!-- 待确认 -->
- 当前 Go 后端是否继续使用 MySQL 作为边缘端唯一业务库。<!-- 待确认 -->

## 改动记录

- 2026-06-01 09:43 | GPT-5 Codex (`review-ai`) | 按用户要求在 `AI_BOARD.md` 新增 EB-054 给后端：要求核查检测配置页枚举语义和变量显示名装配契约，明确页面查询 `/detection-standards`、`/detection-standards/{id}`、`/projects`、`/variables`、`/report-templates`，涉及 `sys_detection_standards/sys_detection_standard_items/sys_projects/sys_tags/sys_report_templates`，运行后冻结到 `detection_run_standard_items`；变量以 `var_id` 关联，`var_name` 只作稳定编码，显示使用 `display_name/display_name_en/display_name_ja`；后端需列清 `check_method`、`quality_policy`、标准 `mode` 等枚举含义供前端三语映射。本次为看板/审阅任务补充，未运行代码测试。
- 2026-05-31 12:24 | GPT-5 Codex (`backend-ai/test-ai`) | EB-006/020/029 后端总 smoke 队列压力门禁：`backend/tools/smokebackend` 的 runtime snapshot 现在校验 `/runtime/channels/detail`、`/task-flows/runtime` 和 `/runtime/notifications` 的 `pressure` 诊断，基线或最终快照出现核心队列、任务流队列或通知 hub 过压即失败，避免“没有 dropped 但已经积压接近阈值”的延迟风险被漏掉。验证通过定向 smoke 工具测试与 build-tags linter、`go test ./...`、覆盖率 72.6%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`、真实 `go run -tags smoke_tools ./tools/smokebackend -latency-n 30 -latency-c 10 -timeout 120s`；完整 smoke steps=4 elapsed=33.08s，延迟 p95=737.0ms、max=784.6ms，核心队列/task-flow/notification hub `dropped=0/pressure=false`，workers=12。
- 2026-05-31 12:14 | GPT-5 Codex (`backend-ai/test-ai`) | EB-006/020 后端总 smoke runtime 诊断重复名称门禁：`backend/tools/smokebackend` 现在拒绝 `/runtime/channels/detail` 和 `/runtime/workers` 返回重复 `name`，避免同名项进入 map 时覆盖 dropped/panic/health/active 等诊断结果。验证通过定向 smoke 工具测试与 build-tags linter、`go test ./...`、覆盖率 72.6%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`、真实 quick `go run -tags smoke_tools ./tools/smokebackend -skip-business -latency-n 5 -latency-c 2 -timeout 60s`；quick 延迟 p95=158.2ms、max=180.0ms，核心队列/task-flow/notification hub `dropped=0`，workers=12。
- 2026-05-31 12:10 | GPT-5 Codex (`backend-ai/test-ai`) | EB-006/020 后端总 smoke dropped 字段门禁补强：`backend/tools/smokebackend` 现在用指针解码 `/runtime/channels/detail items[].dropped`、`/task-flows/runtime.dropped` 和 `/runtime/notifications.dropped`，缺任一计数字段都会失败，避免诊断字段漏掉时被零值误判为 0；真实的 `dropped=0` 仍允许通过。验证通过定向 smoke 工具测试与 build-tags linter、`go test ./...`、覆盖率 72.6%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`、真实 quick `go run -tags smoke_tools ./tools/smokebackend -skip-business -latency-n 5 -latency-c 2 -timeout 60s`；quick 延迟 p95=165.2ms、max=191.5ms，核心队列/task-flow/notification hub `dropped=0`，workers=12。
- 2026-05-31 12:06 | GPT-5 Codex (`backend-ai/test-ai`) | EB-006/020 后端总 smoke 基线 worker 门禁补强：`backend/tools/smokebackend` 的前置 runtime snapshot 现在会立即拒绝核心 worker `active<=0` 或任一 worker `health!=ok`，避免后端开局已不健康还继续跑业务 smoke；最终快照仍继续检查 dropped 增长、panic 增长和 worker 健康。验证通过定向 smoke 工具测试与 build-tags linter、`go test ./...`、覆盖率 72.6%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`、真实 quick `go run -tags smoke_tools ./tools/smokebackend -skip-business -latency-n 5 -latency-c 2 -timeout 60s`；quick 延迟 p95=152.5ms、max=166.4ms，核心队列/task-flow/notification hub `dropped=0`，workers=12。
- 2026-05-31 12:01 | GPT-5 Codex (`backend-ai/test-ai`) | EB-006/020 后端 smoke 增加 worker 生命周期字段门禁：`backend/tools/smokehealth` 和 `backend/tools/smokebackend` 现在都用指针解码 `/runtime/workers` 的 `active/starts/exits/panics`，缺任一字段都会失败，避免 JSON 漏字段被零值掩盖；核心 worker 仍要求存在、`active>0`、`health=ok`。验证通过定向 smoke 工具测试与 build-tags linter、`go test ./...`、覆盖率 72.6%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`、真实 `go run -tags smoke_tools ./tools/smokebackend -latency-n 30 -latency-c 10 -timeout 120s`；真实并发延迟 p95=763.9ms、max=793.1ms，核心队列/task-flow/notification hub `dropped=0`，workers=12。
- 2026-05-31 11:54 | GPT-5 Codex (`backend-ai/test-ai`) | EB-006/020 后端 smoke 增加核心 worker active 门禁：`backend/tools/smokehealth` 现在要求固定常驻后台 worker 不仅存在且 `health=ok`，还必须 `active>0`；`backend/tools/smokebackend` 的最终 runtime snapshot 同样会拒绝核心 worker `active=0`，避免 worker 正常退出但未 panic 时被误判为健康。验证通过定向 smoke 工具测试与 build-tags linter、`go test ./...`、覆盖率 72.6%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`、真实 `go run -tags smoke_tools ./tools/smokehealth` 和 quick `go run -tags smoke_tools ./tools/smokebackend -skip-business -latency-n 5 -latency-c 2 -timeout 60s`；quick 并发延迟 p95=109.0ms、max=163.3ms，核心队列/task-flow/notification hub `dropped=0`，workers=12。
- 2026-05-31 11:50 | GPT-5 Codex (`backend-ai/test-ai`) | EB-006 独立 health smoke 增加 compact `/runtime/channels` 核心队列门禁：`backend/tools/smokehealth` 现在要求旧兼容 `/api/v1/runtime/channels` 返回 `logic/discovery/store/alarm/notify`，和 `/health.channels`、`/runtime/channels/detail` 形成同一核心队列检查，避免兼容诊断接口漏字段但 smoke 只看 JSON 有效。验证通过定向 smokehealth 测试与 build-tags linter、`go test ./...`、覆盖率 72.6%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`、真实 `go run -tags smoke_tools ./tools/smokehealth` 和 quick `go run -tags smoke_tools ./tools/smokebackend -skip-business -latency-n 5 -latency-c 2 -timeout 60s`；quick 并发延迟 p95=176.6ms、max=182.9ms，核心队列/task-flow/notification hub `dropped=0`，workers=12。
- 2026-05-31 11:46 | GPT-5 Codex (`backend-ai/test-ai`) | EB-006 独立 health smoke 增加 `/health` 核心队列门禁：`backend/tools/smokehealth` 现在在登录前要求公开 `/health` 返回 `status=ok` 且 `channels` 包含 `logic/discovery/store/alarm/notify`，避免 sidecar/前端第一健康信号漏掉核心队列但测试仍通过。验证通过定向 smokehealth 测试与 build-tags linter、`go test ./...`、覆盖率 72.6%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`、真实 `go run -tags smoke_tools ./tools/smokehealth` 和 quick `go run -tags smoke_tools ./tools/smokebackend -skip-business -latency-n 5 -latency-c 2 -timeout 60s`；quick 并发延迟 p95=157.4ms、max=182.8ms，核心队列/task-flow/notification hub `dropped=0`，workers=12。
- 2026-05-31 11:41 | GPT-5 Codex (`backend-ai/test-ai`) | EB-006/020 后端 smoke 核心后台 worker 存在性门禁：`backend/tools/smokehealth` 现在要求 `/runtime/workers` 返回固定常驻后台 worker `alarm-worker/channel-pressure-logger/cycle-scanner/discovery/logic/notification-dispatcher/storage-bus/task-flow-schedule-scanner/task-flow-worker`；`backend/tools/smokebackend` 的前后置 runtime snapshot 同时要求核心队列和核心 worker 存在，避免启动装配漏挂某个核心 worker 但测试仍通过。验证通过定向 smoke 工具测试与 build-tags linter、`go test ./...`、覆盖率 72.6%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`、真实 `go run -tags smoke_tools ./tools/smokehealth` 和 quick `go run -tags smoke_tools ./tools/smokebackend -skip-business -latency-n 5 -latency-c 2 -timeout 60s`；quick 并发延迟 p95=211.3ms、max=214.8ms，核心队列/task-flow/notification hub `dropped=0`，workers=12。
- 2026-05-31 11:35 | GPT-5 Codex (`backend-ai/test-ai`) | EB-006/020 独立 health smoke 核心队列存在性门禁：`backend/tools/smokehealth` 现在要求 `/runtime/channels/detail` 返回固定核心运行队列 `logic/discovery/store/alarm/notify`，避免后端误删或漏注册某条核心队列但 smoke 仍因“有任意 channel”而通过。验证通过 `go test ./...`、覆盖率 72.6%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go test -tags smoke_tools ./tools/smokehealth ./tools/smokebackend -count=1`、`golangci-lint run --build-tags smoke_tools ./tools/smokehealth ./tools/smokebackend` 0 issues、`go build ./cmd/edge-backend`、真实 `go run -tags smoke_tools ./tools/smokehealth`；后端 PID 39540 继续运行，`/health` ok，tags=1188。
- 2026-05-31 11:32 | GPT-5 Codex (`backend-ai/test-ai`) | EB-006/020 独立 health smoke 运行诊断断言继续补强：`backend/tools/smokehealth` 现在校验 `/runtime/channels/detail` 的每个 channel 都有 `dropped/impact/next_action`，并校验 `/runtime/notifications` 带 `published/delivered/dropped` 计数，不再只抽查第一个 channel 或只看通知描述字段。验证通过 `go test ./...`、覆盖率 72.6%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`golangci-lint run --build-tags smoke_tools ./tools/smokehealth ./tools/smokebackend` 0 issues、`go build ./cmd/edge-backend`、真实 `go run -tags smoke_tools ./tools/smokehealth`；后端 PID 39540 继续运行，`/health` ok，tags=1188。
- 2026-05-31 11:27 | GPT-5 Codex (`backend-ai/test-ai`) | EB-006/020 独立 health smoke 补齐 worker 健康门禁：`backend/tools/smokehealth` 现在校验 `/api/v1/runtime/workers` 每个 worker 都带 `panics` 计数且 `health=ok`，不再只检查字段存在；这样前端联调前单独运行 health smoke 也能发现后台 worker degraded/stopped。验证通过 `go test ./...`、覆盖率 72.6%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`golangci-lint run --build-tags smoke_tools ./tools/smokehealth ./tools/smokebackend` 0 issues、`go build ./cmd/edge-backend`、真实 `go run -tags smoke_tools ./tools/smokehealth` 和 quick `smokebackend -skip-business -latency-n 5 -latency-c 2 -timeout 60s`；quick 并发延迟 p95=174.9ms、max=176.2ms，运行诊断显示核心队列/任务流/notification hub `dropped=0`、workers=12。
- 2026-05-31 11:22 | GPT-5 Codex (`backend-ai/test-ai`) | EB-006/020 后端总 smoke worker 健康门禁完成验证：`backend/tools/smokebackend` 的 runtime snapshot 现在同时读取 `/api/v1/runtime/workers`，若 smoke 期间任一后台 worker `panics` 增长，或结束时 worker `health!=ok`，则总 smoke 失败；这样后台 worker panic/recover 或停止不会被业务链路表面通过掩盖。完整后端 gate 通过 `go test ./...`、覆盖率 72.6%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；已短重启后端为 PID 39540，`/health` ok，真实 `go run -tags smoke_tools ./tools/smokebackend -latency-n 30 -latency-c 10 -timeout 120s` 通过，steps=4 elapsed=32.416s，并发延迟 min=92.0ms、p50=275.2ms、p90=335.8ms、p95=354.1ms、max=399.2ms、avg=257.1ms，低于 p95<=2s/max<=5s 门禁；总 smoke 内置运行诊断显示核心队列 `alarm/discovery/logic/notify/store dropped=0`、task-flow `dropped=0`、notification hub `dropped=0`、workers=12 且健康门禁通过。
- 2026-05-31 11:15 | GPT-5 Codex (`backend-ai/test-ai`) | EB-006/020 后端总 smoke 丢弃门禁扩大到通知 hub并完成验证：`backend/tools/smokebackend` 的 runtime drop snapshot 现在同时读取 `/api/v1/runtime/notifications`，如果在线通知 hub `dropped` 在 smoke 期间增长则失败；这样通知 WS 即时扇出丢弃不会被总 smoke 漏掉。完整后端 gate 通过 `go test ./...`、覆盖率 72.6%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；已短重启后端为 PID 3580，`/health` ok，真实 `go run -tags smoke_tools ./tools/smokebackend -latency-n 30 -latency-c 10 -timeout 120s` 通过，steps=4 elapsed=31.715s，并发延迟 min=123.9ms、p50=615.7ms、p90=719.3ms、p95=720.7ms、max=744.9ms、avg=566.6ms，低于 p95<=2s/max<=5s 门禁；总 smoke 内置运行诊断显示核心队列 `alarm/discovery/logic/notify/store dropped=0`、task-flow `dropped=0`、notification hub `dropped=0`。
- 2026-05-31 11:09 | GPT-5 Codex (`backend-ai/test-ai`) | EB-006/020 后端总 smoke 运行时丢弃门禁完成验证：`backend/tools/smokebackend` 现在会在套件前后读取 `/api/v1/runtime/channels/detail` 和 `/api/v1/task-flows/runtime`，如果核心 channel 或 task-flow `dropped` 增长则直接失败，避免“延迟通过但队列已经丢弃”的误判。完整后端 gate 通过 `go test ./...`、覆盖率 72.6%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；已短重启后端为 PID 37832，`/health` ok，真实 `go run -tags smoke_tools ./tools/smokebackend -latency-n 30 -latency-c 10 -timeout 120s` 通过，steps=4 elapsed=31.088s，并发延迟 min=126.9ms、p50=623.0ms、p90=689.5ms、p95=693.2ms、max=732.5ms、avg=576.0ms，低于 p95<=2s/max<=5s 门禁；总 smoke 内置运行诊断显示核心队列 `alarm/discovery/logic/notify/store dropped=0`，task-flow `dropped=0`。
- 2026-05-31 11:03 | GPT-5 Codex (`backend-ai/test-ai`) | EB-006/045 任务系统 smoke 覆盖继续补强并完成验证：`backend/tools/smokeeb045` 新增 watched STRING 触发的 JavaScript step，验证 realtime project 读取、`db.query`、脚本日志和 `/task-flow-runs/{id}/sql-logs`；新增临时 `schedule` JavaScript flow，验证 schedule scanner 真实执行后立即禁用，避免持续产生测试记录。完整后端 gate 通过 `go test ./...`、覆盖率 72.6%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；已短重启后端为 PID 39412，`/health` ok，真实 `go run -tags smoke_tools ./tools/smokebackend -latency-n 30 -latency-c 10 -timeout 120s` 通过，steps=4 elapsed=26.598s，并发延迟 min=119.2ms、p50=612.8ms、p90=713.6ms、p95=715.5ms、max=746.0ms、avg=568.2ms，低于 p95<=2s/max<=5s 门禁；真实 `/runtime/channels/detail` 显示核心队列 `dropped=0/pressure=false`，`/task-flows/runtime` 显示 `submitted=239/enqueued=239/dropped=0/pressure=false`。
- 2026-05-31 10:53 | GPT-5 Codex (`backend-ai/test-ai`) | EB-020 核心 channel 丢弃计数完成验证：`Channels` 现在为 `logic/discovery/store/alarm/notify` 记录累计 `dropped`，`GET /api/v1/runtime/channels/detail` 的 `items[].dropped` 可判断核心队列是否曾因满队列拒收；已在 MQTT manager、logic worker、cycle scanner、detection service、task-flow executor、alarm worker 等主要非阻塞发送点接入。完整后端 gate 通过 `go test ./...`、覆盖率 72.6%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；已短重启后端为 PID 40456，`/health` ok，真实 `go run -tags smoke_tools ./tools/smokehealth` 通过，真实 `go run -tags smoke_tools ./tools/smokebackend -latency-n 30 -latency-c 10 -timeout 120s` 通过，steps=4 elapsed=31.835s，并发延迟 min=173.1ms、p50=607.7ms、p90=660.2ms、p95=666.9ms、max=702.4ms、avg=559.1ms，低于 p95<=2s/max<=5s 门禁；真实 `/runtime/channels/detail` 显示 alarm/discovery/logic/notify/store 的 `dropped=0` 且 `pressure=false`。
- 2026-05-31 10:04 | GPT-5 Codex (`review-ai`) | 修正交互看板里的平台化审阅口径：新增 EB-053，明确低代码平台雏形是项目目标，`TaskFlow/steps_json/模块 schema/模板/执行记录/STRING 虚变量触发/开发者高级模块` 不应因抽象本身被打回；后续审查重点改为运行诊断和 worker recover 是否可观测、可处置，普通操作员与开发者入口是否分层，现场动作是否可追溯。本次为看板规则/评审口径修正，未运行代码测试。
- 2026-05-31 10:42 | GPT-5 Codex (`backend-ai/test-ai`) | EB-020/045 任务流队列丢弃诊断完成验证：完整后端 gate 通过 `go test ./...`、覆盖率 72.7%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；已短重启后端为 PID 42256，`/health` ok，真实 `go run -tags smoke_tools ./tools/smokebackend -latency-n 30 -latency-c 10 -timeout 120s` 通过，steps=4 elapsed=29.92s，并发延迟 min=95.9ms、p50=623.6ms、p90=680.5ms、p95=680.8ms、max=749.2ms、avg=576.5ms，低于 p95<=2s/max<=5s 门禁；真实 `/task-flows/runtime` 显示 `submitted=210/enqueued=210/dropped=0/pressure=false`。
- 2026-05-31 10:40 | GPT-5 Codex (`backend-ai/test-ai`) | EB-020/045 任务流队列丢弃诊断：`TaskFlowExecutor` 现在累计 `submitted/enqueued/dropped`，`GET /api/v1/task-flows/runtime` 返回这些计数，能判断变量触发或手动调试任务是否因任务流队列满被拒绝；`smokehealth` 已校验这些字段。定向验证通过 `go test ./internal/pipeline -run "TestTaskFlowRuntimeStats" -count=1` 和 `go test -tags smoke_tools ./tools/smokehealth -count=1`。
- 2026-05-31 10:35 | GPT-5 Codex (`backend-ai/test-ai`) | EB-020/053 worker recover 诊断完成验证：完整后端 gate 通过 `go test ./...`、覆盖率 72.7%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；已短重启后端为 PID 42772，`/health` ok，`/runtime/workers` 真实返回 logic 等 worker 的 `health=ok/impact/next_action`；真实 `go run -tags smoke_tools ./tools/smokebackend -latency-n 30 -latency-c 10 -timeout 120s` 通过，steps=4 elapsed=24.673s，并发延迟 min=129.4ms、p50=603.3ms、p90=679.9ms、p95=680.3ms、max=726.2ms、avg=558.4ms，低于 p95<=2s/max<=5s 门禁。
- 2026-05-31 10:33 | GPT-5 Codex (`backend-ai/test-ai`) | EB-020/053 worker recover 诊断：`pipeline.RunRecovering` 现在记录每类后台 worker 的 `active/starts/exits/panics/last_*` 和最近错误，新增 `GET /api/v1/runtime/workers` 返回 `health/impact/next_action`，可判断 worker 是否因 panic 停止或降级；`smokehealth` 已纳入该接口和字段校验。定向验证通过 `go test ./internal/pipeline ./internal/runtime/handlers -run "TestRunRecovering|TestGatewaysHandler" -count=1` 和 `go test -tags smoke_tools ./tools/smokehealth -count=1`。
- 2026-05-31 10:28 | GPT-5 Codex (`backend-ai/test-ai`) | EB-020/053 运行诊断可处置化完成验证：新增字段已纳入 `smokehealth` 真实校验。完整后端 gate 通过 `go test ./...`、覆盖率 72.6%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；已短重启后端为 PID 36872，`/health` ok，真实 `go run -tags smoke_tools ./tools/smokebackend -latency-n 30 -latency-c 10 -timeout 120s` 通过，steps=4 elapsed=30.448s，并发延迟 min=85.7ms、p50=483.4ms、p90=691.8ms、p95=697.4ms、max=723.4ms、avg=466.8ms，低于 p95<=2s/max<=5s 门禁。
- 2026-05-31 10:24 | GPT-5 Codex (`backend-ai/test-ai`) | EB-020/053 运行诊断可处置化：`/api/v1/runtime/channels/detail` 的队列项和过压列表、`/api/v1/task-flows/runtime` 的任务流队列、`/api/v1/runtime/notifications` 的通知 hub 诊断均新增 `pressure/impact/next_action` 等字段，用于说明哪个队列堵、影响什么业务链路、下一步查什么；旧 `len/cap/usage` 字段保持不变。验证通过 `go test ./internal/pipeline ./internal/services ./internal/runtime/handlers -run "TestChannelsStats|TestTaskFlowRuntimeStats|TestNotificationHubRuntimeStats|TestGatewaysHandler" -count=1`。
- 2026-05-31 10:14 | GPT-5 Codex (`backend-ai/test-ai`) | EB-006/029 并发延迟门禁补强：`smokelatency` 新增 `-p95-max` 和 `-max-max` 阈值参数，超过阈值会失败；`smokebackend` 默认传入 p95<=2s、max<=5s，避免只打印延迟但不作为门禁。验证通过 `go test -tags smoke_tools ./tools/smokelatency ./tools/smokebackend -count=1`、`go test ./...`、覆盖率 72.5%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；真实 `go run -tags smoke_tools ./tools/smokebackend -latency-n 30 -latency-c 10 -timeout 120s` 通过，steps=4 elapsed=24.597s，并发延迟 min=95.1ms、p50=252.5ms、p90=325.2ms、p95=326.3ms、max=336.0ms、avg=243.3ms，低于 p95<=2s/max<=5s 门禁。后端 PID 40988 继续运行。
- 2026-05-31 10:08 | GPT-5 Codex (`backend-ai/test-ai`) | EB-006/029 后端总 smoke runner：新增 `backend/tools/smokebackend`，一键串联 `smokehealth`、`smokeeb017`、`smokeeb045` 和 `smokelatency`，用于在完成看板后端任务前统一验证 HTTP/WS、虚拟变量、多项目检测、任务系统、存储、报警、通知、审计和并发延迟。验证通过 `go test -tags smoke_tools ./tools/smokebackend -count=1`、`go test ./...`、覆盖率 72.5%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；真实 `go run -tags smoke_tools ./tools/smokebackend -latency-n 30 -latency-c 10 -timeout 120s` 通过，steps=4 elapsed=35.79s，并发延迟 min=116.7ms、p50=648.7ms、p90=680.6ms、p95=683.7ms、max=714.9ms、avg=571.9ms。后端 PID 40988 继续运行，`/health` ok，tags=1101。
- 2026-05-31 10:03 | GPT-5 Codex (`backend-ai/test-ai`) | EB-020/006 通知运行诊断与并发延迟 smoke：新增 `GET /api/v1/runtime/notifications`，返回在线通知 hub 的 `subscribers/buffered/capacity/usage/published/delivered/dropped`，并纳入 `smokehealth`；新增 `backend/tools/smokelatency`，在 IO/KIO/MQTT 离线时使用 watched `STRING` 虚拟变量 + data_change task-flow 测量 WS 写入到 `task_flow_runs` 成功落库的可控后端链路延迟。验证通过定向测试、`go test ./...`、覆盖率 72.5%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`、真实 `smokehealth`；并发延迟 smoke `-n 30 -c 10` 通过，min=105.6ms、p50=287.8ms、p90=344.6ms、p95=346.7ms、max=374.5ms、avg=281.0ms。后端已重启为 PID 40988，`/health` ok，tags=1100。
- 2026-05-31 09:52 | GPT-5 Codex (`backend-ai/test-ai`) | EB-020/045 任务流运行诊断补强收口：新增 `GET /api/v1/task-flows/runtime`，返回任务流执行队列 `name/len/cap/usage`、`pressure_threshold`、`pressure` 和当前 guard 数；`smokehealth` 已纳入该接口，新增 `TestTaskFlowRuntimeStats` 和 handler 回归。验证通过定向测试、`go test ./...`、覆盖率 72.5%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend` 和真实 `go run -tags smoke_tools ./tools/smokehealth`；后端已重启为 PID 42904，`/health` ok，tags=1100。
- 2026-05-31 09:41 | GPT-5 Codex (`backend-ai/test-ai`) | EB-020/045 任务流后台异常日志补强：`TaskFlowExecutor.Start` workers、`StartScheduleScanner`、固定时长结束 guard 和合格持续结束 guard 均接入 `GoRecovering`；guard panic 后通过 defer 清理 marker，避免后续同一任务守护无法重启。新增 `TestTaskFlowGuardRecoverClearsGuard` 回归。
- 2026-05-31 09:33 | GPT-5 Codex (`backend-ai/test-ai`) | EB-020 worker 异常日志补强：新增 `pipeline.RunRecovering/GoRecovering`，核心后台 goroutine 通过统一 recover 包装，panic 时写 `[worker] panic recovered name=... err=... stack=...`；已接入 logic worker、cycle scanner、discovery、storage bus/bucket、alarm worker、notification dispatcher/hub 和 channel pressure logger，并补单元回归验证 panic 日志。
- 2026-05-31 09:28 | GPT-5 Codex (`backend-ai/test-ai`) | EB-020 运行诊断接口补强：新增 `GET /api/v1/runtime/channels/detail`，返回 `logic/discovery/store/alarm/notify` 每个队列的 `len/cap/usage`、压力阈值和过压队列列表；旧 `/runtime/channels` 保持原长度 map 兼容。`smokehealth` 已纳入该接口，文档同步。
- 2026-05-31 09:25 | GPT-5 Codex (`backend-ai/test-ai`) | EB-020 运行日志补强：新增 runtime channel pressure 监控，后端每 5 秒检查 `logic/discovery/store/alarm/notify` 队列长度和容量，超过 80% 时写 `[runtime] channel pressure ...` 日志，用于定位实时延迟是否来自内部队列积压；新增 `Channels.DetailedStats/Pressure` 单元回归，数据流文档已同步。
- 2026-05-31 09:19 | GPT-5 Codex (`backend-ai/test-ai`) | EB-006/037 历史宽表兼容修复：真实 `smokehealth` 暴露旧项目宽表缺少 `project_id/project_code` 时 `GET /api/v1/history/data?project_id=...` 返回 500；后端 schema prepare 现在会补齐这两个固定列，默认项目宽表会回填 `project_id`，历史宽表重建 DTO 时也能用冻结 storage route 的项目元数据兜底。新增数据库回归覆盖旧宽表结构，数据流文档已同步。
- 2026-05-31 09:13 | GPT-5 Codex (`backend-ai/test-ai`) | EB-006/029 smokehealth 补强：新增 `GET /api/v1/realtime/variables?project_id=...` 和 `GET /api/v1/history/data?project_id=...&limit=1` 检查，覆盖工位页实时值和历史页基础查询依赖的后端 HTTP 读链路；接口语义未变。
- 2026-05-31 09:10 | GPT-5 Codex (`backend-ai/test-ai`) | EB-006/029/051 smokehealth 补强：新增 `GET /api/v1/limit-alarms?project_id=...&limit=1` 和 `GET /api/v1/detection-runs/current?project_id=...` 检查；当前运行检测查找允许无运行任务时返回 `404`，用于验证报警中心和实时 OK/NG 配置快照依赖的后端入口可达且权限/参数正常。
- 2026-05-31 09:07 | GPT-5 Codex (`backend-ai/test-ai`) | EB-006/029 smokehealth 补强：新增 storage routes 项目筛选、检测标准项目筛选和报表模板列表检查，覆盖前端配置页联调依赖的核心只读 API；接口语义未变。
- 2026-05-31 09:04 | GPT-5 Codex (`backend-ai/test-ai`) | EB-006/020 smokehealth 补强：`backend/tools/smokehealth` 新增 `GET /api/v1/audit-logs?action=auth.login&result=success&limit=1` 检查，把审计日志只读筛选接口纳入联调前一键健康验证；接口语义未变。
- 2026-05-31 09:00 | GPT-5 Codex (`backend-ai/test-ai`) | EB-006/050 smokehealth 补强：`backend/tools/smokehealth` 新增通知筛选列表、筛选未读数和分类已读检查，覆盖 `unread/type/level/project_id/from/to/keyword/limit` 查询组合以及 `POST /notifications/read-all` 筛选批量已读。同步更新 `AI_BOARD.md` 和本记忆，后续前端通知中心联调可先跑 `go run -tags smoke_tools ./tools/smokehealth` 快速确认后端通知筛选接口可用。
- 2026-05-31 08:34 | GPT-5 Codex (`review-ai`) | 按用户要求在 `AI_BOARD.md` 新增 EB-050/EB-051/EB-052：前端需要独立通知中心页面，支持分类筛选和分类一键已读；前端需要独立报警中心页面，点击报警通知只跳转到报警页面并带查询参数；报警类通知发生 90 天后自动过期由后端提供 `expires_at` 或查询排除能力，前端不得只做本地假过滤。本次为看板任务补充，未运行代码测试。
- 2026-05-31 07:21 | GPT-5 Codex (`backend-ai/test-ai`) | EB-006 MQTT/KIO payload 热路径回归补强：新增 `TestProcessMessageKIOPayloadUpdatesOnlyIndexedKnownTags`，用真实 KingIOT/KIO `Objs/PVs` payload 验证业务名重映射后仍按原始 `JSONPath` 更新数值/布尔/字符串变量、PVs fallback 可用、质量码进入 runtime snapshot，且 `gateway/topic` 索引不会误更新其他 topic 变量。验证通过定向 `go test ./internal/pipeline -run TestProcessMessageKIOPayloadUpdatesOnlyIndexedKnownTags -count=1`、`go test ./...`、覆盖率 72.3%、`go vet ./...`、`golangci-lint run ./...`、`go build ./cmd/edge-backend`；`/health` ok，KIO gateway active，tags=1100。
- 2026-05-31 07:13 | GPT-5 Codex (`backend-ai/test-ai`) | EB-045/048 检测静音 smoke 补强：`smokeeb045` 在检测业务报警进入后通过正式 STRING 请求触发 `builtin.mute_detection_alarms`，验证 task-flow 结果 `muted=1`；随后继续验证更严重等级变化、恢复和 NG 结果通知仍可经 WS/HTTP 未读已读闭环到达。现场 smoke 项目 `38` 通过；验证通过 smoke 工具编译/vet、真实 smoke、`go test ./...`、覆盖率 72.3%、`go vet ./...`、`golangci-lint run ./...`、`go build ./cmd/edge-backend`。
- 2026-05-31 07:06 | GPT-5 Codex (`backend-ai/test-ai`) | EB-019/020/045/048 异常停止通知和审计 smoke 补强：`smokeeb045` 新增一条独立异常停止检测，先通过正式 STRING 请求启动，再用 WS `command.detection.abnormal_stop` 停止，验证 `end_type=abnormal_stop`、`detection_run_events(run_abnormal_stop)`、`detection.run_abnormal_stop` 的 WS/HTTP 未读已读闭环和 WS 命令成功审计。现场 smoke 项目 `37` 通过；验证通过 smoke 工具编译/vet、真实 smoke、`go test ./...`、覆盖率 72.3%、`go vet ./...`、`golangci-lint run ./...`、`go build ./cmd/edge-backend`。
- 2026-05-31 06:56 | GPT-5 Codex (`backend-ai/test-ai`) | EB-019/045/048 检测生命周期通知 smoke 补强：`smokeeb045` 将 `topic=notifications` WS 提前到正式任务启动前打开，验证同一主检测任务的 `detection.run_started`、`detection.run_paused`、`detection.run_resumed`、`detection.run_stopped` 均能经 WS 即时到达，并通过 HTTP 通知列表按同一 `event_uid` 读回和标记已读。现场 smoke 项目 `36` 通过；验证通过 smoke 工具编译/vet、真实 smoke、`go test ./...`、覆盖率 72.3%、`go vet ./...`、`golangci-lint run ./...`、`go build ./cmd/edge-backend`。
- 2026-05-31 06:51 | GPT-5 Codex (`backend-ai/test-ai`) | EB-019/020/045/048 写操作审计 smoke 补强：`smokeeb045` 新增 `/api/v1/audit-logs` 断言，验证正式 STRING 任务请求对应 `ws.command.write_variable` 审计按 `command_id` 落库，且 `builtin.write_control_variables` 对控制变量的下设动作写入 `task_flow.write_variable` 成功审计。第一次现场运行暴露 smoke 匹配 JSON 空格问题，已修正为按 command_id 值匹配，并清理失败运行留下的临时 running 任务。最终现场 smoke 项目 `35` 通过；验证通过 smoke 工具编译/vet、真实 smoke、`go test ./...`、覆盖率 72.3%、`go vet ./...`、`golangci-lint run ./...`、`go build ./cmd/edge-backend`。
- 2026-05-31 06:44 | GPT-5 Codex (`backend-ai/test-ai`) | EB-019/040/045/048 默认报警 smoke 补强：`smokeeb045` 新增默认报警虚拟变量，通过变量 PATCH 开启默认上下限，随后写值 `43 -> 50 -> 25` 验证变量默认报警进入、等级变化、恢复均可经 WS 即时到达、HTTP 同一 `event_uid` 读回并标记已读，且 `GET /api/v1/limit-alarms?scope=default` 能读回 `task_id=0/alarm_type=above_hh` 落库记录。现场 smoke 项目 `33` 通过；验证通过 smoke 工具编译、真实 smoke、`go test ./...`、覆盖率 72.3%、`go vet ./...`、`golangci-lint run ./...`、`go build ./cmd/edge-backend`。
- 2026-05-31 06:36 | GPT-5 Codex (`backend-ai/test-ai`) | EB-019/045/048 报警等级变化通知 smoke 补强：`smokeeb045` 的 NG 场景改为进入 H、升级 HH、恢复合格、停止 NG，验证 `alarm.limit.enter`、`alarm.limit.level_change`、`alarm.limit.recover` 和 `detection.result_ng` 均可经 WS 即时到达、HTTP 同一 `event_uid` 读回并标记已读。验证通过 smoke 工具编译、真实 smoke、`go test ./...`、覆盖率 72.3%、`go vet ./...`、`golangci-lint run ./...`、`go build ./cmd/edge-backend`。
- 2026-05-31 06:31 | GPT-5 Codex (`backend-ai/test-ai`) | EB-019/045/048 超限恢复通知闭环修复：检测报警恢复事件补齐项目、任务、变量、报警等级和限值上下文，变量默认报警恢复事件也补齐变量和限值上下文；恢复通知 `limit_value` 改为事件局部快照，避免运行态状态清零后通知限值被清掉。`smokeeb045` 已覆盖 `alarm.limit.enter -> alarm.limit.recover -> detection.result_ng` 的 WS/HTTP 未读已读链路。验证通过 `go test ./internal/pipeline` 定向、`go test -tags smoke_tools ./tools/smokeeb045`、`go test ./...`、覆盖率 72.3%、`go vet ./...`、`golangci-lint run ./...`、`go build ./cmd/edge-backend`；后端已重启为 PID 23492，`/health` ok，真实 smoke 项目 `31` 通过。
- 2026-05-27 09:55 | GPT-5 Codex | 初始化边缘端 AI 协作文档体系。
- 2026-05-27 12:20 | GPT-5 Codex (`frontend-ai`) | 初始化 `desktop/` Electron + React 前端骨架，并记录 AI 友好型前端技术选型。
- 2026-05-28 16:57 | GPT-5 Codex (`frontend-ai`) | 将 `react-design-prototype` 的工位操作与历史查询页面迁入 `desktop/`，新增工作台导航、三语文案、Recharts 与拖拽指标卡依赖；历史查询真实 API 仍待确认。
- 2026-05-28 17:24 | GPT-5 Codex (`frontend-ai`) | 按原型补齐工位操作页左侧卡片池自适应高度、拖拽占位/overlay、胶囊按钮、动态背景与橘黄色物理光源高光。
- 2026-05-28 17:46 | GPT-5 Codex (`frontend-ai`) | 对照运行中的 `react-design-prototype` 继续还原工位页质感：卡片图表改回原型 ChartLine 黑线/MinMax 标签，右侧表格改回 table 结构、独立表头和 hover 滚动条。
- 2026-05-28 18:27 | GPT-5 Codex (`frontend-ai`) | 修复工位右侧固定表头下滚动条默认可见问题；历史查询页按原型拆为图表、表格、甘特弹窗、样式与 mock 数据模块；新增 `.ai/skills/edge-glass-prototype-style` 固化原型玻璃风格还原流程。
- 2026-05-28 19:39 | GPT-5 Codex (`frontend-ai`) | 新增系统设置页 `/#/settings`，覆盖实时数据源服务配置、MQTT 站点配置、项目/设备分组、发现变量与数据库变量、变量归属分配；历史数据源服务仅按项目边界做预留说明。
- 2026-05-28 19:54 | GPT-5 Codex (`frontend-ai`) | 调整设置页为变量优先的信息架构：项目/设备分组、变量列表、变量归属为主，MQTT 站点下沉为一次性采集源配置；配置查询禁用重试并在本地后端离线时明确提示。
- 2026-05-28 20:06 | GPT-5 Codex (`frontend-ai`) | 设置页再次收敛为变量管理主界面：顶部仅显示 MQTT 站点在线概览，左侧提供全部/已知/未知/项目设备筛选，右侧显示变量全字段表；站点新增、编辑和发现变量改为弹窗操作。
- 2026-05-29 14:08 | GPT-5 Codex (`backend-ai`) | 完成 EB-022：新增后端数据库配置读写/测试接口、前端历史数据源 MySQL 配置表单和三语文案；验证 `go test ./...`、覆盖率 70.3%、`go vet ./...`、`go build ./cmd/edge-backend`、`desktop/npm run lint`、`desktop/npm run build`、本地 `/health` smoke。
- 2026-05-29 14:20 | GPT-5 Codex (`backend-ai`) | 在 `AI_BOARD.md` 新增 EB-023 前端历史数据源配置页适配验收，并将后端后续拆为 EB-024 到 EB-029：日志审计、变量属性/存储映射、检测配置、WS 只读、WS 写、检测事件/超限和联调回归。
- 2026-05-29 14:25 | GPT-5 Codex (`backend-ai`) | 开始 EB-020 后端 P0 迭代：新增受保护 HTTP 写请求审计 middleware、请求 ID 回传、成功/失败写操作测试和后端架构文档审计说明。
- 2026-05-29 14:28 | GPT-5 Codex (`backend-ai`) | EB-020 P0 验证留痕：`go test ./...`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 70.5%、`go vet ./...`、`go build ./cmd/edge-backend` 通过；`golangci-lint` 本机未安装；已重启本地后端并 smoke 验证 `/health`、登录、数据库配置测试接口和 `X-Request-ID` 回传。
- 2026-05-30 13:55 | GPT-5 Codex (`backend-ai`) | EB-045 继续迭代任务模块：执行记录/SQL 日志查询、受控变量写入、防递归、任务模板、检测静音/level_change/运行中限值调整、storage prepare、报表登记和 HTTP 请求模块。验证通过：`go test ./...`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 70.1%、`go vet ./...`、`golangci-lint run ./...`、`go build ./cmd/edge-backend`；已重启本地后端并 smoke `/health`、登录、`task-modules/task-flow-templates/task-flow-runs`。
- 2026-05-30 14:13 | GPT-5 Codex (`backend-ai`) | EB-019/027 继续迭代：拆出 `KIOWriteService`，新增 `VariableWriteService`，WS `command.write_variable` 支持虚拟变量内存写入/任务触发和可写物理变量 KIO 下设；HTTP KIO 写与 WS 变量写共用后端 service，写命令继续写 `sys_audit_logs`。验证通过：`go test ./...`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 70.0%、`go vet ./...`、`golangci-lint run ./...`、`go build ./cmd/edge-backend`；后端已重启为 PID 57656，并通过 `/health`、登录、`task-modules` smoke。
- 2026-05-30 23:56 | GPT-5 Codex (`backend-ai`) | EB-040 补齐统一超限报警查询入口：新增 `GET /api/v1/limit-alarms`，可按 `scope=default|detection`、项目、任务、变量、状态、报警类型/等级和时间筛选默认报警与检测业务报警；已补 repository/handler 测试。验证通过：`go test ./... -coverprofile all-coverage.out` 总覆盖率 71.1%、`go test ./...`、`go vet ./...`、`golangci-lint run ./...`、`go build ./cmd/edge-backend`；收紧 `var_id` 正数校验后补跑 handler 测试、handler linter 和 build；后端已重启为 PID 10592，并通过 `/health`、admin 登录和 `limit-alarms` smoke。
- 2026-05-31 00:04 | GPT-5 Codex (`backend-ai/frontend-ai`) | 追平 EB-040 桌面 shared API：新增 `LimitAlarm` 查询类型和 `getLimitAlarms(params)` client，未接页面交互；验证通过 `desktop/npm run lint`、`desktop/npm run test -- --run`、`desktop/npm run build`。
- 2026-05-31 00:12 | GPT-5 Codex (`frontend-ai/test-ai`) | EB-040 工位页报警日志入口落地：新增右侧抽屉、scope 分段切换、报警表格和中英日文案，数据来自 `getLimitAlarms` 并按当前项目筛选。验证通过 `desktop/npm run lint`、`desktop/npm run test -- --run`、`desktop/npm run build`；浏览器 smoke 登录后打开报警日志抽屉成功；完整 `npm run dev` 因 Electron `path.txt` 缺失失败，改用 renderer dev server 验证。
- 2026-05-31 00:58 | GPT-5 Codex (`review-ai`) | 收敛交互看板归档规则：`AI_BOARD.md` 作为唯一实时交互看板，必须保留所有 open/blocked 项；closed 项的结论、施工说明、测试证据和相关解释迁移到对应 `.ai/docs/`、模块文档或 `.ai/docs/archive/`。已同步 `AGENTS.md`、`.ai/instructions/ai-workflow.md`、`.ai/docs/README.md` 和 `.ai/docs/archive/README.md`。
- 2026-05-31 01:08 | GPT-5 Codex (`review-ai`) | 执行看板瘦身和跨工具规则同步：`AI_BOARD.md` 只迁移 `closed` 项和 `closed` Activity Log，保留全部 open/blocked；新增归档 `.ai/docs/archive/2026-05-31-ai-board-closed-records.md`，并同步 `AGENTS.md`、`.ai/instructions/backend-go-edge.md`、`.github/copilot-instructions.md`、`.cursor/rules/*` 的项目/归档规则，明确当前业务分组为 `projects`，旧 `device` 只作为兼容别名。
- 2026-05-31 01:13 | GPT-5 Codex (`review-ai`) | 补充看板增长控制规则：`AI_BOARD.md` 明确 Activity Log 的 `open` 只表示当前仍需交接，历史证据应合并到 EB 行、稳定文档或归档后转 `closed` 迁出；`.ai/docs/archive/README.md` 增加历史索引和新增归档必须更新索引的规则，避免 AI 翻历史时找不到来源。
- 2026-05-31 01:10 | GPT-5 Codex (`backend-ai/test-ai`) | EB-045 任务参数闭环补测试：新增 `TestTaskFlowStringPayloadWritesPLCBeforeStartingDetection`，覆盖 watched STRING 虚拟变量同时携带检测配置、`process_params.inlet_area_m2` 和 `plc_writes` 时，任务流先经后端 writer 下设 PLC 并写审计，再启动检测并冻结追溯参数。验证通过 `go test ./internal/pipeline -run TestTaskFlowStringPayloadWritesPLCBeforeStartingDetection -count=1`、`go test ./...`、覆盖率 71.3%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；本地 `/health` ok。
- 2026-05-31 01:18 | GPT-5 Codex (`backend-ai/test-ai`) | EB-045 PLC 参数失败保护补强：新增 `TestTaskFlowStringPayloadStopsDetectionWhenPLCParamMissing`，证明 `plc_writes.value_from` 指向不存在的 `process_params` 路径时，任务流失败、变量 writer 不被调用、检测任务不创建，并写失败审计。验证通过定向测试、`go test ./...`、覆盖率 71.4%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；`/health` ok，KIO gateway active。
- 2026-05-31 01:23 | GPT-5 Codex (`backend-ai/test-ai`) | EB-045 PLC 写入失败保护补强：新增 `TestTaskFlowStringPayloadStopsDetectionWhenPLCWriteFails`，证明 `process_params` 可解析但后端变量写入 service 返回失败时，任务流失败、检测任务不创建，并保留失败审计。验证通过定向测试、`go test ./...`、覆盖率 71.4%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；`/health` ok，KIO gateway active，队列为 0。
- 2026-05-31 01:26 | GPT-5 Codex (`backend-ai/test-ai`) | EB-045 任务执行状态语义收紧：`task_flow_runs.status=timeout` 只用于真实上下文/网络/脚本超时，不再因为普通业务错误文本包含 `timeout` 就误归类；PLC ack 等业务失败保持 `failed`。新增 `TestTaskFlowStatusForErrDoesNotClassifyBusinessTimeoutTextAsTimeout`。验证通过定向测试、`go test ./...`、覆盖率 71.4%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；`/health` ok，KIO gateway active，队列为 0。
- 2026-05-31 01:30 | GPT-5 Codex (`backend-ai/test-ai`) | EB-045 真超时回归补齐：新增 `TestTaskFlowJavaScriptTimeoutStatus`，用真实 JavaScript 无限循环触发 Goja interrupt，验证 `task_flow_runs` 语义仍会返回 `timeout`，同时继续保证业务错误文本中的 timeout 不误归类。验证通过定向测试、`go test ./...`、覆盖率 71.5%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；`/health` ok，KIO gateway active，队列为 0。
- 2026-05-31 01:36 | GPT-5 Codex (`backend-ai/test-ai`) | EB-045 任务模块 schema 对齐配置参数语义：`GET /api/v1/task-modules` 的 `builtin.start_detection_run.params_schema` 显式暴露 `process_params` 和 `plc_writes`，前端动态表单可展示检测配置、工艺/报表参数和 PLC 下设计划；新增 handler 回归断言 schema 包含进风口面积语义。验证通过定向 handler 测试、`go test ./...`、覆盖率 71.5%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 4984，`/health` ok，登录后 smoke `task-modules=17` 且启动检测模块返回 `process_params/plc_writes`。
- 2026-05-31 01:38 | GPT-5 Codex (`backend-ai/test-ai`) | EB-045 任务执行记录查询回归补强：新增 handler 测试 `TestTaskFlowsHandlerListRunsFiltersFailedAndTimeoutDistinctly`，构造业务失败文本含 timeout 但状态为 `failed` 的记录和真实 `timeout` 记录，验证 `GET /api/v1/task-flow-runs?status=failed|timeout` 分开返回。验证通过定向测试、`go test ./...`、覆盖率 71.5%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；`/health` ok。
- 2026-05-31 01:42 | GPT-5 Codex (`backend-ai/test-ai`) | EB-045 任务执行记录查询参数硬化：`GET /api/v1/task-flow-runs` 对 `status` 和 `trigger_type` 做枚举校验，非法值返回 `400`，避免前端任务页传错筛选项时静默得到空列表；同一 handler 回归覆盖非法 `status=done` 和 `trigger_type=unknown`。验证通过定向测试、`go test ./...`、覆盖率 71.5%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 41508，`/health` ok，登录后 smoke `task-flow-runs?status=done` 返回 400。
- 2026-05-31 02:21 | GPT-5 Codex (`backend-ai/test-ai`) | EB-045 任务流变量绑定硬化：`POST/PATCH /api/v1/task-flows` 拒绝 `vars[].var_id <= 0`，避免保存无法进入 `var_id -> flow_ids` 倒排索引的 watch/read/write 绑定；新增 create/patch 回归。验证通过定向 handler 测试、`go test ./...`、覆盖率 71.7%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 39292，`/health` ok，smoke `var_id=0` 返回 400。
- 2026-05-31 02:26 | GPT-5 Codex (`backend-ai/test-ai`) | EB-045 data_change 任务流配置硬化：`POST/PATCH /api/v1/task-flows` 要求 `trigger_type=data_change` 的任务至少绑定一个 `watch` 变量，因为只有 watch 变量会进入 `var_id -> flow_ids` 倒排索引；新增 create 和 patch 回归。验证通过定向 handler 测试、`go test ./...`、覆盖率 71.8%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 42568，`/health` ok，smoke `data_change` 仅绑定 read 变量返回 400。
- 2026-05-31 02:30 | GPT-5 Codex (`backend-ai/test-ai`) | EB-045 schedule 任务流配置硬化：`POST/PATCH /api/v1/task-flows` 要求 `trigger_type=schedule` 的任务必须配置 `schedule_interval_ms > 0` 或兼容期 `cooldown_ms > 0`，避免扫描器永久跳过保存成功的空转定时任务；新增 create 和 patch 回归。验证通过定向 handler 测试、`go test ./...`、覆盖率 71.8%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 8060，`/health` ok，smoke schedule 无间隔返回 400。
- 2026-05-31 02:42 | GPT-5 Codex (`backend-ai/test-ai`) | EB-045 任务执行记录分页参数硬化：`GET /api/v1/task-flow-runs` 拒绝 `limit<=0` 和 `offset<0`，避免前端任务页分页入参错误时被仓储层静默归一化。新增 handler 回归覆盖 `limit=0`、`limit=-1`、`offset=-1`。验证通过定向 handler 测试、`go test ./...`、覆盖率 71.8%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 25584，`/health` ok，KIO gateway active，smoke 非法分页均返回 400。
- 2026-05-31 02:46 | GPT-5 Codex (`backend-ai/test-ai`) | EB-045 任务 SQL 日志查询参数硬化：`GET /api/v1/task-flow-runs/:id/sql-logs` 拒绝 `limit<=0`，避免前端任务页 SQL 日志传错 limit 后被仓储层静默改成默认 100 条；新增 handler 回归覆盖 `limit=0`。验证通过定向 handler 测试、`go test ./...`、覆盖率 71.8%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 41604，`/health` ok，KIO gateway active，smoke 非法 SQL 日志 limit 返回 400。
- 2026-05-31 02:50 | GPT-5 Codex (`backend-ai/test-ai`) | EB-045 任务执行记录不存在语义修正：`GET /api/v1/task-flow-runs/:id` 和 `/task-flow-runs/:id/sql-logs` 在 run 不存在时返回 `404`，避免前端把不存在的执行记录误显示成空日志或普通参数错误；新增 handler 回归覆盖缺失 run 详情和缺失 run SQL 日志。验证通过定向 handler 测试、`go test ./...`、覆盖率 71.8%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 3440，`/health` ok，KIO gateway active，smoke 两个不存在 run 接口均返回 404。
- 2026-05-31 02:55 | GPT-5 Codex (`backend-ai/test-ai`) | EB-045 任务执行记录筛选参数硬化：`GET /api/v1/task-flow-runs` 要求 `project_id/flow_id/trigger_var_id/origin_flow_id` 为正数，并要求 `from <= to`，避免前端把空 ID 或反向时间范围误传后只得到空列表；新增 handler 回归覆盖四类非法 ID 和反向时间范围。验证通过定向 handler 测试、`go test ./...`、覆盖率 72.1%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 27040，`/health` ok，KIO gateway active，smoke 非法筛选均返回 400。
- 2026-05-31 02:59 | GPT-5 Codex (`backend-ai/test-ai`) | 后端公共错误映射修正：`services.HTTPStatusForError` 现在把 `gorm.ErrRecordNotFound` 映射为 HTTP `404`，引用保护和同项目重复 running 仍为 `409`，普通校验错误仍为 `400`；检测任务、存储路由、任务执行记录和 WS 命令等复用该映射的路径将得到一致缺失资源语义。验证通过定向 service/handler/runtime 测试、`go test ./...`、覆盖率 72.1%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 41456，`/health` ok，KIO gateway active，smoke 缺失检测任务 stop 和缺失 task-flow run 均返回 404。
- 2026-05-31 03:05 | GPT-5 Codex (`backend-ai/test-ai`) | 检测标准缺失资源语义补齐：`PATCH /api/v1/detection-standards/{id}` 和 `PUT /api/v1/detection-standards/{id}/items` 对不存在标准返回 `404`，不再返回 `500`；并再次确认任务配置参数模型中 `process_params` 承载进风口面积等工艺/报表参数，`plc_writes` 才是受控 PLC 下设计划。验证通过定向 handler/runtime/pipeline 测试、`go test ./...`、覆盖率 72.1%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 41100，`/health` ok，tags=1041，smoke 两个缺失检测标准接口均为 404，`task-modules` 返回 `process_params/plc_writes`。
- 2026-05-31 03:12 | GPT-5 Codex (`backend-ai/test-ai`) | 资源编辑/删除缺失语义继续收紧：`PATCH /api/v1/projects/{id}`、`PATCH /api/v1/variables/{id}`、`PATCH /api/v1/variables/{id}/assignment`、`DELETE /api/v1/variables/{id}`、`PATCH /api/v1/gateway-configs/{id}`、`DELETE /api/v1/gateway-configs/{id}` 在目标资源不存在时统一返回 `404`，不再返回 `500` 或静默成功；变量分配/删除和网关删除仓储层检查 `RowsAffected=0`。验证通过定向 handler/kernel 回归、`go test ./...`、覆盖率 72.3%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 27444，`/health` ok，tags=1041，登录后 smoke 六个缺失资源写接口均返回 404。
- 2026-05-31 03:19 | GPT-5 Codex (`backend-ai/test-ai`) | 配置资源缺失语义继续收紧：`POST/DELETE /api/v1/detection-standards/{id}/favorite`、`DELETE /api/v1/detection-standards/{id}`、`PATCH/DELETE /api/v1/report-templates/{id}`、`PATCH/DELETE /api/v1/storage-routes/{id}` 在目标资源不存在时返回 `404`；检测标准和报表模板引用保护仍返回 `409`，storage route 已冻结到检测运行时删除仍返回 `409`。验证通过 handler/kernel/repository 定向回归、`go test ./...`、覆盖率 72.5%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 41596，`/health` ok，tags=1041，登录后 smoke 七个缺失配置资源接口均返回 404。
- 2026-05-31 03:25 | GPT-5 Codex (`backend-ai/test-ai`) | EB-045 预置检测模板补齐 PLC 下设顺序：`GET /api/v1/task-flow-templates` 中变量请求启动、固定时长检测、合格持续检测模板都会先生成 `builtin.write_control_variables` 再生成 `builtin.start_detection_run`，并透传 `process_params/plc_writes/operator_note/report_template_id`，避免固定时长/合格持续模板只冻结 PLC 下设计划但未实际下设。验证通过 handler 定向回归、`go test ./...`、覆盖率 72.5%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 39948，`/health` ok，tags=1041，登录后 smoke `task-flow-templates=13` 且 `builtin.write_control_variables` 出现 4 次。
- 2026-05-31 03:34 | GPT-5 Codex (`frontend-ai/test-ai`) | 系统设置日志体验修正：运行日志正文改为点击“运行日志预览”打开居中大弹窗，设置页只保留日志摘要、刷新和打开日志位置按钮；没有使用侧边抽屉。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，Playwright 登录进入 `/#/settings` 后打开系统模块和日志弹窗，确认 `.settings-log-modal` 可见、`.ant-drawer` 数量为 0，截图 `output/playwright/settings-log-modal-smoke.png`。
- 2026-05-31 03:44 | GPT-5 Codex (`frontend-ai/test-ai`) | 推进 EB-046/048 任务页正式 STRING 触发入口：新增“发送任务请求”弹窗，选择 watched `STRING` 虚拟变量后可结构化填写检测启动参数、`process_params` 工艺/报表参数和 `plc_writes` PLC 下设计划，提交通过 WS `command.write_variable` 写入虚拟变量触发任务系统；进风口面积默认作为 `process_params.inlet_area_m2`，PLC 下设可用 `value_from` 引用同一来源。验证通过 `desktop/npm run lint`、`desktop/npm run test -- --run`、`desktop/npm run build`，Browser smoke 登录 `/#/tasks` 打开弹窗，确认“工艺/报表参数”“PLC 下设计划”和 `inlet_area_m2` 默认行可见，截图 `output/playwright/task-request-modal-smoke.png`。
- 2026-05-31 03:50 | GPT-5 Codex (`frontend-ai/test-ai`) | 继续完善 EB-046/048 任务请求弹窗：新增“请求 JSON 预览”，展示最终写入 watched `STRING` 虚拟变量的 payload；`custom_items` JSON 无效时阻止提交；PLC 下设区明确未选择 `var_id` 的行不会进入最终 JSON。验证通过 `desktop/npm run lint`、`desktop/npm run test -- --run`、`desktop/npm run build`，Playwright smoke 登录 `/#/tasks` 打开弹窗，确认预览、`start_detection`、`process_params`、`inlet_area_m2`、PLC 下设区和 `var_id` 提示可见，截图 `output/playwright/task-request-preview-smoke.png`。
- 2026-05-31 03:57 | GPT-5 Codex (`frontend-ai/test-ai`) | 按用户确认补清任务请求“配置参数”语义：任务请求弹窗新增配置参数说明，明确检测配置、过程参数和 PLC 下设引用同属正式任务 payload；选择 watched `STRING` 请求变量时显示启用任务流监听状态，避免写入变量但没有业务响应。验证通过 `desktop/npm run lint`、`desktop/npm run test -- --run`、`desktop/npm run build`，Playwright smoke 登录 `/#/tasks` 打开弹窗，确认“配置参数”、进风口面积 `inlet_area_m2` 和监听任务流提示可见，截图 `output/playwright/task-request-config-params-smoke.png`。
- 2026-05-31 04:05 | GPT-5 Codex (`frontend-ai/test-ai`) | 继续推进 EB-046/048 正式 STRING 请求入口：任务请求弹窗新增 `stop_detection/pause_detection/resume_detection/mute_detection_alarms/storage_snapshot/update_detection_limits/refresh_detection_features/register_report`，按命令展示运行控制、运行中限值调整和报表登记字段；payload 构造按命令过滤无关字段，避免开始检测参数泄漏到限值调整或报表登记请求。验证通过 `desktop/npm run lint`、`desktop/npm run test -- --run`、`desktop/npm run build`，Playwright smoke 切换 `update_detection_limits` 和 `register_report`，确认字段与 JSON 预览，截图 `output/playwright/task-request-command-coverage-smoke.png`。
- 2026-05-31 04:12 | GPT-5 Codex (`frontend-ai/test-ai`) | 继续补齐 EB-046/048 任务请求低代码入口：新增 `storage_prepare` 和 `http_request` 命令参数区，`storage_prepare` 说明只准备项目宽表/动态列不写采样，`http_request` 支持 `url/headers/body/timeout_ms` 且 headers 作为 JSON 参与预览校验；payload 构造继续按命令过滤无关字段。验证通过 `desktop/npm run lint`、`desktop/npm run test -- --run`、`desktop/npm run build`，Playwright smoke 切换 `storage_prepare` 和 `http_request`，确认字段与 JSON 预览，截图 `output/playwright/task-request-storage-http-smoke.png`。
- 2026-05-31 04:16 | GPT-5 Codex (`frontend-ai/test-ai`) | EB-046/048 正式 STRING 虚拟变量触发链端到端 smoke 通过：登录后创建临时 `STRING` 虚拟变量和临时 data_change 任务流，WS `command.write_variable` 写入 `{command:"codex_e2e_context"}` 返回 `command.ack`，后端 `task-flow-runs` 记录 `run_id=6/status=success`，随后删除临时任务流和变量。首次使用后端自动生成的大 64 位 `var_id` 时暴露 JS number 精度风险，已用显式小范围 `var_id=700766631` 复测通过，并清理首次遗留变量 `9212397624135540849`；后续需要评估前端是否应把大 `var_id` 当 string 处理。
- 2026-05-31 04:25 | GPT-5 Codex (`frontend-ai/test-ai`) | 驾驶舱按 Codex 官网产品页方向重新降噪：移除右侧趋势曲线，右侧改为实时值/上下限/OKNG/优化率信息卡；撤掉导致厚重感的内联玻璃覆盖，统一使用低饱和玻璃卡、细边框、轻阴影和工位同源 600px 暖光边缘；标题 SVG 背板、背景和卡片文字层级改为更克制的产品面板风格，3D 模型继续作为中心主视觉并下移放大。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，Playwright 1920×1080 截图 `output/playwright/model-cockpit-codex-pass-1.png`，确认曲线节点为 0。
- 2026-05-31 04:35 | GPT-5 Codex (`frontend-ai/test-ai`) | EB-034 变量默认报警前端适配：设置中心变量编辑和虚变量创建新增“默认报警”分区，接入 `default_alarm_enabled/default_limit_ll/default_limit_l/default_limit_h/default_limit_hh/default_limit_deadband/default_violation_hold_ms/default_recover_hold_ms`；变量编辑新增 `apply_to_running` 运行中同步选项和黄色风险提示，说明只同步当前 running 任务变量默认报警快照，不修改检测标准业务限值。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`；Playwright 登录 `/#/settings`，打开创建虚变量和编辑已分配变量弹窗，确认默认报警字段、运行中同步选项和风险提示可见，截图 `output/playwright/settings-variable-default-alarm-smoke.png`。
- 2026-05-31 04:53 | GPT-5 Codex (`frontend-ai/test-ai`) | EB-034 工位页补“标准快照”入口：当前 running 任务弹窗调用 `GET /api/v1/detection-runs/{id}` 展示检测标准限值、变量默认报警、变量默认上下限和默认报警策略，明确检测业务限值与变量资产默认值的区别。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`、`git diff --check`；Playwright 1920x1080 登录工位页点击“标准快照”，确认 `GET /api/v1/detection-runs/20` 返回 200，截图 `output/playwright/station-run-standard-snapshot-smoke.png`。
- 2026-05-31 04:59 | GPT-5 Codex (`frontend-ai/test-ai`) | EB-037/047 历史页存储快照和项目参数收口：历史查询页任务链接优先使用 `project_id` 查询历史，旧 `device_id` 只做兼容读取；运行存储快照入口已验证可用。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`；Playwright 1920x1080 登录 `/#/history?task_id=20&project_id=1&test_no=RUN-20260530204958`，确认历史请求为 `/api/v1/history/data?task_id=20&project_id=1&limit=5000` 且不含 `device_id`，点击“存储快照”后 `/api/v1/detection-runs/20/storage-routes` 返回 200，截图 `output/playwright/history-storage-snapshot-project-param-smoke.png`。
- 2026-05-31 05:03 | GPT-5 Codex (`frontend-ai/test-ai`) | EB-034 运行中同步保存 smoke 完成并关闭前端项：Playwright 登录 `/#/settings`，打开已分配变量编辑配置，点击“同步到当前运行任务快照”并保存；捕获到 `PATCH /api/v1/variables/9212397624135540847` 请求体包含 `apply_to_running=true`，后端返回 200，截图 `output/playwright/settings-apply-to-running-save-smoke.png`。
- 2026-05-31 05:14 | GPT-5 Codex (`frontend-ai/test-ai`) | EB-037 storage route 管理端到端闭合：设置中心“存储路由”模块新增搜索和启用状态筛选；Playwright 创建临时 route `codex-smoke-604921`，验证 `POST /api/v1/storage-routes` 携带字符串 `var_id`、`project_id=1`、`enabled=true`，筛选定位后编辑为 `enabled=false` 并 `PATCH /api/v1/storage-routes/529` 成功，再 `DELETE /api/v1/storage-routes/529` 成功且筛选结果消失。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`、`git diff --check`，截图 `output/playwright/settings-storage-route-crud-smoke.png`。
- 2026-05-31 03:46 | GPT-5 Codex (`frontend-ai/test-ai`) | 驾驶舱按 Codex 官网产品页气质再次收敛：移除重 HUD 标题背板，降低蓝色饱和和装饰感，卡片统一为更通透的浅玻璃面板并保留工位同源暖光边缘，去掉 3D 模型临时诊断流线，模型改为更干净的产品展示。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，1920×1080 截图 `output/playwright/model-cockpit-codex-polish-v2-1920x1080.png`。
- 2026-05-31 04:00 | GPT-5 Codex (`frontend-ai/test-ai`) | EB-048 通知中心补齐未读、类型和项目筛选：顶栏通知弹层调用 `GET /api/v1/notifications` 时带 `unread/type/project_id`，并收紧弹层高度和定位，避免筛选条被顶出视口。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`；Playwright 登录后点击通知中心、未读、类型“检测开始”和项目，最终请求为 `/api/v1/notifications?unread=true&type=detection.run_started&project_id=2&limit=20`，截图 `output/playwright/notification-filters-smoke.png`。
- 2026-05-31 02:35 | GPT-5 Codex (`backend-ai/test-ai`) | EB-045 任务流时间字段硬化：`POST /api/v1/task-flows` 拒绝负数 `timeout_ms/cooldown_ms/hold_ms/schedule_interval_ms`，`PATCH` 显式提交 `timeout_ms` 时必须大于 0，其他时间字段不能为负；避免创建时静默归零、编辑时写入即时超时或负数间隔。新增 create/patch 回归。验证通过定向 handler 测试、`go test ./...`、覆盖率 71.8%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 22240，`/health` ok，smoke `timeout_ms=-1` 返回 400。
- 2026-05-31 02:17 | GPT-5 Codex (`backend-ai/test-ai`) | EB-045 任务流 step code 唯一性硬化：`POST/PATCH /api/v1/task-flows` 拒绝重复显式 `steps_json[].code`，执行器也在运行前防御历史脏配置，避免多步骤结果写入 run context 时互相覆盖。新增 handler create/patch 和 pipeline 回归。验证通过定向测试、`go test ./...`、覆盖率 71.7%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 9632，`/health` ok，smoke 重复 step code 返回 400。
- 2026-05-31 02:09 | GPT-5 Codex (`backend-ai/test-ai`) | EB-045 空步骤链配置硬化：`POST/PATCH /api/v1/task-flows` 拒绝显式 `steps_json=[]`，避免保存后任务运行成功但没有执行任何模块；执行器 `taskFlowSteps` 也增加同样防御，防止历史脏配置静默成功。新增 handler 回归覆盖 create/patch 空步骤链。验证通过定向 handler 和 pipeline 测试、`go test ./...`、覆盖率 71.6%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 36744，`/health` ok，登录后 smoke 空步骤链创建 task-flow 返回 `400 steps_json cannot be empty`。
- 2026-05-31 02:05 | GPT-5 Codex (`backend-ai/test-ai`) | EB-045 任务流 event 参数 key 校验硬化：`POST/PATCH /api/v1/task-flows` 在 `steps_json[].params` 使用 `source=event` 时提前校验 `key`，只允许执行器真实支持的 `trigger_type/project_id/trigger_var_id/trigger_value/gateway_id/topic/origin_flow_id/origin_run_id/depth/request_id/at`，避免保存后运行时才报 unknown event key。新增 handler 回归覆盖 create/patch 的坏 event key。验证通过定向 handler 测试、`go test ./...`、覆盖率 71.6%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 41056，`/health` ok，登录后 smoke 坏 event key 创建 task-flow 返回 `400 invalid task flow event key at params.project: project`。
- 2026-05-31 02:00 | GPT-5 Codex (`backend-ai/test-ai`) | EB-045 任务流步骤参数绑定校验硬化：`POST/PATCH /api/v1/task-flows` 会递归校验 `steps_json[].params` 中显式声明的 `source`，只允许 `literal/trigger_param/event/context`，非法来源直接返回 `400`，避免保存后运行时才因参数绑定失败。新增 handler 回归覆盖 create/patch 的坏参数来源。验证通过定向 handler 测试、`go test ./...`、覆盖率 71.6%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 10288，`/health` ok，登录后 smoke 坏参数来源创建 task-flow 返回 `400 invalid task flow param source at params.project_id: trigger`。
- 2026-05-31 01:56 | GPT-5 Codex (`backend-ai/test-ai`) | EB-045 任务流配置必填字段硬化：`POST /api/v1/task-flows` 要求 `project_id > 0`、非空 `flow_code` 和非空 `name`；`PATCH` 拒绝 `project_id=0` 或空白 `flow_code/name`，避免保存不可识别的空壳任务配置。新增 handler 回归覆盖缺项目、空编码、空名称和 patch 改坏字段。验证通过定向测试、`go test ./...`、覆盖率 71.6%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 2648，`/health` ok，登录后 smoke 缺 `project_id` 创建 task-flow 返回 `400 project_id is required`。
- 2026-05-31 01:49 | GPT-5 Codex (`backend-ai/test-ai`) | EB-045 任务流配置写入端硬化：`POST/PATCH /api/v1/task-flows` 和列表 `trigger_type` 筛选新增枚举校验，非法 `trigger_type/action_type/vars.role/steps_json[].module` 直接返回 `400`，避免保存不可运行的任务配置；同时规范化合法枚举大小写和空白。新增 handler 回归覆盖坏触发类型、坏动作类型、坏 step module、坏变量角色和坏列表筛选。验证通过定向测试、`go test ./...`、覆盖率 71.6%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 8608，`/health` ok，登录后 smoke 创建坏 action task-flow 返回 400。
- 2026-05-31 01:11 | GPT-5 Codex (`backend-ai/test-ai`) | EB-017 重启恢复测试补齐：新增 `TestKernelStartRecoversRunningDetectionTask`，覆盖数据库已有 running 检测任务时，新 Kernel `Start()` 能恢复 runtime `TaskManager` active task，并让 `/api/v1/detection-runs/active` 返回该任务；同时断言标准项和 storage route 快照恢复。验证通过 `go test ./internal/runtime -run TestKernelStartRecoversRunningDetectionTask -count=1`、`go test ./...`、覆盖率 71.3%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端 PID 3224 继续运行，`/health` ok。
- 2026-05-30 14:23 | GPT-5 Codex (`backend-ai`) | EB-019/027/045 回归补强：新增 WS `command.write_variable` 写 STRING 虚拟变量触发 task-flow 的 handler 级测试，覆盖变量内存更新、倒排索引触发、`task_flow_runs` 成功记录和 `ws.command.write_variable` 审计落库；本轮未启动 KingIO/IO 服务。验证通过：`go test ./...`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 70.0%、`go vet ./...`、`golangci-lint run ./...`、`go build ./cmd/edge-backend`；本地后端仍监听 PID 57656。
- 2026-05-30 14:44 | GPT-5 Codex (`backend-ai`) | KingIO 现场写入测试与修复：直接写 `台1_39=13.42`、WS `command.write_variable` 写 `var_id=5826452156569908253` 到 `13.43` 均获得 KIO `ProcessStep=100/Result=ok`；修复默认 `Qid` 过大导致 ack 不匹配，修复自动发现 upsert 覆盖人工写入配置。验证通过：`go test ./...`、覆盖率 70.0%、`go vet ./...`、`golangci-lint run ./...`、`go build ./cmd/edge-backend`；后端已重启为 PID 36240 并保持运行。
- 2026-05-30 14:05 | GPT-5 Codex (`frontend-ai`) | 统一 3D 驾驶舱卡片质感：新增 `cockpit-glass-panel`，顶部状态卡、监测数据、模型面板和趋势图改用工位同源磨砂玻璃与动态受光边缘；`CockpitDynamicBackground` 持续写入每个面板的 `--mouse-x/--mouse-y`。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test`，Playwright 截图输出 `output/playwright/model-cockpit-glass.png`。
- 2026-05-30 14:28 | GPT-5 Codex (`frontend-ai`) | 细化驾驶舱玻璃质感：修复 SVG 纹理层未实际叠加的问题，顶部五张卡分别接入 `card-0..4.svg` 并恢复轻微蓝青色差；中心/图表面板接入 `cockpit-center.svg`/`cockpit-chart.svg`，背景改得更接近工位页通透动态光底。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，Playwright 截图输出 `output/playwright/model-cockpit-glass-texture.png`。
- 2026-05-30 14:40 | GPT-5 Codex (`frontend-ai`) | 重绘驾驶舱 SVG 纹理：顶部五张卡补回接近原 PNG 的蓝青渐变底、顶部斜切发光线、暗部衬线、半透明圆形水印和齿轮；中心/图表 SVG 补强顶部折线、暗部压线、边框和水印层，并提高玻璃纹理层 opacity。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，Playwright 截图输出 `output/playwright/model-cockpit-svg-texture-tuned.png`。
- 2026-05-30 15:06 | GPT-5 Codex (`frontend-ai`) | 抽象驾驶舱通用玻璃面板：新增 `CockpitGlassPanel`，统一输出左上标题槽和内容 body；顶部五张状态卡、监测数据、模型面板和趋势图都改为复用该组件，保留 `showGear`、纹理 class 和尺寸变体扩展点。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，Playwright 截图输出 `output/playwright/model-cockpit-panel-component.png`。
- 2026-05-30 15:25 | GPT-5 Codex (`review-ai`) | 在 `AI_BOARD.md` 新增 EB-047 并补充前端项目迁移闭合要求：页面层禁止继续扩散 `device/device_id/device_code`，统一改为 `project/project_id/project_code`；要求前端跑 lint/build/test，并 smoke 覆盖 projects 200、devices 404、变量分配、虚变量、检测、历史、storage routes 和任务页项目筛选。
- 2026-05-30 15:14 | GPT-5 Codex (`frontend-ai`) | 修正驾驶舱通用面板标题槽定位：旧 `.cockpit-glass-panel > *` 规则覆盖了标题槽绝对定位，导致大面板标题参与文档流且纹理线贴近上边缘；已排除 `.cockpit-panel-title` 并提高标题槽选择器优先级，同时为大面板纹理增加 `--cockpit-texture-y` 偏移。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，Playwright 截图输出 `output/playwright/model-cockpit-panel-title-fixed.png`。
- 2026-05-29 15:06 | GPT-5 Codex (`backend-ai`) | EB-024 P1 变量属性地基实施：扩展 `sys_tags` 存储映射、读写约束、防抖与启动快照字段，更新变量 create/patch API、服务层默认值/校验、前端共享 DTO 和后端文档。
- 2026-05-29 15:09 | GPT-5 Codex (`backend-ai`) | EB-024 P1 验证留痕：`go test ./...`、覆盖率 70.6%、`go vet ./...`、`go build ./cmd/edge-backend`、`desktop/npm run lint`、`desktop/npm run build` 通过；`golangci-lint` 本机未安装；后端已重启并 smoke 验证变量存储映射默认值和写入约束。
- 2026-05-29 15:30 | GPT-5 Codex (`backend-ai`) | 完成 EB-025 P2：检测标准项补齐检查方法、目标值、死区、保持时间和质量策略，检测运行快照冻结变量存储映射；`go test ./...`、覆盖率 70.7%、`go vet ./...`、`go build ./cmd/edge-backend`、`desktop/npm run lint`、`desktop/npm run build` 通过；`golangci-lint` 本机未安装；后端已重启为 PID 42268 并保持 `127.0.0.1:18080` 运行。
- 2026-05-29 15:58 | GPT-5 Codex (`backend-ai`) | 完成 EB-026 P3：新增 `/api/v1/ws` 只读实时通道、WS envelope 类型、订阅过滤、变量/检测任务 snapshot 和 heartbeat；`go test ./...`、覆盖率 70.3%、`go vet ./...`、`go build ./cmd/edge-backend`、`desktop/npm run lint`、`desktop/npm run build` 通过；`golangci-lint` 本机未安装；后端已重启为 PID 20420 并通过 `/health` 与 WS 首帧 smoke。
- 2026-05-29 16:14 | GPT-5 Codex (`backend-ai`) | 完成 EB-031：补强 WebSocket 断线重连和连接保护，新增未鉴权失败、主动断开、重连重新 ready/snapshot 和写命令只读保护测试；`go test ./...`、覆盖率 70.1%、`go vet ./...`、`go build ./cmd/edge-backend`、`desktop/npm run lint`、`desktop/npm run build` 通过；`golangci-lint` 本机未安装；后端已重启为 PID 39632 并通过 `/health`、WS 首连和断开重连 smoke。
- 2026-05-29 16:37 | GPT-5 Codex (`backend-ai`) | EB-027 完成检测任务 WS 写命令首段：`command.detection.start/stop/abnormal_stop` 复用 `DetectionRunsService` 并写审计；补 `command.ack` 前端共享类型；为保持门禁修复 `DetectionConfigPage` 当前 Query 类型和 lint 问题；`go test ./...`、覆盖率 70.0%、`go vet ./...`、`go build ./cmd/edge-backend`、`desktop/npm run lint`、`desktop/npm run build` 通过；`golangci-lint` 本机未安装；后端已重启为 PID 70660，并通过 WS start/stop smoke。
- 2026-05-29 15:10 | GPT-5 Codex (`frontend-ai`) | 完成 EB-023 历史数据源 MySQL 配置前端适配验收：修正测试连接 `ok=false` 误判成功的问题，补保存后重启提示状态和三语文案；`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test` 通过，Playwright smoke 验证密码不回显、连接成功和错误端口失败反馈。
- 2026-05-28 20:29 | GPT-5 Codex (`frontend-ai`) | 管理中心模块入口移动到标题右侧展示卡片，移除左侧模块导航，释放主区域宽度给变量筛选和变量表。
- 2026-05-28 20:44 | GPT-5 Codex (`frontend-ai`) | 工位操作改为桌面首屏，边缘概览降级为调试看板；左侧工位操作自动读取项目/设备生成二级菜单，点击设备后工位页按该设备过滤实时变量并切换状态卡。
- 2026-05-28 19:54 | GPT-5 Codex (`backend-ai`) | 新增 `backend/tools/dbinit`，用于在无本机 MySQL CLI 时通过 Docker 映射端口初始化 `spindle_edge` schema；已启动本地后端并通过 health/runtime smoke。
- 2026-05-28 19:58 | GPT-5 Codex (`backend-ai`) | 修复本地前端跨端口访问后端的 CORS/OPTIONS 问题；`/#/settings` 已能从浏览器上下文加载站点、分组和变量 API。
- 2026-05-28 20:14 | GPT-5 Codex (`review-ai`) | 新增 `.ai/docs/登录权限与SSO安全设计.md`，固定 Edge Auth Lite 方案：本地用户 JWT、主站 service token、一次性 SSO ticket、三角色 capability、插入式落地和后续接口维护规则。
- 2026-05-28 23:57 | GPT-5 Codex (`review-ai`) | 将权限落地任务拆到 `AI_BOARD.md`：EB-008 后端认证鉴权与 SSO ticket、EB-009 前端登录/权限守卫/SSO handoff、EB-010 前端用户管理页面。
- 2026-05-29 00:28 | GPT-5 Codex (`backend-ai`) | 完成 EB-008：实现 Edge Auth Lite 后端认证鉴权、service token、一次性 SSO ticket、审计表和 `/api/v1` 路由权限绑定；`go test ./...`、`go build ./cmd/edge-backend`、HTTP smoke 通过，全包覆盖率 74.3%。
- 2026-05-29 00:50 | GPT-5 Codex (`backend-ai`) | 新增根目录 `计划.md`，基于当前采集/业务设计与 JG HJ 旧实现，规划 raw、clean、business、detection、export 和验收的 9 次主迭代落地路径。
- 2026-05-29 00:53 | GPT-5 Codex (`frontend-ai`) | 完成 EB-009：新增桌面登录页、会话恢复、路由守卫、401/403 体验、capability 菜单显隐、退出登录和主站 SSO ticket 入口；同时按工位/历史查询风格统一设置页和调试看板视觉，并用 Playwright 验证登录及主要页面访问。
- 2026-05-29 01:03 | GPT-5 Codex (`frontend-ai`) | 完成 EB-010：补齐后端用户管理 CRUD/重置密码接口与管理中心用户模块；浏览器 smoke 验证新增、改角色、重置密码、新密码登录和删除，测试用户已清理。
- 2026-05-29 01:19 | GPT-5 Codex (`frontend-ai`) | 完成 EB-005：新增托盘、开机自启动、最小化到托盘、sidecar 异常重拉起和系统设置控制面板；补 `desktopBridge` 降级单测，`npm run test` 已通过，打包后 exe smoke 验证内置后端健康。
- 2026-05-29 01:29 | GPT-5 Codex (`frontend-ai`/`backend-ai`) | 完成 EB-001 收口：新增真实历史查询 API 与前端历史数据适配器，统一保留边缘端/主服务器可迁移边界；后端测试/构建、前端 lint/build/test、backend:build、Electron dir package 和打包 exe smoke 均通过。
- 2026-05-29 01:41 | GPT-5 Codex (`backend-ai`) | 为 `sys_devices` 增加 `display_name`、`display_name_en`、`display_name_ja` 三语显示字段；`GET/POST/PATCH /api/v1/devices` 支持 display 字段，旧 `name` 保持兼容，前端应按当前语言选择 display 字段并 fallback 到 `name`/`device_code`。
- 2026-05-29 08:45 | GPT-5 Codex (`review-ai`/`frontend-ai`) | 完成 UI 风格复审与 EB-011 修正：壳层、登录、设置、调试看板统一为工位/历史查询同源玻璃视觉；`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test` 通过，Playwright 截图复审已生成。
- 2026-05-29 09:13 | GPT-5 Codex (`backend-ai`) | 完成 EB-012：新增检测标准配置、变量上下限项和检测运行标准项快照；`GET/POST/PATCH/PUT/DELETE /api/v1/detection-standards` 与 `POST /api/v1/detection-runs` 的 `standard_id` 已可用，后端覆盖率 74.2%，`go vet`/build 通过。
- 2026-05-29 09:16 | GPT-5 Codex (`frontend-ai`) | 按 Apple Vision Pro + Claude 时尚杂志方向微调历史页和侧栏：移除查询按钮旁的“样例数据”伪按钮，状态移到图表说明；二级菜单选中态改为轻玻璃浮层；lint/build/test 通过。
- 2026-05-29 11:10 | GPT-5 Codex (`review-ai`) | 新增 `多设备并行检测任务设计计划.md`，审阅当前检测任务/标准/历史链路和工位/历史前端状态，明确每设备单 running、跨设备并行、标准快照不可变、历史按任务归属、后端事务锁和前端操作闭环计划；同步 `AI_BOARD.md` 新增 EB-015、EB-016、EB-017。
- 2026-05-29 09:46 | GPT-5 Codex (`frontend-ai`) | 完成 EB-013：修正变量管理编辑模型，未分配变量只读并提供分配入口，已分配变量提供完整配置编辑；设备三语 display 字段已接入工位菜单、工位状态和设置筛选；lint/build/test 与 Playwright 截图验证通过。
- 2026-05-29 11:20 | GPT-5 Codex (`frontend-ai`) | EB-013 追加未分配变量小卡片池：未知变量视图只显示基础属性，沿用搜索和项目设备筛选，支持全选当前结果、清空选择和批量分配；lint/build/test 与 Playwright 截图验证通过。
- 2026-05-29 11:25 | GPT-5 Codex (`review-ai`) | 补充虚变量审阅结论：当前只支持发现变量分配和已分配变量编辑，尚未支持用户新建虚变量；已在 `多设备并行检测任务设计计划.md`、AI_BOARD EB-015/EB-016/EB-017 中补充虚变量 API、前端入口和测试要求。
- 2026-05-29 11:35 | GPT-5 Codex (`frontend-ai`) | 修复未知变量池性能和按钮可读性：未知变量视图改为每页 48 个轻量卡片、移除隐藏表格渲染、批量分配按钮改为高对比深色胶囊；lint/build/test 与 Playwright 截图验证通过。
- 2026-05-29 11:15 | GPT-5 Codex (`backend-ai`) | 完成 EB-015 后端模块化增量：虚变量创建、检测任务并发锁、任务详情/notes/异常停止、报表模板元数据、任务报表关联、历史 task_id 过滤和引用删除保护。
- 2026-05-29 11:40 | GPT-5 Codex (`frontend-ai`) | 重试修复批量分配按钮内部文本和图标可读性：覆盖 Ant Design 内部 span/svg 状态色，验证按钮、文字、图标计算颜色均为白色；lint/build/test 与 Playwright 截图验证通过。
- 2026-05-29 11:55 | GPT-5 Codex (`frontend-ai`) | 批量分配工具栏按钮改为原生 button 和自定义胶囊样式，不再使用 Ant Design Button；验证无 ant-btn 类，文字/图标颜色均为白色；lint/build/test 与 Playwright 截图验证通过。
- 2026-05-29 12:00 | GPT-5 Codex (`frontend-ai`) | 修正未分配变量分配概念：确认 `var_group` 只是变量低频标签，不是项目/设备归属；分配和批量分配弹窗移除变量分组输入，只保留项目/设备和启用状态；lint/build/test 与 Playwright 截图验证通过。
- 2026-05-29 11:45 | GPT-5 Codex (`review-ai`) | 深度复查后端模块化现状，发现 `services`、`runtime/handlers`、虚变量、检测运行和报表模板已部分落地；新增 `后端模块化审阅与演进计划.md`，同步 AI_BOARD EB-018，并修正 `多设备并行检测任务设计计划.md` 中过时判断。
- 2026-05-29 11:50 | GPT-5 Codex (`review-ai`) | 在 `AI_BOARD.md` 为 EB-018 补充详细施工流程和文档位置，明确 P0-P5 执行顺序、验收要求和禁止事项。
- 2026-05-29 12:50 | GPT-5 Codex (`backend-ai`) | 完成 EB-018 后端模块化实施：收敛 `kernel.go`，按资源拆分 `runtime/handlers`，按领域拆分 `database.Repository` 和 `models` 文件，保持现有 API、DTO、schema 行为不变；`go test`、覆盖率 70.8%、`go vet`、build 和本地 smoke 通过，后端已保持运行在 `127.0.0.1:18080`。
- 2026-05-29 13:22 | GPT-5 Codex (`backend-ai`) | 在 `AI_BOARD.md` 新增 EB-019/EB-020/EB-021，追踪 WebSocket 实时与命令通道、写操作审计日志、检测超限记录与事件流水；明确 WS 写操作必须经过后端 service，不允许绕过后端直写 MQTT。
- 2026-05-29 13:32 | GPT-5 Codex (`backend-ai`) | 新增 `backend/docs/变量属性检测配置与WS前置设计.md`，对照 JGHJ 旧后端 `sys_variables` 和运行态 `Tag`，明确自动发现变量、业务变量、运行态变量、检测配置项、WS 写约束和超限记录的前置设计。
- 2026-05-29 13:28 | GPT-5 Codex (`frontend-ai`) | 完成 EB-014 检测标准前端管理：管理中心新增标准列表、新增/编辑/删除、标准项选择、上下限、`check_enabled`/`store_enabled`；同步补齐检测运行、报表模板、虚变量创建 typed API，并在 EB-016 中落地管理中心新建虚变量入口。`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test` 与 Playwright 弹窗 smoke 通过。
- 2026-05-29 13:41 | GPT-5 Codex (`backend-ai`) | 新增 `backend/docs/后端日志WS检测实施顺序.md`，整理 EB-019/020/021 的实施与测试顺序，确认先做写操作审计和运行日志，再做变量写入约束、检测配置、WS 实时读写和检测超限事件。
- 2026-05-29 13:52 | GPT-5 Codex (`backend-ai`) | 补充变量存储映射设计：变量属性承担 `storage_name/storage_target/storage_table/storage_value_column/form_field_key/query_alias`，默认历史趋势继续用 `rt_history_data` 窄表模型；检测配置只决定上下限、是否检测和本次检测是否存。
- 2026-05-29 13:43 | GPT-5 Codex (`frontend-ai`) | EB-016 继续推进：工位页新增开始检测弹窗、标准/报表模板选择、start/stop/abnormal-stop mutation、任务历史跳转；历史查询页支持 `task_id`、`device_id`、`test_no` 查询参数。`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test` 通过，Playwright UI smoke 启动并停止一条本地 `UI-SMOKE-*` 测试任务。
- 2026-05-29 13:52 | GPT-5 Codex (`frontend-ai`) | 修复虚变量入口三语显示名缺口：新建虚变量弹窗补齐中文、英文、日文显示名，提交时英文/日文空值回退到中文显示名；`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test` 通过，Playwright smoke 确认弹窗三语字段可见。
- 2026-05-29 16:16 | GPT-5 Codex (`frontend-ai`) | 完成 EB-030：变量编辑补齐存储映射、写入约束、运行保护高级分区和写入校验；检测标准项同步检测方法、目标值、回差、保持时间和质量策略；`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test` 与 Playwright smoke 通过。
- 2026-05-29 16:33 | GPT-5 Codex (`frontend-ai`) | 完成 EB-032：新增检测配置独立页面和左侧菜单入口，按旧 Vue 检测项补参考池并复用检测标准 API；`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test` 与 Playwright smoke 通过。
- 2026-05-29 17:00 | GPT-5 Codex (`frontend-ai`) | EB-032 规则修正：检测标准项来源改为所有变量按 `var_name` 去重，检测配置页和管理中心弹窗统一复用变量列表三语字段；`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test` 与 Playwright smoke 通过。
- 2026-05-29 17:46 | GPT-5 Codex (`backend-ai`) | 新增 `backend/docs/运行态清洗与实时业务视图设计.md`，并同步 `AI_BOARD.md`、`backend/docs/backend-architecture.md`、`backend/docs/后端日志WS检测实施顺序.md`：固定清洗滤网、topic 索引、`GlobalTagMap`、`DeviceRealtimeView`、`DeviceRunContext` 与后续报警/下设/规则触发器站位；仅文档变更，未运行 Go 测试。
- 2026-05-29 18:00 | GPT-5 Codex (`backend-ai`) | 完成 EB-033 实时底座首段实施：新增 topic 与 device 索引、数值清洗滤网、topic 索引热路径和 WS 设备视图；`go test ./...`、覆盖率 70.6%、`go vet ./...`、`go build ./cmd/edge-backend` 通过，`golangci-lint` 未安装；后端已重启为 PID 27756 并通过 `/health`、登录、WS ready、WS start/stop smoke。
- 2026-05-29 18:20 | GPT-5 Codex (`backend-ai`) | 补强 EB-033 运行态准入：自动发现变量默认禁用且不绑定设备，发现候选不直接进入 `TagManager`；运行态加载和内存 upsert 只接受已启用且已分配项目/设备的变量，避免未知变量被清洗、WS 推送或入库。已安装 `golangci-lint v2.12.2` 并清理历史 lint 问题，`golangci-lint run ./...` 为 0 issues；`go test ./...`、覆盖率 70.4%、`go vet ./...`、`go build ./cmd/edge-backend` 通过；后端已重启为 PID 23916 并通过 `/health`、登录、variables、runtime/channels smoke。
- 2026-05-29 20:24 | GPT-5 Codex (`frontend-ai`) | 修正设置页与检测配置页色彩：普通重点统一为蓝色/蓝青色，橙黄色保留为特别重点色；`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test` 与 Playwright 截图验证通过。
- 2026-05-29 23:05 | GPT-5 Codex (`backend-ai`) | 完成变量默认上下限与业务报警装配地基：新增设计文档，扩展 `sys_tags` 默认上下限/报警字段、检测标准项 `alarm_enabled`、运行快照 `variable_default_*`，变量 PATCH 支持 `apply_to_running` 同步 running 快照；同步 `backend/deploy/schema.sql`、后端架构文档、看板和前端共享 DTO。验证通过：`go test ./...`、覆盖率 70.2%、`go vet ./...`、`golangci-lint run ./...`、`go build ./cmd/edge-backend`、`desktop/npm run lint`、`desktop/npm run build`；后端已重启并通过 `/health`、admin 登录和 variables 字段 smoke。
- 2026-05-29 23:46 | GPT-5 Codex (`backend-ai`) | 完成检测配置判断周期和首帧语义补强：新增 `check_cycle_ms/check_on_start` 到检测标准项和运行快照，新增变量默认报警保持时间字段，检测开始首帧存储按变量 `startup_snapshot_enable`，首帧判断按业务 `check_on_start`；验证通过 `go test ./...`、覆盖率 70.0%、`go vet ./...`、`golangci-lint run ./...`、`go build ./cmd/edge-backend`、`desktop/npm run lint`、`desktop/npm run build`；后端已重启为 PID 71284 并通过 `/health`、admin 登录和变量字段 smoke。
- 2026-05-29 23:06 | GPT-5 Codex (`frontend-ai`) | 修正侧边菜单和通用 shell 色彩：移除黄色点缀，统一为蓝色/蓝青色光照与选中态；`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test` 与 Playwright 截图验证通过。
- 2026-05-29 23:38 | GPT-5 Codex (`frontend-ai`) | 修正登录页色彩：移除背景右上光球、登录浮层和面板内部玻璃高光中的黄色，统一为蓝色/蓝青色；`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test` 与 Playwright 截图验证通过。
- 2026-05-29 23:48 | GPT-5 Codex (`frontend-ai`) | 完成 EB-035：系统设置日志诊断区新增运行日志预览和刷新能力，Electron preload 只读读取 sidecar 日志尾部；`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test` 与 Playwright 截图验证通过。
- 2026-05-29 23:58 | GPT-5 Codex (`backend-ai`) | EB-020 追加后端审计日志查询：新增 `GET /api/v1/audit-logs`、repository 分页过滤、路由权限和前端共享 DTO/API；验证通过 `go test ./...`、`go vet ./...`、`golangci-lint run ./...`、`go build ./cmd/edge-backend`、`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test`。
- 2026-05-30 00:07 | GPT-5 Codex (`frontend-ai`) | 系统设置补齐后端审计日志列表，接入 `getAuditLogs({limit:80})` 并展示操作者、动作、对象、结果、请求和错误摘要；`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test` 通过，浏览器验证审计列表可加载真实 `auth.login` 记录。
- 2026-05-30 00:43 | GPT-5 Codex (`backend-ai`) | 完成检测业务上下限报警进入/恢复首段和性能验证：新增 `Alarm` 队列、批量报警入库 worker、运行态超限状态机和 `mqttpubperf/perfsetup` 压测工具；`go test ./...`、覆盖率 70.1%、`go vet ./...`、`golangci-lint run ./...`、`go build ./cmd/edge-backend` 通过。520 变量 10 分钟压测 `PERF-520-20260530-003212` 发布 312000 次更新，延迟 avg 2.8ms/p95 3.7ms/max 12.4ms，报警 31200 条全部恢复，历史 322920 行，结束后队列全 0；后端保持运行在 `127.0.0.1:18080`。
- 2026-05-30 01:20 | GPT-5 Codex (`backend-ai`) | EB-028 继续推进检测运行事件和结果摘要：新增 `detection_run_events`、`detection_run_summaries`、开始/停止事件写入、摘要刷新、`/detection-runs/:id/summary` 与 `/events` 接口，并同步前端共享 DTO/API；高频超限报警继续只保留在 `detection_limit_alarms`。验证通过：`go test ./...`、覆盖率 70.0%、`go vet ./...`、`golangci-lint run ./...`、`go build ./cmd/edge-backend`、`desktop/npm run lint`、`desktop/npm run build`；后端已重启为 PID 48260 并通过事件/摘要 smoke。
- 2026-05-30 01:40 | GPT-5 Codex (`backend-ai`) | 新增任务触发、动态存储与双报警实施计划，并在 `AI_BOARD.md` 拆出 EB-036 到 EB-040：总体设计、设备宽表动态存储、任务系统倒排索引、检测业务静音/等级变化、变量默认报警闭环；本次仅文档和看板更新，未改 Go 代码。
- 2026-05-30 09:20 | GPT-5 Codex (`backend-ai`) | EB-037 继续推进到宽表双写并完成验证：运行任务携带 storage routes，StoreTask 携带路由快照，`InsertHistoryBatch` 保留窄表写入并追加 `rt_device_{device_id}_data` upsert；验证通过 `go test ./...`、覆盖率 70.2%、`go vet ./...`、`golangci-lint run ./...`、`go build ./cmd/edge-backend`、`desktop/npm run lint`、`desktop/npm run build`；后端已重启为 PID 20768 并通过宽表检测启动 smoke。后续还需补宽表历史查询和批量合并优化。
- 2026-05-30 09:27 | GPT-5 Codex (`backend-ai`) | EB-037 补齐宽表批量合并和历史读取：宽表写入按 `table/task_id/sample_bucket_ms` 聚合多变量一次 upsert；历史查询在 `task_id/device_id/test_no` 命中 `detection_run_storage_routes` 时优先读 `rt_device_{device_id}_data` 并返回兼容旧历史 DTO。
- 2026-05-30 09:29 | GPT-5 Codex (`backend-ai`) | EB-037 宽表合并写入和历史读取完成验证：新增单测覆盖多变量合并行、数值 upsert、字符串读取和旧历史 DTO 重建；`go test ./... -coverprofile all-coverage.out` 覆盖率 70.4%、`go vet ./...`、`golangci-lint run ./...`、`go build ./cmd/edge-backend` 通过；后端重启为 PID 67644，`/health`、登录、历史查询和检测任务列表 smoke 通过。
- 2026-05-30 09:48 | GPT-5 Codex (`backend-ai`) | EB-037 存储设计审查后重构：变量级存储频率/触发/目标表列退出热路径，storage routes 成为唯一有效存储配置；默认 route 改为 disabled 建议；新增 `/api/v1/storage-routes` 后端接口和前端 typed API。
- 2026-05-30 09:52 | GPT-5 Codex (`backend-ai`) | EB-037 route 驱动存储重构完成验证：`go test ./... -coverprofile all-coverage.out` 覆盖率 70.6%、`go vet ./...`、`golangci-lint run ./...`、`go build ./cmd/edge-backend`、`desktop/npm run lint`、`desktop/npm run build` 通过；后端重启为 PID 52372 并通过 `/health`、登录和 storage-routes smoke。
- 2026-05-30 10:50 | GPT-5 Codex (`backend-ai`) | EB-041/042/043/044 首段完成：后端只注册 `/api/v1/projects`，`/api/v1/devices` 返回 404；模型/schema/API 切到 `sys_projects/project_id/project_code/rt_project_*`；变量 API 删除 `store_*`、`storage_*`、`startup_snapshot_enable`；新增 `sys_task_rules` 与 `TaskRuleIndex`；存储 worker 改为 `StorageBus` 项目/表 bucket，并避免多宽表 route 重复写 `rt_history_data`。验证通过 `gofmt`、`go test ./...`、覆盖率 70.2%、`go vet ./...`、`golangci-lint run ./...`、`go build ./cmd/edge-backend`；后端重启为 PID 45052，`/health`、登录、projects、storage-routes、history 和 variables smoke 通过。
- 2026-05-30 11:10 | GPT-5 Codex (`frontend-ai`) | 适配 EB-041/042 后端破坏性契约：前端 shared API 将旧 devices 调用转到 `/api/v1/projects` 并映射 `project_id/project_code`，变量创建/编辑/分配和历史查询停止发送旧变量级存储字段；设置中心新增“存储路由”模块管理 `sys_storage_routes`。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test`，HTTP smoke 确认 projects=8、storageRoutes=4、旧 devices=404。
- 2026-05-30 12:06 | GPT-5 Codex (`frontend-ai`) | 完成 EB-046 前端首段：新增“任务”侧边菜单和 `/#/tasks` 页面，对接 task-flows typed API，支持列表、新建/编辑、项目筛选、变量角色、脚本、手动运行和执行记录/SQL 日志占位；验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test`，HTTP smoke 确认 task-flows/projects 可访问。in-app Browser 当前不可用，未做浏览器截图验证。
- 2026-05-30 12:42 | GPT-5 Codex (`frontend-ai`) | 新增 `/#/model-cockpit` 3D 驾驶舱探索页：引入 Three.js，加载根目录暂存 GLB 模型到 `desktop/public/models/edge-air-conditioner.glb`，支持 360 度拖拽、内部流体粒子/流道、模型空间测点浮窗和射线遮挡；按用户要求模型保持不透明，内部流体仍作为 3D 对象以诊断显影/视觉错位方式叠加到模型表面，不使用整机透明或剖切。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test` 和 Playwright WebGL 截图 smoke。
- 2026-05-30 11:30 | GPT-5 Codex (`backend-ai`) | EB-045 条件事件脚本任务系统首段实施：新增任务流模型/迁移/repository/API、goja JS 执行器、执行日志/SQL 日志、数据变化倒排索引投递、priority 小批量排序、内置 storage snapshot 动作和 JS `db.query/db.exec` 能力；新增单测覆盖变量 0->1 触发内置存储任务并经 repository 入库、JS 脚本查询 SQL 并写执行日志。
- 2026-05-30 11:38 | GPT-5 Codex (`backend-ai`) | EB-045 验证留痕：`go test ./...`、覆盖率 70.2%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend` 通过；本地后端已重启为 PID 27880 并保持 `127.0.0.1:18080`，smoke 验证 `/health`、admin 登录、`GET /api/v1/task-flows`、`GET /api/v1/projects` 均通过。
- 2026-05-30 13:25 | GPT-5 Codex (`backend-ai`) | EB-045 内置检测业务模块首轮完成：新增检测启动/停止/暂停/恢复、固定时长结束、合格持续结束、特征值刷新模块；新增 `detection_run_features` 表和 `pause/resume/features` API；单测覆盖可选存储、可选上下限报警、手动结束、暂停、固定时长结束、合格持续时长结束和平均/最小/最大特征值。门禁通过：`go test ./...`、覆盖率 70.5%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`。
- 2026-05-30 13:45 | GPT-5 Codex (`backend-ai`) | 修正检测暂停累计时长：新增 `pause_started_at/paused_duration_ms`，恢复时顺延 `expected_end_at`，summary duration 扣除暂停时间；repository/service 测试覆盖暂停段累计和预计结束时间顺延。门禁通过：`go test ./...`、覆盖率 70.7%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`。
- 2026-05-30 13:50 | GPT-5 Codex (`frontend-ai`) | 按旧 Vue 驾驶舱结构复刻 `/#/model-cockpit`：复用驾驶舱背景、标题、图表、中心背景和五张卡片资产，页面改为标题栏、五卡、实时监测表 + 3D 模型、右侧温湿度趋势图；保留不透明模型、内部 3D 流体诊断显影和遮挡浮窗。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run` 和 Playwright WebGL 截图 smoke。
- 2026-05-30 13:35 | GPT-5 Codex (`frontend-ai`) | 驾驶舱背景资产改为 SVG 矢量还原：按旧 PNG 尺寸新增 `cockpit-background/title/center/chart` 和 `card-0..4` SVG，页面引用全部切到 SVG，保留 PNG 作视觉参考；验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run` 和 Playwright SVG 背景截图 smoke。
- 2026-05-30 13:58 | GPT-5 Codex (`frontend-ai`) | 驾驶舱背景改为“动态底 + SVG 纹理”：SVG 移除大面积蓝色填充，只保留线框、高光、齿轮和水印；`/#/model-cockpit` 新增类似工位页的动态漂移光效背景，卡片、中心框和图表框使用半透明玻璃底承接动态背景。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run` 和 Playwright 截图 smoke。
- 2026-05-30 15:05 | GPT-5 Codex (`backend-ai`) | KingIO 现场链路继续验证：HTTP KIO 多类型写入 `台1_40/41/42` 和 WS `command.write_variable` 多类型写入均拿到 KIO `ProcessStep=100/Result=ok`，实时快照读回 `台1_40=false`、`台1_41=41.68000030517578`、`台1_42=edge-ws-string-1452`；`query-all` 后已配置写入属性保持不被发现刷新覆盖；越界 WS 写 `台1_41=150` 被后端范围校验拦截并写入失败审计，实时值未被污染。门禁通过：`go test ./...`、覆盖率 70.0%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端 PID 36240 继续运行，`/health` ok。
- 2026-05-30 15:32 | GPT-5 Codex (`backend-ai`) | 补强实时快照粒度控制：`GET /api/v1/realtime/variables` 支持 `source_type/gateway_id/project_id/device_id/var_id` 过滤，WS `var_id` 支持逗号或重复参数，var_id 订阅走直接 tag lookup，项目订阅走 project index；前端 shared API 增加 `RealtimeVariableListParams`，工位页有选中项目时按 `project_id` 拉实时变量。验证通过：后端全门禁、前端 lint/build/test；后端重启 PID 36924，HTTP smoke 全量 528、project_id=1 为 8、单点 1、多点 2、非法 var_id 为 400，WS 多点快照为 2；复测确认 WS `Authorization: Bearer <access_token>` 可握手，前次 401 是脚本取错 token 字段。
- 2026-05-30 16:01 | GPT-5 Codex (`backend-ai`) | EB-047 后端首段完成并执行：新增 `POST /api/v1/variables/bulk-remap/kio-projects`，用于批量确保 `AC-01..AC-12` 项目，把 504 个 `台N_xx` KIO 变量归属到对应项目并重映射业务 `var_name/display_name*`，保留 `raw_name/source_path` 和写入约束；disabled 默认 storage route 会随新业务名同步列名，已启用 route 不自动改。验证通过：后端 `go test ./...`、覆盖率 70.1%、`go vet ./...`、`golangci-lint run ./...` 0 issues、build 通过；前端 lint/build/test 通过；后端重启 PID 49904，`/health` ok、tags=1028，`台1_41` 抽查保留 `write_path=台1_41` 且默认 route 列为 `kio_01_41`。
- 2026-05-30 16:28 | GPT-5 Codex (`backend-ai`) | EB-045/046 任务模板继续迭代：后端模板清单补固定时长检测、合格持续检测、暂停/恢复、运行中限值调整、特征值刷新和报表登记；参数绑定支持 `optional/default`；前端 shared API 补 `getTaskModules/getTaskFlowTemplates/getTaskFlowRuns/getTaskFlowRun/getTaskFlowSqlLogs` 和相关类型。验证通过：后端全门禁覆盖率 70.2%，前端 lint/build/test 通过；后端重启 PID 60884，smoke 确认 task modules=16、templates=12。
- 2026-05-30 15:33 | GPT-5 Codex (`frontend-ai`) | 强化驾驶舱大面板分割线和纹理一致性：`cockpit-center.svg`/`cockpit-chart.svg` 增加更白的主分割线和边框线；左下监测数据与模型两张大卡统一 `cockpit-center.svg` 纹理 opacity、tint 和光照层，避免同源纹理呈现为两套风格；修正 `getRealtimeVariables` 在 TanStack Query 中的包装调用以适配新可选过滤参数。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，Playwright 截图输出 `output/playwright/model-cockpit-white-lines-unified.png`。
- 2026-05-30 15:56 | GPT-5 Codex (`frontend-ai`) | 驾驶舱下方大卡片改为纯组件自绘框架：`CockpitGlassPanel` 增加内联矢量标题曲线和角落透光层，监测数据、模型、趋势图面板不再依赖 `cockpit-center.svg`/`cockpit-chart.svg` 整图拉伸，避免不同尺寸下纹理比例和线条位置割裂；修正装饰层脱离内容流，内容区不再被装饰元素挤压。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，Playwright 截图输出 `output/playwright/model-cockpit-code-frame-fixed.png`。
- 2026-05-30 16:22 | GPT-5 Codex (`frontend-ai`) | 驾驶舱纯组件框架视觉降噪：下方大卡标题分割线从强装饰改为低透明细分割线，削弱阴影、发光和角落光斑，保持玻璃内容为视觉主体。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，Playwright 截图输出 `output/playwright/model-cockpit-clean-divider.png`。
- 2026-05-30 16:27 | GPT-5 Codex (`frontend-ai`) | 驾驶舱分割线继续细化：主线改为更干净的青蓝色，发光线降到 0.85px、高光线降到 0.45px、阴影层降到 2px 且更透明，整体减少发白和粗重感。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，Playwright 截图输出 `output/playwright/model-cockpit-thin-blue-divider.png`。
- 2026-05-30 17:00 | GPT-5 Codex (`frontend-ai`) | 驾驶舱继续修正布局：下方大面板标题从内容区上移到标题槽，收窄标题字号上限；3D 模型相机视角从 42° 调整到 38°、相机距离拉近，模型归一化尺寸从 `3.5/maxAxis` 提升到 `4.35/maxAxis`，并同步下移地台，解决模型偏小。重启后恢复 `4173` 预览服务，验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，Playwright 截图输出 `output/playwright/model-cockpit-title-model-scale.png`。
- 2026-05-30 17:10 | GPT-5 Codex (`frontend-ai`) | 驾驶舱按截图继续微调：下方大面板分割线提升到接近顶部卡片的青蓝发光层级，避免暗淡割裂；3D 根节点整体上移并回收到 `root.position.y=0.52`，解决模型偏下且避免顶部烟囱贴边。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，Playwright 截图输出 `output/playwright/model-cockpit-line-match-model-up-final.png`。
- 2026-05-30 17:16 | GPT-5 Codex (`frontend-ai`) | 修正驾驶舱顶部小卡片齿轮比例：从 `card-0..4.svg` 纹理中移除会随背景拉伸变形的齿轮，改为 `CockpitGlassPanel` 内独立 `CockpitGearMark` SVG 装饰层，固定 `aspect-ratio: 1`；Playwright 验证 5 个齿轮均为 52x52、ratio=1。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，截图输出 `output/playwright/model-cockpit-round-gear.png`。
- 2026-05-30 17:24 | GPT-5 Codex (`frontend-ai`) | 顶部小卡片齿轮重新绘制为真实机械齿轮：`CockpitGearMark` 改为 12 个外齿、圆形本体和中心孔组合，不再使用抽象花边路径。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，Playwright 截图输出 `output/playwright/model-cockpit-real-gear.png`。
- 2026-05-30 17:36 | GPT-5 Codex (`backend-ai`) | 完成运行态通知总线首段：新增 `Channels.Notify`、`NotificationHub` 和 WS `topic=notifications`/`notification.event`，报警进入/恢复/等级变化、检测开始/停止/异常停止/暂停/恢复和检测结果 OK/NG 会即时推送；通知不落库，历史追溯仍使用 `detection_limit_alarms`、`detection_run_events` 和 `detection_run_summaries`。验证通过 `go test ./...`、覆盖率 70.3%、`go vet ./...`、`golangci-lint run ./...`、`go build ./cmd/edge-backend`、`desktop/npm run lint/build/test -- --run`；后端监听 PID 25436，WS smoke 收到 `detection.run_started` 和 `detection.run_stopped` 通知。
- 2026-05-30 17:55 | GPT-5 Codex (`backend-ai`) | 新增 `backend/docs/边缘端全链路数据流转与分发图.md`，用中文解释 Mermaid 和文字图中的英文协议/表/模块名，梳理前后端 HTTP/WS、MQTT/KIO 上行、清洗、全局变量图、项目视图、检测任务、存储总线、报警总线、任务流、通知总线和下行写入链路；同步更新 `AGENTS.md` 和 `.ai/instructions/backend-go-edge.md`，要求后端后续不只更新看板，也要维护数据流图。本次为文档/流程规则变更，未运行 Go 测试。
- 2026-05-30 18:02 | GPT-5 Codex (`backend-ai`) | 新增 `backend/docs/边缘端与主服务器协同计划.md` 并同步全链路图和 `AI_BOARD.md` EB-049：固定未来边缘端/主服务器边界，数据库同步只负责只读镜像，现场控制和检测配置修改必须回到边缘端执行，主服务器报表 worker 等待同步数据到位后生成 Excel、归档并通知用户。本次为未来计划文档变更，未运行 Go 测试。
- 2026-05-30 18:35 | GPT-5 Codex (`backend-ai`) | EB-019 通知中心持久化落地：新增 `sys_notifications`、`sys_notification_recipients`、`NotificationDispatcher` 和 HTTP 通知列表/未读数/标记已读接口；WS `notification.event` 继续在线推送，断线补历史走 HTTP。同步更新后端架构文档、全链路图、前端 shared DTO/API 和看板。验证通过：`go test ./...`、覆盖率 71.1%、`go vet ./...`、`golangci-lint run ./...`、`go build ./cmd/edge-backend`、`desktop/npm run lint/build/test -- --run`；后端已重启 PID 18164，smoke 验证通知落库、列表、未读、单条已读和全部已读。
- 2026-05-30 18:52 | GPT-5 Codex (`backend-ai`) | EB-019 通知 recipient 细化：落库时按 `target_type=all/user/role/project` 生成 `sys_notification_recipients`，`user` 精确到本地用户 ID，`role` 按本地角色，`project` 暂无项目成员表所以暂时发给全部启用本地用户；新增 repository 测试覆盖 role/user/无效目标。验证通过：`go test ./...`、覆盖率 71.1%、`go vet ./...`、`golangci-lint run ./...`、`go build ./cmd/edge-backend`；后端已重启 PID 8644，`/health` 和通知接口 smoke 通过。
- 2026-05-30 19:36 | GPT-5 Codex (`backend-ai`) | EB-045 继续按“HTTP 配置/查询，现场动作走 STRING 虚拟变量触发任务系统”迭代：`sys_detection_tasks` 新增 `limit_check_enabled/end_policy/qualified_hold_ms/custom_config_json`；`builtin.start_detection_run` 支持 `custom_items` 自定义检测项、上下限启用、固定时长和合格持续策略；`builtin.update_detection_limits` 支持 `items` 批量调整运行中快照并刷新 active task；新增检测标准收藏/最近使用表和 HTTP 接口；WS read limit 提升到 256 KiB。验证通过：`go test ./... -coverprofile all-coverage.out` 总覆盖率 70.5%、`go vet ./...`、`golangci-lint run ./...`、`go build ./cmd/edge-backend`，后端重启 PID 37496 并通过 `/health`、登录、task modules/templates、favorites/recent smoke。
- 2026-05-30 20:21 | GPT-5 Codex (`backend-ai`) | EB-045 性能复核与优化：新增 520 项 `custom_items` STRING payload 性能预算测试，实测 166453 bytes、解析约 3.25ms、转换约 0.56ms；`sys_detection_tasks.custom_config_json` 改为紧凑追踪 JSON，避免保存完整 GORM 空字段导致膨胀，运行判断继续以 `detection_run_standard_items` 为唯一快照事实。验证通过：`go test ./... -coverprofile all-coverage.out` 总覆盖率 70.6%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端重启 PID 13280，`/health` 和任务/检测配置 smoke 通过。
- 2026-05-30 20:35 | GPT-5 Codex (`backend-ai`) | EB-045 单功能迭代完成：JavaScript 任务模块补齐多变量实时读取 API，`realtime.write` 改为受审计保护且只允许写虚拟变量，默认不继续触发任务；`builtin.write_variable` 同步收紧为只写虚拟变量，避免任务执行器对物理变量形成未走 KIO 的假成功；`task-modules` 暴露 JavaScript `runtime_api` 给前端任务编辑器。验证通过：多变量 JS 条件、JS 写虚拟变量、审计落库、默认不递归、物理变量拒绝假写、`go test ./...`、覆盖率 71.0%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端重启 PID 9892，`/health`、登录、任务模块和模板 smoke 通过。
- 2026-05-30 20:52 | GPT-5 Codex (`backend-ai`) | EB-045 接入项目生命周期任务流：检测启动和结束通过任务流内置检测模块、HTTP/WS `DetectionRunsService` 都会派发 `project_start/project_end`，新增单测覆盖启动/结束生命周期任务被执行；验证通过 `go test ./... -coverprofile all-coverage.out` 总覆盖率 71.2%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端重启 PID 35820，`/health`、登录、任务模块、模板和生命周期 trigger smoke 通过。
- 2026-05-30 21:08 | GPT-5 Codex (`backend-ai`) | EB-045 接入 `schedule` 任务流扫描器，新增 `schedule_interval_ms` 字段和迁移脚本；测试覆盖定时索引间隔、cooldown 兜底、无间隔不执行和 schedule scanner 执行记录。验证通过 `go test ./... -coverprofile all-coverage.out` 总覆盖率 71.2%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端重启 PID 13528，`/health`、登录、任务模块、模板和 schedule 查询 smoke 通过。
- 2026-05-30 21:22 | GPT-5 Codex (`backend-ai`) | EB-045/046 补齐任务流管理能力：PATCH 可改项目并同步变量绑定，DELETE 可删除任务流配置且保留执行历史；验证通过 `go test ./... -coverprofile all-coverage.out` 总覆盖率 71.1%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端重启 PID 12648，`/health`、登录、创建任务流、PATCH 改项目、DELETE 删除 smoke 通过。
- 2026-05-30 23:34 | GPT-5 Codex (`backend-ai`) | EB-045/046 补齐 `GET /api/v1/task-flows/:id` 单条详情接口，测试覆盖可读取配置和变量绑定、删除后不可再读；验证通过 `go test ./... -coverprofile all-coverage.out` 总覆盖率 71.1%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端重启 PID 38180，`/health`、登录、创建任务流、GET 详情和 DELETE smoke 通过。
- 2026-05-30 17:56 | GPT-5 Codex (`frontend-ai`) | 驾驶舱继续按原型修正：顶部齿轮保留机械 12 齿结构并增加水平渐隐 mask，使其像纹理一样逐渐融入卡片；左下中心区域从两张独立玻璃卡合并为一张 `cockpit-main-panel`，内部左侧数据、右侧 3D 模型，统一外框、标题槽、边线和间距；收紧数据表列宽字号避免合并后完成率列截断。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，Playwright 截图输出 `output/playwright/model-cockpit-combined-panel-final.png`。
- 2026-05-30 18:10 | GPT-5 Codex (`frontend-ai`) | 修正驾驶舱面板标题槽：下方大面板标题从 `top:38px` 上移到 `top:24px`，避开横向框线并落入左上角预留标题区；主面板内容 padding 从 86px 收到 78px，右侧模型小标题同步贴近同一标题带。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，Playwright 截图输出 `output/playwright/model-cockpit-title-slot-fix.png`。
- 2026-05-30 19:08 | GPT-5 Codex (`frontend-ai`) | 驾驶舱大改前先清场：`/#/model-cockpit` 隐藏除动态背景外的全部页面内容，背景底色切到与工位操作一致的 `#a6c0df`，关闭旧驾驶舱 SVG 纹理层，仅保留蓝色/深蓝/橘黄动态光源。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，Playwright 确认 `visibleContentNodes=0`，截图输出 `output/playwright/model-cockpit-background-only.png`。
- 2026-05-30 19:29 | GPT-5 Codex (`frontend-ai`) | 驾驶舱按新设计图恢复布局骨架：顶部 5 张空玻璃卡、左侧两列 10 张空卡、右侧 2 张同源空卡，中间留空给后续模型；卡片改回工位同源玻璃底、强受光边缘和更通透的空卡表现，不再挂载 3D canvas 或图表内容。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，Playwright 截图输出 `output/playwright/model-cockpit-glass-layout-strong-edge.png`。
- 2026-05-30 20:21 | GPT-5 Codex (`frontend-ai`) | 继续修正驾驶舱空卡玻璃质感：卡片直接挂载 `glass-panel metric-card`，动态光源改为更新 `.glass-panel`，删除驾驶舱自有灰膜；为适配空卡无图表内容的状态，降低底色不透明度和雾化强度，扩大橘黄色受光边缘半径。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，1920×1080 截图输出 `output/playwright/model-cockpit-glass-stronger-1920x1080.png`。
- 2026-05-30 20:27 | GPT-5 Codex (`frontend-ai`) | 核对工位卡片真实边缘受光实现后回退驾驶舱过度泛光：工位使用 `StationLightBackground` 将运动光源坐标写入 `.glass-panel` 的 `--mouse-x/--mouse-y`，边缘光只由 `::after` 的 `600px radial-gradient` 配合 mask 在光源靠近时显现；驾驶舱已同步光源周期、轨迹和 `::after` 渐变参数，移除 980px 常驻泛光。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，1920×1080 截图输出 `output/playwright/model-cockpit-station-edge-1920x1080.png`。
- 2026-05-30 20:36 | GPT-5 Codex (`frontend-ai`) | 继续对齐驾驶舱与工位玻璃质感：发现构建后的驾驶舱 CSS 只保留 `-webkit-backdrop-filter`，标准 `backdrop-filter` 未进入浏览器 computed style，导致透度与工位不一致；已按工位 `StationCardGridStyles` 的方式为驾驶舱注入运行时玻璃样式，computed style 现与工位一致：`rgba(255,255,255,0.25)`、`blur(24px) saturate(1.5)`、同一 `600px` 受光边缘。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，1920×1080 截图输出 `output/playwright/model-cockpit-runtime-glass-1920x1080.png`。
- 2026-05-30 21:04 | GPT-5 Codex (`frontend-ai`) | 复查工位边缘受光完整逻辑：工位边缘亮边实际为 `.glass-panel::after` 的 `padding:1px` mask 边框，光源坐标由 `StationLightBackground` 每帧写入；驾驶舱修正为页面局部光点与卡片 viewport 坐标同源换算，并在不扩大光斑半径的前提下增强空卡 1px 边缘层的集中白光段与轻微 drop-shadow。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，1920×1080 截图输出 `output/playwright/model-cockpit-focused-edge-latest-1920x1080.png`。
- 2026-05-30 21:04 | GPT-5 Codex (`frontend-ai`) | 按用户要求临时加宽驾驶舱空卡边缘发光宽度：`::after` 外扩从 `-1px` 调到 `-3px`、mask padding 从 `1px` 调到 `3px`，drop-shadow 增强到 `8px`，用于观察更宽亮边效果；不影响工位页面。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，1920×1080 截图输出 `output/playwright/model-cockpit-wide-edge-glow-1920x1080.png`。
- 2026-05-30 21:28 | GPT-5 Codex (`frontend-ai`) | 定位驾驶舱边缘光完全不可见的根因：卡片本体 `overflow: hidden` 会裁掉外扩 `::after` mask 发光环，导致虽然 `--mouse-x/--mouse-y` 和伪元素都存在但视觉上无亮边；已回到工位同源伪元素路径，移除临时 span 实验层，改为卡片外层 `overflow: visible`、内部 body 负责内容裁剪。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，1920×1080 截图输出 `output/playwright/model-cockpit-edge-visible-fixed-1920x1080.png`。
- 2026-05-30 21:48 | GPT-5 Codex (`frontend-ai`) | 驾驶舱中间空白区恢复 3D 模型但不加卡片：复用 `desktop/public/models/edge-air-conditioner.glb` 直接挂载 WebGL canvas；动态光源计算提升到页面层，背景橘黄光点、卡片 `--mouse-x/--mouse-y` 边缘光和模型 directional/point light 共享同一运动光源；卡片边缘光粗细恢复为工位原始 `-1px/padding:1px/600px radial-gradient`。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，1920×1080 截图输出 `output/playwright/model-cockpit-model-fitted-1920x1080.png`。
- 2026-05-30 23:39 | GPT-5 Codex (`frontend-ai`) | 按“隐藏前那版模型效果”恢复驾驶舱中心模型表现：中间空白区继续不加卡片，Three.js 模型补回 OrbitControls 拖拽/轻自动旋转、内部流体管线和粒子诊断层、模型空间测点浮窗投影与 raycast 遮挡判断；模型按包围盒贴地到地台，避免悬浮。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，1920×1080 截图输出 `output/playwright/model-cockpit-restored-model-effect-1920x1080.png`。
- 2026-05-31 00:01 | GPT-5 Codex (`frontend-ai`) | 按反馈调整驾驶舱 3D 模型：模型先取消与屏幕动态光源联动，改为固定中性 Ambient/Hemisphere/Directional 三点光照；模型放大并整体下移，继续保留 OrbitControls、内部流体诊断层和测点浮窗。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，1920×1080 截图输出 `output/playwright/model-cockpit-model-normal-light-lower-larger-1920x1080.png`。
- 2026-05-31 00:20 | GPT-5 Codex (`frontend-ai`) | 驾驶舱模型继续按反馈放大并下移：模型缩放提升到 `3.82/maxAxis`，相机略拉近，地台/模型落点继续下压，同时调整 OrbitControls 目标让主体视觉重心更低。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，1920×1080 截图输出 `output/playwright/model-cockpit-model-lower-bigger-final-1920x1080.png`。
- 2026-05-31 00:32 | GPT-5 Codex (`frontend-ai`) | 驾驶舱模型资产收口为单一新 shaded 模型：从根目录实际存在的 `3d模型nolew/base_basic_shaded.glb` 复制到 `desktop/public/models/cockpit/new-shaded.glb`，页面只加载该路径，移除模型切换器和其他 cockpit 模型副本；相机、OrbitControls 目标和地台同步下移。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，1920×1080 截图输出 `output/playwright/model-cockpit-new-shaded-1920x1080.png`。
- 2026-05-31 00:59 | GPT-5 Codex (`frontend-ai`) | 驾驶舱按新参考图补内容但保留现有 UI 骨架：顶部卡改为左圆形图标、右侧标题/主值横向布局，标题区新增内联 SVG 矢量背板；左侧检测卡接入实时变量轮询和当前运行检测快照，显示实时值、上下限、OK/NG、优化率，按反馈移除小卡迷你曲线；右侧趋势图保留 SVG 曲线；模型相机、目标和地台继续下移。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，1920×1080 截图输出 `output/playwright/model-cockpit-top-card-reference-layout-1920x1080.png`。
- 2026-05-31 01:02 | GPT-5 Codex (`frontend-ai`) | 驾驶舱中心 3D 模型删除滑稽圆环地台：移除 Three.js `RingGeometry` 平台 mesh，仅保留不可见落点高度；模型落点、相机高度和 OrbitControls 目标继续整体下移，让模型更靠近底部。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，1920×1080 截图输出 `output/playwright/model-cockpit-no-platform-lower-1920x1080.png`。
- 2026-05-31 01:11 | GPT-5 Codex (`frontend-ai`) | 驾驶舱模型继续贴近底部：发现相机/目标同步下移会抵消视觉位移，改为主要下压模型落点 `modelFloorY=-4.18`，视角中心仅小幅跟随；模型底部现接近屏幕底部且无圆盘地台。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，1920×1080 截图输出 `output/playwright/model-cockpit-model-near-bottom-1920x1080.png`。
- 2026-05-31 01:20 | GPT-5 Codex (`frontend-ai`) | 驾驶舱大屏信息层级复审：顶部卡移除同义副说明，标题副文案改为“实时感知 · 数据驱动 · 智能运维”；左侧实时卡保留实时值、上下限、OK/NG、优化率并移除迷你曲线；中心模型移除重复测点浮窗和遗留 CSS，避免字体重叠；温度/风量单位显示改为 `°C`、`m³/h`。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，1920×1080 截图输出 `output/playwright/model-cockpit-clean-no-overlap-1920x1080.png`。
- 2026-05-31 01:38 | GPT-5 Codex (`frontend-ai`) | 驾驶舱视觉降噪重调：降低背景蓝色饱和度和动态光斑强度，弱化标题 SVG 背板；顶部卡、指标卡、趋势卡统一改为更轻的磨砂白蓝玻璃，减少重阴影和白字发光；指标卡改为左对齐深蓝文字、弱图标背景和浅色 OK 胶囊，整体从“赛博贴纸感”收敛到更清爽的调试大屏。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，1920×1080 截图输出 `output/playwright/model-cockpit-cleaner-visual-1920x1080.png`。
- 2026-05-31 01:46 | GPT-5 Codex (`frontend-ai`) | 驾驶舱参考 OpenAI Codex 官网产品页风格重调：撤掉 HUD 标题 SVG 背板，改为左对齐大标题、深色字重和浅色背景；页面背景改为官方产品页式低饱和浅灰蓝渐变；卡片统一为白色产品面板质感、细边框、轻阴影和低饱和受光边缘，顶部/指标/趋势卡文字改为深色层级，弱化科技屏蓝光。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，1920×1080 截图输出 `output/playwright/model-cockpit-codex-inspired-1920x1080.png`。
- 2026-05-31 02:08 | GPT-5 Codex (`frontend-ai`) | 按看板推进 EB-046/048 任务页：接入 `GET /api/v1/task-modules` 生成 action 下拉，接入 `GET /api/v1/task-flow-templates` 支持从模板创建任务；编辑弹窗新增 `steps_json` 数组编辑和 `schedule_interval_ms`；执行记录和 SQL 日志由占位改为真实表格，分别调用 `GET /api/v1/task-flow-runs` 与 `GET /api/v1/task-flow-runs/:id/sql-logs`。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`；Playwright 模拟登录、进入 `/#/tasks`、打开模板创建弹窗、查看执行记录、点击 SQL 按钮，确认相关接口均返回 200，截图输出 `output/playwright/task-flows-smoke.png`。
- 2026-05-31 02:25 | GPT-5 Codex (`frontend-ai`) | 重新收敛 `/#/model-cockpit` 视觉：参考 Codex 官网的克制产品页层级，把驾驶舱背景改回工位同源动态蓝色光场，恢复但弱化标题 SVG 矢量背板，顶部卡片改为图标/主值/说明结构，所有卡片重新对齐工位同源玻璃参数和橘黄受光边缘；3D 模型固定中性光照、放大并下移贴近底部。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，1920×1080 截图输出 `output/playwright/model-cockpit-redesign-1920-v2.png`。
- 2026-05-31 02:50 | GPT-5 Codex (`frontend-ai`) | 完成 EB-046 任务页动态参数表单：按 `task-modules.params_schema` 渲染 project/variable/select/boolean/number/object/array/text/code/any 参数，编辑时同步 `action_payload` 与第一条 `steps_json` step；验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，Playwright 模拟登录、模板创建、切换 `builtin.write_variable`，确认字段渲染、接口 200 和 payload 同步，截图 `output/playwright/task-flows-module-params-smoke.png`。
- 2026-05-31 02:55 | GPT-5 Codex (`frontend-ai`) | 驾驶舱继续按 Codex 官网产品页方向收敛：背景改为低饱和蓝白产品层次，标题 SVG 从重 HUD 蓝板改成轻量半透明矢量框，卡片保留工位同源 600px 暖光边缘但降低厚重阴影和杂色，顶部/指标卡文字层级改为深色产品面板风格。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，Playwright 1920×1080 登录后截图 `output/playwright/model-cockpit-codex-clean-1920x1080.png`，相关接口 200。
- 2026-05-31 03:06 | GPT-5 Codex (`frontend-ai`) | 推进 EB-037：工位页新增当前运行任务“存储快照”入口，接入 `GET /api/v1/detection-runs/{id}/storage-routes` typed API，弹窗展示当次冻结 route 的目标、表/列、触发、周期、死区和首帧存储。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，Playwright 模拟登录 `/#/?device_id=1`、开始检测、打开存储快照、确认 storage-routes 接口 200 且渲染 route 行，再停止检测，截图 `output/playwright/station-storage-routes-snapshot-smoke.png`。
- 2026-05-31 03:14 | GPT-5 Codex (`frontend-ai`) | 推进 EB-048：顶栏新增全局通知中心，显示未读角标，弹层接入 `GET /api/v1/notifications/unread-count`、`GET /api/v1/notifications?limit=20`、单条已读和全部已读；验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，Playwright 模拟登录、打开通知弹层确认接口 200 和 20 条通知渲染，截图 `output/playwright/global-notifications-popover-smoke.png`，并验证点击首条通知调用 `POST /api/v1/notifications/22/read` 返回 200。
- 2026-05-31 03:23 | GPT-5 Codex (`frontend-ai`) | 继续推进 EB-048：新增 `subscribeRealtimeWebSocket`，Shell 订阅 WS `topic=notifications`，收到 `notification.event` 后刷新通知列表和未读数，并带 3 秒重连。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，Playwright 确认页面打开 `ws://127.0.0.1:18080/api/v1/ws?...&topic=notifications`，Node WS smoke 确认后端返回 `connection.ready` 且订阅 topics 为 `notifications`。
- 2026-05-31 03:30 | GPT-5 Codex (`frontend-ai`) | 继续推进 EB-048：通知中心支持目标跳转，带 `task_id` 的通知进入历史页并携带 `task_id/device_id/test_no`，仅带 `project_id` 的通知回到对应工位。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，Playwright 点击真实通知 `RUN-20260530190426` 后进入历史页，确认 `GET /api/v1/history/data?task_id=19&project_id=1&limit=5000` 返回 200，截图 `output/playwright/notification-target-navigation-smoke.png`。
- 2026-05-31 04:31 | GPT-5 Codex (`backend-ai`) | EB-045/046/048 精确变量 ID 补强：变量/实时快照/任务流响应新增 `var_id_text/trigger_var_id_text`，WS 写变量、任务流变量绑定和内置模块 var_id 支持字符串 64 位 ID；前端任务请求弹窗改用精确字符串 ID，配置参数继续按检测配置 + `process_params` 工艺/报表参数 + `plc_writes` PLC 下设计划分层。验证通过后端全量测试、覆盖率 72.3%、go vet/build、前端 lint/build/test；后端重启 PID 36880，现场 smoke 用自动大 ID `9212397624135540849` 的 STRING 虚拟变量通过 WS 触发 task-flow success。
- 2026-05-31 04:43 | GPT-5 Codex (`backend-ai/frontend-ai/test-ai`) | 精确变量 ID 补强扩大到检测配置和 storage routes：后端检测标准项、检测运行标准项、存储路由和运行存储路由响应新增 `var_id_text`，对应 `var_id` 请求支持字符串形式 64 位 ID；前端设置页、检测配置页和 shared DTO 优先使用字符串 ID。同步再次明确“配置参数”是检测配置 + `process_params` 工艺/报表参数 + `plc_writes` PLC 下设计划，进风口面积只在 `process_params` 填一次、下设通过 `value_from` 引用。验证通过后端全量测试、覆盖率 72.3%、go vet、golangci-lint、go build、前端 lint/build/test；后端已重启为 PID 11184，`/health` ok。
- 2026-05-31 04:53 | GPT-5 Codex (`backend-ai/frontend-ai/test-ai`) | 精确变量 ID 补强继续扩大到通知、报警、历史和特征值：后端 `RuntimeNotification`、HTTP 通知列表、统一超限报警、历史数据和检测特征值响应新增 `var_id_text`；前端 shared DTO、工位页存储快照和历史页存储快照优先显示/使用字符串 ID。验证通过后端全量测试、覆盖率 72.2%、go vet、golangci-lint、go build、前端 lint/build/test；后端已重启为 PID 3484，`/health` ok。
- 2026-05-31 05:05 | GPT-5 Codex (`backend-ai/frontend-ai/test-ai`) | WS 精确变量 ID 契约补齐：运行期 `subscribe.var_ids[]` 支持数字或字符串形式 64 位 ID，订阅回执新增 `var_id_texts`，WS 写变量回执新增 `result.var_id_text` 并统一 `project_confirmed` 小写字段；任务流内置写变量结果也返回 `var_id_text`。验证通过后端定向和全量测试、覆盖率 72.3%、go vet、golangci-lint、go build、前端 lint/build/test；后端已重启为 PID 8020，`/health` ok。
- 2026-05-31 05:12 | GPT-5 Codex (`backend-ai/frontend-ai/test-ai`) | 精确变量 ID 残留风险继续收敛：任务流 JavaScript `realtime.get/getMany/write` 支持字符串形式 64 位变量 ID，脚本 trigger/context、实时快照和写变量结果均返回精确字符串；bulk remap 响应项新增 `var_id_text`；前端 shared DTO 把变量、实时快照和 bulk remap item 的 `var_id` 改为 `VarIdentifier`。验证通过后端全量测试、覆盖率 72.3%、go vet、golangci-lint、go build、前端 lint/build/test；后端已重启为 PID 40428，`/health` ok。
- 2026-05-31 05:22 | GPT-5 Codex (`backend-ai/test-ai`) | EB-045 配置参数和任务流 event 精确 ID 校验补齐：确认配置参数包含检测配置、`process_params` 和 `plc_writes`，进风口面积只放 `process_params` 并通过 `value_from` 下设 PLC；`steps_json` 保存校验允许 `source=event,key=trigger_var_id_text`，`task-modules` JavaScript runtime note 同步说明 string var_id 与精确 ID 字段。验证通过后端全量测试、覆盖率 72.3%、go vet、golangci-lint、go build；后端已重启为 PID 41496，`/health` ok。
- 2026-05-31 05:31 | GPT-5 Codex (`backend-ai/test-ai`) | EB-045 特征值刷新通知闭环：`builtin.refresh_features` 复用统一特征值刷新路径，刷新后写 `features_updated` 检测事件并发布 `detection.features_updated` 通知；新增 pipeline 回归覆盖事件和通知。验证通过后端全量测试、覆盖率 72.3%、go vet、golangci-lint、go build；后端已重启为 PID 8500，`/health` ok。
- 2026-05-31 05:37 | GPT-5 Codex (`backend-ai/test-ai`) | EB-045/048 正式 STRING 任务请求 smoke 工具落地：新增 `backend/tools/smokeeb045`，通过真实 HTTP/WS 创建临时项目、STRING 请求变量、虚拟控制变量和 data_change flows，验证 WS 写请求触发启动检测、冻结 `process_params/plc_writes`、暂停、恢复和 `task_flow_stop` 结束。现场 smoke 通过，项目 `21`、请求变量 `724990601`、控制变量 `732388972`、任务 `21`；验证通过 `go test ./...`、覆盖率 72.3%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`，后端 PID 8500 `/health` ok。
- 2026-05-31 05:50 | GPT-5 Codex (`backend-ai/test-ai`) | EB-045/048 正式 STRING 任务请求 smoke 扩展：`backend/tools/smokeeb045` 增加测量变量、enabled storage route、`custom_items` 绑定、`builtin.storage_prepare` 和 `builtin.update_detection_limits` 验证；同一个 watched STRING 请求链路已证明配置参数包可同时承载检测配置、`process_params` 工艺/报表参数和 `plc_writes` PLC 下设计划。现场 smoke 通过，项目 `23`、请求变量 `742576737`、控制变量 `739076088`、测量变量 `842238391`、任务 `23`；验证通过 smoke 工具 test/vet/run、`go test ./...`、覆盖率 72.3%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`，后端 PID 8500 `/health` ok。
- 2026-05-31 05:56 | GPT-5 Codex (`backend-ai/test-ai`) | EB-045/048 正式 STRING 任务请求 smoke 继续扩展：`backend/tools/smokeeb045` 增加 `builtin.register_report` 和 `builtin.http_request` 链路验证，前者登记 `reports/smoke-eb045-*.xlsx` 并从检测任务详情读回，后者受控调用本机 `/health` 并确认 200。现场 smoke 通过，项目 `24`、请求变量 `752661683`、控制变量 `824993196`、测量变量 `798429688`、任务 `24`；验证通过 smoke 工具 test/vet/run、`go test ./...`、覆盖率 72.3%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`，后端 PID 8500 `/health` ok。
- 2026-05-31 06:03 | GPT-5 Codex (`backend-ai/test-ai`) | EB-045/048 正式 STRING 任务请求 smoke 继续覆盖组合业务：`backend/tools/smokeeb045` 增加 `start_fixed_duration_detection`、`start_qualified_hold_detection` 和 `refresh_features` 链路验证；同一 watched STRING 任务入口已证明特征值刷新事件、固定时长自动结束和合格持续自动结束可跑通。现场 smoke 通过，项目 `25`、请求变量 `817149116`、控制变量 `848021045`、测量变量 `866666943`、主任务 `25`、固定时长任务 `26`、合格持续任务 `27`；验证通过 smoke 工具 test/vet/run、`go test ./...`、覆盖率 72.3%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`，后端 PID 8500 `/health` ok。
- 2026-05-31 06:11 | GPT-5 Codex (`backend-ai/test-ai`) | EB-019/045/048 通知联动 smoke 补强：`backend/tools/smokeeb045` 在触发 `builtin.refresh_features` 前打开 `topic=notifications` WS，验证收到 `detection.features_updated` 的 `notification.event`，再通过 HTTP 通知列表按同一 `event_uid` 读回未读通知并标记单条已读。现场 smoke 通过，项目 `26`、请求变量 `800423698`、控制变量 `838618853`、测量变量 `831066790`、主任务 `28`、固定时长任务 `29`、合格持续任务 `30`、通知 HTTP ID `66`；验证通过 smoke 工具 test/vet/run、`go test ./...`、覆盖率 72.3%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`，后端 PID 8500 `/health` ok。
- 2026-05-31 06:14 | GPT-5 Codex (`backend-ai/test-ai`) | EB-019/045/048 检测结果通知 smoke 补强：`backend/tools/smokeeb045` 在固定时长和合格持续检测自动结束后验证 `detection.result_ok` 同样能通过 WS `notification.event` 到达，并可通过 HTTP 通知列表按同一 `event_uid` 读回未读通知和标记单条已读。现场 smoke 通过，项目 `27`、请求变量 `806090321`、控制变量 `838500467`、测量变量 `858345221`、主任务 `31`、固定时长任务 `32`、合格持续任务 `33`；验证通过 smoke 工具 test/vet/run、`go test ./...`、覆盖率 72.3%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`，后端 PID 8500 `/health` ok。
- 2026-05-31 06:20 | GPT-5 Codex (`backend-ai/test-ai`) | EB-019/045/048 报警和 NG 结果通知 smoke 补强：`backend/tools/smokeeb045` 新增首帧超上限 NG 场景，先把测量变量写到超上限，再通过正式 STRING 任务请求启动检测，验证 `alarm.limit.enter` 经 WS 即时推送并可通过 HTTP 通知列表读回和标记已读；随后用任务流停止该检测，验证 summary 产出 `detection.result_ng`，同样走 WS/HTTP 未读已读闭环。同步修复 smoke 等待同一 flow 重复执行时可能命中旧 run 的问题，按 `min_run_id` 等待新执行记录。现场 smoke 通过，项目 `29`、请求变量 `833432810`、控制变量 `870345172`、测量变量 `755951840`、主任务 `38`、固定时长任务 `39`、合格持续任务 `40`、NG 任务 `41`；验证通过 smoke 工具 test/vet/run、`go test ./...`、覆盖率 72.3%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`，后端 PID 8500 `/health` ok。
- 2026-05-31 05:35 | GPT-5 Codex (`frontend-ai/test-ai`) | 驾驶舱按 Codex 官网产品页气质再收敛：背景改成低饱和浅灰蓝产品层次，卡片统一为更轻的白蓝玻璃面板、细边框、轻阴影和工位同源暖光边缘；弱化标题 SVG 背板和装饰线，指标卡保留实时值、上下限、OK/NG、优化率；3D 模型缩放和相机重新平衡，完整落在中间区域且不再顶到顶部卡片或底部裁切。同步修复工位页项目参数迁移残留引用。验证通过 `desktop/npm run lint`、`desktop/npm run build`，Playwright 1920×1080 截图 `output/playwright/model-cockpit-codex-pass-3.png`。
- 2026-05-31 05:57 | GPT-5 Codex (`frontend-ai/test-ai`) | EB-047 前端补齐 KIO 项目初始化入口：设置中心变量模块新增“KIO 项目初始化”弹窗，支持 dry-run 预览、真实执行确认、结果摘要和明细表；执行后会刷新项目、变量和 storage routes。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`；Playwright 登录 `/#/settings` 后点击预览，确认 `POST /api/v1/variables/bulk-remap/kio-projects` 请求体含 `dry_run=true`、返回 200，摘要显示匹配 504，截图 `output/playwright/settings-kio-remap-dryrun-smoke.png`。
- 2026-05-31 06:16 | GPT-5 Codex (`frontend-ai/test-ai`) | 驾驶舱按 Codex 官网产品页方向继续重做视觉层级：背景去掉大块科幻光团，改为低饱和蓝白产品页层次和细网格；标题 SVG 降噪，卡片改为更透明的白蓝玻璃、轻阴影、细边框并保留工位同源暖光边缘；3D 模型下移放大但避免继续裁切。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`、`git diff --check`，1920×1080 截图 `output/playwright/model-cockpit-codex-style-v4-1920x1080.png`。
- 2026-05-31 06:27 | GPT-5 Codex (`frontend-ai/test-ai`) | 推进 EB-047 前端“设备”到“项目”迁移闭合：三语资源中清理“项目/设备”“设备/项目”“Project/device”“プロジェクト/設備”等混合文案；工位页开始检测表单页面层改用 `project_id`，`DetectionRunStartPayload` 改为项目主字段，旧 `device_id` 只保留为兼容 alias。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`、`git diff --check`；Playwright 登录后点击左侧项目二级菜单，确认 URL 为 `/#/?project_id=1` 且不含 `device_id`，设置页可见文本无旧混合文案，截图 `output/playwright/project-copy-cleanup-smoke.png`。
- 2026-05-31 06:49 | GPT-5 Codex (`frontend-ai/test-ai`) | 驾驶舱按用户反馈继续参考 Codex 官网产品页风格收敛：`model-cockpit.css` 改为更克制的白底、低饱和软蓝渐变、细网格、轻玻璃卡和干净文字层级，保留动态光源驱动的暖色卡片边缘；同时把设置页变量分配、批量分配、虚变量创建和 storage route 变量联动的表单字段继续收口为 `project_id/project_code`，旧 `device_id/device_code` 仅作为 DTO 兼容字段。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`、`git diff --check`；Playwright 1920×1080 截图 `output/playwright/model-cockpit-codex-style-1920x1080.png`。
- 2026-05-31 07:08 | GPT-5 Codex (`frontend-ai/test-ai`) | 继续推进 EB-047 前端项目语义闭合：检测配置页和设置页检测标准弹窗保存字段从 `device_id/device_code` 改为 `project_id/project_code`；任务页内部项目列表、变量筛选、项目参数编辑和运行日志展示改为项目语义，旧变量 `device_id/device_code` 只通过 helper 兼容读取；三语资源继续清理可见“设备/项目”混合文案。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`、`git diff --check`；Playwright 登录后验证 `/api/v1/projects=200`、`/api/v1/devices=404`，并点击 `/#/detection-config` 新增标准、`/#/tasks` 新增任务和发送任务请求弹窗，截图 `output/playwright/detection-config-project-field-smoke.png`、`output/playwright/task-flows-project-request-smoke.png`。
- 2026-05-31 07:36 | GPT-5 Codex (`frontend-ai/test-ai`) | 驾驶舱再次按 Codex 官网产品页气质重做视觉层级：`/#/model-cockpit` 去掉赛博蓝大屏模板感，改为低饱和浅灰蓝背景、细网格、白色产品面板式玻璃卡、深色高对比文字和克制标题背板；继续保留工位同源动态暖光边缘。同步修复进入 3D 驾驶舱时继承页面滚动位置导致首屏整体上移的问题，并重新平衡 Three.js 相机、缩放和模型贴底位置。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`；Playwright 1920×1080 登录点击截图 `output/playwright/model-cockpit-codex-style-pass5.png`。
- 2026-05-31 07:38 | GPT-5 Codex (`backend-ai/test-ai`) | EB-019 项目通知成员收敛首段落地：新增 `sys_project_members`、`GET/PUT /api/v1/projects/{id}/members`，通知 `target_type=project` 在配置成员后只给 `notify_enabled=true` 的启用成员生成 recipient，未配置成员的项目仍全员兜底。`smokehealth` 追加项目成员接口检查。验证通过定向 repository/handler/model/smokehealth 测试、`go test ./...`、覆盖率 72.4%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端重启为 PID 15684，`/health` ok，真实 smokehealth 通过。
- 2026-05-31 08:05 | GPT-5 Codex (`frontend-ai/test-ai`) | EB-048 项目成员配置 UI 接入：设置页用户管理模块新增项目成员配置区，可按项目读取 `GET /api/v1/projects/{id}/members`，添加/移除本地用户、编辑成员角色和通知接收开关，并通过 `PUT /api/v1/projects/{id}/members` 保存。验证通过 `desktop/npm run lint`、`desktop/npm run test -- --run`、`desktop/npm run build`，后端 `/health` ok；Playwright 登录设置页后切到用户管理，确认“项目成员/通知接收人/接收通知”渲染。
- 2026-05-31 08:08 | GPT-5 Codex (`frontend-ai/test-ai`) | 修复桌面端整体页面滚动：全局 `body/#root/workbench-shell` 固定为视口高度并隐藏外层滚动，`workbench-main` 锁定高度，左侧菜单改为 `.workbench-nav` 内部滚动，业务内容继续只在 `.workbench-content` 内滚动。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`；Playwright 1920×1080 登录后依次检查工位、3D 驾驶舱、设置、历史页，强制 `window.scrollTo(0,800)` 后 `window.scrollY/documentElement.scrollTop/body.scrollTop` 均为 0，`documentElement.scrollHeight=viewportHeight=1080`。
- 2026-05-31 08:08 | GPT-5 Codex (`backend-ai/frontend-ai/test-ai`) | 设置页 smoke 和项目成员空列表契约补齐：新增 `desktop/scripts/smoke-settings.mjs` 与 `desktop/npm run smoke:settings`，覆盖桌面登录、`/projects`、旧 `/devices=404`、项目成员接口、项目成员 UI 和日志预览居中弹窗/no Drawer；后端项目成员空列表规范为 `items:[]`，前端 settings 端防御空数组；kernel MQTT publish/subscribe 测试允许本机 broker 可达时返回 200。验证通过后端定向测试、覆盖率 72.4%、`go vet`、`golangci-lint`、`go build`、桌面 lint/test/build 和 `smoke:settings`；后端已重启 PID 35628，`/health` ok，tags=1100。
- 2026-05-31 08:14 | GPT-5 Codex (`review-ai`) | 清理 `AI_BOARD.md` 最近 8 小时后端 Activity 状态：确认 EB-017/019/045/048 相关 API hardening、任务流参数校验、PLC 参数失败保护、多项目 smoke 和配置参数边界记录已合并到对应 EB 行、`MEMORY.md` 或 `backend/docs/*`，将 `2026-05-31 00:26` 到 `03:25` 期间 32 条已验证完成的后端 Activity Log 从 `open` 转为 `closed`；Active Board 的真实 open 工作项未改。
- 2026-05-31 08:17 | GPT-5 Codex (`review-ai`) | 二次清理 `AI_BOARD.md` Activity Log 状态：将剩余 102 条历史 Activity Log 从 `open` 转为 `closed`，理由是这些旧阶段决策、测试证据和实现记录已由 Active Board EB 行、`MEMORY.md` 或稳定设计文档承载，不应继续误导后续 AI 作为待施工项。复核 Activity Log open=0，Active Board 真实 open 行未改。
- 2026-05-31 08:20 | GPT-5 Codex (`review-ai`) | 完成看板归档闭环：将 `AI_BOARD.md` 中 2 条 closed Active Board 行和 193 条 closed Activity Log 迁入 `.ai/docs/archive/2026-05-31-ai-board-closed-records.md`，根看板只保留真实 open/blocked Active Board 项和“当前无未闭合 Activity Log”说明。复核 Active Board closed=0、Activity Log open/closed=0、Active Board open=15。
- 2026-05-31 08:25 | GPT-5 Codex (`frontend-ai`) | 推进 EB-047 前端项目语义收口：shared API 新增正式 `Project/ProjectPayload/getProjects/createProject/updateProject`，旧 `Device/getDevices/createDevice/updateDevice` 保留为兼容 wrapper；工位页、检测配置页、任务页和设置页创建项目入口改优先使用项目 API 命名，`docs-desktop-packaging.md` 的 Device/Variable Boundary 改为 Project/Variable Boundary。验证通过 `desktop/npm run lint`、`desktop/npm run test -- --run`、`desktop/npm run build`，build 仅保留既有大 chunk 提醒。
- 2026-05-31 08:46 | GPT-5 Codex (`frontend-ai/test-ai`) | 驾驶舱按新布局重组：背景改为复用工位 `StationLightBackground` 动态光源，卡片边缘光照恢复工位同源 `600px` 径向边缘；顶部五张状态卡保留，左侧监测表与中间 3D 模型合并为同一个大玻璃卡，右侧两张趋势卡独立展示，去掉整页最外层卡片壳。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`；Playwright 1920×1080 截图 `output/playwright/model-cockpit-merged-main-card.png`，确认 `page::after=none`、主卡覆盖左表+模型区域且外层滚动为 0。
- 2026-05-31 08:59 | GPT-5 Codex (`frontend-ai/test-ai`) | 驾驶舱细节继续按反馈收敛：去除模型区右侧统计小卡和底部模型工具条，模型区域不再叠加独立 UI 卡片；左侧监测表分区加宽，并新增每个变量的上下限列。同步修复 `ShellLayout` 通知未读数 query/mutation 的 TanStack Query 类型错误。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`；Playwright 1920×1080 截图 `output/playwright/model-cockpit-table-limits-no-model-cards.png`，确认 `stageStats=0`、`modelToolbars=0`、上下限单元格 10 个、外层滚动为 0。
- 2026-05-31 09:32 | GPT-5 Codex (`frontend-ai/test-ai`) | 推进 EB-050/EB-051：新增独立通知中心 `/#/notifications` 和报警中心 `/#/alarms`，侧边菜单新增入口；通知中心支持未读、类型、级别、项目、关键字筛选，筛选条件用于未读数和分类一键已读，并在页面内订阅 WS `topic=notifications` 刷新；报警中心使用 `GET /api/v1/limit-alarms` 支持类型、项目、变量 ID、等级、状态筛选，报警通知从顶栏弹层/通知中心统一跳转报警中心并带查询参数。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`；Playwright 登录后验证 `/notifications` 与 `/alarms?scope=default&status=active` 页面渲染，通知/报警接口返回 200，截图 `output/playwright/notification-alarm-centers-smoke.png`。
- 2026-05-31 09:51 | GPT-5 Codex (`frontend-ai/test-ai`) | 驾驶舱按最新反馈继续收口：删除右侧趋势卡区域、旧 Recharts 依赖和曲线样例数据，主区域只保留一张同源玻璃大卡，内部为左侧加宽监测表和右侧 3D 模型；去除模型区独立伪底座/小卡，左表保留实时值、上下限和 OK/NG。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`；Playwright 1920×1080 验证 `rightStacks=0/chartCards=0/trendPanels=0/stageStats=0/modelToolbars=0/limitCells=10`、页面无整体滚动，截图 `output/playwright/model-cockpit-table-model-only.png`。
- 2026-05-31 10:10 | GPT-5 Codex (`frontend-ai/test-ai`) | EB-051 独立报警中心完成并归档：报警中心新增时间范围筛选，`from/to` 透传到 `GET /api/v1/limit-alarms`；检测报警记录新增“任务详情”弹窗，调用 `GET /api/v1/detection-runs/{id}` 展示检测编号、项目、状态、标准、模板、起止时间和备注，同时保留工位/历史曲线跳转。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`；Playwright 1920×1080 验证带时间范围的报警查询和任务详情弹窗，截图 `output/playwright/alarm-center-time-task-detail-smoke.png`。
- 2026-05-31 10:55 | GPT-5 Codex (`frontend-ai/test-ai`) | 继续推进前端看板项：设置页系统模块新增后端后台 worker 诊断表，接入 `GET /api/v1/runtime/workers` 并展示 health、active、panics、impact、next_action 和最近 panic；EB-052 报警通知 90 天过期规则完成前端/API/UI smoke 并归档。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`；API smoke 返回报警通知 `returned_alarm_notifications=27`、`expired_returned=0`、报警记录 `alarm_records=31249`；Playwright 1920×1080 截图 `output/playwright/settings-runtime-workers-smoke.png`、`output/playwright/notification-effective-smoke.png`、`output/playwright/alarm-record-trace-smoke.png`。
- 2026-05-31 11:19 | GPT-5 Codex (`frontend-ai/test-ai`) | EB-048 设置页系统模块补齐运行队列诊断：shared API 新增 runtime channel、notification hub、task-flow runtime DTO 和接口；设置页展示核心队列、通知扇出、任务流队列的占用、丢弃、过压、影响和下一步处置，并保留 worker 诊断。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`；Playwright 登录设置页系统模块确认 `/runtime/workers`、`/runtime/channels/detail`、`/runtime/notifications`、`/task-flows/runtime` 均 200，运行队列诊断 7 行，截图 `output/playwright/settings-runtime-diagnostics-smoke.png`。
- 2026-05-31 11:38 | GPT-5 Codex (`frontend-ai/test-ai`) | EB-047 设置页项目语义继续收口：`SettingsPage.tsx` 页面层清理旧 `device` 命名残留，变量分配、批量分配、虚变量创建表单类型不再引用 `device_id/device_code`，页面 helper 不再 fallback 旧字段，并补 `project/selectProject/selectVariableProject` 三语 key。验证通过 `desktop/npm run lint`、`desktop/npm run test -- --run`、`desktop/npm run build`；Playwright 登录设置页确认只请求 `/api/v1/projects`、无 `/api/v1/devices` 且无“项目/设备”混合文案，截图 `output/playwright/settings-project-naming-smoke.png`。
- 2026-05-31 12:05 | GPT-5 Codex (`frontend-ai/test-ai`) | EB-047 前端项目语义关闭 smoke 补强：`desktop/scripts/smoke-settings.mjs` 现在自动选择可用 renderer，除原有设置页登录、项目成员和日志弹窗验证外，还创建独立 smoke 项目、未分配虚变量和未分配池变量，验证变量分配到项目、storage routes `project_id/var_id` 筛选、检测任务 start/current/stop/项目列表筛选，以及设置页变量、创建虚变量、未分配池批量分配和存储路由搜索入口。验证通过 `desktop/npm run smoke:settings`、`desktop/npm run lint`、`desktop/npm run test -- --run`、`desktop/npm run build`；本次 smoke 项目 `FE-SMOKE-0404019012`、routes=1、run=159。
