# 多设备并行检测任务设计计划

身份：`review-ai`

日期：2026-05-29

## 结论

当前项目已经具备“按设备并行检测”的基础骨架，且后端已补入一部分闭环能力。后端已有 `sys_detection_tasks`、检测标准、运行标准快照、按设备活动任务缓存、带 `device_id/task_id/test_no` 的历史数据、虚变量创建、任务列表/详情、任务备注、异常停止和报表模板元数据。

下一步不应该推倒重来，而是把现有 `detection-runs` 扩成真正的检测会话闭环：启动、停止、异常停止、恢复、状态展示、历史查询、报告生成都围绕 `task_id` 和 `device_id` 走。

## 需求边界

1. 当前至少支持 6 台设备同时在线、显示和检测，后续扩展到 12 台。
2. 每台设备可以独立创建检测任务，选择模式、模板/标准、绑定变量、设置时长、启动、显示、判断、报告和归档。
3. 允许多台设备同时进行不同检测任务，不同设备可以使用不同检测模板，互不影响。
4. 单台设备同一时间只能有一个活动检测任务，避免数据归属混乱。
5. 检测任务启动时必须冻结实际使用的标准/模板，后续修改标准不能污染历史检测报告。
6. 实时显示和历史存储是两条链路：实时刷新不依赖是否存储，存储受检测任务和标准快照控制。
7. 设备离线、单变量异常、主站不可用不能影响其他设备本地检测。
8. 重启后要能恢复 running 任务，或者明确标记为 interrupted，并留下原因。
9. 中央 Web 的控制要经过权限、二次确认、审计、幂等语义，不能破坏边缘端本地控制。

## 现状审阅

### 后端现状

后端方向基本正确，代码结构符合 Go 项目的常见分层：

- `runtime` 负责 Gin 路由与模块装配。
- `database.Repository` 封装 GORM 操作。
- `pipeline` 负责 MQTT 消息进入后的实时状态和入库门控。
- `models` 放 GORM 模型和运行态 DTO。
- `auth` 独立封装用户 JWT、service token、SSO ticket、角色权限。

当前并行检测相关实现：

- `TaskManager` 使用 `map[uint]ActiveTask`，按设备隔离活动任务，具备并行基础。
- `StartDetectionTaskWithOptions` 会在事务内锁定设备行，检查同设备是否已有 running 任务，防止同设备重复启动。
- `LoadActiveDetectionTasks` 在启动时加载 running 任务，支持基础恢复。
- 历史入库只在变量已绑定设备且设备存在活动任务时写入，带 `device_id/task_id/test_no`。
- 如果带 `standard_id` 启动，会复制标准项快照，入库按快照 `store_enabled` 过滤变量。
- 变量表 `sys_tags` 已有 `discovered`、`placeholder` 和 `source_type` 字段，`POST /api/v1/variables` 可创建虚变量和手工映射变量。
- 后端已拆出 `internal/services` 与 `internal/runtime/handlers` 的部分模块，检测运行、变量创建和报表模板不再全部堆在 `kernel.go`。

主要风险：

1. `kernel.go`、`repository.go`、`models.go` 仍然过大。当前方向应继续沿用 `runtime/handlers + services + database.Repository`，不要另起一套 `internal/detection`。
2. `sys_devices.current_task_id` 已参与启动并发保护，但主站控制通道接入时必须复用同一 service，不能绕过事务和权限。
3. `StopDetectionTaskWithEndType` 已能区分 `manual_stop` 和 `abnormal_stop`，但重启后的 `interrupted` 判定、正常到期 `normal` 结束和恢复策略仍要补。
4. 后端已有 `duration_sec`、`expected_end_at`、`operator_note`、报表模板关联和任务备注，但判定结果、报警摘要、报告文件生成、归档索引仍未形成完整闭环。
5. 标准和报表模板已有引用删除保护，但缺少检测操作业务事件表，包括启动、停止、异常停止、参数修改、标准快照确认。
6. 后端已有 `POST /api/v1/variables`，但前端 typed API、DTO 和“新建虚变量”页面入口还没接上。
7. 检测标准、检测运行、报表模板是三个相关但独立的业务概念。后续不要把报表模板塞进检测标准，也不要让 Repository 承担判定和报告业务编排。

### 前端现状

前端已经按项目规范通过 shared API 接后端，没有直接绕过 HTTP client：

- 工位页会读取设备、实时变量、活动检测任务。
- 左侧设备菜单可以按 `device_id` 切换。
- 工位页会按选中设备过滤实时变量。
- 历史页已接 `GET /api/v1/history/data`，但筛选仍偏原型，未形成按任务查询闭环。
- 检测标准 API 已有 typed client，但没有实际页面使用。

主要缺口：

1. 工位页按钮仍是展示型，没有调用 `POST /api/v1/detection-runs` 和 stop 接口。
2. 没有检测开始弹窗：无法选择标准、模式、检测编号、时长、参数。
3. 没有检测标准管理页面或配置入口。
4. 没有多设备活动任务总览，用户难以确认 6 台设备同时检测状态。
5. 历史页没有按 `task_id`、检测编号、标准快照、报告文件组织查询。
6. 前端没有对 `409 同设备已有任务`、`403 权限不足`、`设备 blocked/offline` 等工业操作错误做清晰提示。
7. 前端当前能分配发现变量、编辑已分配变量，但还没有接入后端新建虚变量接口；检测开始依赖的专用变量需要补入口。

## 设计原则

1. 检测任务是第一业务主线，所有启动、停止、历史、报告、判定都以 `detection_run/task_id` 为核心。
2. 设备是并行边界。不同 `device_id` 可以并行；同一 `device_id` 不允许并行 running。
3. 标准/模板是配置，运行时快照才是证据。报告和历史判定只读运行快照。
4. 实时链路不被检测任务阻塞，检测任务只控制历史入库和判定。
5. 前后端都不直接操作对方内部状态。前端只走 shared API；后端保持 API/DTO 稳定并更新看板。
6. 主站控制是第二入口，不是绕过权限的后门。它必须走 service scope、幂等键、审计和一致的错误码。

## 推荐数据模型增量

保留现有表，增量补字段和少量新表。

### `sys_tags` 虚变量规则

检测开始/停止、模式选择、持续达标状态、判定结果等业务信号，不一定都来自 PLC 原始 JSON。对这类变量，应支持“虚变量/占位变量”，但要明确它们不是随便造的普通实时采集点。

推荐规则：

- `discovered=false`：表示不是 MQTT 自动发现的变量。
- `placeholder=true`：表示系统或用户预建的虚变量。
- `data_type` 必须明确为 `INT`、`BOOL`、`FLOAT` 或 `STRING`。
- `source_path` / `json_path` 对纯虚变量使用稳定命名，例如 `virtual.detection.start`、`virtual.detection.mode`、`virtual.detection.result`。
- `gateway_id` 对纯虚变量建议使用 `0` 或专门的虚拟网关 ID，不能伪装成真实 PLC 网关。
- 纯虚变量如果没有外部实时值来源，不参与普通 MQTT 解包；它的值应由检测服务、控制服务或后续业务状态机写入。
- 可映射 PLC/KIO 的 int 变量仍应优先走发现变量或手工映射真实 `gateway_id + source_path`，不要误标为纯虚变量。

这意味着当前“很多 int 形变量”的处理应分两类：

1. PLC/KIO 已经存在的 int 变量：作为真实变量，发现后分配到设备，必要时编辑显示名、单位、分组和存储策略。
2. 检测业务需要但 PLC 没有直接上报的 int 变量：作为虚变量创建，由检测服务写入状态值，例如 `0=未开始`、`1=运行中`、`2=正常结束`、`3=异常停止`。

### `sys_detection_tasks`

建议补充字段：

- `duration_sec`：计划检测时长，可为空。
- `expected_end_at`：预计结束时间。
- `ended_by`：`user` / `service` / `system`。
- `end_type`：`normal` / `manual_stop` / `abnormal_stop` / `interrupted`。
- `result_status`：`pending` / `pass` / `fail` / `invalid`。
- `alarm_count`：任务内报警数。
- `report_path`：报告文件位置或对象引用。
- `archive_ref`：归档索引。
- `operator_id` / `operator_name`：启动操作者快照。

### 新增 `detection_run_events`

用于审计检测生命周期，不替代现有 `sys_audit_logs`，而是保存业务事件：

- `task_id`
- `device_id`
- `event_type`
- `actor_type`
- `actor_id`
- `detail_json`
- `created_at`

事件类型至少包括：

- `run_started`
- `run_stopped`
- `run_abnormal_stopped`
- `run_interrupted`
- `standard_snapshotted`
- `parameter_changed`
- `judgement_updated`
- `report_generated`

### 标准删除规则

如果标准已被 `detection_run_standard_items` 引用，不允许物理删除。只能：

- `enabled=false` 禁用；
- 或软删除字段 `deleted_at`。

## API 计划

### 后端已落地与仍需补齐

- `POST /api/v1/variables`
  - 后端已落地，用于创建虚变量或手工映射变量。
  - 权限使用 `manage_variables`。
  - 入参至少包括：`source_type`、`var_id`、`var_name`、`display_name`、`device_id`、`var_group`、`data_type`、`unit`、`store_trigger`、`store_mode`、`placeholder`、`discovered`。
  - 纯虚变量要求 `discovered=false`、`placeholder=true`。
  - 真实手工映射变量要求提供 `gateway_id`、`source_path`、`json_path`。
  - 前端仍需补 typed API、DTO 和页面入口。
- `POST /api/v1/detection-runs`
  - 后端已支持 `duration_sec`、`operator_note`、`report_template_id`，同设备已有 running 返回 `409 Conflict`。
  - 仍需为主站控制入口补 `idempotency_key`。
  - 标准不属于该设备/模式时返回 `400`。
- `GET /api/v1/detection-runs`
  - 后端已支持列表查询，后续按前端需要继续收敛筛选字段。
- `GET /api/v1/detection-runs/:id`
  - 后端已支持详情查询，后续补充判定结果和报告文件引用。
- `POST /api/v1/detection-runs/:id/stop`
  - 后端已支持手动停止。
- `POST /api/v1/detection-runs/:id/abnormal-stop`
  - 后端已支持异常停止，并要求 `reason`。
- `GET /api/v1/detection-runs/:id/standard-items`
  - 任务详情应读取运行快照，不读最新标准；是否单独拆该接口按前端页面需要决定。
- `GET /api/v1/history/data`
  - 后端已支持 `task_id` 查询。

### 前端必须使用

- 所有检测启动、停止、标准查询、历史查询都放在 feature API 或 shared API，不允许页面直接拼 `fetch/axios`。
- `tokenStore` 继续只在 auth 层读写，工位页和历史页只关心权限状态。
- 检测启动、停止、异常停止按钮都必须依据 permissions 显隐或禁用。
- 变量管理需要补“新建虚变量”入口，并把虚变量和发现变量用不同标签展示，避免用户把 PLC 真实点和业务占位点混在一起。

## 并发控制设计

必须保证以下不变量：

1. 同一 `device_id` 同一时刻最多一个 `running` 任务。
2. 不同 `device_id` 的任务互不影响。
3. 任务启动成功后，标准快照、设备 current_task_id、任务 status 必须在一个事务中完成。
4. 任务停止成功后，只清理对应设备的 `current_task_id`，不能影响其他设备。
5. 历史样本入库时必须使用当前变量所属设备的活动任务，不允许跨设备串写。

推荐实现：

- 在数据库层给 running 约束增加强保护。MySQL 可用 `sys_devices.current_task_id` 作为设备级锁，启动事务内 `SELECT ... FOR UPDATE` 锁定设备行，再检查并写入任务。
- `TaskManager` 只作为运行态缓存，不作为唯一真相。
- 前端按钮防抖只能改善体验，不能当并发保护。
- 主站控制和边缘端操作都必须进入同一个后端 start/stop service。

## 前端页面设计

### 工位操作页

每个设备页必须显示：

- 设备状态：在线/离线/屏蔽/占位。
- 当前任务：检测编号、模式、标准、开始时间、预计结束、已运行、剩余时间、结果状态。
- 操作按钮：开始检测、正常停止、异常停止、查看标准快照、查看历史。
- 实时变量：只显示该设备变量；标准上下限来自运行快照或选中标准。

### 检测启动弹窗

字段：

- 检测编号 `test_no`
- 检测模式 `mode`
- 检测标准 `standard_id`
- 检测时长 `duration_sec`
- 操作备注 `operator_note`

提交前校验：

- 设备未 blocked。
- 设备当前无 running 任务。
- 标准 enabled。
- 标准适用于当前设备或为通用标准。
- 管理员有 `start_detection` 权限。

### 多设备运行总览

建议在工位页或调试看板增加一个轻量总览：

- 6/12 个设备卡片。
- 每卡片显示 running/stopped/offline/blocked。
- running 卡片展示 `test_no`、已运行时间、标准版本。
- 点击切换到对应设备。

### 历史查询页

从“按时间查样本”升级为“先选检测任务，再看曲线/表格/报告”：

- 查询条件：设备、检测编号、任务状态、时间范围。
- 结果列表：任务维度，不是散点样本维度。
- 进入任务详情后再查 `history/data?task_id=...`。

## 实现复杂度评估

| 模块 | 复杂度 | 原因 |
| --- | --- | --- |
| 虚变量创建与管理 | 中 | 后端 POST API 已落地；前端入口、虚拟值写入边界和端到端测试仍需补。 |
| 后端并发启动/停止硬化 | 中 | 事务锁、409、异常停止已落地；主站幂等、恢复语义和事件表仍需补。 |
| 检测任务状态模型扩展 | 中 | 字段增量可控，但要同步 schema、GORM、DTO、前端类型。 |
| 检测标准删除保护 | 低 | 后端已有引用保护；后续如需要再补软删除和禁用态 UX。 |
| 前端检测启动/停止闭环 | 中 | 需要弹窗、权限、API mutation、错误提示和状态刷新。 |
| 多设备运行总览 | 中 | UI 不复杂，但要处理 6/12 台设备、离线、blocked、running 等状态。 |
| 历史页按任务重构 | 中高 | 后端任务列表/详情和 `task_id` 查询已具备，前端任务维度交互仍需补。 |
| 判定算法/报告生成 | 高 | 公式、持续达标、3 个月长周期、报告模板都涉及业务规则和验收。 |
| 主站控制通道 | 高 | 需要 HTTP/RabbitMQ 选择、幂等、权限、审计、状态一致性和离线策略。 |

建议先做“并行检测启动/停止 + 标准快照 + 历史按任务查询”的最小闭环，再推进复杂判定和报告。

## 后端 Go 规范审阅

总体评价：当前后端结构可继续演进，未发现需要推倒重来的问题。

符合规范的点：

- 包职责清晰，`auth`、`database`、`pipeline`、`runtime` 分层明确。
- 新增的 `internal/services` 与 `internal/runtime/handlers` 方向正确，适合作为后续业务入口。
- 模型字段、JSON 字段、数据库字段以英文命名，符合项目约束。
- 并发运行态使用 `sync.RWMutex` 保护 map，方向正确。
- GORM repository 集中封装数据库操作，前端 API 没有直接穿透数据库。
- 关键路径已有单元测试覆盖：模型、pipeline、runtime/auth。

需要收敛的点：

- `kernel.go` 仍保留大量旧路由，后续应逐步迁移到 `runtime/handlers/*_handler.go`，不要继续膨胀。
- Repository 中部分检测逻辑偏业务，例如启动检测、标准快照、设备 current_task_id 更新，后续由 service 编排，Repository 只保留事务型持久化原语。
- 错误类型已开始结构化，例如 `ErrDeviceAlreadyRunning` 映射到 HTTP 409；后续新增业务错误要继续保持该模式。
- 不要新建 `internal/detection` 包承接检测运行，否则会和现有 `internal/services/detection_runs_service.go` 重叠。
- `models.go` 与 `repository.go` 后续优先按文件拆分，不急于拆 package，避免过度形式化。

## 分阶段计划

### P0：冻结设计和契约

产出：

- 本文档作为实现边界。
- `AI_BOARD.md` 建立后端、前端、测试任务。
- 明确保留 `detection-runs` 命名，不新增另一套 task/run API。

验收：

- 前后端都按本文 API 和不变量实现，不另起模型。

### P1：后端并行检测最小闭环（EB-015 已完成主要后端项）

工作：

- 已增加 `POST /api/v1/variables`，支持创建虚变量和手工映射变量。
- 已增强检测启动为设备行事务锁。
- 已实现同设备 running 返回 `409 Conflict`。
- 已增加 `task_id` 历史查询。
- 已增加 `GET /api/v1/detection-runs` 和 `GET /api/v1/detection-runs/:id`。
- 已增加异常停止接口和 end_type。
- 已增加标准和报表模板删除引用保护。
- 仍需补主站幂等键、恢复 interrupted 语义和业务事件审计。

验收：

- 可以创建一个 `INT` 虚变量并绑定设备，变量返回中 `discovered=false`、`placeholder=true`。
- 设备 1 启动任务 A，设备 2 启动任务 B，可以同时 running。
- 设备 1 第二次启动返回 409。
- 停止设备 1 不影响设备 2。
- 历史数据分别写入正确 `task_id/device_id/test_no`。

### P2：前端检测操作闭环

工作：

- 管理中心变量模块增加“新建虚变量”入口，支持 int/bool/float/string 类型。
- 工位页增加检测启动弹窗。
- 接入检测标准列表，按设备/模式过滤。
- 接入 start/stop/abnormal-stop mutation。
- 活动任务状态按设备展示。
- 错误提示区分 401、403、409、设备禁用/屏蔽。

验收：

- 管理员可新建检测开始/状态类 int 虚变量，并能把它纳入检测标准或工位显示。
- 管理员能在两个设备页面分别启动不同标准的检测任务。
- 游客只能看实时和历史，不能启动/停止。
- 开发者不能启动/停止工业检测，只能管理变量/网关和查看。

### P3：历史按任务组织

工作：

- 历史页先查询检测任务，再进入任务曲线。
- 曲线查询使用 `task_id`。
- 展示标准版本、标准快照、开始/结束标记。

验收：

- 同一设备不同任务历史不混在一起。
- 标准修改后，历史任务仍展示启动时快照。

### P4：恢复、审计和主站控制

工作：

- 重启恢复 running 任务，无法确认连续性时标记 interrupted。
- 增加 `detection_run_events`。
- 主站控制通道进入同一 start/stop service，增加 idempotency key。
- 关键操作写 `sys_audit_logs` 和业务事件。

验收：

- 边缘端离线可继续本地检测。
- 主站不可用不影响本地任务。
- 主站重复发送同一启动命令不会重复创建任务。

### P5：判定与报告

工作：

- 模式 1：公式 + 标准时间。
- 模式 2：上下限 + 持续达标时间。
- 模式 3：3 个月长周期连续性。
- 模式 4：自定义混合模板。
- 报告生成读取运行快照和任务历史。

验收：

- 每个模式至少一条自动化或可重复 smoke 用例。
- 报告不受后续标准修改影响。

## 防止瞎搞的硬规则

1. 不新增第二套“检测任务”表或 API，统一使用 `detection-runs` 语义。
2. 不允许前端页面直接访问后端 endpoint，必须走 feature/shared API。
3. 不允许用前端禁用按钮代替后端并发保护。
4. 不允许报告读取最新标准，必须读取运行快照。
5. 不允许主站 service token 直接获得用户权限，service scope 只能走明确的服务接口。
6. 不允许同一设备并行 running；不同设备并行是必须支持的验收项。
7. 不允许历史数据只按时间查而没有任务归属，检测数据必须有 `task_id`。
8. 任何 API/DTO/错误码变化必须同步 `AI_BOARD.md` 和前端类型。
