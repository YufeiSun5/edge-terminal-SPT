# AI Board Closed Records - 2026-05-31

本文件由 `review-ai` 按 `AGENTS.md` 的看板归档规则生成。

- 当前实时交互看板：根目录 `AI_BOARD.md`。
- 本文件只保存已闭合记录，不能承载 open/blocked 工作项。
- 迁移规则：保留所有未闭合项；只迁移 `closed` 状态的 Active Board 和 Activity Log。

## Closed Active Board Items

| ID | Owner | Type | Status | Item | Next Action |
| --- | --- | --- | --- | --- | --- |
| EB-001 | `frontend-ai` | feature | closed | 创建 Electron + React 边缘端桌面壳，启动 Go sidecar 并展示健康状态。 | 已迁入原型的工位操作与历史查询页面，并完成登录、权限守卫、用户管理、托盘/开机自启动/守护策略、真实历史查询 API 适配和桌面包 smoke；业务页面不依赖 Electron-only API，便于后续迁移到主服务器复用。 |
| EB-002 | `backend-ai` | architecture | closed | 为主服务器提供实时数据出口，协议候选为 SSE 或 WebSocket。 | 已确认实时出口主方向使用 WebSocket；只读通道归 EB-026，写命令通道归 EB-027/EB-019 后续扩展。 |
| EB-004 | `frontend-ai` | security | closed | 边缘端登录与访问主服务器 Web 的单点登录能力总项。 | 已完成 EB-008、EB-009、EB-010：本地用户 JWT、主站 service token 校验、一次性 SSO ticket、三角色 capability、桌面登录/权限守卫/SSO handoff 和用户管理。 |
| EB-005 | `frontend-ai` | platform | closed | Windows 开机自启动、托盘、异常重拉起、日志入口等外围能力。 | 已实现托盘菜单、关闭主窗最小化到托盘、Windows 登录自启动开关、sidecar 异常退出有限重拉起、日志入口和系统设置页控制面板；打包后 smoke 验证内置 backend 启动且 `/health` ok。 |
| EB-007 | `frontend-ai` | feature | closed | 新增设置页面，覆盖实时数据源服务配置、历史数据源服务预留、MQTT 站点、项目/设备分组、发现变量与已入库变量列表。 | 已完成管理中心：变量列表为主工作区，实时数据源接 MQTT 站点与发现接口，项目/设备筛选联动变量，历史数据源保持预留边界说明。 |
| EB-008 | `backend-ai` | security | closed | 实现 Edge Auth Lite 后端认证、鉴权、服务身份和 SSO ticket。 | 已实现 `sys_users`、`sys_service_clients`、`sys_sso_tickets`、`sys_audit_logs`，JWT/service token middleware，`/api/v1/auth/login|me|logout|sso-ticket|sso-ticket/verify`，并为现有 `/api/v1` 路由绑定 capability/scope；`go test ./...`、`go build ./cmd/edge-backend`、HTTP smoke 通过，全包覆盖率 74.3%。 |
| EB-009 | `frontend-ai` | security | closed | 实现桌面端登录、权限守卫、SSO handoff 和 401/403 体验。 | 已完成：接入 `/api/v1/auth/login|me|logout|sso-ticket`，使用内存 tokenStore、Zustand 会话、路由守卫、capability 菜单显隐、401 自动清会话、403 空状态、主站 SSO ticket 打开入口和三语文案；renderer 不拼接主站 token。 |
| EB-010 | `frontend-ai` | feature | closed | 新增用户管理页面。 | 已补齐后端 `/api/v1/users` 列表、新增、编辑、启用/禁用、重置密码、删除接口；管理中心新增用户管理模块，支持 capability 展示、角色编辑、密码重置和当前用户防误禁用/删除。 |
| EB-011 | `frontend-ai` | review | closed | UI 风格复审不通过后重新统一页面视觉。 | 已按工位操作和历史查询为基准重审并修复：壳层由深色/白色割裂改为浅色玻璃系统，登录、设置、调试看板补齐同源背景光、面板边缘高光、胶囊按钮、表格与滚动条规则；业务页面仍保持 shared API/auth store/desktop adapter 边界。 |
| EB-012 | `backend-ai` | feature | closed | 新增检测标准配置、变量上下限项和单次检测标准快照。 | 已新增 `sys_detection_standards`、`sys_detection_standard_items`、`detection_run_standard_items`，检测任务记录 `standard_id/code/version`；`GET/POST/PATCH/PUT/DELETE /api/v1/detection-standards` 可维护标准和标准项，`POST /api/v1/detection-runs` 支持 `standard_id` 并冻结标准项快照，检测期间历史存储按快照 `store_enabled` 过滤变量。 |
| EB-013 | `frontend-ai` | feature | closed | 修正变量管理编辑模型。 | 未分配变量已降级为只读基础属性小卡片池与分配入口，不再允许直接启用/编辑配置；未知变量视图支持现有搜索/筛选、当前结果全选、清空选择和批量分配到项目设备；已分配变量提供完整配置编辑，包括三语显示名、变量名、类型、单位、小数位、缩放、偏移、分组、存储触发、存储模式、周期、死区和启用状态；设备展示已按当前语言读取 display 字段并 fallback。 |
| EB-014 | `frontend-ai` | feature | closed | 接入检测标准前端管理页面。 | 已在管理中心增加检测标准模块，支持标准列表、新增/编辑/删除、设备筛选、标准项选择、上下限、`check_enabled`/`store_enabled`；启动检测时选择 `standard_id` 归 EB-016 工位闭环继续接入。 |
| EB-015 | `backend-ai` | feature | closed | 多设备并行检测任务后端硬化：虚变量创建、同设备单 running、不同设备并行、任务详情、异常停止、任务历史查询和恢复语义。 | 已完成后端模块化增量：`sys_tags.source_type`、`POST /api/v1/variables`、报表模板元数据、检测任务备注/报表关联、设备行事务锁、同设备 running 返回 409、任务列表/详情/notes/异常停止、`history/data?task_id` 和标准/报表模板删除引用保护；恢复 interrupted 语义和检测事件审计留给后续 EB-017/P4 验证与扩展。 |
| EB-018 | `backend-ai` | refactor | closed | 后端模块化边界收敛，沿用 `runtime/handlers + services + database.Repository`，避免过细拆包和继续扩大上帝文件。 | 已完成行为保持型重构：`kernel.go` 收敛为启动装配、middleware、健康检查和模块路由注册；用户、设备、变量、历史、检测标准/任务、网关/KIO、报表模板路由迁入 `runtime/handlers`；`repository.go` 和 `models.go` 已按同 package 多文件拆分。 |
| EB-021 | `backend-ai` | feature | closed | 检测超限记录、检测事件流水和结果摘要。 | 检测业务超限 enter/recover、`Alarm` 队列批量入库、`detection_run_events`、`detection_run_summaries`、summary/events API 已完成；剩余静音、level_change、运行中标准调整、默认报警统一事件语义分别归 EB-039/EB-040/EB-045 后续模块。 |
| EB-022 | `backend-ai`/`frontend-ai` | feature | closed | 历史数据源 MySQL 配置实装：系统设置可读取、测试并保存后端配置文件里的数据库连接。 | 已完成：后端新增受 `system_settings` 权限保护的 `/api/v1/system/database-config` 读取/保存接口和 `/api/v1/system/database-config/test` 测试连接接口；GET 不返回明文密码，PATCH 保存到 `backend/configs/config.json`，运行中连接不热切换并返回 `restart_required=true`；前端历史数据源设置页从占位改为 MySQL 配置表单。 |
| EB-023 | `frontend-ai` | feature | closed | 历史数据源 MySQL 配置前端适配验收。 | 已完成：历史数据源页通过 shared API 调用 `/api/v1/system/database-config`，密码不回显并用 `password_set` 呈现状态；测试连接会识别 `ok=false` 并展示后端错误；保存后显示 `restart_required` 重启提示；三语文案与 Playwright smoke 已补齐。 |
| EB-024 | `backend-ai` | feature | closed | 后端变量属性和写入约束补齐。 | 已完成 P1 基础并追加变量默认检测属性：`sys_tags`/`TagConfig`/变量 API 保留采集、清洗、读写约束、默认报警字段；变量级存储映射已在 EB-042 删除，存储目标/周期/列统一归 `storage-routes`；服务层默认自动发现/虚变量不可写，`writable=true` 必须有 `rw_mode=W|RW`、`write_path`、`write_data_type`；默认上下限校验 `LL <= L <= H <= HH`。 |
| EB-025 | `backend-ai` | feature | closed | 检测配置语义补齐和运行快照收敛。 | 已完成并追加 `alarm_enabled/check_cycle_ms/check_on_start`：检测标准项补齐 `check_method/target_value/limit_deadband/violation_hold_ms/recover_hold_ms/quality_policy/alarm_enabled/check_cycle_ms/check_on_start`，启动检测时冻结检测语义和变量默认属性快照 `variable_default_*` 到 `detection_run_standard_items`；检测开始首帧存储由 enabled storage route 的 `store_on_start` 控制，检测项 `check_on_start` 控制检测开始首帧判断。 |
| EB-026 | `backend-ai` | feature | closed | WebSocket 实时读通道。 | 已完成 `/api/v1/ws` 只读通道：连接需 Edge 用户 JWT 和 `view_realtime` 权限，浏览器可用 `access_token` query 鉴权；支持 `topic/source_type/gateway_id/project_id/var_id` 订阅过滤，推送 `connection.ready`、`realtime.variables.snapshot`、`detection.runs.snapshot`、`heartbeat`；`command.*` 或带 `command_id` 的消息返回 `read_only`，写命令留给 EB-027。 |
| EB-027 | `backend-ai` | feature | closed | WebSocket 写命令通道。 | 已完成：`command.detection.start`、`command.detection.stop`、`command.detection.abnormal_stop` 复用 `DetectionRunsService`；`command.write_variable` 复用 `VariableWriteService` 和 `KIOWriteService`；所有 WS 命令要求 `request_id/command_id/payload`，返回 `command.ack` 或结构化 error，并写 `sys_audit_logs`。 |
| EB-028 | `backend-ai` | feature | closed | 检测超限、事件流水和结果摘要落地。 | 已完成超限记录、任务生命周期事件和摘要首段：`GET /api/v1/detection-runs/:id/summary`、`GET /api/v1/detection-runs/:id/events` 已可用；剩余项已拆到 EB-039、EB-040 和前端摘要展示任务，不再作为后端独立待办。 |
| EB-030 | `frontend-ai` | feature | closed | 变量属性和写入约束前端适配。 | 已完成旧版变量编辑适配；但 EB-041/042 已改变契约，前端需重新迁移：变量编辑不再展示或提交变量级存储字段，存储目标/周期/列改由 storage routes UI 管理，设备字段改为项目字段。写入变量前端校验仍应保持 `writable=true` 必须有 `rw_mode=W|RW`、`write_path`、`write_data_type`。 |
| EB-031 | `backend-ai` | hardening | closed | WebSocket 断线重连和连接保护补强。 | 已完成：服务端设置 32 KiB read limit、read/write deadline、15 秒 ping/heartbeat、pong 延长读 deadline；断开后读 goroutine 和连接循环退出，重连会重新鉴权、订阅并收到 ready/snapshot；测试覆盖未鉴权失败、正常连接、写命令只读保护、主动断开和重连。 |
| EB-032 | `frontend-ai` | feature | closed | 检测配置独立页面与左侧菜单入口。 | 已完成：左侧新增“检测配置”一级菜单，新增 `/#/detection-config` 页面；页面复用检测标准 API，按标准展示上下限、检测方法、目标值、超限/恢复保持时间、质量策略和存储策略，并保留旧 Vue 前端 19 个检测项作为参考池；标准项选择改为从所有变量按 `var_name` 去重，变量显示复用变量列表三语字段；三语文案、lint/build/test 和 Playwright smoke 已通过。 |
| EB-033 | `backend-ai` | feature | closed | 运行态清洗、topic 索引、项目实时视图和变量下设上下行链路设计与实时底座首段实施。 | 已完成首段：新增 `TagManager.ForMessage(gateway_id, topic)` 与 `gateway/topic -> tags` 索引、`project_id -> tags` 项目实时视图、数值清洗滤网的系数偏移/可疑值/防抖/运行态死区/首帧策略；自动发现变量默认只作为 `enabled=false/project_id=null` 候选入库，运行态只加载 `enabled=true AND project_id IS NOT NULL` 的已知业务变量；`logic_worker` 改为按 topic 索引取变量，WS 项目过滤改用项目实时视图；报警状态机、运行中配置即时生效、KIO/WS 下设完整闭环、多变量组合触发任务保留站位，后续拆独立项。 |
| EB-035 | `frontend-ai` | feature | closed | 系统设置日志诊断区增强。 | 已完成系统设置日志诊断区：Electron preload 新增只读 `readLogs`，设置页系统模块支持刷新并预览 Go sidecar 日志文件尾部，同时保留打开日志文件位置；后端审计日志已接入 `GET /api/v1/audit-logs`，展示最近 80 条登录、HTTP 写操作和 WS 命令审计记录。浏览器预览态会明确显示本机日志不可读取，但后端审计列表可正常通过 API 加载。 |
| EB-036 | `backend-ai` | architecture | closed | 任务触发、动态存储与双报警总体计划。 | 总体设计已沉淀到 `backend/docs/任务触发动态存储与双报警实施计划.md`，后续实施已拆到 EB-037/039/040/045；其中任务触发主线已从旧 `sys_task_rules` 转为 EB-045 `sys_task_flows + STRING 虚拟变量`。 |
| EB-038 | `backend-ai` | feature | closed | 任务系统倒排索引和变量触发检测任务。 | 旧 `sys_task_rules` 路线已被 EB-045 `sys_task_flows + var_id -> flow_ids + STRING 虚拟变量 task_params` 取代；本项关闭，后续任务编排统一在 EB-045 推进。 |
| EB-039 | `backend-ai` | feature | closed | 检测业务报警增强：静音、level_change、运行中标准调整事件。 | 已完成后端首段：TaskManager 支持当前检测报警静音，静音只作用于已处于报警的业务项；H/HH/L/LL 更严重方向变化写 `level_change` 并继续使用同一张 `detection_limit_alarms`；任务模块 `builtin.update_detection_limits` 可调整 running 快照并刷新运行态。 |
| EB-040 | `backend-ai` | feature | closed | 变量默认报警完整闭环。 | 已完成：`detection_limit_alarms` 使用 `scope=default|detection` 统一记录默认报警和检测业务报警；变量默认报警使用 `scope=default/task_id=0`，复用 `Channels.Alarm`、`alarm_worker` 和通知总线；`TaskManager.EvaluateDefaultLimitAlarm` 按 `sys_tags.default_*` 维护 enter/recover/level_change，不依赖检测任务、不带静音、不参与检测摘要。`GET /api/v1/limit-alarms` 支持两类报警统一查询，`status=closed` 已兼容内部 `recovered` 状态；工位页“报警日志”已改为居中弹窗。现场端到端 smoke 已验证虚拟变量写 25 触发默认 HH，写 5 恢复并可由 `status=closed` 查回。 |
| EB-043 | `backend-ai` | feature | closed | 任务系统倒排索引首段：变量触发规则驱动检测和存储业务。 | 旧 `sys_task_rules` 首段已被 EB-045 任务流系统替代；后续只维护 `sys_task_flows/sys_task_flow_vars` 的 `var_id -> flow_ids` 倒排索引。 |
| EB-044 | `backend-ai` | feature | closed | 存储总线首段：统一入口、项目/表 bucket、批量合并 flush。 | StorageBus 首段已完成：按 `project_id + table_name` 分 bucket、支持多项目/多表并发、宽表 route 避免重复窄历史写入；后续存储扩展归 EB-037。 |

## Closed Activity Log

- 2026-05-31 00:49 | backend-ai/test-ai | test | EB-040 默认报警端到端 smoke 闭合并修复查询兼容：创建启用默认上下限的 FLOAT 虚拟变量 `var_id=769664751`，WS `command.write_variable` 写入 25 后 `GET /api/v1/limit-alarms?scope=default&status=active` 查到 HH 报警 `id=44203`，再写入 5 后 `status=closed` 能查回同一条内部 `status=recovered` 记录；同时修复 `limit-alarms` 查询把 `status=closed` 归一到 `recovered`。验证通过 `go test ./internal/runtime/handlers -run TestLimitAlarmsHandlerListFilters`、`go test ./...`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 71.3%、`go vet ./...`、`golangci-lint run ./...`、`go build ./cmd/edge-backend`；后端已重启 PID 3224，`/health` ok | closed
- 2026-05-31 00:26 | frontend-ai | feature | EB-040 工位页报警日志交互调整：按现场反馈把右侧抽屉改为居中弹窗，保留 `GET /api/v1/limit-alarms` 查询、scope 切换、当前项目筛选和 5 秒打开态轮询；验证通过 `desktop/npm run lint`、`desktop/npm run test -- --run`、`desktop/npm run build`，Playwright 验证弹窗可见且不再生成 drawer，1440x900 下弹窗约 1120x685；Vite 构建仅保留既有大 chunk 提醒 | closed
- 2026-05-30 23:52 | backend-ai/test-ai | test | EB-040 默认报警首段门禁通过：测试覆盖默认报警无 running 检测时 enter、level_change、recover，repository 覆盖 `scope` 自动迁移、检测报警默认 `scope=detection`、默认报警 `scope=default/task_id=0` 恢复且不污染检测 summary；`go test ./...`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 71.2%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend` 通过；后端已重启 PID 34060，smoke 验证 `/health`、admin 登录、通知列表和存储快照接口 | closed
- 2026-05-30 23:59 | backend-ai/test-ai | test | EB-040 查询接口门禁通过：新增 repository 和 handler 测试覆盖默认/检测报警筛选、非法 `scope` 400；`go test ./... -coverprofile all-coverage.out` 总覆盖率 71.1%、`go test ./...`、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend` 通过；后续收紧 `var_id` 正数校验后补跑 `go test ./internal/runtime/handlers`、`golangci-lint run ./internal/runtime/handlers`、`go build ./cmd/edge-backend` 通过；后端已重启 PID 10592，smoke 验证 `/health`、admin 登录和 `GET /api/v1/limit-alarms?scope=default&limit=5` 200 | closed
- 2026-05-31 00:04 | backend-ai/frontend-ai | api | EB-040/048 追平桌面 shared API：新增 `LimitAlarm`、`LimitAlarmListParams`、`LimitAlarmListResponse` 类型和 `getLimitAlarms(params)` client，支持前端后续报警列表按默认/检测 scope 查询；验证通过 `desktop/npm run lint`、`desktop/npm run test -- --run`、`desktop/npm run build` | closed
- 2026-05-31 00:12 | frontend-ai/test-ai | feature | EB-040 报警日志前端入口落地：工位页“报警日志”按钮打开右侧抽屉，调用 `getLimitAlarms`，按当前项目筛选最近 100 条，支持全部/默认报警/检测报警切换，展示变量、等级、状态、开始/阈值/恢复值和时间；新增中英日文案与抽屉样式。验证通过 `desktop/npm run lint`、`desktop/npm run test -- --run`、`desktop/npm run build`；纯 renderer 浏览器 smoke 登录后打开报警日志抽屉成功。完整 `npm run dev` 因本地 `node_modules/electron/path.txt` 缺失导致 Electron 子进程失败，已记录为环境问题 | closed
- 2026-05-30 23:41 | backend-ai/test-ai | test | EB-037 存储快照接口门禁通过：新增 handler 测试覆盖自定义 enabled storage route 在检测启动时被冻结并可由 `/detection-runs/:id/storage-routes` 读回；`go test ./...`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 71.1%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend` 通过；后端已重启 PID 10012，smoke 验证 `/health`、admin 登录和 `GET /api/v1/detection-runs/14/storage-routes` 返回 `route_count=2` | closed
- 2026-05-30 23:34 | backend-ai/test-ai | test | EB-045/046 任务流详情接口门禁通过：测试覆盖创建后按 ID 读取配置和变量绑定、DELETE 后详情不可读且执行历史仍保留；`go test ./... -coverprofile all-coverage.out` 总覆盖率 71.1%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend` 通过；后端已重启 PID 38180，smoke 验证 `/health`、admin 登录、创建任务流、`GET /task-flows/:id` 和 DELETE 均通过 | closed
- 2026-05-30 21:22 | backend-ai/test-ai | test | EB-045/046 任务流管理门禁通过：新增测试覆盖 PATCH 修改 `project_id` 并同步 `sys_task_flow_vars.project_id`、DELETE 删除任务流配置和变量绑定、执行历史保留；`go test ./... -coverprofile all-coverage.out` 总覆盖率 71.1%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend` 通过；后端已重启 PID 12648，smoke 验证 `/health`、admin 登录、创建任务流、PATCH 改项目、DELETE 删除均通过 | closed
- 2026-05-30 21:08 | backend-ai/test-ai | test | EB-045 定时任务门禁通过：新增测试覆盖 `schedule_interval_ms` 到期触发、`cooldown_ms` 兼容兜底、无间隔 schedule 不执行，以及 schedule scanner 产生 `task_flow_runs` 成功记录；`go test ./... -coverprofile all-coverage.out` 总覆盖率 71.2%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend` 通过；后端已重启 PID 13528，smoke 验证 `/health`、admin 登录、`task-modules=16`、`task-flow-templates=12`、`task-flows?trigger_type=schedule` 和 JavaScript 模块支持 `schedule` 均通过 | closed
- 2026-05-30 20:52 | backend-ai/test-ai | test | EB-045 生命周期任务流门禁通过：新增测试覆盖任务流内置检测启动/结束触发 `project_start/project_end`，以及 `DetectionRunsService` 启动/停止同样触发生命周期任务；`go test ./... -coverprofile all-coverage.out` 总覆盖率 71.2%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend` 通过；后端已重启 PID 35820，smoke 验证 `/health`、admin 登录、`task-modules=16`、`task-flow-templates=12`、JavaScript runtime API 和生命周期 trigger 暴露均通过 | closed
- 2026-05-30 20:35 | backend-ai/test-ai | test | EB-045 单功能和全量门禁通过：新增测试覆盖多变量 JS 条件读取、按项目读取变量、JS 写 STRING 虚拟变量、写审计落库、默认不触发递归，以及任务流拒绝物理变量假写；门禁通过 `go test ./... -coverprofile all-coverage.out` 总覆盖率 71.0%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启 PID 9892，smoke 验证 `/health`、admin 登录、`task-modules=16`、`task-flow-templates=12` 和 JavaScript `runtime_api` 均通过 | closed
- 2026-05-30 20:21 | backend-ai | decision | EB-045 性能复核后做轻量优化：520 个 `custom_items` 的 STRING 任务参数 payload 实测 166453 bytes，低于 256 KiB 上限，JSON 解析约 3.25ms、转换运行快照约 0.56ms；该参数只用于低频业务动作，不进入 MQTT 热路径，因此暂不需要继续扩大 WS 限制或改成实时通道配置流。已将 `sys_detection_tasks.custom_config_json` 改为紧凑追踪 JSON，只保留必要自定义项字段，运行判断唯一事实仍是 `detection_run_standard_items` 快照 | closed
- 2026-05-30 20:21 | backend-ai/test-ai | test | EB-045 回归门禁通过：新增/更新测试覆盖 520 项 STRING 参数性能预算和紧凑 `custom_config_json` 不落 `created_at` 等膨胀字段；`go test ./... -coverprofile all-coverage.out` 总覆盖率 70.6%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend` 通过；后端已重启 PID 13280，smoke 验证 `/health`、admin 登录、`task-modules=16`、`task-flow-templates=12`、`detection-standards/favorites`、`detection-standards/recent` 均通过 | closed
- 2026-05-30 19:36 | backend-ai/test-ai | test | EB-045 新增单测覆盖 STRING 虚拟变量 JSON 启动自定义检测配置、首帧存储、首帧超限报警、`custom_items` 运行快照冻结、520 个自定义项 payload 解析、超限 payload 拦截、检测配置收藏/最近使用和自定义运行快照；门禁通过：`go test ./... -coverprofile all-coverage.out` 总覆盖率 70.5%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启 PID 37496，smoke 验证 `/health`、admin 登录、`task-modules`、`task-flow-templates`、`detection-standards/favorites`、`detection-standards/recent` 均通过 | closed
- 2026-05-30 17:18 | backend-ai | test | EB-048 交付接口轻量 smoke 通过：`/health` ok 且 KIO gateway active、队列全 0；登录后 `GET /api/v1/variables?source_type=virtual` 返回 5 条、`GET /api/v1/storage-routes?project_id=1` 返回 50 条、`GET /api/v1/task-modules` 返回 16 个模块、`GET /api/v1/task-flow-templates` 返回 12 个模板、`GET /api/v1/task-flow-runs?limit=5` 返回 5 条；后端 PID 22656 继续运行 | closed
- 2026-05-30 17:36 | backend-ai/test-ai | test | EB-019 通知总线验证通过：`gofmt`、`go test ./...`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 70.3%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`、`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run` 均通过；后端监听 PID 25436，`/health` ok 且队列含 `notify=0`，WS `topic=notifications` smoke 新建并停止 `SMOKE-NOTIFY-20260530173551`，收到 `detection.run_started` 和 `detection.run_stopped` | closed
- 2026-05-30 18:35 | backend-ai/test-ai | test | 通知中心持久化验证通过：repository 测试覆盖通知落库、启用用户 recipient、未读过滤、标记单条/全部已读和重复 `event_uid` 幂等；handler 测试覆盖认证路由、列表 payload 对象、未读数、404 和 bad query；dispatcher 测试覆盖落库后 WS 发布。门禁通过 `go test ./...`、覆盖率 71.1%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`、`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run` | closed
- 2026-05-30 18:36 | backend-ai/test-ai | test | 后端已用新二进制重启并保持 `127.0.0.1:18080` PID 18164；smoke 通过 `/health`、admin 登录、通知未读数、通知列表；启动并停止 `SMOKE-NOTIFY-PERSIST-20260530183332` 后 HTTP 通知列表出现 `detection.run_started`、`detection.run_stopped`、`detection.result_ok` 3 条，随后单条已读和全部已读接口验证 unread=0 | closed
- 2026-05-30 18:55 | backend-ai/test-ai | test | 通知分发对象细化验证通过：新增 repository 测试覆盖 `role=operator` 只给 operator、`user=<id>` 只给指定用户、无效 user target 不生成 recipient；门禁通过 `go test ./...`、覆盖率 71.1%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启 PID 8644，`/health`、admin 登录、通知未读数和通知列表 smoke 通过 | closed
- 2026-05-30 17:55 | backend-ai/test-ai | test | 当前检测配置接口验证通过：handler 测试覆盖 running/paused current run 和缺 `project_id` 400；HTTP smoke 启动 `SMOKE-CURRENT-*` 检测任务 `task_id=12`，`GET /api/v1/detection-runs/current?project_id=1` 返回同一 running 任务，随后 stop 成功。门禁通过 `go test ./...`、覆盖率 70.2%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`、`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`；后端 PID 14700 继续运行，`/health` ok、KIO gateway active、队列全 0 | closed
- 2026-05-30 14:42 | backend-ai | decision | 现场 KingIO 写入测试暴露并修复两个问题：默认 KIO `Qid` 从纳秒级时间戳改为 `<1_000_000_000` 的小范围递增值，避免 KingIO ack 匹配失败；自动发现 upsert 不再覆盖已分配业务变量的 `rw_mode/writable/write_path/write_data_type` 等人工写入配置 | closed
- 2026-05-30 14:43 | backend-ai | test | KingIO 现场写入通过：直接 KIO 写 `台1_39=13.42` 返回 `ProcessStep=100/Result=ok`；WS `command.write_variable` 写 `var_id=5826452156569908253` 到 `13.43` 返回 `command.ack`、`Project_confirmed=true`、KIO `status=confirmed`，实时快照回刷为 `13.430000305175781`，审计日志写入 `ws.command.write_variable` success；后端保持 `127.0.0.1:18080` PID 36240 | closed
- 2026-05-30 14:44 | backend-ai | test | 修复后门禁通过：`go test ./...`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 70.0%、`go vet ./...`、`golangci-lint run ./...`、`go build ./cmd/edge-backend` | closed
- 2026-05-30 15:30 | backend-ai | decision | 实时快照粒度控制补强：`GET /api/v1/realtime/variables` 新增 `source_type/gateway_id/project_id/device_id/var_id` 过滤；WS `var_id` query 支持逗号或重复参数，并在后端走直接 tag lookup，项目视图走 project index；无过滤仍表示有意全量快照，前端项目页应传 `project_id` 防止大快照 | closed
- 2026-05-30 15:30 | frontend-ai | decision | 工位页实时变量查询改为有选中项目时调用 `getRealtimeVariables({project_id})`，shared API 新增 `RealtimeVariableListParams`；调试/全局页面仍可不传参数获取全量 | closed
- 2026-05-30 15:32 | backend-ai/test-ai | test | 实时粒度控制验证通过：新增后端单测覆盖 HTTP realtime `project_id/device_id/var_id` 过滤和 WS comma `var_id` 订阅；门禁通过 `go test ./...`、覆盖率 70.0%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；前端 `npm run lint`、`npm run build`、`npm run test -- --run` 通过；后端已重启 PID 36924，smoke 验证全量 528、project_id=1 返回 8、单点 1、多点 2、非法 var_id 返回 400，WS 多点快照返回 2；复测确认 WS `Authorization: Bearer <access_token>` 可握手，前次 401 是测试脚本取错 token 字段 | closed
- 2026-05-30 14:22 | backend-ai | test | EB-019/027/045 回归补强：新增 WS `command.write_variable` 写 STRING 虚拟变量触发 task-flow 的 handler 级测试，覆盖变量内存更新、倒排索引触发、task_flow_runs 成功记录和 `ws.command.write_variable` 审计落库；本轮未启动 KingIO/IO 服务 | closed
- 2026-05-30 14:23 | backend-ai | test | 验证通过：`go test ./...`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 70.0%、`go vet ./...`、`golangci-lint run ./...`、`go build ./cmd/edge-backend`；本地后端仍监听 `127.0.0.1:18080` PID 57656 | closed
- 2026-05-30 14:12 | backend-ai | decision | EB-019/027 继续迭代：拆出 `KIOWriteService`，新增 `VariableWriteService`，WS `command.write_variable` 支持写 STRING 虚拟变量触发任务流，也支持满足 `writable/rw_mode/write_path` 约束的物理变量经 KIO 下设；前端/主站不得绕过后端直写 MQTT | closed
- 2026-05-30 14:13 | backend-ai | test | EB-019/027 验证通过：`go test ./...`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 70.0%、`go vet ./...`、`golangci-lint run ./...`、`go build ./cmd/edge-backend` 通过；后端已重启为 PID 57656，并通过 `/health`、登录、`task-modules` smoke | closed
- 2026-05-29 00:28 | backend-ai | test | 完成 EB-008：新增 Edge Auth Lite 后端认证鉴权、服务身份、SSO ticket、审计表和路由权限绑定；`go test ./... -coverprofile all-coverage.out` 通过且全包覆盖率 74.3%，`go build ./cmd/edge-backend` 通过，HTTP smoke 验证 `/health` 200、未登录实时变量 401、admin 登录和 `/auth/me`/`runtime/channels` 200 | closed
- 2026-05-29 00:51 | frontend-ai | decision | 完成 EB-009：新增玻璃风格登录页、内存 token 会话恢复、路由守卫、401 自动清会话、403 权限页、capability 菜单显隐、退出登录和主站 SSO ticket 入口；Electron openExternal 保持在 desktop adapter，业务页面保持可迁移到主服务器 | closed
- 2026-05-29 00:53 | frontend-ai | test | `desktop/npm run lint`、`desktop/npm run build` 通过；`desktop/npm run test` 因当前无 test/spec 文件按 Vitest 默认返回 1；Playwright 验证未登录跳转登录、`admin/Admin@12345` 登录、设置/历史/调试看板/工位访问、退出登录和 SSO 未配置提示 | closed
- 2026-05-29 01:01 | backend-ai | decision | 为 EB-010 补齐用户管理后端接口：`GET/POST/PATCH/DELETE /api/v1/users` 与 `POST /api/v1/users/:id/reset-password`，并限制当前用户自禁用/自删除 | closed
- 2026-05-29 01:02 | frontend-ai | decision | 管理中心新增用户管理模块，用户入口与变量/实时/历史/系统入口同属标题右侧展示卡片；业务页面只依赖 shared API 与 auth store，避免 Electron-only 逻辑，便于后续迁移到主服务器 | closed
- 2026-05-29 01:03 | frontend-ai | test | `desktop/npm run lint`、`desktop/npm run build`、`backend/go test ./...`、`backend/go build ./cmd/edge-backend` 通过；`desktop/npm run test` 因无 test/spec 文件返回 1；浏览器 smoke 验证 admin 登录、用户新增、改角色、重置密码、新密码登录、删除和测试用户清理 | closed
- 2026-05-29 01:18 | frontend-ai | decision | 完成 EB-005：Electron 主进程新增托盘、最小化到托盘、Windows 开机自启动开关、sidecar 异常退出最多 5 次重拉起；设置中心系统模块提供对应控制，浏览器预览态明确降级 | closed
- 2026-05-29 01:19 | frontend-ai | test | `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test` 通过；新增 `desktopBridge` 浏览器降级单测；`backend:build` 与 `electron-builder --dir --config.directories.output=release-platform-smoke` 通过，打包后 exe 启动内置 `resources/backend/edge-backend.exe` 且 `/health` 返回 ok、tags=521 | closed
- 2026-05-29 01:29 | backend-ai | decision | 补齐 `GET /api/v1/history/data` 历史查询接口，支持 device/test/time/limit 过滤，返回 `rt_history_data` 持久化样本；历史数据仍只由检测任务入库，外部数据库同步边界不变 | closed
- 2026-05-29 01:29 | frontend-ai | decision | 历史查询页接入 shared API 的真实历史数据适配器，可将后端变量样本按时间透视为图表/表格行；接口无数据时保留样例数据标识，业务页面不依赖 Electron preload | closed
- 2026-05-29 01:29 | test-ai | test | `backend/go test ./...`、`backend/go build ./cmd/edge-backend`、`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test`、`desktop/npm run backend:build`、`electron-builder --dir --config.directories.output=release-platform-smoke` 通过；打包 exe smoke 验证内置后端 `/health` ok、`/api/v1/history/data?limit=1` 返回 200 | closed
- 2026-05-29 01:43 | backend-ai | test | 设备三语显示名后端变更验证：`backend/go test ./...`、`backend/go build ./cmd/edge-backend`、`desktop/npm run build`、`desktop/npm run lint` 通过；已重启本地后端并用 `/api/v1/devices`、`PATCH /api/v1/devices/:id` smoke 验证返回三语字段 | closed
- 2026-05-29 08:45 | review-ai | review | UI 风格复审发现壳层深色侧栏、纯白顶栏、登录/设置/调试看板玻璃质感和控件样式未完全对齐工位/历史查询基准 | closed
- 2026-05-29 08:45 | frontend-ai | decision | 完成 EB-011：统一 shell、login、settings、debug 的浅色动态背景、玻璃面板、边缘光照、胶囊按钮、表格和滚动条样式，保留页面可迁移到主服务器的架构边界 | closed
- 2026-05-29 08:45 | frontend-ai | test | `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test` 通过；Playwright 截图复审 `login/station/history/settings/debug`，产物位于 `desktop/output/playwright/style-audit-after/` | closed
- 2026-05-29 09:13 | backend-ai | decision | 完成 EB-012：新增检测标准主表、标准项表和检测运行标准项快照表；变量项拆分 `check_enabled` 和 `store_enabled`，检测启动时冻结标准版本，历史存储按运行快照的 `store_enabled` 过滤 | closed
- 2026-05-29 09:13 | backend-ai | test | EB-012 验证通过：`backend/go test ./...`、`backend/go test ./... -coverprofile all-coverage.out`、`go tool cover -func all-coverage.out` 总覆盖率 74.2%、`backend/go vet ./...`、`backend/go build ./cmd/edge-backend`、`desktop/npm run build`、`desktop/npm run lint` 通过；`golangci-lint` 本机未安装 | closed
- 2026-05-29 09:14 | backend-ai | test | 已重启本地后端并 smoke 验证 EB-012：创建检测标准含 1 个标准项、带 `standard_id` 启动检测、返回 1 条运行标准快照、停止检测、删除标准配置；`/health` 返回 ok、tags=521 | closed
- 2026-05-29 09:16 | frontend-ai | decision | 按 Apple Vision Pro + Claude 时尚杂志方向微调：历史页“样例数据”从查询按钮区移到图表说明，只保留只读状态；二级菜单选中态去掉蓝色内阴影，改为轻玻璃浮层 | closed
- 2026-05-29 09:16 | frontend-ai | test | `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test` 通过；Playwright 验证工具栏不再出现 `history-data-source`，状态移入图表说明，截图位于 `desktop/output/playwright/visionpro-style-fix-final/` | closed
- 2026-05-29 09:46 | frontend-ai | decision | 完成 EB-013：变量管理区分未分配发现变量和已分配设备变量；未分配变量只读基础属性并只能分配，已分配变量开放完整配置编辑；工位菜单和设置筛选按当前语言读取设备 display 字段 | closed
- 2026-05-29 09:46 | frontend-ai | test | `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test` 通过；Playwright 截图验证未分配变量分配弹窗和已分配变量完整编辑弹窗，产物位于 `desktop/output/playwright/variable-edit-model/` | closed
- 2026-05-29 11:20 | frontend-ai | decision | EB-013 追加未分配变量小卡片池：未知变量视图只显示基础属性，沿用关键词和左侧筛选，支持全选当前结果、清空选择和批量分配到项目设备 | closed
- 2026-05-29 11:20 | frontend-ai | test | `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test` 通过；Playwright 验证未分配卡片池、批量分配弹窗和隐藏表格态，截图位于 `desktop/output/playwright/unassigned-card-batch/` | closed
- 2026-05-29 11:35 | frontend-ai | decision | EB-013 性能修正：未知变量池从一次性渲染全部玻璃卡片改为每页 48 个轻量卡片，移除隐藏表格渲染，批量按钮改为高对比深色胶囊样式 | closed
- 2026-05-29 11:35 | frontend-ai | test | `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test` 通过；Playwright 验证未知变量页只渲染 48 张卡、有分页、无隐藏表格、批量按钮文字为白色，截图位于 `output/playwright/unassigned-performance-fix/` | closed
- 2026-05-29 11:15 | backend-ai | decision | 完成 EB-015 后端：新增虚变量创建、`source_type`、检测任务并发锁、任务列表/详情/notes/异常停止、报表模板元数据与任务报表关联；前端可见 API/DTO/schema 已同步后端架构文档 | closed
- 2026-05-29 11:23 | backend-ai | test | EB-015 验证通过：`go test ./...`、`go test ./... -coverprofile all-coverage.out`、`go tool cover -func all-coverage.out` 总覆盖率 72.6%、`go vet ./...`、`go build ./cmd/edge-backend` 通过；已重启本地后端并 smoke 验证 `/health`、登录、`GET /detection-runs`、`GET /report-templates` | closed
- 2026-05-29 11:40 | frontend-ai | decision | 修复批量分配按钮内部文本和图标被 Ant Design 样式压暗的问题，对按钮本体、内部 span、图标、hover/active/disabled 状态统一做高对比颜色覆盖 | closed
- 2026-05-29 11:40 | frontend-ai | test | `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test` 通过；Playwright 读取按钮、文字、图标计算颜色均为 `rgb(255, 255, 255)`，截图位于 `output/playwright/batch-button-readable-retry/` | closed
- 2026-05-29 11:55 | frontend-ai | decision | 批量分配工具栏按钮不再使用 Ant Design Button，改为原生 `button` + 自定义胶囊样式，彻底绕开 Ant 内部状态色覆盖 | closed
- 2026-05-29 11:55 | frontend-ai | test | `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test` 通过；Playwright 验证按钮 DOM 为原生 BUTTON、无 ant-btn 类、文字/图标颜色均为白色，截图位于 `output/playwright/batch-button-native-retry/` | closed
- 2026-05-29 12:00 | frontend-ai | decision | 修正未分配变量分配概念：`var_group` 只是变量自身低频标签，不是项目/设备归属；未分配变量单个/批量分配弹窗移除变量分组输入，只保留项目/设备和启用状态，提交时 `var_group` 自动为空 | closed
- 2026-05-29 12:00 | frontend-ai | test | `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test` 通过；Playwright 验证批量分配弹窗不再出现“变量分组”，只显示“选择项目/设备”和“启用”，截图位于 `output/playwright/variable-assignment-scope/` | closed
- 2026-05-29 12:50 | backend-ai | decision | EB-018 后端模块化实施完成：旧 `kernel.go` 内联业务路由迁入 `runtime/handlers`，变量变更统一通过 `VariablesService` 重载 TagManager，`database.Repository` 与 `models` 保持同 package 按领域拆文件；无 API/DTO/schema 变更 | closed
- 2026-05-29 12:50 | backend-ai | test | EB-018 验证通过：`gofmt`、`go test ./...`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 70.8%、`go vet ./...`、`go build ./cmd/edge-backend` 通过；已重启本地后端并 smoke 验证 `/health`、登录、devices、variables、history/data、detection-standards、detection-runs、report-templates、runtime/channels | closed
- 2026-05-29 13:28 | frontend-ai | decision | 完成 EB-014：管理中心新增检测标准模块，接入标准列表、新增/编辑/删除、标准项选择、上下限、`check_enabled`/`store_enabled`；同步补齐检测运行、报表模板、虚变量创建等前端 typed API | closed
- 2026-05-29 13:28 | frontend-ai | test | EB-014/EB-016 前端验证通过：`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test` 通过；Playwright smoke 验证检测标准弹窗和虚变量弹窗，截图位于 `desktop/output/playwright/detection-standards-ui/` 与 `desktop/output/playwright/virtual-variable-ui/` | closed
- 2026-05-29 13:43 | frontend-ai | test | EB-016 工位闭环验证通过：`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test` 通过；Playwright UI smoke 启动本地测试任务 `UI-SMOKE-*` 后成功停止，确认任务历史按钮可见，截图位于 `desktop/output/playwright/station-run-ui/` | closed
- 2026-05-29 13:52 | frontend-ai | test | 虚变量三语名验证通过：`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test` 通过；Playwright smoke 确认弹窗出现中文显示名、英文显示名、日文显示名 | closed
- 2026-05-29 14:08 | backend-ai | test | EB-022 已完成并验证：`backend/go test ./...`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 70.3%、`go vet ./...`、`go build ./cmd/edge-backend`、`desktop/npm run lint`、`desktop/npm run build` 均通过；已重启本地后端并确认 `/health` ok、`/api/v1/system/database-config` 可读、`/api/v1/system/database-config/test` 返回 ok | closed
- 2026-05-29 15:28 | backend-ai | decision | 完成 EB-025：检测标准项新增检查方法、目标值、恢复死区、超限/恢复保持时间和质量策略；检测运行快照冻结检测语义与变量存储映射，前端 DTO 已同步 | closed
- 2026-05-29 15:29 | backend-ai | test | EB-025 验证通过：`go test ./...`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 70.7%、`go vet ./...`、`go build ./cmd/edge-backend`、`desktop/npm run lint`、`desktop/npm run build` 通过；`golangci-lint` 本机未安装 | closed
- 2026-05-29 15:31 | backend-ai | test | 已重启本地后端 PID 42268 并保持 `127.0.0.1:18080` 运行；smoke 验证 `/health` 返回 ok、admin 登录成功、`detection-standards` 与 `detection-runs` 代表接口返回 200 | closed
- 2026-05-29 15:55 | backend-ai | decision | 完成 EB-026：新增 `/api/v1/ws` 只读实时通道、`RealtimeWSService`、WS handler 和前端共享 WS envelope 类型；本阶段只推变量快照、检测任务快照和 heartbeat，写命令统一返回 `read_only` | closed
- 2026-05-29 15:56 | backend-ai | test | EB-026 验证通过：`go test ./...`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 70.3%、`go vet ./...`、`go build ./cmd/edge-backend`、`desktop/npm run lint`、`desktop/npm run build` 通过；`golangci-lint` 本机未安装 | closed
- 2026-05-29 15:57 | backend-ai | test | 已重启本地后端 PID 20420 并保持 `127.0.0.1:18080` 运行；smoke 验证 `/health` ok、admin 登录成功、WS 连接 `/api/v1/ws?access_token=...&topic=realtime.variables` 首帧返回 `connection.ready` | closed
- 2026-05-29 16:12 | backend-ai | decision | 完成 EB-031：WS handler 增加 read limit、read/write deadline、ping/pong 保活、断开清理和重连后重新 ready/snapshot；后端架构文档与实施顺序文档已同步 | closed
- 2026-05-29 16:13 | backend-ai | test | EB-031 验证通过：`go test ./...`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 70.1%、`go vet ./...`、`go build ./cmd/edge-backend`、`desktop/npm run lint`、`desktop/npm run build` 通过；`golangci-lint` 本机未安装 | closed
- 2026-05-29 16:15 | backend-ai | test | 已重启本地后端 PID 39632 并保持 `127.0.0.1:18080` 运行；smoke 验证 `/health` ok、WS 首连和主动断开后重连均返回 `connection.ready` | closed
- 2026-05-29 15:08 | frontend-ai | decision | 完成 EB-023：历史数据源 MySQL 配置页确认使用 shared API，密码不回显，测试连接成功/失败均有明确反馈，保存后展示 `restart_required` 重启 sidecar 提示 | closed
- 2026-05-29 15:08 | frontend-ai | test | EB-023 验证通过：`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test` 通过；Playwright smoke 验证历史数据源页、密码空值、连接测试成功和错误端口失败反馈，截图位于 `desktop/output/playwright/database-config-ui/` | closed
- 2026-05-29 16:02 | frontend-ai | decision | 完成 EB-030：变量编辑补存储映射、写入约束、运行保护高级分区和前端写入校验；检测标准项同步 EB-025 的检测方法、目标值、回差、保持时间和质量策略字段 | closed
- 2026-05-29 16:02 | frontend-ai | test | EB-030 验证通过：`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test` 通过；Playwright smoke 验证变量高级编辑与检测标准语义字段，截图位于 `desktop/output/playwright/variable-attributes-ui/` | closed
- 2026-05-29 15:09 | backend-ai | test | EB-024 P1 验证通过：`go test ./...`、`go test ./... -coverprofile all-coverage.out` 生成 `backend/all-coverage.out` 且总覆盖率 70.6%、`go vet ./...`、`go build ./cmd/edge-backend`、`desktop/npm run lint`、`desktop/npm run build` 均通过；`golangci-lint` 本机仍未安装；已重启本地后端并 smoke 验证虚变量默认 `writable=false/rw_mode=R`、存储映射字段可读回、缺少 `write_path` 的可写变量返回 400 | closed
- 2026-05-29 16:33 | frontend-ai | decision | 完成 EB-032：新增左侧“检测配置”一级菜单和 `/#/detection-config` 页面，复用检测标准 API 展示/维护标准项上下限、检测方法、保持时间、质量策略和存储策略，并把旧 Vue 前端 19 个检测项作为参考池 | closed
- 2026-05-29 16:33 | frontend-ai | test | EB-032 验证通过：`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test` 通过；Playwright smoke 登录后打开 `/#/detection-config`，确认检测配置页、旧系统检测项、19 个参考项和新增标准入口可见，截图位于 `desktop/output/playwright/detection-config-page/detection-config-page.png` | closed
- 2026-05-29 17:00 | frontend-ai | decision | EB-032 规则修正：检测标准项来源从“已分配变量”改为“所有变量按 `var_name` 去重”，检测配置页和管理中心标准弹窗保持一致；变量项显示直接复用变量列表已有中/英/日字段，不在标准配置里单独维护翻译 | closed
- 2026-05-29 17:00 | frontend-ai | test | EB-032 追加验证通过：`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test` 通过；Playwright 打开新增标准弹窗，确认“从所有变量名中选择标准项”提示和变量名选择器可用，截图位于 `desktop/output/playwright/detection-config-variable-names/standard-variable-names.png` | closed
- 2026-05-29 18:00 | backend-ai | decision | EB-033 实时底座首段完成：`TagManager` 新增 `gateway/topic -> tags` 与 `device_id -> tags` 索引，`logic_worker` 改用 `ForMessage`，`Tag.UpdateNumeric` 实施 scale/offset 后的 suspicious/debounce/deadband/startup 清洗，WS 设备过滤改用设备实时视图；暂缓项继续只做站位 | closed
- 2026-05-29 18:00 | backend-ai | test | EB-033 验证通过：`gofmt`、`go test ./internal/models ./internal/pipeline ./internal/services`、`go test ./...`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 70.6%、`go vet ./...`、`go build ./cmd/edge-backend` 通过；`golangci-lint` 本机未安装；已重启后端 PID 27756 并 smoke 验证 `/health`、登录、WS ready、WS detection start/stop | closed
- 2026-05-29 18:20 | backend-ai | decision | EB-033 追加运行态准入：自动发现变量默认 `enabled=false/device_id=null`，只作为候选来源；`Repository.LoadTags()` 与 `TagManager` 统一只接收 `enabled=true AND device_id IS NOT NULL` 的已知业务变量，未知候选不清洗、不推 WS、不入库 | closed
- 2026-05-29 18:20 | backend-ai | test | EB-033 运行态准入补强验证通过：已安装 `golangci-lint v2.12.2`，`golangci-lint run ./...` 为 0 issues；`gofmt`、`go test ./internal/database ./internal/discovery ./internal/pipeline ./internal/services`、`go test ./...`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 70.4%、`go vet ./...`、`go build ./cmd/edge-backend` 通过；已重启后端 PID 23916，`/health` ok 且运行态 tags=4，登录、variables、runtime/channels smoke 通过 | closed
- 2026-05-29 20:24 | frontend-ai | decision | UI 色彩规则修正：设置页与检测配置页普通重点统一使用蓝色/蓝青色，移除普通卡片、选中态和玻璃高光中的橙黄色；橙黄色保留为特别重点色，不再作为普通工作区强调色 | closed
- 2026-05-29 20:24 | frontend-ai | test | 色彩修正验证通过：`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test` 通过；Playwright 截图检查 `/#/settings` 与 `/#/detection-config`，产物位于 `desktop/output/playwright/blue-accent-audit/` | closed
- 2026-05-29 23:06 | frontend-ai | decision | 侧边菜单色彩修正：移除 shell 背景、侧栏光照、通用玻璃面板和菜单选中态中的黄色点缀，改为蓝色/蓝青色体系；工位/历史页面内部特殊光源不在本轮调整 | closed
- 2026-05-29 23:06 | frontend-ai | test | 侧边菜单色彩验证通过：`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test` 通过；Playwright 截图验证 `/#/settings` 侧边栏，产物位于 `desktop/output/playwright/sidebar-blue-accent/settings-sidebar-blue.png` | closed
- 2026-05-29 23:38 | frontend-ai | decision | 登录页色彩修正：移除登录背景右上光球、登录浮层和面板内部玻璃高光中的黄色，统一改为蓝色/蓝青色；登录、shell、设置、检测配置均不再使用普通黄色点缀 | closed
- 2026-05-29 23:38 | frontend-ai | test | 登录页色彩验证通过：`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test` 通过；Playwright 截图验证 `/#/login`，产物位于 `desktop/output/playwright/login-blue-accent/login-blue-accent.png` | closed
- 2026-05-29 20:40 | backend-ai | test | EB-021 表地基验证通过：`gofmt`、`go test ./internal/models ./internal/database`、`go test ./...`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 70.4%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend` 通过；已重启后端 PID 74708，`/health` ok | closed
- 2026-05-29 23:05 | backend-ai | test | 变量默认上下限/业务报警装配地基验证通过：`gofmt`、`go test ./internal/runtime/handlers ./internal/services ./internal/database`、`go test ./...`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 70.2%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`、`desktop/npm run lint`、`desktop/npm run build` 均通过；已重启后端并 smoke 验证 `/health`、admin 登录和 variables 默认报警字段返回 | closed
- 2026-05-29 23:48 | frontend-ai | decision | 完成 EB-035：系统设置日志诊断区新增本机运行日志预览，Electron preload 暴露只读 `readLogs` 读取 Go sidecar 日志尾部；保留打开日志文件位置。审计日志已有写入地基但暂无查询接口，前端暂不做审计列表 | closed
- 2026-05-29 23:48 | frontend-ai | test | EB-035 验证通过：`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test` 通过；Playwright 验证 `/#/settings` 系统设置里“运行日志预览”和“刷新日志”可见，截图位于 `desktop/output/playwright/settings-runtime-logs/runtime-logs-settings.png` | closed
- 2026-05-30 00:07 | frontend-ai | decision | EB-035 追加前端后端审计日志列表：系统设置模块新增“后端审计日志”卡片，调用 `getAuditLogs({limit:80})`，展示时间、操作者、动作、对象、结果、request/command/status 和错误摘要，并补齐中英日文案 | closed
- 2026-05-30 00:07 | frontend-ai | test | 审计日志前端验证通过：`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test` 通过；已重启本地后端为 PID 71332，HTTP 验证 `/api/v1/audit-logs?limit=5` 返回 145+ 条记录，浏览器验证 `/#/settings` 系统设置中“后端审计日志”显示 147 条且可见 `auth.login` 行 | closed
- 2026-05-29 23:46 | backend-ai | test | 检测配置判断周期和首帧语义验证通过：`gofmt`、`go test ./internal/models ./internal/database ./internal/runtime/handlers ./internal/services`、`go test ./...`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 70.0%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`、`desktop/npm run lint`、`desktop/npm run build` 均通过；已重启后端 PID 71284 并 smoke 验证 `/health`、admin 登录和变量默认保持时间字段返回 | closed
- 2026-05-30 00:43 | backend-ai | test | 业务上下限报警性能验证通过：`go test ./...`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 70.1%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`、`go build -tags perf_tools ./tools/mqttpubperf ./tools/perfsetup` 通过；520 变量 10 分钟压测 `PERF-520-20260530-003212` 发布 312000 次更新，MQTT 延迟 avg 2.8ms/p95 3.7ms/max 12.4ms，报警表 31200 条全部 recovered，其中 `above_h=15600`、`below_l=15600`，历史 322920 行；health 采样 120 次，队列最大 `alarm=520/store=120/logic=0`，结束后队列全 0，已通过 API 停止 PERF 任务并保持后端运行 | closed
- 2026-05-30 01:20 | backend-ai | test | EB-028 事件/摘要验证通过：`gofmt`、`go test ./...`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 70.0%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`、`desktop/npm run lint`、`desktop/npm run build` 通过；已重启后端 PID 48260，`/health` ok，API smoke 新建并停止 `SMOKE-EVENT-20260530005535`，`/summary` 返回 `result_status=ok`，`/events` 返回 `run_started,run_stopped` | closed
- 2026-05-30 09:20 | backend-ai | test | EB-037 storage route/schema/wide writer 验证通过：`gofmt`、`go test ./...`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 70.2%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`、`desktop/npm run lint`、`desktop/npm run build` 通过；已重启后端 PID 20768，`/health` ok，API smoke 新建并停止 `SMOKE-WIDE-20260530092033-1`，验证检测启动可通过真实 MySQL AutoMigrate、冻结 storage routes 并准备设备宽表 schema | closed
- 2026-05-30 09:29 | backend-ai | test | EB-037 宽表合并写入和历史读取验证通过：新增单测覆盖同一采样点多变量合并成一条设备宽表记录、数值列 upsert 更新、字符串列读取和 `QueryHistoryData` 从宽表重建旧 DTO；`go test ./... -coverprofile all-coverage.out` 总覆盖率 70.4%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend` 通过；已重启后端 PID 67644，`/health` ok，登录、`history/data?limit=1`、`detection-runs?limit=1` smoke 返回 200 | closed
- 2026-05-30 09:52 | backend-ai | test | EB-037 route 驱动存储重构验证通过：`go test ./... -coverprofile all-coverage.out` 总覆盖率 70.6%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`、`desktop/npm run lint`、`desktop/npm run build` 通过；已重启后端 PID 52372，`/health` ok，admin 登录和 `GET /api/v1/storage-routes?enabled=false` smoke 返回 200 | closed
- 2026-05-30 10:50 | backend-ai | test | EB-041/042/043/044 首段验证通过：`gofmt`、`go test ./...`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 70.2%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend` 通过；已重启后端 PID 45052 并保持 `127.0.0.1:18080` 运行，smoke 验证 `/health` ok、admin 登录 ok、`GET /api/v1/projects` 200、`GET /api/v1/storage-routes?enabled=false&limit=1` 200、`GET /api/v1/history/data?limit=1` 200、`GET /api/v1/devices` 404、变量响应不再包含 `device_id/store_cycle_sec/storage_table` | closed
- 2026-05-30 11:10 | frontend-ai | test | EB-041/042 前端适配验证通过：`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test` 均通过；HTTP smoke 登录 admin 后验证 `GET /api/v1/projects` 返回 8 个项目、`GET /api/v1/storage-routes` 返回 4 条路由、旧 `GET /api/v1/devices` 返回 404 | closed
- 2026-05-30 11:38 | backend-ai | test | EB-045 首段验证通过：新增单测覆盖 `start_flag` 变量 0->1 后命中条件事件任务、执行 `builtin.storage_snapshot`、投递 StoreTask 并通过 repository 写入历史；新增 JS 任务测试覆盖 goja 脚本、`db.query`、执行日志和 SQL 日志；新增倒排索引 priority/cooldown/hold 测试。门禁通过：`go test ./...`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 70.2%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启 PID 27880，`/health`、登录、`GET /api/v1/task-flows` 和 `GET /api/v1/projects` smoke 200 | closed
- 2026-05-30 12:06 | frontend-ai | test | EB-046 验证通过：`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test` 均通过；HTTP smoke 登录 admin 后验证 `GET /api/v1/task-flows` 返回 0 条、`GET /api/v1/projects` 返回 8 个项目。本轮尝试连接 in-app Browser 验证页面，但当前 Browser 插件返回 `iab` 不可用，已改用构建和 HTTP smoke 验证 | closed
- 2026-05-30 12:42 | frontend-ai | decision | 新增 3D 驾驶舱探索页 `/#/model-cockpit`：左侧菜单新增“3D 驾驶舱”，引入 Three.js 与 `three/examples` GLTFLoader/OrbitControls，模型资源从根目录 `3d模型暂存/base_basic_pbr.glb` 复制到 `desktop/public/models/edge-air-conditioner.glb`；页面支持 360 度拖拽、内部流体粒子/流道、模型空间浮窗和射线遮挡。按用户修正要求，模型保持不透明，内部流体仍是独立 3D 对象，以诊断显影/视觉错位方式叠加到模型表面，不使用整机透明或剖切 | closed
- 2026-05-30 12:42 | frontend-ai | test | 3D 驾驶舱验证通过：`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test` 均通过；Playwright 登录打开 `/#/model-cockpit`，桌面与窄视口均检测到 WebGL、非空 canvas 和 4 个浮窗，桌面拖拽旋转后检测到浮窗遮挡态；最终诊断显影截图位于 `desktop/output/playwright/model-cockpit/opaque-diagnostic-overlay-1440x920.png` | closed
- 2026-05-30 12:30 | backend-ai | decision | EB-045 初版任务数据流转设计已被废弃：HTTP 手动运行业务参数入口不符合“变量是一切根基”的平台模型；后续以 STRING 虚拟变量数据变化作为正式任务参数入口 | closed
- 2026-05-30 12:35 | backend-ai | test | EB-045 多步骤任务链初版验证通过，但其中 HTTP 手动运行业务参数路径已作废；保留有效部分：多步骤 `steps_json`、`builtin.context_set`、`builtin.storage_snapshot`、JS 步骤和 StorageBus 投递验证 | closed
- 2026-05-30 12:55 | backend-ai | test | EB-045 变量流转修正验证通过：单测覆盖 watched STRING 虚拟变量变更后解析 JSON 参数、条件脚本读取 `task_params.command`、步骤用 `trigger_param.project_id/duration_sec`、`builtin.storage_snapshot` 投递 StorageBus、JS 读取 context；门禁通过：`go test ./...`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 70.0%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启 PID 26684，smoke 验证 `/health`、admin 登录、`GET /api/v1/task-modules` 返回 source=`literal,trigger_param,event,context`、`GET /api/v1/task-flows` 200 | closed
- 2026-05-30 13:05 | backend-ai | review | 后端待办审查并清理过时项：关闭 EB-002、EB-021、EB-028、EB-036、EB-038、EB-043、EB-044；修正 EB-017 设备为项目；修正 EB-019 剩余范围为变量写入/KIO service/防递归；修正 EB-040，废弃默认报警独立表，改为统一超限事件语义；EB-045 继续作为任务编排主线 | closed
- 2026-05-30 13:25 | backend-ai | test | EB-045 内置检测业务模块验证通过：单测覆盖可选存储首帧、可选上下限报警、暂停/恢复、手动结束、固定时长结束、合格持续时长结束、结束后平均/最小/最大特征值计算；门禁通过 `gofmt`、`go test ./...`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 70.5%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend` | closed
- 2026-05-30 13:45 | backend-ai | test | 暂停时长不计入累计时长验证通过：repository/service 测试覆盖恢复顺延预计结束时间和累计暂停时长；门禁通过 `go test ./...`、覆盖率 70.7%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend` | closed
- 2026-05-30 13:50 | frontend-ai | decision | 3D 驾驶舱按旧 Vue `SPDShowPage00.vue` 结构复刻：复制驾驶舱背景、标题、图表、中心背景和五张卡片资产到 `desktop/public/cockpit/`；`/#/model-cockpit` 改为标题栏、五张顶部信息卡、中部实时监测表 + 3D 模型、右侧温湿度趋势图布局，同时保留不透明模型、内部 3D 流体诊断显影和模型空间遮挡浮窗 | closed
- 2026-05-30 13:50 | frontend-ai | test | Vue 结构复刻验证通过：`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run` 通过；Playwright 登录打开 `/#/model-cockpit`，验证 WebGL canvas、5 张卡、8 行监测数据、2 张趋势图、4 个模型浮窗、无缺失 i18n key 且数值列不截断，截图位于 `desktop/output/playwright/model-cockpit/vue-structure-1440x920.png` | closed
- 2026-05-30 13:35 | frontend-ai | decision | 驾驶舱背景资产矢量化：按旧 Vue PNG 原始尺寸重建 `cockpit-background/title/center/chart` 与 `card-0..4` SVG，页面引用全部切到 SVG，PNG 仅保留为参考；矢量层复刻旧图的蓝色渐变、标题斜边、中心/图表折线框、卡片角线、水印圆和齿轮装饰 | closed
- 2026-05-30 13:35 | frontend-ai | test | SVG 背景验证通过：`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run` 通过；Playwright 验证 `/#/model-cockpit` 的页面、标题、中心、图表、卡片背景均引用 `.svg`，WebGL/浮窗正常，截图位于 `desktop/output/playwright/model-cockpit/svg-vector-background-1440x920.png` | closed
- 2026-05-30 13:58 | frontend-ai | decision | 驾驶舱 SVG 背景改为纹理层：移除 SVG 中大面积实心蓝色填充，保留线框、高光、齿轮、水印和标题斜边纹理；新增 `.cockpit-dynamic-background`，按工位页动态光效模式提供漂移蓝/青光底，卡片、中心框、图表框改为半透明玻璃底承接动态背景 | closed
- 2026-05-30 13:58 | frontend-ai | test | 动态背景 + SVG 纹理验证通过：`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run` 通过；Playwright 验证 `/#/model-cockpit` 存在动态背景层和 SVG 纹理层，root 不再使用静态 SVG 实心背景，WebGL/5 卡/2 图/4 浮窗正常，截图位于 `desktop/output/playwright/model-cockpit/dynamic-svg-texture-1440x920.png` | closed
- 2026-05-30 15:05 | backend-ai | test | KingIO 开发版现场写入验证继续通过：HTTP KIO 多类型写入 `台1_40=true`、`台1_41=41.67`、`台1_42=edge-ws-string-1450` 均收到 `ProcessStep=100/Result=ok`；WS `command.write_variable` 多类型写入 `台1_40=false`、`台1_41=41.68`、`台1_42=edge-ws-string-1452` 均确认成功，实时快照读回对应 BOOL/FLOAT/STRING 值；`query-all` 后 `台1_39~台1_42` 的 `rw_mode/writable/write_path/write_data_type/write_min/write_max` 保持不被发现刷新覆盖；越界 WS 写入 `台1_41=150` 被后端校验拦截，返回 `command_failed: value 150 is above write_max 100`，审计表记录 `ws.command.write_variable failed`，实时值保持 `41.68000030517578`；门禁通过 `go test ./...`、覆盖率 70.0%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端 PID 36240 继续运行，`/health` ok、KIO gateway active、队列全 0 | closed
- 2026-05-30 17:00 | backend-ai | test | 重启电脑后已恢复后端并完成虚拟变量任务链 smoke：创建 FLOAT 虚拟值变量和 STRING 请求变量，任务请求变量写入 `{"command":"start_detection",...}` 后触发 task-flow，启动检测任务 `task_id=11`，首帧历史 `history_rows=1`，业务超限 `alarm_total=1`，手动停止后生成特征值 `sample_count=1 avg/min/max=35`；门禁通过 `go test ./...`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 70.2%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端 PID 22656 继续运行，`/health` ok、MQTT/KIO gateway active、队列全 0 | closed

## Incremental Board Cleanup - 2026-05-31 08:20

来源：从根目录 `AI_BOARD.md` 迁出的闭合记录；本节不承载 open/blocked 工作项。

### Closed Active Board Items

| ID | Owner | Type | Status | Item | Next Action |
| --- | --- | --- | --- | --- | --- |
| EB-034 | `frontend-ai` | feature | closed | 前端适配变量默认上下限、默认报警开关和运行中同步选项。 | 变量编辑和虚变量创建已接入 `default_alarm_enabled/default_limit_ll/default_limit_l/default_limit_h/default_limit_hh/default_limit_deadband/default_violation_hold_ms/default_recover_hold_ms`；变量编辑已提供 `apply_to_running` 运行中同步选项和风险提示，明确只同步当前 running 快照、不修改检测标准业务限值。检测配置标准项已支持 `alarm_enabled/check_cycle_ms/check_on_start` 等字段。工位页当前运行任务已新增“标准快照”入口，调用 `GET /api/v1/detection-runs/{id}` 展示检测业务限值、变量默认报警、变量默认限值和默认报警策略。Playwright 已验证字段可见、运行快照展示和 `apply_to_running=true` 保存请求 200。后续可按归档规则迁出本 closed 项。 |
| EB-037 | `backend-ai`/`frontend-ai` | feature | closed | 项目动态宽表存储：storage routes、检测运行快照、自动建表扩列和宽表 writer。 | 后端已完成 route 驱动存储主路径；前端工位页已接入当前运行任务“存储快照”弹窗，调用 `GET /api/v1/detection-runs/{id}/storage-routes` 展示当次冻结 route 的目标、表/列、触发、周期、死区和首帧存储，并完成端到端开始检测、查看快照、停止检测 smoke。历史查询页已接入同一“存储快照”入口，任务链接使用 `project_id` 查询历史并兼容旧 `device_id` 链接读取。设置中心存储路由新增搜索和启用状态筛选，Playwright 已验证创建 enabled route、筛选定位、编辑禁用和删除全链路。后续可按归档规则迁出本 closed 项。 |

### Closed Activity Log

- 2026-05-31 08:17 | review-ai | board-cleanup | 看板 Activity Log 二次清理：在不改 Active Board open/blocked 项的前提下，将剩余 102 条历史 Activity Log 从 `open` 转为 `closed`；这些记录是旧阶段决策、测试证据或已由 EB 行/稳定文档承载的实现记录，不再作为当前待施工事项。复核结果：Activity Log open=0，Active Board 真实 open 行保留。 | closed
- 2026-05-31 08:14 | review-ai | board-cleanup | 看板状态清理：按新规则复核最近 8 小时后端 Activity Log，确认 EB-017/019/045/048 等 API hardening、任务流参数校验、PLC 参数失败保护、多项目 smoke 和配置参数边界记录均已被对应 EB 行、`MEMORY.md` 或 `backend/docs/*` 承载；将 `2026-05-31 00:26` 到 `03:25` 期间 owner 以 `backend-ai` 开头且已验证完成的 32 条 Activity Log 从 `open` 转为 `closed`。未改 Active Board 的真实 open 工作项。 | closed
- 2026-05-31 08:08 | frontend-ai/test-ai | bugfix/test | 修复桌面端整体页面滚动：全局 `body/#root/workbench-shell` 固定为视口高度并隐藏外层滚动，`workbench-main` 锁定高度，左侧菜单改为 `.workbench-nav` 内部滚动，业务内容继续只在 `.workbench-content` 内滚动。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`；Playwright 1920×1080 登录后依次检查工位、3D 驾驶舱、设置、历史页，强制 `window.scrollTo(0,800)` 后 `window.scrollY/documentElement.scrollTop/body.scrollTop` 均为 0，`documentElement.scrollHeight=viewportHeight=1080`。 | closed
- 2026-05-31 08:08 | backend-ai/frontend-ai/test-ai | test/api | 设置页 smoke 和项目成员空列表契约补齐：新增 `desktop/scripts/smoke-settings.mjs` 与 `desktop/npm run smoke:settings`，覆盖桌面登录、`GET /api/v1/projects`、旧 `/api/v1/devices=404`、`GET /api/v1/projects/{id}/members`、设置页项目成员 UI，以及“运行日志预览”使用居中 `Modal` 且无侧边 `Drawer`；后端 `ListProjectMembers` 空结果规范为 `items:[]`，前端 settings 端兼容空数组；kernel MQTT publish/subscribe 回归允许本机 broker 可达时返回 200。验证通过 `go test ./internal/database ./internal/runtime/handlers`、`go test ./internal/runtime -run TestKernelAuthAndBusinessRoutes -count=1`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 72.4%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`、`desktop/npm run lint`、`desktop/npm run test -- --run`、`desktop/npm run build`、`desktop/npm run smoke:settings`；后端已重启 PID 35628，`/health` ok，tags=1100。 | closed
- 2026-05-31 08:05 | frontend-ai/test-ai | feature/test | EB-048 项目成员配置 UI 接入：设置页用户管理模块新增项目成员配置区，可按项目读取 `GET /api/v1/projects/{id}/members`，添加/移除本地用户、编辑成员角色和通知接收开关，并通过 `PUT /api/v1/projects/{id}/members` 保存。验证通过 `desktop/npm run lint`、`desktop/npm run test -- --run`、`desktop/npm run build`，后端 `/health` ok；Playwright 登录设置页后切到用户管理，确认“项目成员/通知接收人/接收通知”渲染。 | closed
- 2026-05-31 07:38 | backend-ai/test-ai | api/test | EB-019 项目通知成员收敛首段落地：新增 `sys_project_members`、`GET /api/v1/projects/:id/members`、`PUT /api/v1/projects/:id/members`，项目通知在配置成员后按 `notify_enabled=true` 的启用用户生成 `sys_notification_recipients`，未配置成员的项目继续全员兜底以避免升级后通知无人接收；`smokehealth` 已追加项目成员接口真实检查。验证通过定向 repository/handler/model/smokehealth 测试、`go test ./...`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 72.4%、`go vet ./...`、`go vet -tags smoke_tools ./tools/smokehealth`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 15684，`/health` ok，KIO gateway active，tags=1100，真实 `smokehealth` 通过。 | closed
- 2026-05-31 07:36 | frontend-ai/test-ai | feature/test | 驾驶舱按 Codex 官网产品页气质重做视觉层级：`/#/model-cockpit` 改为低饱和浅灰蓝背景、细网格、白色产品面板式玻璃卡、深色高对比文字和克制标题背板，保留工位同源动态暖光边缘；修复进入 3D 驾驶舱时继承滚动位置导致首屏整体上移的问题，并重新平衡 Three.js 相机、缩放和模型贴底位置。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，Playwright 1920×1080 截图 `output/playwright/model-cockpit-codex-style-pass5.png`。 | closed
- 2026-05-31 07:27 | backend-ai/test-ai | test/tool | EB-006 后端 health/API smoke 工具落地：新增 `backend/tools/smokehealth`，使用 `go run -tags smoke_tools ./tools/smokehealth` 验证 `/health`、登录、`auth/me`、projects、variables、runtime channels、gateway configs、task modules/templates/runs、notifications unread count、active detection runs，以及旧 `/api/v1/devices` 返回 404。验证通过 `go test -tags smoke_tools ./tools/smokehealth`、真实 smoke、`go test ./...`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 72.3%、`go vet ./...`、`go vet -tags smoke_tools ./tools/smokehealth`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；`/health` ok，KIO gateway active，tags=1100。 | closed
- 2026-05-31 07:21 | backend-ai/test-ai | test | EB-006 MQTT/KIO payload 热路径回归补强：新增 `backend/internal/pipeline.TestProcessMessageKIOPayloadUpdatesOnlyIndexedKnownTags`，用真实 KingIOT/KIO `Objs/PVs` payload 直接触发 `processMessage`，验证业务名重映射后仍按原始 KIO `JSONPath` 更新数值/布尔/字符串变量、PVs fallback 可用、质量码进入 runtime snapshot，并证明 `gateway/topic` 索引不会误更新其他 topic 变量。验证通过 `backend/go test ./internal/pipeline -run TestProcessMessageKIOPayloadUpdatesOnlyIndexedKnownTags -count=1`、`backend/go test ./...`、`backend/go test ./... -coverprofile all-coverage.out` 总覆盖率 72.3%、`backend/go vet ./...`、`backend/golangci-lint run ./...` 0 issues、`backend/go build ./cmd/edge-backend`；`/health` ok，KIO gateway active，tags=1100。 | closed
- 2026-05-31 07:13 | backend-ai/test-ai | test/tool | EB-045/048 检测静音 smoke 补强：`backend/tools/smokeeb045` 在 NG 场景中于 `alarm.limit.enter` 后写入正式 STRING 请求 `{"command":"mute_detection_alarms","task_id":...}`，验证 `builtin.mute_detection_alarms` 通过 task-flow 返回 `muted=1`；随后继续写入更高超限值验证 `alarm.limit.level_change` 仍会触发，再写回合格值验证 `alarm.limit.recover` 和停止后的 `detection.result_ng` 仍走 WS/HTTP 未读已读闭环。现场实测通过：项目 `38`，请求变量 `735730343`，控制变量 `810150200`，测量变量 `755186266`，默认报警变量 `773322031`，主任务 `72`，固定任务 `73`，合格持续任务 `74`，异常停止任务 `75`，NG 任务 `76`。验证通过 `backend/go test -tags smoke_tools ./tools/smokeeb045`、`backend/go vet -tags smoke_tools ./tools/smokeeb045`、`backend/go run -tags smoke_tools ./tools/smokeeb045 -wait 12s`、`backend/go test ./...`、`backend/go test ./... -coverprofile all-coverage.out` 总覆盖率 72.3%、`backend/go vet ./...`、`backend/golangci-lint run ./...` 0 issues、`backend/go build ./cmd/edge-backend`。 | closed
- 2026-05-31 07:06 | backend-ai/test-ai | test/tool | EB-019/020/045/048 异常停止通知和审计 smoke 补强：`backend/tools/smokeeb045` 新增独立异常停止检测场景，通过正式 STRING 请求启动一条临时检测，再用 WS `command.detection.abnormal_stop` 停止，验证任务 `end_type=abnormal_stop`、`detection_run_events` 存在 `run_abnormal_stop`、`detection.run_abnormal_stop` 通过 WS 即时到达、HTTP 通知列表按同一 `event_uid` 读回并可标记单条已读，同时通过 `/api/v1/audit-logs?action=ws.command.detection.abnormal_stop&target_type=ws_command&target_id=command.detection.abnormal_stop&result=success` 查到成功审计。现场实测通过：项目 `37`，请求变量 `844112758`，控制变量 `861440106`，测量变量 `760675846`，默认报警变量 `801264795`，主任务 `67`，固定任务 `68`，合格持续任务 `69`，异常停止任务 `70`，NG 任务 `71`。验证通过 `backend/go test -tags smoke_tools ./tools/smokeeb045`、`backend/go vet -tags smoke_tools ./tools/smokeeb045`、`backend/go run -tags smoke_tools ./tools/smokeeb045 -wait 12s`、`backend/go test ./...`、`backend/go test ./... -coverprofile all-coverage.out` 总覆盖率 72.3%、`backend/go vet ./...`、`backend/golangci-lint run ./...` 0 issues、`backend/go build ./cmd/edge-backend`。 | closed
- 2026-05-31 06:56 | backend-ai/test-ai | test/tool | EB-019/045/048 检测生命周期通知 smoke 补强：`backend/tools/smokeeb045` 将 `topic=notifications` WS 提前到正式任务启动前打开，验证同一主检测任务的 `detection.run_started`、`detection.run_paused`、`detection.run_resumed`、`detection.run_stopped` 均通过 WS 即时到达，并通过 HTTP 通知列表按同一 `event_uid` 读回和标记单条已读。现场实测通过：项目 `36`，请求变量 `884347133`，控制变量 `864933664`，测量变量 `887256240`，默认报警变量 `816235002`，主任务 `63`，固定任务 `64`，合格持续任务 `65`，NG 任务 `66`。验证通过 `backend/go test -tags smoke_tools ./tools/smokeeb045`、`backend/go vet -tags smoke_tools ./tools/smokeeb045`、`backend/go run -tags smoke_tools ./tools/smokeeb045 -wait 12s`、`backend/go test ./...`、`backend/go test ./... -coverprofile all-coverage.out` 总覆盖率 72.3%、`backend/go vet ./...`、`backend/golangci-lint run ./...` 0 issues、`backend/go build ./cmd/edge-backend`。 | closed
- 2026-05-31 06:51 | backend-ai/test-ai | test/tool | EB-019/020/045/048 写操作审计 smoke 补强：`backend/tools/smokeeb045` 新增审计断言，启动检测的正式 STRING 请求会通过 `GET /api/v1/audit-logs?action=ws.command.write_variable&target_type=ws_command&target_id=command.write_variable&result=success` 按 `command_id` 查到 `ws.command.write_variable` 审计；任务流 `builtin.write_control_variables` 下设控制变量后会通过 `GET /api/v1/audit-logs?action=task_flow.write_variable&target_type=variable&target_id=<control_var>&result=success` 查到成功审计。第一次现场运行发现 smoke 匹配 JSON 空格过严，已修正为按 command_id 值匹配，并清理失败运行留下的临时 running 任务 `58`。最终现场实测通过：项目 `35`，请求变量 `731948864`，控制变量 `836796853`，测量变量 `749522000`，默认报警变量 `762652944`，主任务 `59`，固定任务 `60`，合格持续任务 `61`，NG 任务 `62`。验证通过 `backend/go test -tags smoke_tools ./tools/smokeeb045`、`backend/go vet -tags smoke_tools ./tools/smokeeb045`、`backend/go run -tags smoke_tools ./tools/smokeeb045 -wait 12s`、`backend/go test ./...`、`backend/go test ./... -coverprofile all-coverage.out` 总覆盖率 72.3%、`backend/go vet ./...`、`backend/golangci-lint run ./...` 0 issues、`backend/go build ./cmd/edge-backend`。 | closed
- 2026-05-31 06:44 | backend-ai/test-ai | test/tool | EB-019/040/045/048 默认报警 smoke 补强：`backend/tools/smokeeb045` 新增独立默认报警虚拟变量，先通过 `PATCH /api/v1/variables/{id}` 开启 `default_alarm_enabled/default_limit_h/default_limit_hh/default_limit_deadband/default_*_hold_ms`，再用 WS 写值 `43 -> 50 -> 25` 验证变量默认报警 `alarm.limit.enter`、`alarm.limit.level_change`、`alarm.limit.recover` 均通过 `topic=notifications` WS 即时到达，并通过 HTTP 通知列表按同一 `event_uid` 读回和标记单条已读；最后通过 `GET /api/v1/limit-alarms?scope=default&project_id=33&var_id=880491729&status=recovered` 读回 `task_id=0/alarm_type=above_hh` 默认报警落库记录。现场实测通过：项目 `33`，请求变量 `740493979`，控制变量 `797483878`，测量变量 `813498905`，默认报警变量 `880491729`，主任务 `54`，固定任务 `55`，合格持续任务 `56`，NG 任务 `57`。验证通过 `backend/go test -tags smoke_tools ./tools/smokeeb045`、`backend/go run -tags smoke_tools ./tools/smokeeb045 -wait 12s`、`backend/go test ./...`、`backend/go test ./... -coverprofile all-coverage.out` 总覆盖率 72.3%、`backend/go vet ./...`、`backend/golangci-lint run ./...` 0 issues、`backend/go build ./cmd/edge-backend`。 | closed
- 2026-05-31 06:36 | backend-ai/test-ai | test/tool | EB-019/045/048 报警等级变化通知 smoke 补强：`backend/tools/smokeeb045` 的 NG 场景改为先以测量值 `43` 启动检测触发 `alarm.limit.enter`，再写入 `50` 触发 `alarm.limit.level_change`，再写回 `25` 触发 `alarm.limit.recover`，最后停止检测验证 `detection.result_ng`；四类通知均通过 `topic=notifications` WS 即时到达，并通过 HTTP `/api/v1/notifications` 按同一 `event_uid` 读回、确认未读、标记单条已读。现场实测通过：项目 `32`，请求变量 `795489926`，控制变量 `831328992`，测量变量 `760750926`，主任务 `50`，固定任务 `51`，合格持续任务 `52`，NG 任务 `53`。验证通过 `backend/go test -tags smoke_tools ./tools/smokeeb045`、`backend/go run -tags smoke_tools ./tools/smokeeb045 -wait 12s`、`backend/go test ./...`、`backend/go test ./... -coverprofile all-coverage.out` 总覆盖率 72.3%、`backend/go vet ./...`、`backend/golangci-lint run ./...` 0 issues、`backend/go build ./cmd/edge-backend`。 | closed
- 2026-05-31 06:31 | backend-ai/test-ai | bugfix/test | EB-019/045/048 超限恢复通知闭环修复：`TaskManager` 的检测报警恢复事件补齐 `project_id/project_code/test_no/var_name/display_name/alarm_level/limit_value` 等上下文，变量默认报警恢复事件也补齐变量和限值上下文；同时修复恢复事件 `limit_value` 指针指向运行态字段、状态清零后通知限值被清掉的问题。`backend/tools/smokeeb045` 新增 NG 场景中的恢复验证：首帧超上限触发 `alarm.limit.enter` 后写回合格值，等待 `alarm.limit.recover` 经 WS 即时推送，并通过 HTTP 通知列表按同一 `event_uid` 读回和标记已读，再停止检测验证 `detection.result_ng`。验证通过 `backend/go test ./internal/pipeline -run "TestProcessMessageEnqueuesDetectionLimitAlarmEnterAndRecover|TestProcessMessageEnqueuesDefaultLimitAlarmWithoutDetection" -count=1`、`backend/go test -tags smoke_tools ./tools/smokeeb045`、`backend/go test ./...`、`backend/go test ./... -coverprofile all-coverage.out` 总覆盖率 72.3%、`backend/go vet ./...`、`backend/golangci-lint run ./...` 0 issues、`backend/go build ./cmd/edge-backend`；后端已重启为 PID 23492，`/health` ok，真实 smoke 项目 `31`、主任务 `46`、固定任务 `47`、合格持续任务 `48`、NG 任务 `49` 通过。 | closed
- 2026-05-31 06:03 | backend-ai/test-ai | test/tool | EB-045/048 正式 STRING 任务请求 smoke 继续覆盖组合业务：`backend/tools/smokeeb045` 新增 `start_fixed_duration_detection`、`start_qualified_hold_detection` 和 `refresh_features` 三个 data_change flow，通过 WS 写 watched STRING 请求变量验证特征值刷新会写 `features_updated` 事件，固定时长检测会自动以 `end_type=fixed_duration` 停止，合格持续检测会在测量变量落在运行快照限值内后自动以 `end_type=qualified_hold` 停止。现场实测通过：项目 `25`，请求变量 `817149116`，控制变量 `848021045`，测量变量 `866666943`，主任务 `25`，固定时长任务 `26`，合格持续任务 `27`。门禁通过 `backend/go test -tags smoke_tools ./tools/smokeeb045`、`backend/go vet -tags smoke_tools ./tools/smokeeb045`、`backend/go run -tags smoke_tools ./tools/smokeeb045 -wait 12s`、`backend/go test ./...`、`backend/go test ./... -coverprofile all-coverage.out` 总覆盖率 72.3%、`backend/go vet ./...`、`backend/golangci-lint run ./...` 0 issues、`backend/go build ./cmd/edge-backend`；`/health` ok，后端仍运行 PID 8500。 | closed
- 2026-05-31 06:11 | backend-ai/test-ai | test/tool | EB-019/045/048 通知联动 smoke 补强：`backend/tools/smokeeb045` 在 `builtin.refresh_features` 前打开 `topic=notifications` WS，触发后验证收到 `detection.features_updated` 的 `notification.event`，再通过 HTTP `/api/v1/notifications` 按同一 `event_uid` 读回未读通知并调用单条已读接口。现场实测通过：项目 `26`，请求变量 `800423698`，控制变量 `838618853`，测量变量 `831066790`，主任务 `28`，固定时长任务 `29`，合格持续任务 `30`，通知 HTTP ID `66`。门禁通过 `backend/go test -tags smoke_tools ./tools/smokeeb045`、`backend/go vet -tags smoke_tools ./tools/smokeeb045`、`backend/go run -tags smoke_tools ./tools/smokeeb045 -wait 12s`、`backend/go test ./...`、`backend/go test ./... -coverprofile all-coverage.out` 总覆盖率 72.3%、`backend/go vet ./...`、`backend/golangci-lint run ./...` 0 issues、`backend/go build ./cmd/edge-backend`；`/health` ok，后端仍运行 PID 8500。 | closed
- 2026-05-31 06:14 | backend-ai/test-ai | test/tool | EB-019/045/048 检测结果通知 smoke 补强：`backend/tools/smokeeb045` 在固定时长检测和合格持续检测自动结束后，继续验证 `detection.result_ok` 通过 `topic=notifications` WS 即时推送，并通过 HTTP 通知列表按同一 `event_uid` 读回未读通知且可标记单条已读。现场实测通过：项目 `27`，请求变量 `806090321`，控制变量 `838500467`，测量变量 `858345221`，主任务 `31`，固定时长任务 `32`，合格持续任务 `33`。门禁通过 `backend/go test -tags smoke_tools ./tools/smokeeb045`、`backend/go vet -tags smoke_tools ./tools/smokeeb045`、`backend/go run -tags smoke_tools ./tools/smokeeb045 -wait 12s`、`backend/go test ./...`、`backend/go test ./... -coverprofile all-coverage.out` 总覆盖率 72.3%、`backend/go vet ./...`、`backend/golangci-lint run ./...` 0 issues、`backend/go build ./cmd/edge-backend`；`/health` ok，后端仍运行 PID 8500。 | closed
- 2026-05-31 06:20 | backend-ai/test-ai | test/tool | EB-019/045/048 报警和 NG 结果通知 smoke 补强：`backend/tools/smokeeb045` 新增首帧超上限 NG 场景，先把测量变量写到超上限，再通过正式 STRING 任务请求启动检测，验证 `alarm.limit.enter` 经 WS 即时推送并可通过 HTTP 通知列表读回和标记已读；随后用任务流停止该检测，验证 summary 产出 `detection.result_ng`，同样走 WS/HTTP 未读已读闭环。修复 smoke 等待同一 flow 重复执行时可能命中旧 run 的问题，新增按 `min_run_id` 等待新执行记录。现场实测通过：项目 `29`，请求变量 `833432810`，控制变量 `870345172`，测量变量 `755951840`，主任务 `38`，固定时长任务 `39`，合格持续任务 `40`，NG 任务 `41`。门禁通过 `backend/go test -tags smoke_tools ./tools/smokeeb045`、`backend/go vet -tags smoke_tools ./tools/smokeeb045`、`backend/go run -tags smoke_tools ./tools/smokeeb045 -wait 12s`、`backend/go test ./...`、`backend/go test ./... -coverprofile all-coverage.out` 总覆盖率 72.3%、`backend/go vet ./...`、`backend/golangci-lint run ./...` 0 issues、`backend/go build ./cmd/edge-backend`；`/health` ok，后端仍运行 PID 8500。 | closed
- 2026-05-31 05:57 | frontend-ai/test-ai | feature/test | EB-047 前端补齐 KIO 项目初始化入口：设置中心变量模块新增“KIO 项目初始化”按钮和弹窗，支持 `project_count/project_code_prefix/raw_project_prefix/var_group/var_name_prefix/remap_var_name/enable` 配置、dry-run 预览、真实执行确认、结果摘要和明细表；执行成功后刷新项目、变量和 storage routes 查询。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`；Playwright 登录 `/#/settings` 后打开弹窗并点击预览，确认 `POST /api/v1/variables/bulk-remap/kio-projects` 请求体包含 `dry_run=true` 且返回 200，摘要显示匹配 504，截图 `output/playwright/settings-kio-remap-dryrun-smoke.png`。 | closed
- 2026-05-31 05:56 | backend-ai/test-ai | test/tool | EB-045/048 正式 STRING 任务请求 smoke 继续扩展：`backend/tools/smokeeb045` 新增 `register_report` 和 `http_request` 两个 data_change flow，通过 WS 写 watched STRING 请求变量验证 `builtin.register_report` 把报表结果写入 `detection_run_reports` 并可从检测任务详情读回，`builtin.http_request` 受控调用本机 `/health` 返回 200。现场实测通过：项目 `24`，请求变量 `752661683`，控制变量 `824993196`，测量变量 `798429688`，任务 `24`。门禁通过 `backend/go test -tags smoke_tools ./tools/smokeeb045`、`backend/go vet -tags smoke_tools ./tools/smokeeb045`、`backend/go run -tags smoke_tools ./tools/smokeeb045 -wait 12s`、`backend/go test ./...`、`backend/go test ./... -coverprofile all-coverage.out` 总覆盖率 72.3%、`backend/go vet ./...`、`backend/golangci-lint run ./...` 0 issues、`backend/go build ./cmd/edge-backend`；`/health` ok，后端仍运行 PID 8500。 | closed
- 2026-05-31 05:50 | backend-ai/test-ai | test/tool | EB-045/048 正式 STRING 任务请求 smoke 扩展：`backend/tools/smokeeb045` 现在会创建测量变量并启用 storage route，通过同一个 watched STRING 请求验证 `custom_items` 冻结到 `detection_run_standard_items`、`process_params/plc_writes` 追溯、`builtin.storage_prepare`、`builtin.update_detection_limits` 运行中更新 `limit_h/limit_hh/check_cycle_ms/hold_ms`，再验证暂停、恢复和 `task_flow_stop`。现场实测通过：项目 `23`，请求变量 `742576737`，控制变量 `739076088`，测量变量 `842238391`，任务 `23`。门禁通过 `backend/go test -tags smoke_tools ./tools/smokeeb045`、`backend/go vet -tags smoke_tools ./tools/smokeeb045`、`backend/go run -tags smoke_tools ./tools/smokeeb045 -wait 12s`、`backend/go test ./...`、`backend/go test ./... -coverprofile all-coverage.out` 总覆盖率 72.3%、`backend/go vet ./...`、`backend/golangci-lint run ./...` 0 issues、`backend/go build ./cmd/edge-backend`；`/health` ok，后端仍运行 PID 8500。 | closed
- 2026-05-31 05:37 | backend-ai/test-ai | test/tool | EB-045/048 正式 STRING 任务请求 smoke 工具落地：新增 `backend/tools/smokeeb045`，使用 `go run -tags smoke_tools ./tools/smokeeb045` 创建临时项目、STRING 请求虚拟变量、虚拟控制变量和 4 个 data_change task-flow，通过 WS `command.write_variable` 依次验证启动检测、冻结 `process_params/plc_writes`、暂停、恢复和 `task_flow_stop` 结束。现场实测通过：项目 `21`，请求变量 `724990601`，控制变量 `732388972`，任务 `21`。门禁通过 `backend/go test ./...`、`backend/go test ./... -coverprofile all-coverage.out` 总覆盖率 72.3%、`backend/go vet ./...`、`backend/golangci-lint run ./...` 0 issues、`backend/go build ./cmd/edge-backend`；`/health` ok，后端仍运行 PID 8500。 | closed
- 2026-05-31 05:31 | backend-ai/test-ai | api/test | EB-045 通知闭环补强：`builtin.refresh_features` 不再只刷新 `detection_run_features`，现在会复用统一特征值刷新路径，写入 `detection_run_events(features_updated)` 并通过通知总线发布 `detection.features_updated`，让手动/任务流触发的特征值刷新和检测停止后的自动刷新语义一致。验证通过 `backend/go test ./internal/pipeline`、`backend/go test ./...`、`backend/go test ./... -coverprofile all-coverage.out` 总覆盖率 72.3%、`backend/go vet ./...`、`backend/golangci-lint run ./...`、`backend/go build ./cmd/edge-backend`。后端已重启为 PID 8500，`/health` ok，tags=1041。 | closed
- 2026-05-31 05:35 | frontend-ai/test-ai | feature/test | 驾驶舱按 Codex 官网产品页气质再收敛：背景改成低饱和浅灰蓝产品层次，卡片统一为更轻的白蓝玻璃面板、细边框、轻阴影和工位同源暖光边缘；弱化标题 SVG 背板和装饰线，指标卡保留实时值、上下限、OK/NG、优化率；3D 模型缩放和相机重新平衡，完整落在中间区域且不再顶到顶部卡片或底部裁切。顺手收口工位页项目参数迁移残留，把可见状态和报警弹窗从旧 `selectedDevice/statusDevice` 引用改为项目语义。验证通过 `desktop/npm run lint`、`desktop/npm run build`；Playwright 1920×1080 登录后截图 `output/playwright/model-cockpit-codex-pass-3.png`。 | closed
- 2026-05-31 05:22 | backend-ai/test-ai | api/test | EB-045 任务流配置参数边界和精确 ID 校验补齐：确认配置参数包含检测配置、`process_params` 工艺/报表参数和 `plc_writes` PLC 下设计划，进风口面积等值只放 `process_params`，PLC 下设用 `value_from` 引用；后端 `steps_json` 参数绑定的 `source=event` 允许 `trigger_var_id_text`，`GET /api/v1/task-modules` 的 JavaScript runtime note 同步说明 string var_id 与精确 ID 字段。验证通过 `backend/go test ./internal/runtime/handlers`、`backend/go test ./internal/pipeline ./internal/services`、`backend/go test ./...`、`backend/go test ./... -coverprofile all-coverage.out` 总覆盖率 72.3%、`backend/go vet ./...`、`backend/golangci-lint run ./...`、`backend/go build ./cmd/edge-backend`。后端已重启为 PID 41496，`/health` ok，tags=1041。 | closed
- 2026-05-31 05:14 | frontend-ai/test-ai | feature/test | EB-037 storage route 管理端到端闭合：设置中心“存储路由”模块新增搜索框和启用状态筛选，可按路由编号、变量、表/列、项目定位 route。Playwright 登录 `/#/settings` 后进入存储路由，创建临时 route `codex-smoke-604921`，请求 `POST /api/v1/storage-routes` 携带字符串 `var_id`、`project_id=1`、`enabled=true`；筛选定位该行后编辑为 `enabled=false`，`PATCH /api/v1/storage-routes/529` 返回成功；再通过删除确认调用 `DELETE /api/v1/storage-routes/529` 并确认筛选结果消失。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`、`git diff --check`，截图 `output/playwright/settings-storage-route-crud-smoke.png`。EB-037 前端侧闭合。 | closed
- 2026-05-31 05:12 | backend-ai/frontend-ai/test-ai | api/test | 精确变量 ID 残留风险继续收敛：任务流 JavaScript `realtime.get/getMany/write` 支持字符串形式 64 位变量 ID，脚本内 `trigger.var_id_text`、`context.trigger_var_id_text`、实时快照和写变量结果都能拿到精确字符串；`POST /api/v1/variables/bulk-remap/kio-projects` 响应项新增 `var_id_text`；前端 shared DTO 把 `VariableConfig`、`TagSnapshot`、bulk remap item 的 `var_id` 改为 `VarIdentifier`。验证通过 `backend/go test ./internal/pipeline ./internal/services`、`backend/go test ./...`、`backend/go test ./... -coverprofile all-coverage.out` 总覆盖率 72.3%、`backend/go vet ./...`、`backend/golangci-lint run ./...`、`backend/go build ./cmd/edge-backend`、`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`；后端已重启为 PID 40428，`/health` ok，tags=1041。 | closed
- 2026-05-31 05:05 | backend-ai/frontend-ai/test-ai | api/test | WS 精确变量 ID 契约补齐：运行期 `subscribe.var_ids[]` 支持数字或字符串形式 64 位 ID，`connection.ready/subscription.updated` 响应新增 `var_id_texts`，`command.write_variable` 回执 `result` 新增 `var_id_text` 并统一 `project_confirmed` 小写 JSON 字段；任务流内置写变量结果也返回 `var_id_text`。前端 shared DTO 增加 `RealtimeWebSocketSubscription.var_id_texts` 和 `VariableWriteResult`。验证通过 `backend/go test ./internal/runtime/handlers ./internal/services ./internal/pipeline`、`backend/go test ./...`、`backend/go test ./... -coverprofile all-coverage.out` 总覆盖率 72.3%、`backend/go vet ./...`、`backend/golangci-lint run ./...`、`backend/go build ./cmd/edge-backend`、`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`；后端已重启为 PID 8020，`/health` ok，tags=1041。 | closed
- 2026-05-31 05:03 | frontend-ai/test-ai | test | EB-034 运行中同步保存 smoke 完成：Playwright 登录 `/#/settings`，打开已分配变量“编辑配置”，点击“同步到当前运行任务快照”并保存；捕获到 `PATCH /api/v1/variables/9212397624135540847` 请求体包含 `apply_to_running=true`，后端返回 200。截图 `output/playwright/settings-apply-to-running-save-smoke.png`。本项此前已验证默认报警字段、风险提示、工位运行标准快照展示；EB-034 前端侧闭合。 | closed
- 2026-05-31 04:59 | frontend-ai/test-ai | feature/test | EB-037/047 历史页存储快照和项目参数收口：历史查询页运行存储快照入口已验证可用，任务链接优先使用 `project_id` 查询历史，旧 `device_id` 只作为兼容读取；验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`。Playwright 1920x1080 登录 `/#/history?task_id=20&project_id=1&test_no=RUN-20260530204958`，确认历史请求为 `/api/v1/history/data?task_id=20&project_id=1&limit=5000` 且不含 `device_id`，点击“存储快照”后 `/api/v1/detection-runs/20/storage-routes` 返回 200，弹窗标题和 route 列可见，截图 `output/playwright/history-storage-snapshot-project-param-smoke.png`。 | closed
- 2026-05-31 04:53 | backend-ai/frontend-ai/test-ai | api/test | 精确变量 ID 契约继续补齐到通知、报警、历史和特征值：后端 `RuntimeNotification`、HTTP 通知列表、`GET /api/v1/limit-alarms`、`GET /api/v1/history/data`、`GET /api/v1/detection-runs/{id}/features` 响应新增 `var_id_text`；前端 shared DTO、工位页存储快照和历史页存储快照优先显示/使用字符串 ID。验证通过 `backend/go test ./internal/models ./internal/runtime/handlers`、`backend/go test ./...`、`backend/go test ./... -coverprofile all-coverage.out` 总覆盖率 72.2%、`backend/go vet ./...`、`backend/golangci-lint run ./...`、`backend/go build ./cmd/edge-backend`、`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`；后端已重启为 PID 3484，`/health` ok，tags=1041。 | closed
- 2026-05-31 04:53 | frontend-ai/test-ai | feature/test | EB-034 运行标准快照前端展示：工位页当前 running 任务新增“标准快照”按钮，复用运行快照接口 `GET /api/v1/detection-runs/{id}`，在弹窗表格中并排展示检测标准限值、变量默认报警开关、变量默认上下限、默认回差和超限/恢复保持时间，便于确认检测业务限值与变量资产默认值的区别。三语文案已补齐。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`、`git diff --check`；Playwright 1920x1080 登录工位页点击“标准快照”，确认接口 `GET /api/v1/detection-runs/20` 返回 200，标题、检测限值、变量默认限值和默认报警列可见，截图 `output/playwright/station-run-standard-snapshot-smoke.png`。 | closed
- 2026-05-31 04:43 | backend-ai/frontend-ai/test-ai | api/test | 精确变量 ID 契约扩大到检测配置和 storage routes：后端 `POST /api/v1/variables`、检测标准项和存储路由 `var_id` 请求支持字符串形式 64 位 ID；`DetectionStandardItem`、`DetectionRunStandardItem`、`StorageRoute`、`DetectionRunStorageRoute` 响应新增 `var_id_text`。前端 shared DTO、设置页和检测配置页改为优先使用 `var_id_text`/字符串 ID，避免浏览器 number 精度丢失。同步确认“配置参数”包含检测配置、`process_params` 工艺/报表参数和 `plc_writes` PLC 下设计划，进风口面积等参数只放 `process_params`，下设通过 `value_from` 引用。验证通过 `backend/go test ./internal/runtime/handlers`、`backend/go test ./...`、`backend/go test ./... -coverprofile all-coverage.out` 总覆盖率 72.3%、`backend/go vet ./...`、`backend/golangci-lint run ./...`、`backend/go build ./cmd/edge-backend`、`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`；后端已重启为 PID 11184，`/health` ok，tags=1041。 | closed
- 2026-05-31 04:35 | frontend-ai/test-ai | feature | EB-034 变量默认报警前端适配：设置中心变量编辑和虚变量创建新增“默认报警”分区，接入 `default_alarm_enabled/default_limit_ll/default_limit_l/default_limit_h/default_limit_hh/default_limit_deadband/default_violation_hold_ms/default_recover_hold_ms`；变量编辑新增 `apply_to_running` 运行中同步选项和黄色风险提示，说明只同步当前 running 任务变量默认报警快照，不修改检测标准业务限值。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`；Playwright 登录 `/#/settings`，打开创建虚变量和编辑已分配变量弹窗，确认默认报警字段、运行中同步选项和风险提示可见，截图 `output/playwright/settings-variable-default-alarm-smoke.png`。 | closed
- 2026-05-31 04:31 | backend-ai/test-ai | feature/test | EB-045/046/048 精确变量 ID 和任务配置参数契约补强：后端变量、实时快照、任务流变量和任务流执行记录响应新增 `var_id_text/trigger_var_id_text` 精确字符串字段；WS `command.write_variable`、任务流 `vars[].var_id` 和内置模块 `var_id/plc_writes[].var_id/custom_items[].var_id` 支持字符串形式 64 位 ID，避免浏览器 `number` 精度丢失导致 STRING 虚拟变量或 PLC 下设写错目标。前端任务请求弹窗改用精确字符串 ID 选择 watched STRING 变量和 PLC 下设变量；配置参数口径保持为检测配置 + `process_params` 工艺/报表参数 + `plc_writes` PLC 下设计划，进风口面积等参数只在 `process_params` 填一次，PLC 下设用 `value_from` 引用。验证通过：`backend/go test ./internal/runtime/handlers`、`backend/go test ./internal/pipeline`、`backend/go test ./...`、`backend/go test ./... -coverprofile all-coverage.out` 总覆盖率 72.3%、`backend/go vet ./...`、`backend/go build ./cmd/edge-backend`、`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`；后端已重启为 PID 36880，`/health` ok，现场 smoke 创建自动大 ID STRING 虚拟变量 `9212397624135540849`、用字符串 var_id 建 task-flow、WS 写入触发成功，run_id=8/status=success。 | closed
- 2026-05-31 04:25 | frontend-ai/test-ai | feature | 驾驶舱按 Codex 官网产品页方向重新降噪：移除右侧趋势曲线，右侧改为实时值、上下限、OK/NG 和优化率信息卡；撤掉驾驶舱内联玻璃覆盖，统一使用低饱和玻璃卡、细边框、轻阴影和工位同源 600px 暖光边缘；标题 SVG 背板、背景和卡片文字层级改为更克制的产品面板风格，3D 模型继续作为中心主视觉并下移放大。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`；Playwright 1920×1080 截图 `output/playwright/model-cockpit-codex-pass-1.png`，确认曲线节点为 0。 | closed
- 2026-05-31 04:00 | frontend-ai/test-ai | test | EB-048 通知中心补齐筛选：顶栏通知弹层新增未读/全部、通知类型和项目筛选，查询走 `GET /api/v1/notifications` 的 `unread/type/project_id` 参数，并收紧弹层高度/定位避免筛选条被顶出视口。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`；Playwright 登录后打开通知中心，点击未读、选择“检测开始”和项目，确认最终请求为 `/api/v1/notifications?unread=true&type=detection.run_started&project_id=2&limit=20`，截图 `output/playwright/notification-filters-smoke.png` | closed
- 2026-05-31 03:46 | frontend-ai/test-ai | test | `/#/model-cockpit` 视觉按 Codex 官网产品页气质再次收敛：移除重 HUD 标题背板，背景改为更低饱和蓝白产品层次，卡片统一为更通透的浅玻璃面板并保留工位同源暖光边缘，去掉 3D 模型上的临时诊断流线，模型改为更干净的产品展示。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，1920×1080 截图 `output/playwright/model-cockpit-codex-polish-v2-1920x1080.png` | closed
- 2026-05-31 03:34 | frontend-ai/test-ai | test | 系统设置日志体验修正：运行日志正文不再嵌在设置页或侧向窗口里，改为点击“运行日志预览”打开居中大弹窗；设置页只保留日志摘要、刷新和打开日志位置按钮。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，Playwright 登录进入 `/#/settings` 后打开系统模块和日志弹窗，确认 `.settings-log-modal` 可见、`.ant-drawer` 数量为 0，截图 `output/playwright/settings-log-modal-smoke.png` | closed
- 2026-05-31 03:44 | frontend-ai/test-ai | feature | EB-046/048 任务页新增正式 STRING 虚拟变量触发入口：“发送任务请求”弹窗可选择 watched `STRING` 虚拟变量，结构化填写 `command/project_id/test_no/standard_id/duration_sec/qualified_hold_ms/limit_check_enabled/enable_storage/enable_alarm/operator_note/report_template_id/custom_items`，并把进风口面积等工艺参数放入 `process_params`、PLC 下设计划放入 `plc_writes`；提交时通过 WS `command.write_variable` 写入虚拟变量触发任务系统，不走手动运行接口也不绕过后端写 PLC。验证通过 `desktop/npm run lint`、`desktop/npm run test -- --run`、`desktop/npm run build`，Browser smoke 登录 `/#/tasks` 后打开任务请求弹窗，确认“工艺/报表参数”“PLC 下设计划”和 `inlet_area_m2` 默认行可见，截图 `output/playwright/task-request-modal-smoke.png` | closed
- 2026-05-31 03:50 | frontend-ai/test-ai | feature | EB-046/048 任务请求弹窗补“请求 JSON 预览”和校验：预览区使用同一套 payload 构造逻辑展示最终写入 watched `STRING` 虚拟变量的 JSON；`custom_items` JSON 无效时禁用提交；PLC 下设区提示必须选择 `var_id` 才会进入最终 JSON，避免用户误以为未完成行已发出。验证通过 `desktop/npm run lint`、`desktop/npm run test -- --run`、`desktop/npm run build`；Playwright smoke 登录 `/#/tasks` 打开弹窗，确认预览、`start_detection`、`process_params`、`inlet_area_m2`、PLC 下设区和 `var_id` 提示可见，截图 `output/playwright/task-request-preview-smoke.png` | closed
- 2026-05-31 03:57 | frontend-ai/test-ai | feature | EB-046/048 任务请求弹窗补清“配置参数”语义：配置参数明确包含检测配置、过程参数和 PLC 下设引用；选择请求变量时显示是否有启用任务流监听该 watched `STRING` 变量，避免写入后没有业务响应。验证通过 `desktop/npm run lint`、`desktop/npm run test -- --run`、`desktop/npm run build`；Playwright smoke 登录 `/#/tasks` 打开弹窗，确认“配置参数”、进风口面积 `inlet_area_m2` 和监听任务流提示可见，截图 `output/playwright/task-request-config-params-smoke.png` | closed
- 2026-05-31 04:05 | frontend-ai/test-ai | feature | EB-046/048 任务请求弹窗扩展正式业务命令：新增 `stop_detection/pause_detection/resume_detection/mute_detection_alarms/storage_snapshot/update_detection_limits/refresh_detection_features/register_report`，按命令展示运行控制、运行中限值调整和报表登记字段；payload 构造按命令过滤无关字段，避免开始检测参数泄漏到限值调整或报表登记请求。验证通过 `desktop/npm run lint`、`desktop/npm run test -- --run`、`desktop/npm run build`；Playwright smoke 切换 `update_detection_limits` 和 `register_report`，确认对应字段和 JSON 预览，截图 `output/playwright/task-request-command-coverage-smoke.png` | closed
- 2026-05-31 04:12 | frontend-ai/test-ai | feature | EB-046/048 任务请求弹窗补齐剩余低代码命令：新增 `storage_prepare` 和 `http_request` 参数区，`storage_prepare` 只准备项目宽表/动态列不写采样，`http_request` 支持 `url/headers/body/timeout_ms` 且 headers 作为 JSON 参与预览校验；payload 构造继续按命令过滤无关字段。验证通过 `desktop/npm run lint`、`desktop/npm run test -- --run`、`desktop/npm run build`；Playwright smoke 切换 `storage_prepare` 和 `http_request`，确认字段与 JSON 预览，截图 `output/playwright/task-request-storage-http-smoke.png` | closed
- 2026-05-31 04:16 | frontend-ai/test-ai | test | EB-046/048 正式 STRING 虚拟变量触发链端到端 smoke 通过：登录后创建临时 `STRING` 虚拟变量和临时 data_change 任务流，WS `command.write_variable` 写入 `{command:"codex_e2e_context"}` 返回 `command.ack`，后端 `task-flow-runs` 记录 `run_id=6/status=success`，随后删除临时任务流和变量。首次使用后端自动生成的大 64 位 `var_id` 时暴露 JS number 精度风险，已用显式小范围 `var_id=700766631` 复测通过，并清理首次遗留变量 `9212397624135540849`；后续需要评估前端是否应把大 `var_id` 当 string 处理。 | closed
- 2026-05-31 03:30 | frontend-ai | test | EB-048 通知中心目标跳转完成：通知项点击后会先标记已读；若通知带 `task_id` 且账号有 `view_history`，跳转到 `/#/history?task_id=...&device_id=...&test_no=...`，否则按 `project_id` 回到对应工位。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，Playwright 点击真实通知 `RUN-20260530190426` 后进入历史页并触发 `GET /api/v1/history/data?task_id=19&project_id=1&limit=5000` 200，截图 `output/playwright/notification-target-navigation-smoke.png` | closed
- 2026-05-31 03:25 | backend-ai/test-ai | api | EB-045 预置检测模板补齐 PLC 下设顺序：`GET /api/v1/task-flow-templates` 中变量请求启动、固定时长检测、合格持续检测模板都会先生成 `builtin.write_control_variables` 再生成 `builtin.start_detection_run`，并透传 `process_params/plc_writes/operator_note/report_template_id`，避免固定时长/合格持续模板只冻结 PLC 下设计划但未实际下设。验证通过 handler 定向回归、`go test ./...`、覆盖率 72.5%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 39948，`/health` ok，tags=1041，登录后 smoke `task-flow-templates=13` 且 `builtin.write_control_variables` 出现 4 次 | closed
- 2026-05-31 03:23 | frontend-ai | test | EB-048 通知中心接入 WS 即时刷新：新增 `subscribeRealtimeWebSocket` 封装，顶栏 Shell 订阅 `topic=notifications`，收到 `notification.event` 后刷新通知列表和未读数，并带 3 秒重连；验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，Playwright 登录后确认页面打开 `ws://127.0.0.1:18080/api/v1/ws?...&topic=notifications`，Node WS smoke 确认后端返回 `connection.ready` 且订阅 topics 为 `notifications` | closed
- 2026-05-31 03:19 | backend-ai/test-ai | api | 配置资源缺失语义继续收紧：`POST/DELETE /api/v1/detection-standards/{id}/favorite`、`DELETE /api/v1/detection-standards/{id}`、`PATCH/DELETE /api/v1/report-templates/{id}`、`PATCH/DELETE /api/v1/storage-routes/{id}` 在目标资源不存在时返回 `404`；检测标准和报表模板引用保护仍返回 `409`，storage route 已冻结到检测运行时删除仍返回 `409`。验证通过 handler/kernel/repository 定向回归、`go test ./...`、覆盖率 72.5%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 41596，`/health` ok，tags=1041，登录后 smoke 七个缺失配置资源接口均返回 404 | closed
- 2026-05-31 03:14 | frontend-ai | test | EB-048 全局通知中心首段接入：顶栏新增通知铃铛和未读角标，弹层调用 `GET /api/v1/notifications/unread-count` 与 `GET /api/v1/notifications?limit=20`，支持单条点击标记已读和全部已读；验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，Playwright 模拟登录、打开通知弹层，确认未读/列表接口 200、渲染 20 条通知，截图 `output/playwright/global-notifications-popover-smoke.png`；另验证点击首条通知调用 `POST /api/v1/notifications/22/read` 返回 200 | closed
- 2026-05-31 03:12 | backend-ai/test-ai | api | 资源编辑/删除缺失语义继续收紧：`PATCH /api/v1/projects/{id}`、`PATCH /api/v1/variables/{id}`、`PATCH /api/v1/variables/{id}/assignment`、`DELETE /api/v1/variables/{id}`、`PATCH /api/v1/gateway-configs/{id}`、`DELETE /api/v1/gateway-configs/{id}` 在目标资源不存在时统一返回 `404`，不再返回 `500` 或静默成功；变量分配/删除和网关删除仓储层会检查 `RowsAffected=0`。验证通过定向 handler/kernel 回归、`go test ./...`、覆盖率 72.3%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 27444，`/health` ok，tags=1041，登录后 smoke 六个缺失资源写接口均返回 404 | closed
- 2026-05-31 03:05 | backend-ai/test-ai | api | 检测标准缺失资源错误语义收紧：`PATCH /api/v1/detection-standards/{id}` 和 `PUT /api/v1/detection-standards/{id}/items` 在标准不存在时返回 `404`，不再暴露为 `500`；handler 和 kernel 回归已覆盖。同步确认 EB-045 配置参数语义：`process_params` 承载进风口面积等工艺/报表参数，`plc_writes` 只描述受控 PLC 下设计划，正式入口仍通过 watched `STRING` 虚拟变量触发任务系统。验证通过定向 handler/runtime/pipeline 测试、`go test ./...`、覆盖率 72.1%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 41100，`/health` ok，tags=1041，smoke 两个缺失检测标准接口均为 404，`task-modules` 返回 `process_params/plc_writes` | closed
- 2026-05-31 03:06 | frontend-ai | test | EB-037 工位页接入运行存储路由快照：新增 `getDetectionRunStorageRoutes` typed API 和“存储快照”弹窗，当前检测运行中可查看当次冻结 route 的目标、表/列、触发、周期、死区和首帧存储；验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，Playwright 模拟登录 `/#/?device_id=1`、点击开始检测、打开存储快照、确认 `GET /api/v1/detection-runs/19/storage-routes` 200 且弹窗渲染 route 行，再停止检测，截图 `output/playwright/station-storage-routes-snapshot-smoke.png` | closed
- 2026-05-31 02:55 | frontend-ai | test | `/#/model-cockpit` 按 Codex 官网产品页方向继续收敛：背景改为低饱和蓝白产品层次，标题 SVG 从重 HUD 蓝板改为轻量半透明矢量框，卡片保留工位同源 600px 暖光边缘但降低厚重阴影和杂色，顶部/指标卡文字层级改为深色产品面板风格；验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，Playwright 1920×1080 登录后截图 `output/playwright/model-cockpit-codex-clean-1920x1080.png`，相关接口 200 | closed
- 2026-05-31 02:50 | frontend-ai | test | EB-046 任务页动态参数表单完成：按 `task-modules.params_schema` 渲染模块参数，支持 project/variable/select/boolean/number/object/array/text/code/any，并在编辑时同步 `action_payload` 与第一条 `steps_json` step；验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，Playwright 模拟登录后进入 `/#/tasks`、打开模板创建弹窗并切换 `builtin.write_variable`，确认参数字段渲染、接口 200、payload 同步，截图 `output/playwright/task-flows-module-params-smoke.png` | closed
- 2026-05-31 02:25 | frontend-ai | test | `/#/model-cockpit` 视觉重调：参考 Codex 官网的克制产品页层级，恢复工位同源动态蓝色光场和玻璃受光边缘，弱化标题 SVG 背板，顶部卡片改为图标/主值/说明结构，3D 模型固定中性光照并放大下移；验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`，1920×1080 截图 `output/playwright/model-cockpit-redesign-1920-v2.png` | closed
- 2026-05-31 02:59 | backend-ai/test-ai | api | 后端公共错误映射修正：`services.HTTPStatusForError` 现在把 `gorm.ErrRecordNotFound` 映射为 HTTP `404`，引用保护和同项目重复 running 仍为 `409`，普通校验错误仍为 `400`；复用该映射的检测任务、存储路由、任务执行记录和 WS 命令错误语义更一致。新增 service 回归断言。验证通过定向 service/handler/runtime 测试、`go test ./...`、覆盖率 72.1%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 41456，`/health` ok 且 KIO gateway active，登录后 smoke 缺失检测任务 stop 和缺失 task-flow run 均返回 404 | closed
- 2026-05-31 02:55 | backend-ai/test-ai | api | EB-045 任务执行记录筛选参数硬化：`GET /api/v1/task-flow-runs` 现在要求 `project_id/flow_id/trigger_var_id/origin_flow_id` 为正数，并要求 `from <= to`，避免前端把空 ID 或反向时间范围误传后只得到空列表；新增 handler 回归覆盖四类非法 ID 和反向时间范围。验证通过定向 handler 测试、`go test ./...`、覆盖率 72.1%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 27040，`/health` ok 且 KIO gateway active，登录后 smoke `project_id=0`、`trigger_var_id=-1`、反向时间范围均返回 400 | closed
- 2026-05-31 02:50 | backend-ai/test-ai | api | EB-045 任务执行记录不存在语义修正：`GET /api/v1/task-flow-runs/:id` 和 `/task-flow-runs/:id/sql-logs` 在 run 不存在时返回 `404`，避免前端把不存在的执行记录误显示成空日志或普通参数错误；新增 handler 回归覆盖缺失 run 详情和缺失 run SQL 日志。验证通过定向 handler 测试、`go test ./...`、覆盖率 71.8%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 3440，`/health` ok 且 KIO gateway active，登录后 smoke 两个不存在 run 接口均返回 404 | closed
- 2026-05-31 02:46 | backend-ai/test-ai | api | EB-045 任务 SQL 日志查询参数硬化：`GET /api/v1/task-flow-runs/:id/sql-logs` 现在拒绝 `limit<=0`，避免前端任务页 SQL 日志传错 limit 后被仓储层静默改成默认 100 条；新增 handler 回归覆盖 `limit=0`。验证通过定向 handler 测试、`go test ./...`、覆盖率 71.8%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 41604，`/health` ok 且 KIO gateway active，登录后 smoke `task-flow-runs/1/sql-logs?limit=0` 返回 400 | closed
- 2026-05-31 02:42 | backend-ai/test-ai | api | EB-045 任务执行记录分页参数硬化：`GET /api/v1/task-flow-runs` 现在拒绝 `limit<=0` 和 `offset<0`，与通知、报警等分页接口保持一致，避免前端任务页传错分页时被仓储层静默归一化成默认页；新增 handler 回归覆盖 `limit=0`、`limit=-1`、`offset=-1`。验证通过定向 handler 测试、`go test ./...`、覆盖率 71.8%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 25584，`/health` ok 且 KIO gateway active，登录后 smoke `task-flow-runs?limit=0` 和 `offset=-1` 均返回 400 | closed
- 2026-05-31 02:35 | backend-ai/test-ai | api | EB-045 任务流时间字段硬化：`POST /api/v1/task-flows` 拒绝负数 `timeout_ms/cooldown_ms/hold_ms/schedule_interval_ms`，`PATCH` 显式提交 `timeout_ms` 时必须大于 0，其他时间字段不能为负；避免创建时静默归零、编辑时写入即时超时或负数间隔。新增 create/patch 回归。验证通过定向 handler 测试、`go test ./...`、覆盖率 71.8%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 22240，`/health` ok 且 KIO gateway active，登录后 smoke `timeout_ms=-1` 返回 `400 timeout_ms cannot be negative` | closed
- 2026-05-31 02:30 | backend-ai/test-ai | api | EB-045 schedule 任务流配置硬化：`POST/PATCH /api/v1/task-flows` 要求 `trigger_type=schedule` 的任务必须配置 `schedule_interval_ms > 0` 或兼容期 `cooldown_ms > 0`，避免扫描器永久跳过保存成功的空转定时任务；新增 create 和 patch 回归。验证通过定向 handler 测试、`go test ./...`、覆盖率 71.8%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 8060，`/health` ok 且 KIO gateway active，登录后 smoke schedule 无间隔返回 `400 schedule task flow requires schedule_interval_ms or cooldown_ms greater than 0` | closed
- 2026-05-31 02:26 | backend-ai/test-ai | api | EB-045 data_change 任务流配置硬化：`POST/PATCH /api/v1/task-flows` 要求 `trigger_type=data_change` 的任务至少绑定一个 `watch` 变量，因为只有 watch 变量会进入 `var_id -> flow_ids` 倒排索引；新增 create 和 patch 回归。验证通过定向 handler 测试、`go test ./...`、覆盖率 71.8%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 42568，`/health` ok 且 KIO gateway active，登录后 smoke `data_change` 仅绑定 read 变量返回 `400 data_change task flow requires at least one watch var` | closed
- 2026-05-31 02:21 | backend-ai/test-ai | api | EB-045 任务流变量绑定硬化：`POST/PATCH /api/v1/task-flows` 拒绝 `vars[].var_id <= 0`，避免保存无法进入 `var_id -> flow_ids` 倒排索引的 watch/read/write 绑定；新增 create/patch 回归。验证通过定向 handler 测试、`go test ./...`、覆盖率 71.7%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 39292，`/health` ok 且 KIO gateway active，登录后 smoke `var_id=0` 创建 task-flow 返回 `400 invalid task flow var_id: 0` | closed
- 2026-05-31 02:17 | backend-ai/test-ai | api | EB-045 任务流 step code 唯一性硬化：`POST/PATCH /api/v1/task-flows` 拒绝 `steps_json` 中重复的显式 `code`，避免多步骤结果写入 run context 时互相覆盖；执行器 `taskFlowSteps` 同步修剪 code 并防御历史脏配置。新增 handler create/patch 回归和 pipeline 重复 code 回归。验证通过定向 handler/pipeline 测试、`go test ./...`、覆盖率 71.7%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 9632，`/health` ok 且 KIO gateway active，登录后 smoke 重复 step code 创建 task-flow 返回 `400 duplicate task flow step code: same` | closed
- 2026-05-31 02:09 | backend-ai/test-ai | api | EB-045 空步骤链配置硬化：`POST/PATCH /api/v1/task-flows` 拒绝显式 `steps_json=[]`，避免保存后任务运行成功但没有执行任何模块；执行器 `taskFlowSteps` 也增加同样防御，防止历史脏配置静默成功。新增 handler 回归覆盖 create/patch 空步骤链。验证通过定向 handler 和 pipeline 测试、`go test ./...`、覆盖率 71.6%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 36744，`/health` ok，登录后 smoke 空步骤链创建 task-flow 返回 `400 steps_json cannot be empty` | closed
- 2026-05-31 02:05 | backend-ai/test-ai | api | EB-045 任务流 event 参数 key 校验硬化：`POST/PATCH /api/v1/task-flows` 在 `steps_json[].params` 使用 `source=event` 时提前校验 `key`，只允许执行器真实支持的 `trigger_type/project_id/trigger_var_id/trigger_value/gateway_id/topic/origin_flow_id/origin_run_id/depth/request_id/at`，避免保存后运行时才报 unknown event key。新增 handler 回归覆盖 create/patch 的坏 event key。验证通过定向 handler 测试、`go test ./...`、覆盖率 71.6%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 41056，`/health` ok，登录后 smoke 坏 event key 创建 task-flow 返回 `400 invalid task flow event key at params.project: project` | closed
- 2026-05-31 02:00 | backend-ai/test-ai | api | EB-045 任务流步骤参数绑定校验硬化：`POST/PATCH /api/v1/task-flows` 现在会递归校验 `steps_json[].params` 中显式声明的 `source`，只允许 `literal/trigger_param/event/context`，非法来源直接返回 `400`，避免保存后运行时才因参数绑定失败。新增 handler 回归覆盖 create/patch 的坏参数来源。验证通过定向 handler 测试、`go test ./...`、覆盖率 71.6%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 10288，`/health` ok，登录后 smoke 坏参数来源创建 task-flow 返回 `400 invalid task flow param source at params.project_id: trigger` | closed
- 2026-05-31 01:56 | backend-ai/test-ai | api | EB-045 任务流配置必填字段硬化：`POST /api/v1/task-flows` 要求 `project_id > 0`、非空 `flow_code` 和非空 `name`；`PATCH` 拒绝 `project_id=0` 或空白 `flow_code/name`，避免保存不可识别的空壳任务配置。新增 handler 回归覆盖缺项目、空编码、空名称和 patch 改坏字段。验证通过定向测试、`go test ./...`、覆盖率 71.6%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 2648，`/health` ok，登录后 smoke 缺 `project_id` 创建 task-flow 返回 `400 project_id is required` | closed
- 2026-05-31 01:49 | backend-ai/test-ai | api | EB-045 任务流配置写入端硬化：`POST/PATCH /api/v1/task-flows` 和列表 `trigger_type` 筛选新增枚举校验，非法 `trigger_type/action_type/vars.role/steps_json[].module` 直接返回 `400`，避免保存不可运行的任务配置；同时规范化合法枚举大小写和空白。新增 handler 回归覆盖坏触发类型、坏动作类型、坏 step module、坏变量角色和坏列表筛选。验证通过定向测试、`go test ./...`、覆盖率 71.6%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 8608，`/health` ok，登录后 smoke 创建坏 action task-flow 返回 400 | closed
- 2026-05-31 01:42 | backend-ai/test-ai | api | EB-045 任务执行记录查询参数硬化：`GET /api/v1/task-flow-runs` 对 `status` 和 `trigger_type` 做枚举校验，非法值返回 `400`，避免前端任务页传错筛选项时静默得到空列表；同一 handler 回归覆盖非法 `status=done` 和 `trigger_type=unknown`。验证通过定向测试、`go test ./...`、覆盖率 71.5%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 41508，`/health` ok，登录后 smoke `task-flow-runs?status=done` 返回 400 | closed
- 2026-05-31 01:38 | backend-ai/test-ai | test | EB-045 任务执行记录查询回归补强：新增 handler 测试 `TestTaskFlowsHandlerListRunsFiltersFailedAndTimeoutDistinctly`，构造业务失败文本含 timeout 但状态为 `failed` 的记录和真实 `timeout` 记录，验证 `GET /api/v1/task-flow-runs?status=failed|timeout` 分开返回，支撑前端任务页执行记录筛选。验证通过定向测试、`go test ./...`、覆盖率 71.5%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`，`/health` ok | closed
- 2026-05-31 01:36 | backend-ai/test-ai | api | EB-045 任务模块 schema 对齐配置参数语义：`GET /api/v1/task-modules` 的 `builtin.start_detection_run.params_schema` 显式暴露 `process_params` 和 `plc_writes`，前端动态表单可把检测配置、工艺/报表参数和 PLC 下设计划作为同一个 STRING 虚拟变量业务参数包展示；新增 handler 回归断言 schema 包含进风口面积语义。验证通过定向 handler 测试、`go test ./...`、覆盖率 71.5%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；后端已重启为 PID 4984，`/health` ok，登录后 smoke `task-modules=17` 且启动检测模块返回 `process_params/plc_writes` | closed
- 2026-05-31 01:30 | backend-ai/test-ai | test | EB-045 真超时回归补齐：新增 `TestTaskFlowJavaScriptTimeoutStatus`，用真实 JavaScript 无限循环触发 Goja interrupt，验证 `task_flow_runs` 语义仍会返回 `timeout`，同时继续保证业务错误文本中的 timeout 不误归类。验证通过定向测试、`go test ./...`、覆盖率 71.5%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`，`/health` ok 且 KIO gateway active、队列为 0 | closed
- 2026-05-31 01:26 | backend-ai/test-ai | test | EB-045 任务执行状态语义收紧：`task_flow_runs.status=timeout` 只用于真实上下文/网络/脚本超时，不再因为普通业务错误文本包含 `timeout` 就误归类；PLC ack 等业务失败保持 `failed`。新增 `TestTaskFlowStatusForErrDoesNotClassifyBusinessTimeoutTextAsTimeout`。验证通过定向测试、`go test ./...`、覆盖率 71.4%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`，`/health` ok 且 KIO gateway active、队列为 0 | closed
- 2026-05-31 01:23 | backend-ai/test-ai | test | EB-045 PLC 写入失败保护补强：新增 `TestTaskFlowStringPayloadStopsDetectionWhenPLCWriteFails`，覆盖 `process_params` 可解析但后端变量写入 service 返回失败时，任务流必须失败、检测任务不创建，并保留失败审计。验证通过定向测试、`go test ./...`、覆盖率 71.4%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`，`/health` ok 且 KIO gateway active、队列为 0 | closed
- 2026-05-31 01:18 | backend-ai/test-ai | test | EB-045 PLC 参数失败保护补强：新增 `TestTaskFlowStringPayloadStopsDetectionWhenPLCParamMissing`，覆盖 watched STRING 请求里的 `plc_writes.value_from` 指向不存在参数时，任务流必须失败、变量 writer 不被调用、检测任务不创建，并写入失败审计。验证通过定向测试、`go test ./...`、覆盖率 71.4%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`，`/health` ok 且 KIO gateway active | closed
- 2026-05-31 01:18 | backend-ai | decision | EB-045 配置参数语义再次对齐：一次检测启动的 STRING 虚拟变量 payload 是业务参数包，不只是检测上下限；检测配置进入运行快照，`process_params` 承载进风口面积等工艺/报表参数，`plc_writes` 通过 `value_from` 引用同一来源并由 `builtin.write_control_variables` 受控下设 PLC。已同步 `backend/docs/边缘端全链路数据流转与分发图.md` 和 `backend/docs/backend-architecture.md` | closed
- 2026-05-31 01:11 | backend-ai/test-ai | test | EB-017 重启恢复证据补强：新增 `TestKernelStartRecoversRunningDetectionTask`，先在数据库创建带检测标准快照和 storage route 快照的 running 检测任务，再新建 Kernel 调 `Start()`，断言该任务恢复进 runtime `TaskManager`，且 `/api/v1/detection-runs/active` 返回恢复后的任务。验证通过 `go test ./internal/runtime -run TestKernelStartRecoversRunningDetectionTask -count=1`、`go test ./...`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 71.3%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`，`/health` ok，后端 PID 3224 继续运行 | closed
- 2026-05-31 01:10 | backend-ai/test-ai | test | EB-045 任务参数闭环补强：新增 `TestTaskFlowStringPayloadWritesPLCBeforeStartingDetection`，覆盖 watched STRING 虚拟变量请求携带 `process_params.inlet_area_m2` 和 `plc_writes` 时，任务流先调用 `builtin.write_control_variables` 通过后端 writer 下设 PLC，再调用 `builtin.start_detection_run` 创建检测任务，并把 `process_params/plc_writes` 冻结进 `custom_config_json`；测试同时断言下设审计写入。验证通过 `go test ./internal/pipeline -run TestTaskFlowStringPayloadWritesPLCBeforeStartingDetection -count=1`、`go test ./...`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 71.3%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`，`/health` ok 且后端继续运行 | closed
- 2026-05-31 00:59 | backend-ai/test-ai | test | EB-017 多项目并行检测 smoke 工具落地：新增 `backend/tools/smokeeb017`，需用 `go run -tags smoke_tools ./tools/smokeeb017` 执行，避免常规覆盖率把 smoke main 包计入产品门禁；实测创建 `SMOKE-EB017-20260531005715-A/B` 项目 `19/20`、INT/STRING 虚变量 `731570092/782246637/825972572`，启动并行任务 `17/18`，同项目重复启动返回 409，WS `command.write_variable` 写虚变量后历史入库可查，停止项目 A 后项目 B 仍 running，随后正常停止。验证通过 `go run -tags smoke_tools ./tools/smokeeb017`、`go test ./...`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 71.3%、`go vet ./...`、`golangci-lint run ./...`、`go build ./cmd/edge-backend`，`/health` ok 且后端继续运行 | closed
- 2026-05-31 00:39 | backend-ai/test-ai | api | EB-045 配置参数与 PLC 下设链路落地：新增任务模块 `builtin.write_control_variables` 和模板 `write-control-variables`，支持从 `task_params.process_params` 通过 `value_from` 解析进风口面积等工艺值，调用后端 `VariableWriteService` 统一走写入约束、KIO/PLC service 和审计；`builtin.start_detection_run` 会把 `process_params/plc_writes` 冻结进 `custom_config_json`。验证通过 `go test ./internal/pipeline` 定向、`go test ./internal/runtime/handlers` 定向、`go test ./...`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 71.3%、`go vet ./...`、`golangci-lint run ./...`、`go build ./cmd/edge-backend`；后端已重启 PID 9252，smoke 验证 `/health`、登录、`task-modules=17`、`task-flow-templates=13` | closed
- 2026-05-31 00:26 | backend-ai/review-ai | decision | EB-045 任务配置参数边界补充：正式 `task_params` 需要区分检测配置、工艺/报表参数和 PLC 下设参数；进风口面积等参数应从同一 `process_params` 来源冻结到检测追溯，并可通过后续 `builtin.write_control_variables` 受控下设到 PLC，仍必须走后端写变量 service、审计和可选回读确认 | closed
- 2026-05-30 23:49 | backend-ai | api | EB-040 默认报警首段落地：`detection_limit_alarms` 新增 `scope` 字段，检测业务报警默认 `scope=detection`，变量默认报警写 `scope=default/task_id=0`；新增 `TaskManager.EvaluateDefaultLimitAlarm`，数据变化和周期扫描都会按 `sys_tags.default_alarm_enabled/default_limit_*/default_*_hold_ms` 触发 enter/recover/level_change，默认报警不带静音且不参与检测摘要 | closed
- 2026-05-30 23:56 | backend-ai | api | EB-040 新增统一超限报警查询接口：`GET /api/v1/limit-alarms` 受 `view_realtime` 权限保护，返回 `{items,total,limit,offset}`，支持 `scope=default|detection` 以及 `project_id/task_id/test_no/var_id/status/alarm_type/level/from/to/limit/offset` 筛选；前端可用同一入口展示变量默认报警和检测业务报警明细 | closed
- 2026-05-30 23:39 | backend-ai | api | EB-037 补齐检测运行存储快照查询：新增 `GET /api/v1/detection-runs/:id/storage-routes`，权限 `view_realtime`，返回 `{items,count}`；同时修正检测启动冻结逻辑，默认 route 仍作为 disabled 建议自动保证存在，但运行快照会冻结同一变量的所有 enabled storage routes，包括用户自定义 route | closed
- 2026-05-30 23:34 | backend-ai | api | EB-045/046 继续补齐任务流编辑闭环：新增 `GET /api/v1/task-flows/:id`，返回单条任务流配置和变量绑定，供前端编辑器直达、刷新和删除后确认使用；权限仍为 `system_settings` | closed
- 2026-05-30 21:18 | backend-ai | api | EB-045/046 继续补齐任务流管理闭环：`PATCH /api/v1/task-flows/:id` 支持修改 `project_id`，替换 `vars` 时后端同步变量绑定项目；新增 `DELETE /api/v1/task-flows/:id` 删除任务流配置和变量绑定，执行历史 `task_flow_runs/task_flow_sql_logs` 保留用于审计；前端任务页可以取消“编辑已有任务时项目不可改”的限制并增加删除动作 | closed
- 2026-05-30 21:04 | backend-ai | api | EB-045 继续迭代定时任务：`sys_task_flows` 新增 `schedule_interval_ms`，`POST/PATCH /api/v1/task-flows` 读写该字段；`TaskFlowExecutor` 新增轻量 schedule scanner，每秒扫描 `trigger_type=schedule` 的启用任务，到期后投递现有任务流队列并写 `task_flow_runs`；兼容期若 schedule flow 未填 `schedule_interval_ms` 但填了 `cooldown_ms`，后端用 `cooldown_ms` 作为定时间隔；前端任务表单需新增定时间隔字段 | closed
- 2026-05-30 20:48 | backend-ai | api | EB-045 继续迭代项目生命周期任务流：检测启动成功后派发 `TaskFlowEvent(project_start)`，检测正常结束、任务流结束或异常结束后派发 `TaskFlowEvent(project_end)`；触发源覆盖任务流内置检测模块和 HTTP/WS 复用的 `DetectionRunsService`；生命周期 `task_params` 携带 `task_id/project_id/project_code/test_no/status/end_type/trigger_type`，可用于存储准备、特征值刷新、报表登记和通知；`schedule` 触发类型仍为配置元数据，尚未接定时调度 worker | closed
- 2026-05-30 20:30 | backend-ai | api | EB-045 继续迭代：JavaScript 任务模块补齐多变量实时读取 API：`realtime.getMany([var_id])`、`realtime.getByName(var_name, project_id?)`、`realtime.project(project_id?)`；`realtime.write(var_id,value,options?)` 改为复用任务流写变量审计，只允许写虚拟变量，默认 `trigger=false`，需要继续触发任务时必须显式传 `trigger:true/max_depth`；`builtin.write_variable` 同步收紧为只写虚拟变量，物理变量下设必须走 WS/HTTP 的 KIO 写服务；`GET /api/v1/task-modules` 的 JavaScript 模块返回 `runtime_api`，前端无需硬编码脚本能力说明 | closed
- 2026-05-30 19:35 | backend-ai | api | EB-045 继续迭代：检测业务正式入口保持 STRING 虚拟变量触发任务系统；`sys_detection_tasks` 新增 `limit_check_enabled/end_policy/qualified_hold_ms/custom_config_json`；`builtin.start_detection_run` 支持 `custom_items` 自定义检测项、`limit_check_enabled` 是否启用上下限、`end_policy=manual|fixed_duration|qualified_hold` 和 `qualified_hold_ms`；`builtin.update_detection_limits` 支持 `items` 批量调整运行中检测项并刷新 active task；WS read limit 从 32 KiB 提升到 256 KiB 以承载低频业务配置 payload；新增 `GET /api/v1/detection-standards/favorites|recent`、`POST/DELETE /api/v1/detection-standards/:id/favorite` | closed
- 2026-05-30 19:35 | backend-ai | decision | 前端边界确认：检测配置模板、收藏、最近使用、任务历史和当前运行快照可走 HTTP；开始/停止/暂停/恢复/消音/运行中改上下限等改变现场业务状态的动作必须写入 STRING 虚拟变量，由 `sys_task_flows` data_change 触发内置业务模块执行；本轮不做拖拽式前端，只给未来拖拽编辑器保留模块 schema 和参数来源 | closed
- 2026-05-30 17:15 | backend-ai | decision | 已给 frontend-ai 新增 EB-048 交付清单：明确变量属性设定、storage routes、检测配置/运行接口、任务模块/模板/执行记录 API、正式 STRING 虚拟变量触发链、`steps_json` 数组契约、`apply_to_running` 风险提示和旧变量级存储字段废弃范围 | closed
- 2026-05-30 17:35 | backend-ai | api | EB-019 通知总线首段完成：新增非持久运行态 `Channels.Notify` 和 `NotificationHub`；WS 新增 `topic=notifications` 和 `notification.event`，payload 为 `RuntimeNotification`，字段包括 `id/type/level/project_id/project_code/task_id/test_no/var_id/var_name/display_name/message/payload/occurred_at`；当前通知类型覆盖 `alarm.limit.enter/recover/level_change`、`detection.run_started/stopped/abnormal_stop/paused/resumed`、`detection.result_ok/result_ng`、`detection.features_updated`。前端 shared type 已同步，前端需接入全局通知/工位通知展示 | closed
- 2026-05-30 18:35 | backend-ai | api | EB-019 通知中心持久化落地：新增 `sys_notifications`、`sys_notification_recipients`，`Channels.Notify` 由 `NotificationDispatcher` 先落库再通过 `NotificationHub` 在线推送；新增 `GET /api/v1/notifications`、`GET /api/v1/notifications/unread-count`、`POST /api/v1/notifications/{id}/read`、`POST /api/v1/notifications/read-all`，权限为 `view_realtime`。前端 shared DTO/API 已同步，EB-048 需接 UI 未读数、列表和已读操作 | closed
- 2026-05-30 18:52 | backend-ai | api | EB-019 通知分发对象细化：`NotificationDispatcher` 落库生成 recipient 时已按 `target_type` 分发，`all` 给全部启用本地用户，`user` 给指定用户 ID，`role` 给指定角色用户；`project` 的项目成员收敛已由 2026-05-31 07:38 记录补齐。 | closed
- 2026-05-30 17:30 | backend-ai | decision | 明确前端实时检测配置获取方式：配置管理走 HTTP `detection-standards` CRUD；“当前配置”不是最新标准表，而是检测启动后冻结的 `detection_run_standard_items`，前端通过 `GET /api/v1/detection-runs?project_id=...&status=running&limit=1` 或 `GET /api/v1/detection-runs/{id}` 获取；实时值仍走 WS/`realtime/variables`，由前端按 run snapshot 拼 OK/NG 展示 | closed
- 2026-05-30 17:45 | backend-ai | api | 新增 `GET /api/v1/detection-runs/current?project_id=...`，返回某项目当前 running 或 paused 检测详情，包含冻结 `standard_items`、`storage_routes`、notes 和 reports；缺 `project_id` 返回 400，无当前任务返回 404。前端 shared API 新增 `getCurrentDetectionRun(projectId)` | closed
- 2026-05-30 15:55 | backend-ai | api | EB-047 首段实施：新增 `POST /api/v1/variables/bulk-remap/kio-projects`，用于本地 KIO 调试批量确保 12 个项目并按 `台N_xx` 原名把变量归属到项目 N，重映射 `var_name/display_name*`，创建 disabled 默认 storage route，并重载 `TagManager`；前端 shared API 已补 `bulkRemapKioProjects` 类型与调用 | closed
- 2026-05-30 16:01 | backend-ai/test-ai | test | EB-047 执行与验证：本地 dry-run 命中 504 个 `台N_xx` KIO 变量；真实执行创建缺失项目 6 个并更新 504 个变量，形成 `AC-01..AC-12`，项目实时快照为 AC-01 46 个、AC-02..AC-12 各 42 个，`/health` tags=1028；复测 `台1_41` 保留 `raw_name/write_path=台1_41` 和 `RW` 写入约束，业务名为 `kio_01_41`，disabled 默认 route 列同步为 `kio_01_41` | closed
- 2026-05-30 16:01 | backend-ai/test-ai | test | EB-047 门禁通过：`go test ./...`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 70.1%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；前端 `npm run lint`、`npm run build`、`npm run test -- --run` 通过；后端已用新二进制重启并保持 `127.0.0.1:18080` PID 49904 | closed
- 2026-05-30 16:28 | backend-ai | api | EB-045/046 任务模板继续迭代：`GET /api/v1/task-flow-templates` 增加固定时长检测、合格持续检测、暂停、恢复、运行中限值调整、特征值刷新和报表登记模板；任务流参数绑定支持 `optional=true` 和 `default`，前端 shared API 补任务模块、模板、执行记录和 SQL 日志 typed 调用 | closed
- 2026-05-30 16:28 | backend-ai/test-ai | test | EB-045/046 验证通过：`go test ./...`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 70.2%、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`；前端 `npm run lint`、`npm run build`、`npm run test -- --run` 通过；后端重启 PID 60884，`/health` ok，smoke 确认 task modules=16、task-flow templates=12 | closed
- 2026-05-30 13:55 | backend-ai | decision | EB-045 任务模块继续迭代：新增 task-flow-runs/SQL 日志查询 API、受控 write_variable、origin/depth 防递归、任务模板、检测静音/level_change/运行中限值调整、storage_prepare、register_report、http_request；前端需适配 EB-046 新契约 | closed
- 2026-05-30 13:58 | backend-ai | test | EB-045 验证通过：`go test ./...`、覆盖率 70.1%、`go vet ./...`、`golangci-lint run ./...`、`go build ./cmd/edge-backend`；后端已重启并通过 `/health`、登录、`task-modules/task-flow-templates/task-flow-runs` smoke | closed
- 2026-05-27 09:55 | review-ai | decision | AI 协作文档体系初始化 | closed
- 2026-05-27 09:55 | test-ai | test | `backend/go test ./...` 通过，当前无测试文件 | closed
- 2026-05-27 12:20 | frontend-ai | decision | 前端技术选型固定为 Electron + React 19 + TS 6 + Vite 8 + Ant Design + TanStack Query + Zustand + XState；Luckysheet 仅允许走隔离 adapter | closed
- 2026-05-27 12:20 | frontend-ai | decision | 初始化 `desktop/`，Electron main 负责 Go sidecar 启动、健康轮询和日志入口，renderer 只走 preload 与 typed HTTP client | closed
- 2026-05-27 12:35 | test-ai | test | `desktop/npm run build`、`desktop/npm run lint`、`backend/go test ./...`、`desktop/npx electron-builder --dir`、`desktop/npm run package` 通过；Playwright 已截图验证 `http://127.0.0.1:4173` 首屏 | closed
- 2026-05-28 16:57 | frontend-ai | decision | 从 `react-design-prototype` 迁入工位操作与历史查询页面，新增工作台导航；历史查询暂用前端样例数据，真实历史 API 待 backend-ai 提供 | closed
- 2026-05-28 17:24 | frontend-ai | decision | 工位操作页按原型补齐左侧卡片池自适应高度、拖拽占位/overlay、胶囊按钮、动态背景与橘黄色物理光源高光 | closed
- 2026-05-28 17:46 | frontend-ai | decision | 对照运行中的 `react-design-prototype` 继续还原工位页：卡片图表改回原型 ChartLine 黑线/MinMax 标签，右侧表格改回 table 结构、独立表头和 hover 滚动条 | closed
- 2026-05-28 18:26 | frontend-ai | decision | 修复工位右侧固定表头下滚动条默认可见问题；历史查询页按原型拆分为图表、表格、甘特弹窗与样式组件，并新增 `.ai/skills/edge-glass-prototype-style` 风格还原 skill | closed
- 2026-05-28 18:27 | frontend-ai | test | `desktop/npm run lint` 与 `desktop/npm run build` 通过；Playwright 已对 `/#/station`、`/#/history`、历史甘特弹窗和原型 `/reports` 同视口截图 | closed
- 2026-05-28 18:34 | frontend-ai | decision | 设置页范围确认：实时数据源服务接 MQTT 站点、设备/项目分组、发现变量与数据库变量；历史数据源服务仅预留数据库连接来源配置位 | closed
- 2026-05-28 19:38 | frontend-ai | decision | 新增 `/#/settings` 系统设置页、侧边栏入口、三语文案、typed API 扩展；实时数据源页接 gateway-configs/gateways/devices/variables/discover/assignment，历史数据源保持预留说明 | closed
- 2026-05-28 19:39 | frontend-ai | test | `desktop/npm run lint` 与 `desktop/npm run build` 通过；Playwright 已截图验证 `/#/settings` 实时数据源与历史数据源两个 tab | closed
- 2026-05-28 19:53 | frontend-ai | decision | 设置页信息架构调整为变量优先：项目/设备分组与变量列表为主，MQTT 站点降级为一次性采集源配置；本地后端离线时明确提示无法加载站点/分组/变量 | closed
- 2026-05-28 19:54 | frontend-ai | test | `desktop/npm run lint` 与 `desktop/npm run build` 通过；确认当前 `127.0.0.1:18080` 未监听导致站点数据无法加载，Playwright 已截图验证离线提示 | closed
- 2026-05-28 20:05 | frontend-ai | decision | 设置页进一步收敛为“站点概览 + 左侧变量筛选 + 右侧变量全信息表”；MQTT 站点新增/编辑/发现变量移动到弹窗，不再占主工作区 | closed
- 2026-05-28 20:06 | frontend-ai | test | `desktop/npm run lint` 与 `desktop/npm run build` 通过；Playwright 已验证真实站点/变量数据渲染和站点弹窗 | closed
- 2026-05-28 20:20 | frontend-ai | decision | 设置页重构为管理中心：左侧模块导航覆盖变量列表、实时数据源、历史数据源、系统设置；变量列表为默认主工作区并占主要面积 | closed
- 2026-05-28 20:21 | frontend-ai | test | `desktop/npm run lint` 与 `desktop/npm run build` 通过；Playwright 已验证管理中心默认变量工作区 | closed
- 2026-05-28 20:28 | frontend-ai | decision | 管理中心模块入口移动到标题右侧展示卡片，移除左侧模块导航，释放主区域宽度给变量筛选与变量表 | closed
- 2026-05-28 20:29 | frontend-ai | test | `desktop/npm run lint` 与 `desktop/npm run build` 通过；Playwright 已验证卡片入口版管理中心 | closed
- 2026-05-28 20:43 | frontend-ai | decision | 工位操作改为桌面首屏，边缘概览降级为调试看板；左侧工位操作按项目/设备自动扩展二级菜单，工位页按选中设备过滤实时变量与状态卡 | closed
- 2026-05-28 20:44 | frontend-ai | test | `desktop/npm run lint` 与 `desktop/npm run build` 通过；Playwright 验证 `/#/` 首页工位、6 个设备二级菜单、`device_id=1` 精确高亮与状态卡切换 | closed
- 2026-05-29 00:03 | frontend-ai | test | `desktop/npm run package` 完成 Go 后端与前端构建但旧 `release/win-unpacked` 文件锁导致首次安装包输出失败；改用全新输出目录验证 `electron-builder --dir` 可运行包、打包后 Electron 启动内置 `resources/backend/edge-backend.exe`，`/health` 返回 ok 且 tags=521；随后 `release-installer-smoke` 完整生成 nsis、portable 与 blockmap 产物 | closed
- 2026-05-28 19:54 | backend-ai | test | 新增 `backend/tools/dbinit` 初始化 Docker 映射的本地 MySQL schema；`go test ./...`、`go build ./cmd/edge-backend` 通过；启动 `go run ./cmd/edge-backend`，`/health` 与 `/api/v1/runtime/channels` 返回 200 | closed
- 2026-05-28 19:58 | backend-ai | answer | 为本地桌面/浏览器来源补充 Gin CORS 与 OPTIONS 预检；`go test ./...`、`go build ./cmd/edge-backend` 通过，Playwright 验证 `/#/settings` 无离线提示且浏览器上下文可读取 3 个变量 | closed
- 2026-05-28 20:14 | review-ai | decision | 新增登录权限与 SSO 安全设计，明确本地用户 JWT、主站 service token、一次性 SSO ticket、三角色 capability 和后续 API 维护规则 | closed
- 2026-05-28 23:57 | review-ai | decision | 将登录权限落地从 EB-004 总项拆分为后端认证鉴权 EB-008、前端登录权限守卫 EB-009、前端用户管理页面 EB-010 | closed
- 2026-05-29 00:34 | backend-ai | answer | 在看板新增 Local Test Credentials，集中记录本地登录、MySQL、MQTT、KIO 写入和 service token 配置状态，供前端联调使用 | closed
- 2026-05-29 00:52 | frontend-ai | decision | 以工位操作和历史查询为基准统一设置页、调试看板的浅色动态背景、玻璃面板、胶囊按钮和滚动条风格；调试看板登录/主站文案更新为当前认证状态 | closed
- 2026-05-29 00:50 | backend-ai | decision | 新增根目录 `计划.md`，基于当前后端设计和 JG HJ 旧实现，明确采集与业务模块按 raw、clean、business、detection、export、验收分 9 次主迭代实现 | closed
- 2026-05-29 01:41 | backend-ai | decision | 前端可见 DTO 变更：`GET/POST/PATCH /api/v1/devices` 增加/支持 `display_name`、`display_name_en`、`display_name_ja`；`name` 保留兼容，前端展示应按语言读取 display 字段并 fallback 到 `name`/`device_code` | closed
- 2026-05-29 11:10 | review-ai | review | 新增 `多设备并行检测任务设计计划.md`，结合根目录需求、Word 需求、当前 Go 后端与 React 前端状态，明确并行检测不变量、API 增量、复杂度、Go 规范审阅和防瞎搞硬规则 | closed
- 2026-05-29 11:10 | review-ai | decision | 看板新增 EB-015/EB-016/EB-017，分别留给后端、前端、测试推进多设备并行检测闭环 | closed
- 2026-05-29 09:46 | frontend-ai | decision | 新增 EB-014：后端检测标准 EB-012 已完成但前端管理入口缺失，需要作为 frontend-ai 下一项处理 | closed
- 2026-05-29 11:25 | review-ai | review | 补充虚变量设计边界：当前模型有 `placeholder/discovered` 但没有创建 API/UI；检测开始、状态、结果类 int 变量应作为虚变量由后端服务写入，不应伪装成 PLC 发现变量 | closed
- 2026-05-29 11:45 | review-ai | review | 深度复查后发现后端已存在 `internal/services` 与 `internal/runtime/handlers`，虚变量、检测运行、报告模板后端已部分落地；新增 `后端模块化审阅与演进计划.md`，修正为沿用现有 handler/service，不再建议新增 `internal/detection` 包 | closed
- 2026-05-29 11:50 | review-ai | decision | 在交互看板为 EB-018 补充详细施工流程，列明文档位置、P0-P5 执行顺序、每阶段验收和禁止事项，避免后续后端模块化按错误边界施工 | closed
- 2026-05-29 13:22 | backend-ai | review | 确认实时与写操作首选 WebSocket，但必须经过后端 service，不允许客户端绕过后端直接写 MQTT；新增 EB-019/EB-020/EB-021 追踪 WS 通道、写操作审计和检测超限/事件流水 | closed
- 2026-05-29 13:28 | frontend-ai | decision | EB-016 部分完成：管理中心新增“创建虚变量”入口，支持选择项目/设备并创建 `source_type=virtual` 的 `INT/FLOAT/BOOL/STRING` 变量；工位页检测 start/stop/abnormal-stop 与任务历史入口继续 open | closed
- 2026-05-29 13:43 | frontend-ai | decision | EB-016 继续推进：工位页接入开始检测弹窗、标准/报表模板选择、start/stop/abnormal-stop mutation、任务历史跳转；历史查询页支持 `task_id`、`device_id`、`test_no` 查询参数 | closed
- 2026-05-29 13:52 | frontend-ai | decision | 修复 EB-016 虚变量入口：新建虚变量弹窗补齐中文/英文/日文显示名，提交时英文/日文空值回退到中文显示名，避免三语显示断层 | closed
- 2026-05-29 13:28 | backend-ai | decision | 补充 EB-019 前置约束：变量可以自动发现，但自动发现只生成候选来源变量；必须由变量属性/设备绑定/检测标准配置确认后，才允许参与检测、历史存储、超限判断或 WS 写命令 | closed
- 2026-05-29 13:32 | backend-ai | decision | 新增 `backend/docs/变量属性检测配置与WS前置设计.md`，对照 JGHJ 旧后端变量模型，明确来源变量、业务变量、运行态变量和检测配置项的分层，以及 WS 写和检测超限的前置数据结构约束 | closed
- 2026-05-29 13:41 | backend-ai | decision | 新增 `backend/docs/后端日志WS检测实施顺序.md`，明确先补全 EB-020 写操作审计和运行日志，再推进变量属性、检测配置、WS 实时读、WS 写命令和检测超限事件 | closed
- 2026-05-29 13:52 | backend-ai | decision | 补充变量存储映射设计：变量属性负责 `storage_name/storage_target/storage_table/storage_value_column/form_field_key/query_alias`，上下限仍留在检测配置/运行快照，`store_enabled` 只表示某检测标准是否存该变量 | closed
- 2026-05-29 14:02 | backend-ai | decision | 新增 EB-022：历史数据源 MySQL 配置从前端占位改为后端配置文件读写能力；接口必须隐藏明文密码，保存后要求重启后端生效，不在运行中热切换当前 GORM 连接 | closed
- 2026-05-29 14:20 | backend-ai | decision | 新增 EB-023 给前端适配验收历史数据源 MySQL 配置页；新增 EB-024 到 EB-029 将后端后续拆为日志审计、变量属性/存储映射、检测配置、WS 只读、WS 写、检测事件/超限和联调回归七个阶段 | closed
- 2026-05-29 15:05 | backend-ai | decision | 已给前端留下 EB-030：适配变量属性、存储映射、写入约束、防抖和启动快照高级编辑 UI；后端继续推进 EB-025 检测配置语义和运行快照冻结 | closed
- 2026-05-29 16:02 | backend-ai | decision | 新增 EB-031：在 WS 写命令前先补强只读 WS 的断线重连和连接保护，避免后续写命令建立在脆弱连接模型上 | closed
- 2026-05-29 16:35 | backend-ai | decision | EB-027 完成检测任务 WS 写命令首段：`command.detection.start/stop/abnormal_stop` 复用 `DetectionRunsService`，成功返回 `command.ack`，失败返回结构化 error，所有命令写 `sys_audit_logs`；KIO WS 写留到拆出 KIO service 后实施 | closed
- 2026-05-29 16:36 | backend-ai | test | EB-027 首段验证通过：`go test ./...`、`go test ./... -coverprofile all-coverage.out` 总覆盖率 70.0%、`go vet ./...`、`go build ./cmd/edge-backend`、`desktop/npm run lint`、`desktop/npm run build` 通过；`golangci-lint` 本机未安装 | closed
- 2026-05-29 16:38 | backend-ai | test | 已重启本地后端 PID 70660 并保持 `127.0.0.1:18080` 运行；smoke 验证 `/health` ok、WS `command.detection.start` 返回 `cmd-smoke-start` ack、WS `command.detection.stop` 返回 `cmd-smoke-stop` ack | closed
- 2026-05-29 14:25 | backend-ai | decision | EB-020 P0 实施：新增 runtime HTTP 写操作审计 middleware，受保护 `POST/PATCH/PUT/DELETE` 自动写 `sys_audit_logs`；审计 detail 只保留 request/command/route/status/actor/latency/error 元数据，不保存请求体或响应体，避免泄露密码、token、KIO 凭据和数据库连接密码 | closed
- 2026-05-29 14:28 | backend-ai | test | EB-020 P0 验证通过：`go test ./...`、`go test ./... -coverprofile all-coverage.out` 生成 `backend/all-coverage.out` 且总覆盖率 70.5%、`go vet ./...`、`go build ./cmd/edge-backend` 通过；`golangci-lint` 本机未安装；已重启本地后端并 smoke 验证 `/health` ok、登录 ok、`POST /api/v1/system/database-config/test` 返回 `{"ok":true}` 且回传 `X-Request-ID=smoke-audit-1` | closed
- 2026-05-29 15:06 | backend-ai | decision | EB-024 P1 实施：`sys_tags`、`TagConfig`、`POST/PATCH /api/v1/variables` 和 `desktop/src/shared/api/types.ts` 补齐变量存储映射、读写约束、防抖与启动快照字段；服务层增加默认值和写入约束校验，自动发现变量和虚变量默认不可写，缺少 `write_path` 的可写变量会被拒绝 | closed
- 2026-05-29 20:40 | backend-ai | decision | EB-021 先落地超限报警生命周期表：新增 `detection_limit_alarms`，一条记录覆盖某任务内某变量从超限开始到恢复，字段包含任务/设备/标准项/变量快照、报警类型和等级、开始/峰值/恢复值、阈值、质量、开始/最后/恢复时间和持续时长；暂不实现实时判定状态机 | closed
- 2026-05-29 23:05 | backend-ai | decision | 新增 `backend/docs/变量默认上下限与业务报警装配设计.md`；变量默认上下限作为 `sys_tags` 资产属性保留，检测标准项新增 `alarm_enabled`，检测启动时把变量默认属性冻结到运行快照 `variable_default_*`，变量 PATCH 可通过 `apply_to_running=true` 只同步 running 任务变量默认快照 | closed
- 2026-05-29 23:05 | backend-ai | decision | 新增 EB-034 给前端：适配变量默认上下限、默认报警开关、检测标准项 `alarm_enabled`、任务详情运行快照 `variable_default_*` 和变量编辑 `apply_to_running` 风险提示 | closed
- 2026-05-29 23:58 | backend-ai | decision | EB-020 追加后端审计日志查询：新增 `GET /api/v1/audit-logs`，受 `system_settings` 权限保护，支持 actor/action/target/result/time/limit/offset 过滤，响应 `{items,total,limit,offset}`；同步前端共享 DTO/API 便于设置页后续接入 | closed
- 2026-05-29 23:59 | backend-ai | test | EB-020 审计日志查询验证通过：`gofmt`、`backend/go test ./...`、`go vet ./...`、`golangci-lint run ./...` 0 issues、`go build ./cmd/edge-backend`、`desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test` 均通过；新增 kernel 测试覆盖查询成功、非法 limit、非法时间 | closed
- 2026-05-29 23:46 | backend-ai | decision | 检测配置补 `check_cycle_ms/check_on_start` 并同步运行快照；变量属性补 `default_violation_hold_ms/default_recover_hold_ms`。检测开始首帧存储按变量 `startup_snapshot_enable`，检测开始首帧判断按业务 `check_on_start`，二者不绑定；`check_cycle_ms=0` 时快照继承变量 `store_cycle_sec` | closed
- 2026-05-30 00:43 | backend-ai | decision | EB-021/EB-028 首段完成：新增 `Alarm` 队列和批量入库 worker，`TaskManager` 在设备运行上下文维护业务超限状态；数据变化和周期扫描按运行快照 `alarm_enabled/check_enabled/check_cycle_ms/limit_*/limit_deadband/violation_hold_ms/recover_hold_ms/quality_policy` 触发上限、下限报警进入和恢复，清洗层仍只更新实时值和质量戳 | closed
- 2026-05-30 01:05 | backend-ai | decision | EB-028 继续推进：新增 `detection_run_events` 和 `detection_run_summaries`，检测开始、正常停止、异常停止写轻量事件；摘要按需从任务、历史数据和超限报警表刷新，避免把高频报警明细重复写成第二套事件表造成双倍写入压力 | closed
- 2026-05-30 01:05 | backend-ai | api | 新增前端可见接口：`GET /api/v1/detection-runs/:id/summary` 返回 `result_status/history_rows/alarm_total/alarm_active/alarm_recovered/alarm_above_h/alarm_above_hh/alarm_below_l/alarm_below_ll/first_alarm_at/last_alarm_at`；`GET /api/v1/detection-runs/:id/events?limit=` 返回任务生命周期事件列表；前端共享 DTO/API 已同步 | closed
- 2026-05-30 01:55 | backend-ai | decision | EB-037 首段实施：新增 storage route 模型和运行快照，`sys_tags.storage_*` 保留兼容，设备宽表主路径由 `sys_storage_routes/detection_run_storage_routes` 承载；检测启动时冻结 routes 并确保 `rt_device_{device_id}_data` 和动态列存在，写入热路径暂未切到宽表 writer | closed
- 2026-05-30 09:16 | backend-ai | decision | EB-037 追加宽表写入路径：`DetectionTask`/`ActiveTask` 携带运行 storage routes，`StoreTask` 携带路由快照；`InsertHistoryBatch` 保持写 `rt_history_data` 兼容表，同时对 `wide_table` routes 写入 `rt_device_{device_id}_data`，同一 `(task_id,sample_bucket_ms)` 使用 upsert 更新动态列 | closed
- 2026-05-30 09:27 | backend-ai | decision | EB-037 补齐宽表历史读取和批量合并：宽表 writer 先按 `table/task_id/sample_bucket_ms` 聚合多变量后一次 upsert；`GET /api/v1/history/data` 在 `task_id/device_id/test_no` 命中 `detection_run_storage_routes` 时优先读取 `rt_device_{device_id}_data`，并重建旧 `HistoryData` DTO，前端接口字段不变 | closed
- 2026-05-30 09:48 | backend-ai | decision | EB-037 存储设计审查后推翻变量级存储频率：变量属性只保留采集、清洗、写入约束和默认报警；存储目标表/列、周期、变化死区、首帧存储和启用状态全部归 `sys_storage_routes`；新增 storage routes API，前端需停止把 `store_cycle_sec/store_deadband/store_trigger/store_mode/storage_table` 作为变量编辑主入口 | closed
- 2026-05-30 10:15 | backend-ai | decision | 用户确认本轮不保留旧 `/devices` 兼容入口；新增 EB-041/042/043/044：项目命名统一、变量存储属性删除、任务系统倒排索引、存储总线。前端需同步迁移到 `/api/v1/projects`、`project_id/project_code` 和 `storage-routes` 配置 UI | closed
- 2026-05-30 10:40 | backend-ai | decision | EB-041/042/043/044 首段实施：后端只注册 `/api/v1/projects`，模型和 schema 切到 `sys_projects/project_id/project_code/rt_project_*`；启动迁移会把旧 `sys_devices/device_id/device_code` 改到项目命名并删除变量存储属性列；变量 API 不再接收/返回 `store_*`、`storage_*`、`startup_snapshot_enable`；新增 `sys_task_rules` 和 `TaskRuleIndex` 倒排索引地基；存储 worker 改为 `StorageBus` 按 `project_id + table_name` 分 bucket 批量 flush | closed
- 2026-05-30 11:10 | frontend-ai | decision | 适配 EB-041/042 后端破坏性契约：前端 shared DTO/API 将项目字段 `project_id/project_code` 映射到旧页面别名，`getDevices/createDevice/updateDevice` 实际调用 `/api/v1/projects`；变量创建/编辑/分配和历史查询参数会发送项目字段并过滤变量级存储字段；设置中心新增“存储路由”模块，通过 `/api/v1/storage-routes` 管理项目宽表目标、列、触发、周期、死区和启用状态 | closed
- 2026-05-30 11:30 | backend-ai | decision | EB-045 条件事件任务系统首段实施：新增任务流模型、任务变量引用、执行日志、SQL 日志、goja JS 引擎、异步执行队列和 priority 小批量排序；数据变化热路径只通过 `var_id -> flow_ids` 倒排索引投递任务，不直接执行脚本；内置 `builtin.storage_snapshot` 复用 StorageBus 写入，不新建数据库连接 | closed
- 2026-05-30 11:45 | backend-ai | decision | 已给 frontend-ai 新增 EB-046：侧边菜单新增“任务”模块承载后端 task-flows，列明当前可接 API、页面字段、权限、三语、手动运行、脚本编辑器、执行记录和 SQL 日志占位要求；后端仍需后续补执行记录/SQL 日志查询 API | closed
- 2026-05-30 12:06 | frontend-ai | decision | EB-046 前端首段完成：新增 `/#/tasks` 路由和侧边菜单“任务”入口，受 `system_settings` 权限保护；新增 task-flows typed API、任务列表、新建/编辑弹窗、项目筛选、变量角色绑定、条件脚本、动作脚本/参数、手动运行按钮、执行记录和 SQL 日志占位 tab；编辑已有任务时项目选择禁用，因为当前后端 PATCH 未开放 project_id 修改 | closed
- 2026-05-30 12:30 | backend-ai | api | 新增 `GET /api/v1/task-modules`；扩展 `POST/PATCH /api/v1/task-flows` 支持 `steps_json`；`sys_task_flows` 新增 `steps_json` 字段，旧 `action_type/action_payload/action_script` 保持单步骤兼容；正式业务参数不由手动运行接口承载 | closed
- 2026-05-30 12:50 | backend-ai | decision | 已删除 `backend/docs/任务数据流转与内置模块设计.md`，新增 `backend/docs/任务变量流转与内置模块设计.md`；任务业务参数只从 watched STRING 虚拟变量的 JSON 字符串解析为 `task_params`，参数绑定来源改为 `trigger_param`，HTTP 手动运行只保留开发者调试触发 | closed
- 2026-05-30 13:20 | backend-ai | api | EB-045 新增任务流内置检测模块：`builtin.start_detection_run` 支持 `enable_storage/enable_alarm/duration_sec/standard_id/report_template_id`，新增 `stop_detection_run/pause_detection_run/resume_detection_run/fixed_duration_guard/qualified_hold_guard/refresh_features`；`sys_detection_tasks.status` 新增 `paused`，`end_type` 新增 `fixed_duration/qualified_hold/task_flow_stop`；新增 `detection_run_features`，并开放 `POST /api/v1/detection-runs/:id/pause|resume`、`GET /api/v1/detection-runs/:id/features` | closed
- 2026-05-30 13:40 | backend-ai | api | 暂停时长语义修正：`sys_detection_tasks` 新增 `pause_started_at`、`paused_duration_ms`；暂停记录起点，恢复累计暂停时长并顺延 `expected_end_at`，停止 paused 任务会把当前暂停段计入 `paused_duration_ms`，`detection_run_summaries.duration_ms` 扣除所有暂停时间 | closed
- 2026-05-30 16:58 | backend-ai | api | EB-045 任务流执行器兼容补强：`steps_json` 正式契约仍推荐多步骤数组，但后端现在也接受单个 step object 并包装为单步骤执行，避免调试脚本或 PowerShell 单元素数组序列化成对象后导致任务失败 | closed
- 2026-05-31 06:16 | frontend-ai/test-ai | test | 驾驶舱按 Codex 官网产品页方向继续重做视觉层级：背景去掉大块科幻光团，改为低饱和蓝白产品页层次和细网格；标题 SVG 降噪，卡片改为更透明的白蓝玻璃、轻阴影、细边框并保留工位同源暖光边缘；3D 模型下移放大但避免继续裁切。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`、`git diff --check`，1920×1080 截图 `output/playwright/model-cockpit-codex-style-v4-1920x1080.png` | closed
- 2026-05-31 06:27 | frontend-ai/test-ai | test | EB-047 前端项目迁移继续收口：三语资源中清理“项目/设备”“设备/项目”“Project/device”“プロジェクト/設備”等混合文案；工位页开始检测表单页面层改用 `project_id`，`DetectionRunStartPayload` 改为项目主字段，旧 `device_id` 仅保留为兼容 alias。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`、`git diff --check`；Playwright 登录后点击左侧项目二级菜单，确认 URL 为 `/#/?project_id=1` 且不含 `device_id`，设置页可见文本无旧混合文案，截图 `output/playwright/project-copy-cleanup-smoke.png` | closed
- 2026-05-31 06:49 | frontend-ai/test-ai | test | 驾驶舱按用户反馈继续参考 Codex 官网产品页风格收敛：`model-cockpit.css` 改为白底、低饱和软蓝渐变、细网格、轻玻璃卡和更干净的深色文字层级，保留动态光源驱动的暖色卡片边缘；设置页变量分配、批量分配、虚变量创建和 storage route 变量联动继续收口到 `project_id/project_code`，`VariableAssignmentPayload.device_code` 改为兼容可选字段。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`、`git diff --check`；Playwright 1920×1080 截图 `output/playwright/model-cockpit-codex-style-1920x1080.png` | closed
- 2026-05-31 07:08 | frontend-ai/test-ai | test | EB-047 前端项目语义继续闭合：检测配置页和设置页检测标准弹窗保存字段从 `device_id/device_code` 改为 `project_id/project_code`；任务页内部项目列表、变量筛选、动态参数项目选择和运行日志展示改为项目语义，旧变量 `device_id/device_code` 只通过 helper fallback 读取；三语资源继续清理可见混合文案。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`、`git diff --check`；Playwright 登录后验证 `/api/v1/projects=200`、`/api/v1/devices=404`，并点击 `/#/detection-config` 新增标准、`/#/tasks` 新增任务和发送任务请求弹窗，截图 `output/playwright/detection-config-project-field-smoke.png`、`output/playwright/task-flows-project-request-smoke.png` | closed
- 2026-05-31 10:10 | frontend-ai/test-ai | feature/test | EB-051 独立报警中心完成：`/#/alarms` 支持 `scope=default|detection`、项目、变量 ID、等级、状态和时间范围筛选，`from/to` 透传到 `GET /api/v1/limit-alarms`；报警记录可跳工位、历史曲线，检测报警额外提供“任务详情”弹窗并调用 `GET /api/v1/detection-runs/{id}` 展示检测编号、项目、状态、标准、模板、起止时间和备注。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`；Playwright 1920×1080 验证 `/alarms?scope=detection&from=...&to=...&status=active` 页面渲染、limit-alarms 请求带 `from/to`、任务详情请求 `detection-runs/41` 返回 200，截图 `output/playwright/alarm-center-time-task-detail-smoke.png` | closed
- 2026-05-31 10:55 | backend-ai/frontend-ai/test-ai | feature/test | EB-052 报警通知自动过期闭合：后端 `sys_notifications.expires_at` 和通知查询过滤已完成；前端通知中心不做本地绕过过滤，只展示后端返回的有效通知，报警中心继续查询 `detection_limit_alarms` 用于追溯。验证通过 `desktop/npm run lint`、`desktop/npm run build`、`desktop/npm run test -- --run`；API smoke 返回报警通知 `returned_alarm_notifications=27`、`expired_returned=0`、报警中心记录 `alarm_records=31249`；Playwright 1920×1080 验证 `/#/notifications` 渲染 20 行有效通知、`/#/alarms?scope=detection&status=active` 渲染 3 行报警记录，接口 `/notifications`、`/notifications/unread-count`、`/limit-alarms` 均为 200，截图 `output/playwright/notification-effective-smoke.png`、`output/playwright/alarm-record-trace-smoke.png` | closed
