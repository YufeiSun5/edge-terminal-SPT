# 采集与业务模块落地计划

编写时间：2026-05-29
执行身份：`backend-ai`
建议实现节奏：9 次主迭代，最后一次包含联调、压测和发布门禁。

## 1. 目标与边界

本计划面向 `edge-terminal` 当前 Go 后端，目标是把现有“MQTT 接入、变量发现、实时内存、检测任务、检测期入库”的早期骨架，升级为可长期维护的采集与业务模块。设计参考当前仓库文档和 `D:\DEV_D\spindle\V2_1\SPD_JGHJ 数据采集与任务` 的旧实现，但不迁移 JG HJ 的生产语义。

核心目标：

- 支持多 MQTT 数据源和不同厂商报文策略。
- 明确原始变量、清洗变量、内部变量和业务绑定的边界。
- 平时只维护实时态，检测会话打开后才写检测历史。
- 支持 0 值防抖、死区、范围、布尔映射、质量码等清洗规则。
- 通过清洗变量事件驱动业务模块，而不是让 MQTT 或清洗层直接写业务表。
- 支持运行时热重载和配置版本，避免检测过程被隐式配置漂移污染。
- 为前端和主服务器提供稳定 API、实时出口和控制接口。

明确不做：

- 不把 JG HJ 的工单、班次、产量、OEE 作为新平台核心语义。
- 不让 MQTT callback 直接写 MySQL。
- 不在应用层做历史库同步到主服务器，历史同步仍交给外部数据库同步软件。
- 不让自由 SQL 直接创建或关闭检测会话，检测核心必须走内置服务。

## 2. 已阅读依据

当前仓库：

- `MEMORY.md`
- `AI_BOARD.md`
- `.ai/instructions/ai-workflow.md`
- `.ai/instructions/backend-go-edge.md`
- `backend/docs/backend-architecture.md`
- `backend/docs/前端接口-站点与变量发现.md`
- `backend/deploy/schema.sql`
- `.ai/docs/后端原始数据到清洗数据之间缺口.md`
- `.ai/docs/变量存储触发与业务多语言设计.md`
- `.ai/docs/热重载与配置版本设计.md`
- 当前 `backend/internal/pipeline`、`storage`、`mqttx`、`protocol/kio` 结构

JG HJ 参考实现：

- `docs/边缘检测平台MQTT采集与检测业务设计.md`
- `docs/项目架构总览.md`
- `docs/采集层防抖与启动快照说明.md`
- `docs/任务管理功能说明.md`
- `core/channels.go`
- `core/tag_manager.go`
- `workers/logic_worker.go`
- `workers/store_worker.go`
- `workers/task_scheduler.go`
- `workers/event_processor.go`
- `models/tag.go`
- `models/task.go`
- `database/migrations/add_variable_acquisition_debounce.sql`
- `database/create_sys_tasks.sql`

## 3. 当前状态判断

当前后端已经具备：

- Go sidecar 监听 `127.0.0.1:18080`。
- `sys_gateways`、`sys_tags`、`sys_devices`、`sys_detection_tasks`、`rt_history_data` 等基础表。
- KingIOT/KIO 报文解析、首包变量发现、实时 TagManager、检测期历史批量写入。
- `/api/v1` 基础 API、健康检查、runtime channel 状态。
- Edge Auth Lite：JWT、service token、SSO ticket、路由权限。

主要缺口：

- `sys_tags` 当前同时承载原始变量和业务变量语义，后续会越来越混乱。
- 还没有清洗变量、清洗规则、清洗态、质量态和内部变量模型。
- 业务层仍偏“检测任务开关 + 入库”，没有独立的业务分发器和模块化规则。
- 当前热重载只适合网关/变量早期形态，缺少配置版本和按 scope reload。
- 还没有正式的主服务器 realtime export 和 control channel。
- 前端需要稳定 DTO，API/DTO 变动必须同步 `AI_BOARD.md` 和接口文档。

## 4. JG HJ 可复用与禁用内容

可复用的工程思想：

- Memory First：变量配置和实时值放内存 map，数据库只做配置、历史和审计。
- Channel 解耦：MQTT 入口、逻辑处理、业务触发、存储、报警、SSE 分层。
- 双阶段处理：先建立同一时刻输入快照，再基于快照执行业务逻辑。
- 采集层防抖：可疑 0 值先进入观察窗，不能污染实时态、任务、历史和 SSE。
- 启动快照：启动首帧可疑值要和历史非零值比较后再决定是否放行。
- 任务触发与执行分离：快速条件判断和慢速 HTTP/MQTT/DB/脚本动作分离。
- 热重载保留运行态：配置重载时保留当前值、质量、报警、防抖观察窗等运行状态。

不能直接迁移的内容：

- 设备状态、工单、产量、NG、OEE、班次等生产业务语义。
- Wails/Vue 直连式接口。
- 原始 SQL 动作直接改核心业务表的做法。
- 按旧 `sys_variables` 一张表承载全部原始、清洗、业务语义的做法。

## 5. 目标运行链路

目标链路：

```text
MQTT Gateway
  -> Parser Strategy
  -> RawVariableRegistry
  -> RawStateManager
  -> CleanWorker
  -> CleanStateManager
  -> VariableChangedEvent
  -> BusinessDispatcher
  -> DetectionService / QualityService / ExportService
  -> StorageWorker / SSE or WebSocket / Control Ack
```

关键原则：

- MQTT callback 只校验、打时间戳、入队。
- Parser 只负责协议解析，产出统一 `RawVariableUpdate`。
- Raw 层记录上位机事实，不做业务判断。
- Clean 层产出对业务可用的变量、质量、清洗版本。
- Business 层决定是否触发检测、写历史、下发控制或推送主站。
- StorageWorker 只消费业务层确认过的写入任务。

## 6. 数据模型规划

第一阶段尽量兼容现有 `sys_tags`，避免大拆；第二阶段逐步明确表名。

新增或调整建议：

- `sys_raw_variables`：原始变量注册表。唯一键建议为 `gateway_id + source_path` 或 `source_id + raw_key`。短期可由 `sys_tags` 兼容，但 API 文案要说明其 raw role。
- `sys_clean_variables`：清洗变量定义。包含 code、三语 display、data_type、unit、store_mode、store_trigger、enabled。
- `sys_clean_rules`：清洗规则。包含 source raw var、target clean var、rule_type、deadband、zero_debounce、min/max、bool_map、expression、quality_policy。
- `sys_internal_variables`：内部变量。用于 `current_detection_machine`、`active_session_id`、`manual_start_signal` 等。
- `sys_business_modules`：业务模块注册。第一版内置 `detection_session`、`quality_monitor`、`main_export`。
- `sys_business_bindings`：业务模块绑定哪些清洗变量/内部变量。
- `sys_business_rules`：业务规则配置。第一版只支持固定 rule type 和 JSON 参数，不开放任意 SQL。
- `sys_config_versions`：配置版本。scope 包含 `gateways`、`raw_variables`、`clean_rules`、`clean_variables`、`business`、`quality`、`all`。
- `sys_detection_tasks` 或后续 `detection_sessions`：记录检测会话、开始/结束、触发变量、配置版本。
- `rt_history_data`：短期继续作为检测历史表；后续需要补充 clean variable id、quality、rule_version 或 task config version。

迁移策略：

- 不一次性删除现有 `sys_tags` 字段。
- API 先新增清洗相关端点，旧变量列表继续服务前端现有设置页。
- 检测历史表先兼容旧字段，再按清洗变量扩展。

## 7. API 规划

需要新增或稳定的后端 API：

- `GET /api/v1/raw-variables`：原始变量列表。
- `POST /api/v1/raw-variables/discover`：主动发现或查询全量点位。
- `GET /api/v1/clean-variables`：清洗变量列表。
- `POST /api/v1/clean-variables`、`PUT /api/v1/clean-variables/:id`：维护清洗变量。
- `GET /api/v1/clean-rules`：清洗规则列表。
- `POST /api/v1/clean-rules`、`PUT /api/v1/clean-rules/:id`：维护规则。
- `GET /api/v1/realtime/raw`：原始实时态。
- `GET /api/v1/realtime/clean`：清洗实时态。
- `GET /api/v1/business/modules`：内置业务模块与绑定。
- `POST /api/v1/detection/control`：手动启动、停止、切换检测对象。
- `GET /api/v1/detection/sessions`：检测会话查询。
- `POST /api/v1/runtime/reload`：按 scope 热重载。
- `GET /api/v1/runtime/versions`：配置版本和加载状态。
- `GET /api/v1/export/realtime/stream`：主服务器实时 SSE，或 WebSocket 版本。
- `POST /api/v1/control/commands`：主服务器 HTTP 控制命令入口；如果确定 RabbitMQ，则替换为 RabbitMQ consumer 配置和状态 API。

所有新增 API/DTO 必须同步：

- `AI_BOARD.md`
- `backend/docs/backend-architecture.md`
- 必要时补 `backend/docs/前端接口-*.md`

## 8. 9 次迭代计划

### 迭代 1：原始采集契约与当前骨架固化

目标：把当前 MQTT/KIO/TagManager 的职责写清并补齐测试，保证后续拆层不破坏已有联调。

后端任务：

- 梳理 `sys_tags` 当前 raw role，补字段注释和 DTO 说明。
- 固化 KIO parser 的首包、PVs fallback、质量码、write ack 测试。
- 为 `TagManager`、LogicWorker、StoreWorker 补关键单测。
- 增加最小 smoke 脚本或命令说明：health、login、变量列表、runtime channels。

验收：

- `go test ./...` 通过。
- `go build ./cmd/edge-backend` 通过。
- 本地 `/health`、登录、变量实时查询可用。

### 迭代 2：配置版本与 RuntimeReloader

目标：先建设热重载基础，后续 raw/clean/business 都挂在同一套版本机制上。

后端任务：

- 新增 `sys_config_versions`。
- 实现 `RuntimeReloader` 接口：`ReloadGateways`、`ReloadRawVariables`、`ReloadCleanRules`、`ReloadBusiness`、`ReloadAll`。
- 新增 `POST /api/v1/runtime/reload` 和 `GET /api/v1/runtime/versions`。
- 重载时保留实时值、质量、更新时间、未决防抖状态。

验收：

- 修改网关/变量配置后无需重启即可重载。
- 重载前后已存在变量实时值不丢。
- API 有权限控制和审计记录。

### 迭代 3：Raw Variable 与采集层防抖

目标：把 JG HJ 的防抖和启动快照机制落到新系统 raw 层，但不直接引入生产语义。

后端任务：

- 明确 `RawVariableUpdate`、`RawState`、`RawStateManager`。
- 增加可疑值防抖配置：`suspicious_value`、`debounce_threshold`、`startup_snapshot_enable`。
- 启动时预热最近非零历史值或最近有效 raw state。
- 防抖发生在进入清洗层之前，假值不进入 raw realtime、clean realtime、业务事件、历史或 SSE。
- KIO 首包 `metadata_only` 不触发业务、不入库。

验收：

- 序列 `10,0,12` 丢弃 `0`。
- 序列 `10,0,1` 判定真实复位并补发。
- 启动历史 `77`、首帧 `0`、下一帧 `77` 不产生错误变化。
- 单测覆盖防抖状态机。

### 迭代 4：清洗变量、清洗规则与 CleanStateManager

目标：建立业务可消费的清洗态，避免业务直接依赖上位机原始变量。

后端任务：

- 新增 `sys_clean_variables`、`sys_clean_rules`。
- 实现 rule types：`pass_through`、`deadband`、`zero_debounce`、`range_limit`、`bool_map`。
- 实现 `CleanStateManager`，按 clean variable id 和 code 建索引。
- 清洗结果包含 value、data_type、quality、source_time、cleaned_at、rule_version。
- 清洗变量支持三语 display name、store_mode、store_trigger。

验收：

- 一个 raw variable 可以 fanout 到多个 clean variable。
- 一个 clean variable 可以通过不同规则屏蔽抖动、死区和越界值。
- 实时 API 能区分 raw 和 clean。

### 迭代 5：CleanWorker 与 VariableChangedEvent

目标：把清洗层从同步函数升级为可观测、可测试、可扩展的事件链路。

后端任务：

- 新增 `CleanChan` 或在现有 LogicWorker 中引入清洗阶段，保持 MQTT callback 非阻塞。
- 清洗完成后产出 `VariableChangedEvent`。
- 事件包含 old/new、quality、project/device binding、config_version、gateway_id。
- SSE 推送优先基于 clean state，raw state 仅作为管理/诊断用途。
- 增加 channel stats 和队列堆积告警。

验收：

- 清洗层慢或队列堆积不会阻塞 MQTT callback。
- SSE/实时接口可稳定显示 clean values。
- 条件判断读取的是同一批输入快照后的 clean state。

### 迭代 6：业务分发器、内部变量与内置动作

目标：引入业务模块边界，把检测、质量、主站出口等从清洗层解耦。

后端任务：

- 新增 `BusinessDispatcher`。
- 新增 `sys_internal_variables`。
- 新增业务模块注册和绑定：`detection_session`、`quality_monitor`、`main_export`。
- 支持固定触发类型：`ANY`、`INCREASE`、`DECREASE`、`THRESHOLD`、`FALSE_TO_TRUE`、`TRUE_TO_FALSE`、`QUALITY_GOOD`、`QUALITY_BAD`。
- 支持内置动作：`START_DETECTION_FROM_CONTROL_VALUE`、`STOP_DETECTION`、`SWITCH_DETECTION_PROJECT`、`MQTT_WRITE`、`HTTP_NOTIFY`。
- 禁止用自由 SQL 直接改检测核心表。

验收：

- `current_detection_machine` 从 `0 -> N` 能触发内置检测开始。
- `N -> 0` 能关闭检测。
- `N -> M` 能先关闭旧检测再打开新检测。
- 重复 `N -> N` 被忽略并有日志。

### 迭代 7：检测会话门控与历史存储

目标：把“检测期间才入库”作为正式业务规则，而不是散落在存储 worker 条件里。

后端任务：

- 强化 `DetectionService`，统一管理打开/关闭检测窗口。
- 记录检测会话开始/结束、触发变量、项目/设备、配置版本。
- StorageWorker 只消费业务层确认的 `DetectionStoreTask`。
- `rt_history_data` 写入 clean variable id、quality、session id、source timestamp。
- 支持 store_mode/store_trigger：变化、定时、混合、检测窗口。

验收：

- 检测未开始时 clean state 更新但不写历史。
- 检测开始后指定绑定变量写入历史。
- 检测关闭后立即停止写入。
- 会话切换不会把 A 设备数据写入 B 会话。

### 迭代 8：质量、报警、主站实时出口与控制入口

目标：补齐对前端和主服务器可用的运行能力。

后端任务：

- 质量码监控：Good/Bad 边沿事件、离线状态、质量统计。
- 报警/异常事件先做轻量日志和 API，不扩成生产报警系统。
- 实现主服务器实时出口，优先 SSE；如果确认双向控制强需求，再加 WebSocket。
- 控制入口二选一：
  - HTTP：`POST /api/v1/control/commands`，带幂等 key 和明确错误码。
  - RabbitMQ：consumer、ack/nack、重试、死信或失败日志。
- service token 权限绑定到主站出口和控制入口。

验收：

- 主服务器可订阅 clean realtime。
- 未授权主站请求返回 401/403。
- 控制命令具备幂等语义和审计日志。
- RabbitMQ/HTTP 选择结果更新到 `AI_BOARD.md`。

### 迭代 9：联调、压测、发布门禁与文档收口

目标：完成可交付闭环。

后端任务：

- 补完整测试矩阵：parser、raw、debounce、clean rules、business dispatcher、detection service、storage worker、auth 权限。
- 压测 MQTT burst、clean fanout、SSE 连接、历史批量写入。
- 输出本地 smoke 脚本或明确命令。
- 更新 schema、GORM model、接口文档、`MEMORY.md`、`AI_BOARD.md`。
- 与前端确认所有新增 DTO、权限、错误码和三语显示字段。

验收：

- `gofmt` 已执行。
- `go test ./...` 通过。
- `go build ./cmd/edge-backend` 通过。
- 本地 MySQL + MQTT + health + login + realtime + detection session smoke 通过。
- 未能运行的门禁必须记录原因。

## 9. 迭代数量结论

建议按 9 次主迭代实现。

原因：

- 少于 6 次会把 raw、clean、business、detection、export 混在一起，容易形成新一代“大 LogicWorker”。
- 9 次可以把基础能力、业务能力和联调验收分开，每次都有可验证产物。
- 现有后端已经有 MQTT、KIO、TagManager、检测任务和存储骨架，所以不需要 12 次以上的大重建。

推荐节奏：

- 迭代 1-3：采集基础稳定。
- 迭代 4-5：清洗变量体系成型。
- 迭代 6-7：业务分发和检测闭环。
- 迭代 8：主服务器与质量控制能力。
- 迭代 9：测试、压测、发布门禁和文档收口。

## 10. 风险与待确认

待确认项：

- 主服务器实时协议最终选 SSE 还是 WebSocket。
- 控制通道最终选 RabbitMQ 还是 HTTP。
- 主站 service token 的下发、轮换和失效策略。
- `developer` 角色是否允许临时 KIO 写变量。
- 检测业务的正式变量清单、检测对象映射和判定规则。
- `rt_history_data` 是否继续作为唯一检测历史表，还是新增专门 `detection_data_records`。

主要风险：

- 如果前端在清洗模型未稳定前直接绑定 raw variable，后续 DTO 会频繁变动。
- 如果业务模块允许自由 SQL，检测会话一致性会失控。
- 如果配置版本不先落地，检测过程中的规则变更难以追溯。
- 如果主站控制通道迟迟不定，权限、审计和错误码会反复调整。

## 11. 最近可执行下一步

建议下一步直接开迭代 1：

1. 固化当前 raw 变量和 KIO parser 契约。
2. 补 `TagManager`、KIO parser、LogicWorker、StoreWorker 的关键单测。
3. 把当前变量/发现/实时 API 文档补齐到前端可用。
4. 保持后端常驻 `127.0.0.1:18080`，给前端继续联调。
