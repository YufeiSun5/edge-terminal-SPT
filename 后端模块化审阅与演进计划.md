# 后端模块化审阅与演进计划

身份：`review-ai`

日期：2026-05-29

## 结论

当前后端不适合继续按“全部放进 `runtime/kernel.go` + `database/repository.go`”推进，但也不需要引入复杂的 controller/service/repository 全套形式化分层。

最稳的路线是沿用当前已经出现的中等粒度结构：

```text
backend/internal/runtime/handlers/   # HTTP 入参、权限注册、状态码、响应
backend/internal/services/           # 业务不变量、状态转换、跨 repo/cache 协调
backend/internal/database/            # GORM 持久化，先按文件拆，不拆 package
backend/internal/models/              # GORM 模型 + 少量运行态模型，先按文件拆
backend/internal/pipeline/            # 热路径实时状态、任务缓存、入库门控
backend/internal/mqttx/               # MQTT/KIO 连接和发布订阅
backend/internal/protocol/kio/        # KIO 协议解析/写入载荷
backend/tools/                        # 离线工具、迁移、压测、探测，不进入运行时分层
```

不要再新建 `internal/detection` 包。当前已经有 `internal/services/detection_runs_service.go`，如果再建一个 detection 包，会形成两套检测服务入口，后续更难判断业务不变量在哪里维护。

## 当前取证

### 已存在的运行时拆分

- `backend/internal/runtime/kernel.go` 仍然很大，约 1462 行，但已经开始接入 handler/service。
- `backend/internal/runtime/handlers/detection_runs_handler.go` 已承接检测任务列表、详情、开始、停止、异常停止、备注接口。
- `backend/internal/runtime/handlers/variables_handler.go` 已承接 `POST /api/v1/variables`。
- `backend/internal/runtime/handlers/report_templates_handler.go` 已承接报告模板接口。
- `backend/internal/services/detection_runs_service.go` 已承接检测开始、停止、异常停止、备注和 task cache 更新。
- `backend/internal/services/variables_service.go` 已承接虚变量/手工变量创建和 `TagManager` reload。
- `backend/internal/services/report_templates_service.go` 已承接报告模板基本校验。

### 已存在的业务能力

- 虚变量后端已落地：`source_type=virtual`、`gateway_id=0`、`discovered=false`、`placeholder=true`。
- 手工映射变量后端已落地：`source_type=manual`，要求 `gateway_id/source_path/json_path`。
- 检测启动已使用设备行 `SELECT ... FOR UPDATE` 锁，避免同设备并发 running。
- 检测任务已支持 `duration_sec`、`expected_end_at`、`end_type`、`operator_note`、`report_template_id`。
- 检测运行备注 `detection_run_notes` 已落地。
- 报告模板 `sys_report_templates` 和检测运行报告 `detection_run_reports` 已落地。
- `backend/docs/backend-architecture.md` 已更新到新 API 面。

### 仍然集中的地方

- `backend/internal/runtime/kernel.go` 仍保留大量旧路由和 DTO，包括用户、网关、KIO 写入、设备、变量旧接口、历史、检测标准。
- `backend/internal/database/repository.go` 已约 914 行，聚合用户、SSO、审计、网关、变量、设备、检测任务、检测标准、报告模板、历史查询。
- `backend/internal/models/models.go` 已约 512 行，同时承载 GORM 表模型、运行态 `Tag`、`StoreTask`、`TagSnapshot`、检测/报告模型。

### 前端契约滞后

后端已经新增 `POST /api/v1/variables`、`GET /api/v1/detection-runs`、检测任务详情、异常停止、备注、报告模板等接口，但前端 shared API 目前还主要停留在：

- 变量列表、更新、分配、删除。
- active detection runs。
- 检测标准管理。

前端类型也还缺少：

- `source_type`。
- 创建变量 payload。
- 检测任务列表/详情 DTO。
- 报告模板 DTO。
- 检测备注/报告记录 DTO。

这不是后端模块化冲突，但会造成“后端已存在、前端不可用”的交付风险。

## 与业务是否冲突

不冲突，但需要调整原建议。

### 不冲突的原因

1. 业务主线已经自然形成三个服务：变量、检测运行、报告模板。继续沿用 `internal/services` 正好贴合业务。
2. 检测任务是状态型业务，放 service 层维护并发锁、任务缓存、停止语义，比放 handler 或 repository 更合适。
3. 变量创建会影响 `TagManager` 热路径，放 `VariablesService` 协调 repo 和 runtime cache 是合理边界。
4. 报告模板是配置型业务，先保留轻 service，不需要引入复杂报告生成包。
5. MQTT、pipeline、storage 的采集热路径没有被 handler/service 重构触碰，符合边缘端实时链路要求。

### 会冲突的做法

1. 再新增 `internal/detection` 包承接检测任务，会和 `internal/services/detection_runs_service.go` 重叠。
2. 把报告模板塞进检测标准 handler，会混淆“判定规则”和“输出模板”。
3. 为每个 CRUD 接口建独立 package，会让项目过度形式化。
4. 把 `tools/`、`dist/`、`coverage` 纳入运行时模块设计，会污染边界。
5. 在 repository 里继续增加检测判定、报告生成、主站控制逻辑，会让持久化层变成业务层。

## 推荐目标结构

短期目标不是大搬家，而是把已经开始的方向补完整。

```text
backend/internal/runtime/
  kernel.go                 # Kernel 装配、Start/Stop、公共中间件
  routes.go                 # 统一注册 handler
  handlers/
    users_handler.go
    gateways_handler.go
    variables_handler.go
    devices_handler.go
    history_handler.go
    detection_standards_handler.go
    detection_runs_handler.go
    report_templates_handler.go

backend/internal/services/
  variables_service.go
  detection_runs_service.go
  detection_standards_service.go      # 后续需要标准校验/删除保护时再加
  report_templates_service.go
  control_service.go                  # 主站控制通道确认后再加

backend/internal/database/
  repository.go                       # Repository struct、NewRepository、共用错误
  users_repo.go
  gateways_repo.go
  variables_repo.go
  devices_repo.go
  history_repo.go
  detection_repo.go
  report_templates_repo.go

backend/internal/models/
  common.go                           # 常量和小工具
  auth.go
  gateway.go
  device.go
  variable.go
  detection.go
  report.go
  runtime_tag.go
```

以上是文件级拆分，不是 package 级拆分。这样能降低风险：外部 import 路径不变，单次改动范围可控，测试也容易保持稳定。

## 演进计划

### P0：冻结边界

目标：先统一团队认知。

- `runtime/handlers` 是 HTTP 层。
- `services` 是业务不变量层。
- `database.Repository` 是持久化层。
- `pipeline` 是实时热路径，不能为了普通业务重构而污染。
- `tools` 是离线工具，不参与运行时分层。

验收：

- `AI_BOARD.md` 记录模块化边界任务。
- 新接口优先放 `runtime/handlers + services`，不再直接加进 `kernel.go`。

### P1：把新增业务保持在新结构内

目标：不继续扩大旧文件。

- 虚变量新增、检测任务新增、报告模板新增都继续走现有 handler/service。
- 前端可见 DTO 变化同步 `desktop/src/shared/api/types.ts` 和 feature API。
- 新增错误用明确 error，而不是字符串判断。

验收：

- 新增业务不再直接扩大 `kernel.go` 的 route 闭包。
- 新增业务不再直接把复杂状态转换塞进 repository。

### P2：拆旧 `kernel.go`

目标：把旧路由迁到 handler 文件，保持行为不变。

建议顺序：

1. `users_handler.go`
2. `devices_handler.go`
3. `history_handler.go`
4. `detection_standards_handler.go`
5. `gateways_handler.go`
6. `kio_handler.go`

KIO 写入和网关管理可以暂时在同一个 `gateways_handler.go`，不要拆太细。

验收：

- `kernel.go` 降到约 400 行以内。
- `mountRoutes` 只做 group、middleware、handler 注册。
- `go test ./...` 通过。

### P3：拆 `repository.go` 文件

目标：减轻持久化层文件风险，不改变 package。

建议文件：

- `repository.go`：Repository struct、NewRepository、共用错误。
- `auth_repo.go`
- `gateways_repo.go`
- `variables_repo.go`
- `devices_repo.go`
- `history_repo.go`
- `detection_repo.go`
- `reports_repo.go`

验收：

- `database.Repository` 对外方法名保持兼容。
- 不新增循环依赖。
- `repository_test.go` 可继续跑，后续再按领域拆测试。

### P4：拆 `models.go` 文件

目标：降低模型文件认知负担。

建议只做文件拆分，不拆包：

- `models/variable.go`：`TagConfig`、`TagSnapshot`。
- `models/runtime_tag.go`：`Tag`、存储判断方法。
- `models/detection.go`：检测任务、标准、运行快照、备注。
- `models/report.go`：报告模板、运行报告。
- `models/auth.go`：用户、service client、SSO、audit。
- `models/gateway.go`、`models/device.go`、`models/history.go`。

验收：

- GORM table name 不变。
- JSON 字段不变。
- `models_test.go` 继续覆盖表名和关键运行逻辑。

### P5：补前端契约追平任务

目标：避免后端模块化成果无法被前端使用。

需要补：

- `source_type` 到 `VariableConfig`。
- `createVariable` API 和 payload。
- `DetectionRun` 列表/详情 DTO。
- `ReportTemplate` DTO 和 API。
- 工位页 start/stop/abnormal-stop mutation。
- 历史页按 `task_id` 查询。

验收：

- 前端能创建 int 虚变量。
- 前端能启动两个设备不同检测任务。
- 前端能进入某个 task 的历史曲线。

## 不建议做的事

- 不要为每个 endpoint 建一个 service。
- 不要为简单 CRUD 引入 interface。
- 不要把 `services` 拆成多个 package，除非出现明确循环依赖或超过 8-10 个业务服务。
- 不要把 pipeline 热路径的 `TagManager` 交给 handler 直接操作，必须通过 service。
- 不要把报告生成二进制文件直接塞数据库；当前 `file_ref` 方案更合适。

## 风险清单

1. `kernel.go`、`repository.go`、`models.go` 继续增长，会重新吞掉 handler/service 的收益。
2. 前端契约未追平，会让后端接口处于“存在但不可用”的状态。
3. 检测标准、检测运行、报告模板三个概念如果混用，会影响后续报告和历史追溯。
4. 主站控制通道确认后，如果不复用 `DetectionRunsService`，可能绕过本地并发保护。
5. 工具目录和运行时目录如果混在一起，会干扰发布和测试边界。

## 最小下一步

下一步只做三件事：

1. 看板新增一个后端模块化任务，明确“沿用 `runtime/handlers + services`，不新增 detection 包”。
2. 后端后续新增接口必须进 handler/service。
3. 前端补齐新后端接口的 typed API，否则虚变量和检测运行闭环无法被桌面端使用。
